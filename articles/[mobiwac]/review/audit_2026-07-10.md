<!-- Per-item audit of germano_collage.md + advisor_collage.md + the M1 methodology complaint.
     Produced 2026-07-10 by the review-audit workflow (2 adversarial judges per theme + reconciler).
     STATUS: awaiting author approval - NO paper changes applied. -->

# Consolidated Review Audit — MobiWac 2026 paper (reconciled from venue + discipline lenses)

Scope: 97 review items (G1–G62, F1–F9, E1–E26) + the M1 methodology complaint. Where both judges agreed, the verdict is adopted with merged reasons. Where they split, I ruled (see §6). No file edits made; every rewrite below is proposed text for author approval.

---

## 1. Executive summary

**Verdict counts (97 items):**

| Verdict | Count |
|---|---|
| DONE (already applied by the sweep) | 30 |
| ACCEPT (apply as asked) | 11 |
| ADAPT (apply the kernel, our wording) | 31 |
| REJECT (do not apply) | 25 |
| ESCALATE | 0 (one *conditional* author call inside M1, see §5) |

**Top-10 highest-value ACCEPT/ADAPT items, ranked:**

1. **G34 + G38** — displayed loss Eq. (1) + tuned-provenance sentence in §4.2. Cures the zero-equation rigor signal and the §2.3/§4.2 tuned-vs-chosen contradiction. The core of the M1 fix.
2. **G35** — delete §4.1's triple-stated novelty-defusal paragraph. Funds all §4 additions (−3/−4 lines).
3. **G54** (absorbs G49, G50, G51, G53) — compress §5.2 Integrity to ~270 words, every number kept, plain-agency sentences. Fixes four "não entendi" hits and banks −5 lines.
4. **F9** — move code/data URLs to a page-1 intro footnote (discipline's 4-step spec, incl. re-targeting the §5.4 pointer). Reproducibility visible early; conclusion ends on the thesis. Net −1 line.
5. **F4 (+F3)** — problem-form opener for §3 ("Given a user's time-ordered check-in history…"). Advisor-mandated, GLOSSARY-law, −1 line.
6. **G23** — fix the DGI mischaracterization (general graph self-supervised method, not a place-embedding model). Factual accuracy a TPC member can check.
7. **G56** — add the owed Holm + TOST citations (no prose definitions). +3–4 bib lines, offset by G54.
8. **E20 + E21 + E22** — three paragraph breaks in the flagship §6.1/§6.2 monster paragraphs (E20 at the corrected seam). Scanability for a skimming PC.
9. **G13 + G26** — depersonalize the intro self-citation; add the one mandated self-delta sentence in §2. Kills the "incremental over own prior work" optics.
10. **G16 (+G20)** — cut the intro's defensive "not new on its own" aside (−2/−3 lines) and use the room to split the 63-word headline-scaling bullet that Germano could not parse.

**Page budget:** adds ≈ +16 to +19 lines (Eq. 1 +2, provenance +1, edge sentence +1, bib entries +3–4, §5.4 breaks +2, §5.3 break +1, §6 breaks +3 worst case, G20 +1, G26 +1.5, F9 footnote +4 at 8pt). Offsets ≈ −16 to −19 lines (G35 −3, G54 −5, G16 −2/−3, G22 −1, F4 −1, F9 conclusion −5 body lines, E21 Holm trim −1, E15 ~0/−1). **Net ≈ 0; the paper stays at 8 pages.** Recompile (pdflatex → bibtex → pdflatex ×2) after each bundle is mandatory; the F9 footnote has an explicit revert fallback.

---

## 2. Per-theme verdicts

### 2.1 Abstract & Introduction

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| G1 | "Two coarse questions… category… region" | margin rewrite "Two categories are usually enough" | REJECT | Salvageable part already swept; rewrite calls the region a "category," colliding with the paper's task terms. | — | 0 |
| G2 | "We therefore ask whether" | "ask" → "test" | DONE | Applied verbatim; matches GLOSSARY 9.1. | — | 0 |
| G3 | "what sharing a single model costs" | insert trade-off phrasing | ADAPT | Two readers stumbled; sweep's "in per-task accuracy" names the quantity; residual is the bare "both" — fixed by E2 alone. | Apply E2 only ("learn both tasks"); no further rewrite. | 0 |
| G4 | "gain comes from per-visit context" | → "characteristics" | REJECT | "Per-visit context" is the coined, load-bearing term; swap breaks one-term rule and detaches abstract from the ablation. | — | 0 |
| G5 | "(about +5 to +9 macro-F1)" | strike the "+9" | REJECT | Deletes a true frozen number (AZ +9.40); named reviewer error; GLOSSARY 9.4. | — | 0 |
| G6 | "and on region it outperforms" | say "next-region task" | ACCEPT | Names the canonical task at the claim site, symmetric with "next-category prediction" earlier; +2 words, no frozen verb touched. (Split; ruled §6.) | "and on the next-region task it outperforms the dedicated region model at four of the six datasets" | 0 |
| G7 | "comes out slightly ahead on region" | upgrade to "superior performance" | REJECT | Smoothing already in; the upgrade is banned puffery and overclaims the untested Istanbul region delta. | — | 0 |
| G8 | "planning capacity there" | remove "there" | DONE | Now "at the destination". | — | 0 |
| G9 | "Predicted handovers already let…" | felt out of place | REJECT | It is the deliberate MobiWac venue bridge (network-level anticipatory action); removing it weakens fit, the paper's #1 risk. | — | 0 |
| G10 | "the kind of place such as food" | → "type of place" | ADAPT | Intro instance swept; identical register problem survives unflagged in §III ¶2. | 03_problem.tex: "the category says what **type** of place comes next" | 0 |
| G11 | "One captures intent, the other geography" | first/second construction | DONE | "The first captures intent, the second captures geography". | — | 0 |
| G12 | "is not free" | → "is not straightforward" | DONE | Now "has a cost:", better than the suggestion (names the measured quantity). | — | 0 |
| G13 | "Our earlier work [7] observed…" | cite [6,7] jointly | ADAPT | Concern real (codified 9.3; author agreed), but the literal merge mis-attributes the task-pair observation to Caruana. Venue's scoped fix wins (see §6). | R‑G13 below | −1 word |
| G14 | "where it costs" | fix phrasing | DONE | Now "what it costs". | — | 0 |
| G15 | "We introduce two changes." | → "we propose two enhancements" | DONE | "We propose two enhancements." — the C2 ruling. | — | 0 |
| G16 | "Per-visit context is not new on its own…" | remove passage | ACCEPT | GLOSSARY 9.3 mandates the cut; novelty stated positively in §2.1 + bullet 1; defensive aside plants doubt. Banks the G20 offset. | Delete the sentence "Per-visit context is not new on its own; … (Section~\ref{sec:results-part1})." from ¶4. | −2/−3 lines |
| G17 | "two deliberately different settings" | strike "deliberately" | DONE | Now "two settings chosen to differ". | — | 0 |
| G18 | "outperforms at four… non-inferior" | compress to "4 out of 6 scenarios" | REJECT | Deletes the mandated §1 occurrence of the frozen TOST phrase and hides the two remaining datasets. | — | 0 |
| G19 | "holds everywhere, and…" | scope + "while" | REJECT | Scope half already fixed; "while" wrongly signals contrast between two co-true positives; mooted by G20's split. | — | 0 |
| G20 | "from a non-inferior match… at the large" | didn't understand | ADAPT | Confirmed comprehension failure on the headline scaling claim; still a 63-word braid. Venue's 3-sentence split keeps TOST gloss, pp definition, matches/outperforms bindings. (Split; ruled §6.) | R‑G20 below | +1 line (paid by G16) |
| G21 | "comes out ahead… seeds… arms" | improve phrasing | DONE | Sweep reworked all three offending terms. | — | 0 |
| F1 | abstract | more context/motivation, fewer results | ADAPT | Full restructure wrong for this venue (quantitative rubric; numbers frozen), but the author-endorsed kernel is real: motivation is generic. Minimal concrete-actions edit, booster as offset. (Split; ruled §6.) | R‑F1 below | 0 |
| F2 | "what sharing… costs" | advisor didn't understand | ADAPT | Same defect as G3; sweep's "in per-task accuracy" plus E2's "both tasks" resolves it. | Covered by E2. | 0 |
| E1 | "normally handled" | → "usually" | REJECT | "Normally" is correct; "usually" appears two sentences earlier — creates repetition. | — | 0 |
| E2 | "learn both, and what" | → "learn both tasks" | ACCEPT | One word disambiguates the bare "both"; closes G3/F2 residual. | "whether one model can learn both tasks, and what sharing a single model costs in per-task accuracy" | 0 |
| E3 | "$+28$ to $+40$" | de-math the abstract deltas | REJECT | Math mode is correct for signed quantities and matches the body; text-mode minus renders as hyphen (flagged by the collage itself). | — | 0 |
| E4 | two abstract commas | add commas | DONE | Both present. | — | 0 |
| E5 | keyword order | "mobility data" first | ACCEPT | Costless senior-author edit; leads with the venue's own noun. | `mobility data, next-category prediction, next-region prediction, multi-task learning, check-in-level representation, location-based social networks` | 0 |
| E6 | "where it costs" | → "what" | DONE | Same fix as G14. | — | 0 |
| E7 | "two changes" → "two novelties" | strengthen claim | DONE (obsolete) | Sentence rewritten a third way ("two enhancements"); "novelties" banned by the C2 ruling. | — | 0 |
| E8 | "We evaluate on… (Gowalla)" | past tense; drop "(Gowalla)" | REJECT | Past tense breaks the present-tense voice; dropping the attribution deletes trace provenance at first mention. | — | 0 |
| E9 | "Our contributions are the following." | → "summarized as follows:" | ADAPT | Change yes, but "summarized" is false (bullets are the full statement). Discipline's formula wins (see §6). | "Our contributions are as follows." | 0 |
| E10 | "(Table~\ref{tab:substrate})" | expand cross-ref | REJECT | Table ref already locates the evidence; pure length in a zero-slack bullet. | — | 0 |
| E11 | "In one forward pass," | add comma | DONE | Present. | — | 0 |
| E12 | "(Table~\ref{tab:results})" | expand cross-ref | REJECT | Same as E10, in the densest (TOST-carrying) bullet. | — | 0 |
| E13 | "across five states" | → "five U.S. states" | ACCEPT | Only unqualified instance in the paper; one word buys consistency and the Istanbul contrast. | "across five U.S. states and one international city" | 0 |
| E14 | "(Fig.~\ref{fig:deltas})" | expand cross-ref | REJECT | Redundant, and the proposed "see in Section" is ungrammatical (collage's own note). | — | 0 |

**Rewrite texts (abstract/intro):**
- **R‑G13** (01_introduction.tex ¶3): replace `Our earlier work~\cite{silva2025mtlnet} observed exactly this for next-category and next-region;` with `prior work observed exactly this for next-category and next-region~\cite{silva2025mtlnet};` *(keeps Caruana scoped to the general negative-transfer claim; the one self-positioning sentence moves to §2 via G26).*
- **R‑G20** (contribution bullet 3): `The category gain holds at all six datasets. Across the U.S. states, the region result moves monotonically with the region count: the joint model matches the dedicated model (TOST, $\pm2$ percentage points, or pp) at the small region counts and outperforms it at the large region counts, with the largest state (California) showing the largest region gain (Fig.~\ref{fig:deltas}). Istanbul, the smallest dataset, is also ahead on region.`
- **R‑F1** (abstract): (1) `mobile and urban services can act ahead of demand.` → `mobile and urban services can cache content or reserve capacity ahead of demand.` (2) offset: `this lifts next-category prediction by a wide gap over a standard place embedding` → `this lifts next-category prediction over a standard place embedding` *(author check: confirm "by a wide gap" is not frozen; it is a GLOSSARY §7 booster).*

### 2.2 Related work & Problem

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| G22 | "turns a POI into a vector… bare, arbitrary index" | transforms; "where?"; strike index clause | ADAPT | "transforms" done; "in the vector space" fixes real ambiguity; the index clause is a decorative contrast — strike it and fund G23. (Split; ruled §6.) | `A place embedding transforms a POI into a vector so that similar or nearby places sit close together in the vector space.` | −1 line |
| G23 | "DGI learns such vectors self-supervised" | DGI is not a place-embedding model | ADAPT | Confirmed factual catch (codified 9.3); "self-supervisedly" is not English. Discipline's appositive adopted; "tell X from Y" is the codified gloss. | `Deep Graph Infomax (DGI)~\cite{velickovic2019dgi}, a general self-supervised method for graphs, learns such vectors on a place network linked by similarity, time, and distance, training them to tell the real network from a shuffled copy.` | ~0 (offset G22) |
| G24 | "from a shuffled copy" | strike | REJECT | Leaves "tell X from —" ungrammatical and deletes the infomax mechanism (the mandated gloss). | — | 0 |
| G25 | "not to make the task easier" | strike | REJECT | Claim-scoping contrast that preempts the "you coarsened the target" attack; backed by the class-count fact. | — | 0 |
| G26 | "follows our earlier line of work [7]" | reviewers can't be assumed to know [7] | ADAPT | Real: reads as unstated incrementalism. Fix = the one mandated §2 self-delta sentence, not deletion. | R‑G26 below | +1.5 lines (offset G22/G13) |
| G27 | "stop short of our pairing" | weird phrasing | DONE | Now "do not study our exact pairing". | — | 0 |
| G28 | §II-C jargon ("headline", "instrumental" ×3) | revise dense paragraph | ADAPT | Right catch; full restructure risks mandated content. Targeted plain-word swaps + one semicolon split. | R‑G28 below | 0 |
| G29 | "We confirm this: none of the balancers…" | recast as hypothesis | REJECT | Named reviewer error: it is a measured finding with its mechanism stated; recasting converts evidence into speculation. | — | 0 |
| G30 | "form windows" | → "build" | DONE | Applied (sentence also superseded by F4). | — | 0 |
| G31 | "about five hundred… eight thousand five hundred" | raw numbers or a table | ADAPT | Digits applied; add the near-free Table I anchor (known-right pattern). | `from 520 classes (Istanbul) to 8,501 (California; Table~\ref{tab:datasets}).` | 0 |
| F3 | §3 as a whole | problem, not solution details | ADAPT | Codified law; after F4 the only violation is gone. No further restructure at zero slack — label sets and class ranges ARE problem content. | Apply F4 only. | 0 |
| F4 | "We order… form windows of nine…" | first sentence is HOW not WHAT | ADAPT | Right catch; window mechanics fully live in §5.2. Replacement is shorter. | `Given a user's time-ordered check-in history, we predict two properties of the next visit.` | −0.5/−1 line |

**Rewrite texts (related/problem):**
- **R‑G26** (02_related.tex, replacing "The category task itself follows our earlier line of work…"): `Our earlier work~\cite{silva2025mtlnet} established this two-task setup and observed negative transfer between the tasks; this paper adds the check-in-level representation, on which the transfer reverses (Section~\ref{sec:results-part2}).` *(Verify the label `sec:results-part2` compiles; else use the §6.2 label.)*
- **R‑G28** (02_related.tex): line 67 `headline output` → `primary output`; `an instrumental step` → `an intermediate step`; lines 68–69 `likewise uses category instrumentally, to filter candidate places` → `likewise uses the predicted category only to filter candidate places`; line 72 `so neither task is instrumental to a third; a chain would have to promote` → `so neither task is an intermediate step toward a third. A chain would have to promote`; line 46 `a headline next-place task` → `a primary next-place task`.

### 2.3 Method

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| G32 | "Edges tie the levels… weighted by closeness in time" | edges tie levels? reword weighting | ADAPT | Both halves real; reviewer's wording is broken English and factually inverted (code: `exp(-gap/decay)`, i.e., decays with gap). Discipline's minimal, code-verified fix adopted (see §6). | `Edges connect each level to the one above it and link a user's consecutive check-ins with a weight that decays as the time gap between the visits grows;` (rest unchanged) | +1 line (paid by G35) |
| G33 | "enter as input features" | → "act" | REJECT | "Enter as X, not as Y" is the coherent contrast; "act as" breaks it and drifts toward the banned "serves as" family. (Split; ruled §6.) | — | 0 |
| G34 | "train… with an infomax objective" | add an equation | ADAPT | The known-right zero-equation catch, but the settled ruling routes it to the §4.2 loss; the infomax objective stays a citation. | R‑G34 below | +2 lines (paid by G35) |
| G35 | "Each ingredient appears in prior work…" | remove paragraph | ACCEPT | Third statement of the same novelty scoping (§2.1 + bullet 1 carry it); positioning, not evidence. THE offset for §4. | Delete the closing ¶ of §4.1 ("Each component appears in prior work on its own; … far easier to learn."). | −3/−4 lines |
| G36 | "train one model" | → "a model" | REJECT | "One" is the thesis anchor (one model, one forward pass); deliberate count-emphasis, not an article. | — | 0 |
| G37 | "stream(s)" ×6 | too repetitive | DONE | Sweep cut to 3×, the term-consistency minimum. | — | 0 |
| G38 | "0.75 / 0.25" | chosen or tuned? | ADAPT | Known-right provenance catch; §2.3 already says "tuned" — §4.2's silence is a reviewer trap. | Add after Eq. (1): `The weights were tuned once on validation during development and held fixed across all six datasets.` | +1 line (paid by G35) |
| G39 | "ordinary fixed-weight" | "ordinary"? | DONE | Now "standard" + citation. | — | 0 |
| G40 | "deliberately plain" | didn't understand | DONE | Now "kept simple by design". | — | 0 |
| G41 | "state it plainly" | circled "plainly" | DONE | Now "Serving both tasks this way has a cost." | — | 0 |
| G42 | parameter counts | how many params in the MTL model? | DONE | Now explicitly attributed: joint = 4.2M (AL) / 5.2M (CA). | — | 0 |
| E15 | "of Table~\ref{tab:results}" | remove forward ref | ACCEPT | Mid-method forward jump to an F1 table that says nothing about sizes; the term is anchored where needed (§1, §6.2). | Delete `of Table~\ref{tab:results}` in the §4.2 cost sentence. | 0/−1 line |

**Rewrite text (method):**
- **R‑G34** (04_method.tex §4.2): replace `The training loss is a fixed-weight sum of the two outputs, the category output weighted 0.75 and the region output 0.25, and both outputs use plain unweighted cross-entropy.` with:
  `The training loss is a fixed-weight sum of the two outputs,`
  `\begin{equation}\label{eq:loss} L = 0.75\,L_{\mathrm{cat}} + 0.25\,L_{\mathrm{reg}}, \end{equation}`
  `where $L_{\mathrm{cat}}$ and $L_{\mathrm{reg}}$ are plain unweighted cross-entropy losses. The weights were tuned once on validation during development and held fixed across all six datasets.`
  *(Do NOT add any equation to §4.1.)*

### 2.4 Setup & Statistics

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| G43 | state ranges in §5.1 | wants a descriptive table | DONE (obsolete) | Table I already is that table, anchored at the top of §5.1; author said so inline. | — | 0 |
| G44 | "giving both tasks more examples" | strike | REJECT | Five words of design rationale for the stride-1 choice a leak-suspicious reviewer will question; G54 provides the space instead. (Split; ruled §6.) | — | 0 |
| G45 | padded-duplicate dedup sentence | cut preprocessing detail | REJECT | Defines the test-set composition behind Table I's Windows column; forecloses the inflation question. | — | 0 |
| G46 | "a test user's visits never appear in training" | assume no leakage | REJECT | That assumption is what got the prior submission rejected; the clause is the checkable mechanism. Half a line. | — | 0 |
| G47 | Food majority-class sentence | wants a chart | ADAPT | Chart unaffordable/redundant (Table I Majority column); apply the table anchor. | `…from about 25 percent of visits in Florida to 34 percent in Alabama (Table~\ref{tab:datasets}).` | 0 |
| G48 | "no usable information about the test visits" | strike + link table | REJECT | Striking un-scopes the central no-leak claim; the verifying numbers follow two sentences later. | — | 0 |
| G49 | "only test-side exposure is graph structure" | didn't understand | ADAPT | Real jargon hit; resolved inside R‑G54 ("the only information that can cross from the test side is the structure of the graph itself"). | via R‑G54 | 0 |
| G50 | per-fold rebuild sentences | didn't understand | ADAPT | Dangling "it", stacked participles; floor content so clarify-not-cut. Resolved inside R‑G54. | via R‑G54 | 0 |
| G51 | "we cannot fully measure" | why not? | ADAPT | Answer existed but the causal link was implicit; R‑G54 makes it explicit ("because the representation cannot score a place never seen in training"). | via R‑G54 | 0 |
| G52 | "cannot" | no contractions | REJECT | Reviewer error: "cannot" is the standard formal one-word form (codified 9.2). | — | 0 |
| G53 | "the audit finds no inflation" | audit? | ADAPT | Dangling definite reference; R‑G54 names the act ("this measurement finds no inflation"). | via R‑G54 | 0 |
| G54 | whole Integrity paragraph | didn't understand; candidate to remove | ADAPT | Removal barred (leak defense answers the BRACIS rejection; venue penalizes traces without a protocol). Author agreed to summarize: compress ~325→~270 words, EVERY number kept. This is the offset bank. | R‑G54 below | −5 lines |
| G55 | §V-C title | strike title tail | DONE | Now "Metrics and Statistical Tests". | — | 0 |
| G56 | "Holm correction" | invented? citation? | ADAPT | Half right: Holm is standard, no prose definition (reads as unfamiliarity), but the citations are genuinely owed — neither Holm nor TOST is in references.bib. | R‑G56 below | +3/+4 lines (paid by G54) |
| G57 | "single-seed" | what is single-seed? | ADAPT | Seed gloss exists; naming the two states costs ~4 words and answers "which?". (Split; ruled §6.) | `at the two single-seed states (Texas and California), a paired Wilcoxon signed-rank test over the folds` | 0 |
| G58 | "on deployment grounds" | what grounds? | DONE | Reason now spelled ("because a mobility-aware service acts on which region will be busy…"). | — | 0 |
| G59 | power paragraph | confusing | ADAPT | Residual confusion is structural. Merge both judges' compatible fixes: verb insertion + de-nesting + one paragraph break. | R‑G59 below | +1 line (paid by G54) |
| G60 | "faithful re-implementation" | strike "faithful" | DONE | Now "re-implemented from its published architecture and hyperparameters". | — | 0 |
| G61 | "the next-place head we do not predict" | insert "that" | DONE | Applied verbatim. | — | 0 |
| G62 | "faithful STAN… audited recipe" | "audited"? | DONE | Both self-certifiers replaced by act descriptions. | — | 0 |
| F5 | "$+0.00$ at Florida" | nil? none? | DONE | Now "no change ($0.00$)"; preserved inside R‑G54. | — | 0 |
| E16 | "percent" → "\%" | typography | REJECT | The draft has a consistent two-register convention (spelled in prose, \% in math/CI/tables); a 7-edit paper-wide swap is deadline churn. (Split; ruled §6 — ripples into G47/G54/E21 wording.) | — | 0 |
| E17 | §5.4 baselines paragraph | split into three paragraphs | ACCEPT | One 376-word wall covering three roles; role sentences already renamed to match. Whitespace only. | Insert breaks before "The second role is two representation controls" and before "The third role compares the two ways of coupling the tasks." | +2 lines (paid by G54) |
| E18 | "(Section~\ref{sec:conclusion})" | remove pointer | REJECT as standalone | The pointer is the only anchor to the code URL — **but F9 step 3 supersedes this**: after the URL moves, the pointer is retargeted to `footnote~\ref{fn:code}`. | Handled by F9. | 0 |

**Rewrite texts (setup/stats):**
- **R‑G54** (05_setup.tex, replace the full Integrity paragraph; keep the hidden `%` provenance comments):
  `\emph{Integrity of the representation.} We train the representation once on the whole dataset and feed it to every model, dedicated and joint; we verify that it holds no usable information about the test visits, for three reasons. First, its objective is label-free: it contrasts real graph neighborhoods against shuffled ones, its auxiliary terms reconstruct only input-derived quantities, and it never sees a next-category or next-region target. Second, because the graph is built over all places, the only information that can cross from the test side is the structure of the graph itself. We measured this directly: for each fold, we rebuilt the representation from that fold's training users only and re-ran both tasks against the full-corpus version. The differences are within fold noise, with no systematic gain: on region, $-0.33$ points at Alabama, $+0.01$ at Arizona, and $-0.12$ at Florida; on category, $+0.29$ at Alabama, $+0.27$ at Arizona, and no change ($0.00$) at Florida, measured on the visits whose places appear in training (about 67 percent at Alabama to 87 percent at Florida). The remaining visits are the one residual this measurement cannot reach, because the representation cannot score a place never seen in training. Third, the one component that could pass visit-to-visit information, a region-transition prior (a table of how often one region follows another), inflated region accuracy by 13 to 27 points when an earlier version built it on the whole dataset; any such table is therefore built per fold, from training data only, seed by seed. The joint and dedicated models do not use this prior; it survives only in the HMT-GRN baseline (Section~\ref{sec:setup-baselines}). The baseline representations meet the same standard: HGI is pre-trained once on the whole dataset, like ours, and CTLE is pre-trained per fold on training users only.`
- **R‑G56**: `a Holm correction~\cite{holm1979} across the comparisons`; `a two-one-sided-tests (TOST) procedure~\cite{schuirmann1987tost}`. Add to references.bib: `@article{holm1979, author={S. Holm}, title={A simple sequentially rejective multiple test procedure}, journal={Scandinavian Journal of Statistics}, volume={6}, number={2}, pages={65--70}, year={1979}}` and `@article{schuirmann1987tost, author={D. J. Schuirmann}, title={A comparison of the two one-sided tests procedure and the power approach for assessing the equivalence of average bioavailability}, journal={Journal of Pharmacokinetics and Biopharmaceutics}, volume={15}, number={6}, pages={657--680}, year={1987}}`. No definition sentences; Wilcoxon/paired-t stay uncited.
- **R‑G59** (05_setup.tex §5.3): (a) insert the verb: `…under different random initializations), we use a paired test over the per-seed means…`; (b) paragraph break before `We fix the two-point margin in advance`; (c) de-nest the scale clause into its own sentence: `…(Section~\ref{sec:problem}), so a two-point shift in Acc@10 is below the granularity at which such a service would behave differently. For scale, with 520 to 8,501 regions, a random top-ten guess includes the true region at most about two percent of the time.`

### 2.5 Results & Tables

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| F6 | "$n{=}20$ on both arms" | "arms"? | DONE | All prose instances swept ("for both models" etc.); "arms" survives only in % comments. | — | 0 |
| F7 | "Read this as:" openers | too abrupt; occurs elsewhere | DONE | Both openers removed; the surviving "We read this as a defense…" is a mandated frozen contrast, untouched. | — | 0 |
| E19 | standalone parenthetical sentence | fold into preceding sentence | ADAPT | Oddity real, but the fold-in would stack parentheticals on an already-braided sentence and regress sweep fixes. Drop the parentheses instead. | Remove the parentheses: `The check-in-level column keeps one fixed configuration, intentionally not the per-dataset-tuned ceiling of Table~\ref{tab:results}.` | 0 |
| E20 | break before "A feature-concat control" | paragraph break | ADAPT | Break yes, seam wrong: it orphans "either" and severs the two controls from their joint conclusion. Break one sentence earlier. | Insert blank line before `CTLE~\cite{lin2021ctle}, the closest prior contextual embedding,` | +1 line worst case |
| E21 | break before "Table~\ref{tab:results} reports" | paragraph break | ACCEPT | %-comments don't break LaTeX paragraphs, so §6.2 runs as one ~350-word block; natural seam. Funded by the Holm trim. | Break + offset: `each per-dataset gain is significant after a Holm correction across the four datasets (paired $t$, corrected $p<0.001$ at all four)` → `each gain is significant after a Holm correction across the four datasets (paired $t$, corrected $p<0.001$)` | net 0 |
| E22 | break before "Figure~\ref{fig:deltas} plots" | paragraph break | ACCEPT | Same monster paragraph; gives three coherent units (claim+calibration \| table+protocol \| figure+gains). | Insert blank line before `Figure~\ref{fig:deltas} plots the signed gains ordered by region count.` | shared offset with E21 |
| E23 | "Across the five U.S. states," | comma | DONE | Present. | — | 0 |
| E24 | "all four are tested equivalences" | → "tested for equivalence" | ACCEPT | Genuine grammar slip, still in the text; the fix is standard TOST phrasing, no frozen number touched. | `On region, all four are tested for equivalence (TOST, $\pm2$~pp), and all four pass` | 0 |
| E25 | "On principle," | comma | DONE | Present. | — | 0 |
| E26 | commas in external-validity sentence | two commas | ADAPT | Comma yes, but the verbatim quote would restore the deliberately-trimmed booster "far"; mirror the comma on both parallel phrases. | `…the same ordering: on category, the joint model is above the Markov floor ($24.55$) and POI-RGNN~\cite{capanema2023poirgnn} ($30.12$), and on region, it is above ReHDM~\cite{li2025rehdm} ($69.33$), STAN~\cite{luo2021stan} ($61.86$), and HMT-GRN~\cite{lim2022hmtgrn} ($60.4$).` | 0 |

*Note:* the venue judge's alternative E21 offset ("two percent" → "$2\%$") is NOT adopted because E16 was rejected (keep "percent" in prose). **Recompile after the three breaks is mandatory**; if the page overflows, the additional discipline reserve is: "the region gains above that margin are at Texas and California" → "only Texas and California exceed that margin".

### 2.6 Conclusion & Misc

| id | quote | ask | verdict | rationale | rewrite/action | cost |
|---|---|---|---|---|---|---|
| F8 | "One model is enough. …" | too abrupt, more inviting | ADAPT | Concern ratified by the paper's own style law (9.1 bans this clipped opener verbatim); sweep's "+for both tasks" keeps the flagged shape. Discipline's merge is the minimal, frozen-safe fix (see §6). | R‑F8 below | 0 |
| F9 | code/data URLs in conclusion | move to intro as footnotes | ADAPT | Right call: reproducibility visible on page 1 (valuable after a leak-based rejection), conclusion ends on the thesis, long \url typesets better as a footnote. Discipline's spec adopted — the venue version missed the §5.4 pointer retarget. | R‑F9 below | net −1 line |

**Rewrite texts (conclusion/misc):**
- **R‑F8** (07_discussion.tex, replace the first two sentences): `One model serves both tasks: a shared trunk carries the semantic context that lifts the next-category task over a dedicated model at every dataset, and a private spatial path keeps the next-region task competitive, a non-inferior match (TOST, $\pm2$~pp) at the two small U.S. states and a tested gain at the other four.` *("serves both tasks" is the file's own plan-comment wording; not the banned "serves as".)*
- **R‑F9** (four steps): (1) In 01_introduction.tex line 24, append to the sentence ending `…and one international city (Istanbul).` the footnote `\footnote{\label{fn:code}Code for the model, representation, baselines, and statistical tests: \url{https://github.com/VitorHugoOli/PoiMtlNet/tree/mobiwac}. Both data sources are public: the category-annotated Gowalla dump (\url{https://figshare.com/articles/dataset/gowalla_data/22126586}) and Massive-STEPS~\cite{wongso2025massivesteps}.}` — no new body sentence. (2) In 08_conclusion.tex delete the availability sentence so the section ends at `…anticipates both what a user will do next and where.` (3) In 05_setup.tex change `…is in the released code (Section~\ref{sec:conclusion}).` to `…is in the released code (footnote~\ref{fn:code}).` (4) Recompile; confirm exactly 8 pages; if the page-1 footnote breaks a float, revert (fallback: keep the sentence in the conclusion).

---

## 3. Rejected with reasons (author may veto)

**Frozen numbers / claims / evidence (named reviewer errors):**
- **G5** — strike the "+9": deletes a true frozen number (AZ +9.40).
- **G7** — "achieves superior performance": banned puffery; overclaims the untested Istanbul region delta.
- **G18** — compress bullet 2: deletes the mandated §1 TOST phrase and hides two datasets.
- **G29** — recast the balancer finding as a hypothesis: converts measured evidence into speculation.
- **G46** — "assume no leakage": the exact assumption behind the prior rejection; the clause is the verifiable mechanism.
- **G48** — striking "about the test visits" un-scopes the central no-leak claim.
- **G45** — the dedup sentence defines test-set composition (Table I Windows); cutting reopens the inflation question.
- **G25** — "not to make the task easier" scopes a claim and preempts the coarsened-target attack.
- **G24** — striking "from a shuffled copy" is ungrammatical and deletes the infomax mechanism.

**Load-bearing terms / venue fit:**
- **G4** — "per-visit context" is the coined term binding abstract → ablation.
- **G36** — "one model" is the thesis anchor, not an article.
- **G9** — the handover sentence is the deliberate MobiWac bridge.
- **G1** — margin rewrite calls the region a "category" (factually confusing).
- **G33** — "act as input features" breaks the "not as edges" contrast; drifts toward banned "serves as".
- **G19** — "while" wrongly signals contrast between two co-true positives (also mooted by G20).
- **G44** — "giving both tasks more examples" is the stride-1 design rationale; 5 words of cheap insurance.

**Style/typography with negative or zero value:**
- **E1** — "usually" would repeat the previous sentence.
- **E3** — de-mathing the deltas: text-mode minus renders as hyphen; body uses math mode.
- **E8** — past tense breaks voice; "(Gowalla)" is the provenance at first mention.
- **E10 / E12 / E14** — cross-ref expansions: pure length; E14 is ungrammatical as proposed.
- **G52** — "cannot" is correct formal English (codified).
- **E16** — the two-register percent convention is consistent; paper-wide swap is deadline churn.
- **E18** — standalone deletion orphans the reproducibility claim; superseded properly by F9 step 3.

---

## 4. Already done (30 ids, no action)

G2, G8, G10*(intro instance)*, G11, G12, G14, G15, G17, G21, G27, G30, G37, G39, G40, G41, G42, G43, G55, G58, G60, G61, G62, F5, F6, F7, E4, E6, E7*(obsolete)*, E11, E23, E25.
*(Count note: G10's flagged instance is done; the ADAPT above covers the surviving §III twin.)*

---

## 5. M1 methodology complaint — full treatment

**Verdict: SUBSTANTIATED, with a mitigating nuance; fix by minimal rebalancing, not the proposed ~30-line restructure.** Both judges agree on this.

**What is substantiated (measured):** mechanism prose for both contributions ≈ 500 words vs ≈ 740 words of integrity+metrics in §5 alone (~1,030 with §6.2's stats paragraph); the single §5.2 leak paragraph (338 w) outweighs the entire Check2HGI description (257 w); the paper has **zero displayed equations** — the +28…+40 headline's training objective, the 0.75/0.25 loss, and the 0.3/0.1 aux weights exist only as prose clauses. **The nuance:** the integrity emphasis is rational (the prior rejection was leak-based; the venue's named weakness is "real trace without a leakage protocol"), figs 1–2 carry real structure, and the code is released.

**Missing-explanations list, disposition:**

| # | Missing item | Disposition |
|---|---|---|
| 1 | Infomax objective formalized | **DEFER** (camera-ready). The settled ruling is "the infomax objective stays a citation"; also, the M1 sketch itself contains an unverified error (writes aux weights "0.3 L_rec + 0.5…" against the paper's 0.3/0.1) — proof that new technical sentences at T−1 are factual risk. |
| 2 | Concrete graph inventory (features, edges, weights, encoder) | **PARTIAL NOW**: the code-verified edge sentence (G32) fixes the two flagged ambiguities. Full inventory → camera-ready, verified against `research/embeddings/check2hgi/`. |
| 3 | Cross-attention walkthrough (direction, heads, softmax path) | **DEFER** (camera-ready, verify against `src/models/mtlnet.py`). |
| 4 | Readout = trained node state (links to spatial-stream input and §7 transductivity) | **DEFER** — discipline ranks this FIRST in the camera-ready queue (3 lines, high value). |
| 5 | Displayed training loss | **ADOPTED NOW** — G34 Eq. (1). |
| 6 | Worked example of one check-in | **DEFER** (nice-to-have; partly substituted by item 2). |
| 7 | Hyperparameter one-liner | **SKIP** — already satisfied by §5.4's closing configuration-release sentence. |

**Adopted tonight (the ruling-compliant core, net 0 lines):** Eq. (1) + tuned-provenance sentence in §4.2 (G34+G38, +3), the code-verified edge sentence in §4.1 (G32, +1), funded by deleting the §4.1 novelty paragraph (G35, −3/−4).

**Space plan, reconciled:**
- **Adopted:** G54 §5.2 prose compression, every number kept (−5); G35 (−3/−4); G16 (−2/−3); F9 URL move (net −1); F4 (−1); E15/E19 micro-savings. Total bank ≈ −13 to −16 lines — covers the +16–19 of adds; net ≈ 0 at 8 pages.
- **Rejected:** the §6.3 → §6.2 merge (both judges: the standalone External-validity subsection is a visible second validation layer); the ~30-line §4/§5.2/§6.3 restructure; any unverified technical sentence tonight.
- **⚠ CONDITIONAL AUTHOR CALL (the one escalation):** the *emergency reserve* — collapsing the six §5.2 audit numbers to per-ground ranges ("region within −0.33…+0.01; category within +0.29, at AL/AZ/FL", −3/−4 lines). The venue judge calls it a sanctioned 9.4 target; the discipline judge bars it (floor = the audit numbers). **Use only if the final build overflows 8 pages, and only with your explicit approval.**
- Camera-ready backlog (log in IMPROVEMENTS_BACKLOG.md): M1 items 4 → 2 → 3 → 1 → 6, each verified against code before writing.

---

## 6. Disagreements and escalations (how I ruled)

| Item | Venue | Discipline | Ruling |
|---|---|---|---|
| G3/F2 | full clause rewrite | keep; E2 suffices | **Discipline (minimal).** The sweep already names the quantity; E2's one word fixes the only residual ambiguity without touching a working abstract clause. |
| G6 | ACCEPT (+"next-region task") | REJECT (established shorthand) | **ACCEPT.** +2 words, reviewer-requested, symmetric with "next-category prediction"; no frozen verb touched. Easy to veto. |
| G13 | depersonalize, citations scoped | merge into Caruana sentence, joint cite | **Venue.** The joint citation attaches Caruana to a task-pair observation he never made; venue's version is attribution-correct and satisfies Germano's actual concern. |
| G20 | 3-sentence split | already fixed; don't touch | **Venue.** A confirmed out-of-field comprehension failure on the headline scaling claim outweighs the risk; the rewrite preserves every mandated element and is funded by G16. |
| F1 | minimal concrete-actions edit | REJECT (abstract complies) | **Venue (minimal edit only).** The author himself endorsed the kernel in-thread; the edit is zero-length with a booster as offset; the restructure both judges reject. |
| E9 | "summarized as follows:" | "as follows." | **Discipline.** "Summarized" is false (the bullets are the full statement). |
| G22 | keep the index clause | strike it | **Discipline.** Germano asked for the strike, the clause is decorative, and the saving funds G23 in the same paragraph. |
| G32 | full graph-inventory sentence | minimal edge/weight fix | **Discipline.** Smaller verified edit answers both flagged confusions; the full inventory is camera-ready work (consistent with the M1 ruling). |
| G33 | ACCEPT "act as" | REJECT | **REJECT.** "Enter as X, not as Y" is the coherent contrast; "act as" breaks it. |
| G44 | keep the rationale clause | strike it | **Keep (REJECT the strike).** It preempts a "why stride-1 overlap?" question from exactly the leak-suspicious reviewer class; the line isn't needed given G54's bank. |
| G51/G53/G49/G50 | separate micro-fixes | some REJECT/fold into G54 | **Converged:** all four resolve inside R‑G54; no separate edits. |
| G57 | name TX/CA | DONE, leave it | **Venue.** ~4 words, zero risk, directly answers "which states?". |
| E16 | numeral+\% paper-wide | keep the convention | **Discipline.** The two-register convention is defensible; T−1 churn rejected. Ripple: G47/G54 keep "percent"; E21's offset is the Holm trim, not "$2\%$". |
| F8 | promote closing claim sentence, delete aphorism | merge opener into mechanism sentence | **Discipline.** Smaller diff, keeps claim-first, preserves the frozen TOST phrase and the Germano-settled wording verbatim. |
| F9 | ACCEPT (new intro sentence + footnote) | ADAPT (footnote on existing sentence, retarget §5 pointer, fallback) | **Discipline.** The venue spec leaves §5.4's `(Section~\ref{sec:conclusion})` pointing at a conclusion that no longer contains the URL; discipline's step 3 closes that hole. |

**Internal judge contradictions caught and resolved:** (a) venue's M1 rejects "the conclusion URL shuffle" while venue's own F9 accepts it — resolved: the URL move proceeds via F9 on its own merits; M1's space plan does not depend on it. (b) discipline's M1 says "do NOT compress §5.2 tonight" while discipline's G54 compresses it — resolved: prose compression **keeping every number** (G54) is in; collapsing numbers to ranges is the barred move, held as the conditional reserve in §5.

**Formal escalations: none.** The single item requiring the author's live decision is the §5.2 number-range reserve (§5), and only if the final compile overflows 8 pages.