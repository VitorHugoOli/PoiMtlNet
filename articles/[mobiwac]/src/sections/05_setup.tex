% !TeX root = ../main.tex
% sections/05_setup.tex -- Section V of the MobiWac 2026 camera-ready (accepted 2026-08-26).
% Base text: the dissertation's Chapter 5 (articles/dissertacao/src/chapters/5_mobiwac/05_setup.tex),
% ported 2026-09-06 (CONSOLIDATION_PLAN 3.22). Camera-ready edits carry a dated comment beside them.
\section{Experimental Setup}
\label{sec:setup}

\subsection{Data}
\label{sec:setup-data}
Table~\ref{tab:datasets} summarizes the six datasets. Five come from Gowalla, collected from 2009 to 2011, and cover the U.S. states of Alabama, Arizona, Florida, Texas, and California~\cite{cho2011gowalla}. The sixth contains check-ins from Istanbul in the Massive-STEPS collection~\cite{wongso2025massivesteps}, which lets us examine the findings outside the United States.
% Data provenance (hidden): the Gowalla source is the public CC0 category-annotated dump,
% figshare.com/articles/dataset/gowalla_data/22126586 (gowalla_checkins + gowalla_spots_subset1/2,
% the exact files the ETL consumes; 36,001,959 check-ins). Date range MEASURED on the parquet
% 2026-07-09: 2009-01-21 .. 2011-08-16 -> "collected 2009 to 2011". The SNAP/cho2011 dump
% (Feb 2009-Oct 2010) is NOT the data source; cho2011gowalla is cited as the LBSN reference only.
% [2026-09-07, cut S1] The range sentence dropped (it named California as the upper end of a
% check-in range that Texas exceeds, Table I) together with the categories, the Istanbul mapping
% and the region unit, all stated in Section III-A, which stays the definitional home.


\subsection{Windows, splitting, and the integrity of the representation}
\label{sec:setup-windows}
\emph{Windows.} For each user with at least ten visits, we order the visits by time. We then form overlapping sliding windows of nine visits, one starting at each visit, and use the next visit as the target. Where several start positions share a target, we keep only the full-length window. The median time from the last visit in a window to its target ranges from 0.4 hours in Florida to 5.5 hours in Istanbul. Across the datasets, 5 to 27 percent of the targets occur more than 3 days later.

\emph{Splitting.} We use stratified five-fold cross-validation and split the data by user. All windows from one user remain in the same fold. Therefore, the overlapping windows cannot cross the training and test sets, and the visits of a test user never appear in training. The split is by user and not by time, so the evaluation does not measure drift or the effect of sporadic events. The held-out fold provides the validation data, and we do not reserve a third split. We stratify the split by the next-category label because the seven classes are imbalanced (Table~\ref{tab:datasets}).

\emph{Configuration search.} The search was not uniform across the three model families.
% [2026-09-06, camera-ready, DP-7] COMPRESSED, not deleted: GLOSSARY section 9.4 forbids deleting a
% fairness signal without an act-replacement, and this paragraph is the fairness signal behind the
% conservatism argument of Sections VI and VII. 206 -> ~135 words. Every fact kept: batch size at six
% datasets (five folds IST/AL/AZ, one fold TX); learning rate at four, with FL/CA on the
% large-dataset value that only the single-fold Texas screen tested; joint model searched at
% IST/AL/AZ over five folds and screened at FL, TX/CA transferred; dedicated region model one fixed
% configuration, logit-adjustment strength the only knob tested (set to zero). Dropped: the framing
% sentence "Reported results come from searched configurations ... rather than left to be inferred"
% and the sentence "why the dedicated category arm is a per-dataset-tuned comparison rather than one
% fixed recipe", which carried two banned words (arm, recipe; GLOSSARY section 3) and restated the
% previous clause. No number or dataset name changed.
% [2026-08-12] SCOPE CORRECTED TWICE, and the second correction overturned the first. A census of
% docs/results/closing_data/v18_sweep/ ALONE says batch size never varied at California; adding the
% earlier generation (docs/studies/closing_data/v17_completion/cat_ceiling_sweep/ and
% docs/results/closing_data/h2_v17_cat_ceiling/) shows eight California arms varying batch size over
% {2048, 8192}. The real gap is the LEARNING RATE: California holds a single point (0.005) in both
% generations, and so does Florida. Texas varies it over three points but on single folds.
% docs/studies/closing_data/v18/FINAL_SETTINGS.md corroborates, grading max_lr "[5f] small; [1f] TX
% flat 0.005-0.01" and crediting neither Florida nor California. Both the earlier "at every dataset"
% and my own "except California" were false; this states the two knobs separately, which is the only
% form the artifacts support.
% [2026-09-01, ERR-6/ERR-7] THIRD CORRECTION, on the batch-size clause the two above never touched.
% "on fewer folds at Florida and Texas" was wrong: FINAL_SETTINGS.md's dedicated-category family (a)
% grades batch size "[5f] AL; [1f] TX" and never mentions Florida -- the single-fold Florida screen at
% :97 ("[1f] FL null") belongs to the JOINT model family (c), already described one sentence later in
% this paragraph. Corrected to "on a single fold at Texas". Same source also fixes the closing clause:
% :77 shows the small searches selected 0.0025 (AL) / 0.0005 (AZ, IST), while FL/CA carry 0.005, the
% large-dataset value -- and the only search that ever touched that value is the single-fold Texas
% screen ("[1f] TX flat 0.005-0.01"), not "the smaller searches" the old wording implied. Verified: no
% probe fixes this sentence (positive-control check against two known-pinned fragments returned hits;
% seven fragments of this sentence returned zero). Do not restore "the value the smaller searches
% selected" -- Florida and California never had a search of their own to select it.
For the dedicated category model, batch size was searched at all six datasets and the learning rate at four of them, in both cases over five folds at Istanbul, Alabama and Arizona and on single folds at Texas. Florida and California carry the large-dataset value, which only the single-fold Texas screen tested, and the selected learning rate differs by dataset. For the joint model, batch size and the category-head learning rate were searched at Istanbul, Alabama, and Arizona over five folds and screened at Florida, with Texas and California carrying the configuration transferred from those searches. The dedicated region model uses one configuration throughout, with only the logit-adjustment strength tested and set to zero.
% [2026-09-06, cut C3] The pointer "Section VII returns to what the uneven joint search may mean"
% removed with the paragraph it pointed at.

\emph{Whole-dataset training.} The prediction models are trained without the validation users, but Check2HGI is trained once on the whole dataset and has seen their visits. Its training objective uses neither the next-category nor the next-region label. To check whether this gives the prediction models an advantage, we built a new representation for each fold using only its training users. This check covers three datasets at
one seed, on an earlier build of the representation.
% [2026-09-06, camera-ready, DP-9] "on an earlier build of the representation" ADDED. The
% whole-dataset-training check ran on the pre-v18 build (docs/studies/pre_freeze_gates/
% A4_RESULTS.md, seed 0, AL/AZ/FL) and was not re-run on the reported forward-only build; the
% numbers below are that measurement's (CAMERA_READY section 5, S14). Without the clause the
% paragraph defends the reported build with evidence taken on another one.
The differences were $-0.33$ to $+0.01$ Acc@10 points for region and $0.00$ to
$+0.29$ macro-F1 points for category.

However, the region comparison uses region vectors directly. For category, a graph built only
with training users has no visit vectors for validation users. We therefore used one
vector per place and kept only windows whose input places occurred in training. These
windows cover 67 to 87 percent of the validation data. Within this
coverage, whole-dataset training did not meaningfully change the results.

% [2026-09-06, camera-ready] The commented-out "Links between consecutive visits" block (the
% linear-probe screen, 0.4090 vs 0.4976, that the chapter kept as dead prose) was removed from this
% copy: it described the pre-v18 two-way graph ("connected in both directions"), which the reported
% build no longer has (Section IV states the forward-only construction). The chapter keeps the block.

\subsection{Metrics and statistical tests}
\label{sec:setup-metrics}
For the category task, we report macro-averaged F1 (macro-F1), the mean of the seven per-category F1 scores, so each category has equal weight. For the region task, we report accuracy at ten (Acc@10), the fraction of test visits whose true region is among the model's ten highest-scoring predictions. If the true region does not occur in the training data for that fold, the visit counts as an error. The reference point for this task is the dedicated model.

A non-significant difference does not provide evidence of a match. A written analysis plan, fixed during development and before any result was read, assigned a superiority test to next-category prediction and a non-inferiority test to next-region prediction. The plan did not define a superiority test for next-region prediction. Therefore, the two next-region gains in Section~\ref{sec:results-part2} are secondary results outside the plan. On next category the plan registered no equivalence margin, so a difference that fails the superiority test is reported as unresolved rather than as a match.

% [2026-09-02, AUTHOR RULING, errata Table B.5 of the supplement] The promise "We also report the
% registered Wilcoxon test on the 20 matched fold differences as a sensitivity analysis." is REMOVED:
% no Wilcoxon result appears in the text (the reporting sentence left 06_results.tex in ce5a7006).
% His words: "se nao reportamos numeros do Wilcoxon no texto, nao vejo porque adicionar essa
% informacao a mais e trazer mais complexidade para o leitor." What STAYS is the pre-registration
% trail: the plan registered the test, the seed-level footing is why it is not used (p floors at
% 0.0625 at n=4), and the code release carries both tests. "The primary analysis" -> "The reported
% analysis": with no sensitivity analysis named, "primary" implied a secondary that is not there.
The plan registered a paired Wilcoxon signed-rank test on the matched fold differences. The final
evaluation uses four seeds, each with its own user partition, so each configuration yields
$4\times5=20$ fitted models, and the reported analysis is a paired $t$-test on the four per-seed
means ($n{=}4$) with the 90\% confidence interval of the paired difference. At this footing the
exact one-sided Wilcoxon $p$-value cannot fall below $0.0625$, which is why the $t$ carries the
verdict. The code release includes both tests and documents this departure from the plan.
% [2026-09-06, cut C17] ~90 -> ~75 words; the four facts the ledger requires all kept: the
% registered test, the paired t on four per-seed means, the 0.0625 floor as the reason, and "the
% code release includes both tests".


A \emph{seed} is one complete repetition of the five-fold experiment. It determines the random initialization and the user partition, so each seed produces a different division of the users. Within one seed, the compared models use the same partition. The paired $t$ and TOST analyses compare the four per-seed means. We apply a Holm correction~\cite{holm1979} across the six next-category comparisons and, separately, across the six next-region comparisons.

The non-inferiority claim states that the joint model is no worse than the dedicated model by more than a two-point margin. We test this claim with the two one-sided tests (TOST) procedure~\cite{lakens2017tost}. The analysis plan also fixed the two-point margin in advance. A mobility-aware service acts on which region will be busy, rather than on a single position in the ranking (Section~\ref{sec:problem}). A two-point change in Acc@10 is below the level at which this service would behave differently. Across the four user partitions, the standard deviation of the paired difference ranges from 0.02 to 0.16 points.
% [2026-09-06, cut C15] The one-point-margin sentence (Istanbul/Arizona/Florida narrow enough; Alabama
% not) dropped; the registered margin is two points and the intervals are printed in Section V-B.
% [2026-09-06, AUTHOR-APPROVED errata, Table B.5 of the supplement] "it is the dataset with the
% largest region difference" -> "among these four datasets it has the largest region difference".
% Not false: within the four datasets the sentence discusses, Alabama's -0.88 is the largest. But
% the superlative carried no scope, and the cross-reference beside it lands the reader on Texas
% (+1.21) and California, whose differences are larger, and on "all six". The superlative is scoped;
% the cross-reference stays, because it is where the reader goes for the region result. No number,
% verdict or interval changes. Found by the mobiwac session (first reported as "false", retracted
% by them: the defect is ambiguity, not fact).
% WITHIN-SEED SHARING: WHAT IS ESTABLISHED AND WHAT IS NOT. Corrected 2026-08-05 after a reviewer found
% this stated as a measured fact. It is a CODE-LEVEL property, not something measured on the artifacts:
% src/data/folds.py builds the joint-model folds at :1247 and the dedicated next-task folds at :1159 with
% the SAME call, StratifiedGroupKFold(n_splits, shuffle=True, random_state=self.seed) split on the next-task
% rows grouped by user identifier. Given the same input rows and the same seed, both arms therefore receive
% the same partition by construction. That conditional matters: LANE2_OVERLAP_VALIDATION.md:75 records
% partitions NOT bit-identical across arms, and its own parenthetical gives the reason -- "(different
% windowed rows)". That caveat is about the overlap-versus-non-overlap comparison, where the arms consume
% different windowings, not about two models reading one windowing.
% STATISTICAL_PROTOCOL.md:187-190 asserts the same-folds property for the headline family and warns that "If
% a future re-run regenerates folds for one arm, the headline silently becomes unpaired". The reported runs
% did generate on the fly (see fold_partition_and_seeds.md §6), so the guarantee rests on the arms consuming
% identical rows rather than on a frozen cache. [VERIFY: a direct comparison of the saved fold indices of a
% joint run against a dedicated run at the same seed and dataset would settle it by measurement. Not run;
% no fold_indices*.pt survives under output/ in this checkout.]
% CORRECTED 2026-08-04, on the author's explicit instruction to fix the dissertation copy AND the
% original MobiWac source so the two stay in sync. He caught the underlying error in Appendix A of the
% dissertation and it reached this section too.
% WHAT WAS FALSE: the seed definition said the five-fold experiment is repeated "over the same folds",
% and the equivalence sentence said the precision is measured "on this fixed partition". Neither holds.
% src/data/folds.py passes the run's seed straight into StratifiedGroupKFold(random_state=...) (:1159,
% :1247, :1453); the only literal 42 in that file is the DEFAULT value of the seed parameter (:1061), and
% scripts/train.py:1874 passes seed=config.seed from --seed. The canon guard (train.py:1961) calls 42 the
% development seed and requires --seed in {0,1,7,100} for paper-grade numbers. This project's own
% LANE2_OVERLAP_VALIDATION.md:75 already recorded that partitions "are NOT bit-identical across arms".
% Measured on a synthetic 300-user frame, with a same-seed control that reproduces the partition exactly:
% roughly four users in five change fold when the seed changes. Full record and the reverification
% commands: articles/dissertacao/science/fold_partition_and_seeds.md.
% THE TESTS REPORTED HERE ARE UNAFFECTED, and the new wording says why rather than leaving it implicit.
% Pairing happens WITHIN a seed, where both arms share one partition, and the tests pair the per-seed
% MEANS (n=4), which never requires fold k of one seed to be fold k of another. No number in this
% section changes. What changes is that the reported intervals now correctly cover partition variability
% as well, which makes the equivalence claim stronger rather than weaker; the sentence therefore says
% "across these four partitions" instead of implying a single one.
% [COD-006a, round8, 2026-07-30] "The equivalence is well powered:" -> "The precision of the equivalence
% test is measured on this fixed partition:". Author decision, PENDENCIAS_RESOLVIDOS 5.2 (arquivado 2026-07-30), quoted: "Let's change only
% the second point about the: 'The equivalence is well powered'." The defect is a category error, not a
% number: "well powered" is PROSPECTIVE power, a property of a design under an assumed effect size, and at
% n=4 seeds it is not something this study estimated. What the sentence then presents is OBSERVED
% precision, the paired difference's standard deviation and the interval widths on the folds that were
% run, so the clause now names that instead. Both numbers are the ones already in the sentence and neither
% moved; no test, no verdict and no dataset scope changes. The earlier revision of this sentence also
% carried "giving power near 1.0 to declare equivalence when the true difference is near zero", which had
% already been cut before this round; nothing reinstates it.
% DELIBERATELY NOT TOUCHED, and this is the author's ruling, not an oversight: "fixed during development
% and before any result was read" stays. He scoped the decision to "only the second point", and the
% analysis plan genuinely was fixed before results were read (protocol committed 2026-06-21, first board
% cells 2026-06-22, provenance in the block above). check_audit_claims.py COD-006b therefore still reports
% NOT APPLIED, which is the CORRECT state for it: nobody asked for that removal.
% Applied to BOTH trees, since Chapter 5 is under review and the two texts must stay identical:
% src/chapters/5_mobiwac/05_setup.tex and articles/[mobiwac]/src/sections/05_setup.tex. Verified after
% editing: the two paragraph lines still differ only in the three "mobiwac:" \ref label prefixes.

\subsection{Baselines}
\label{sec:setup-baselines}
Every external system is run on our data, each with the adaptations its change of task requires:
HMT-GRN, STAN, and POI-RGNN on our user-disjoint folds, and ReHDM~\cite{li2025rehdm} under its
published chronological split.

\emph{Task comparisons.} For next-category prediction we re-implement POI-RGNN~\cite{capanema2023poirgnn}. It reads non-overlapping nine-visit windows,
whereas our category models read overlapping ones. We also use a Markov model over the recent
categories, with the best order selected per dataset, and, for region, a Markov-1 floor over region
transitions. The primary next-region comparison is
HMT-GRN~\cite{lim2022hmtgrn}, an end-to-end recurrent model. We keep its shared multi-task structure and add a region-transition prior built from the training data in
each fold. During development, our own region output used such a prior, and a version built from the whole
dataset inflated its region accuracy by 13 to 27 points, which is why the prior is built per fold.
Only the HMT-GRN comparison model uses this prior, and the reported joint and dedicated models do
not. We remove
its graph components and hierarchical beam search, which support exact next-place prediction, so
the resulting model predicts region as one of its original targets but is
not a reproduction of the complete published system. STAN~\cite{luo2021stan} is re-implemented with its own
representations and sequences, its output adapted to rank candidate regions.

\emph{Representation controls.} Two controls test whether the category gain comes from our representation design rather than from contextualization in general or from additional features: CTLE~\cite{lin2021ctle}, the closest prior contextual check-in representation, and the standard place embedding (HGI~\cite{huang2023hgi}) concatenated with raw per-visit features (Section~\ref{sec:results-part1}). HGI is pre-trained once on the whole dataset, as our
representation is, while CTLE is trained for each fold using training users only. All other choices, including the nine-visit window, were fixed during
development, and the released code provides the full training configuration for every model
(footnote~\ref{fn:code}).
% [2026-09-06, author-approved compression to ~280 words] The four fairness sentences are verbatim
% (run on our data; the 13-27 point prior disclosure with "only HMT-GRN"; the HMT-GRN adaptation
% "not a reproduction"; the POI-RGNN window asymmetry), and the CTLE/HGI training-scope sentence
% stays. Compressed: the Markov description (44 -> ~20 words), STAN (44 -> ~25), and the
% representation controls (113 -> ~60, now pointing at Section V-A, which reports them). The
% code pointer closes the block, per the author.
