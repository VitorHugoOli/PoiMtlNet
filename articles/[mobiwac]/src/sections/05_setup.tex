% Section 5: Experimental Setup (about 1.1 pages). Plan: PAPER_PLAN 4 / §5.
%   5.1 Data -> Table~\ref{tab:datasets}. Five Gowalla states + Istanbul (Massive-STEPS). Seven categories.
%   5.2 Windows, splitting, integrity of the representation (overlapping windows; user-split 5-fold CV;
%       no-leakage argument; per-fold train-only region-transition prior).
%   5.3 Metrics, superiority vs non-inferiority (macro-F1 category; Acc@10 region; paired Wilcoxon for beats;
%       TOST at a two-point margin for matches, justified a-priori with a deployment-grounded rationale + TOST power).
%   5.4 Baselines (POI-RGNN + Markov-9-cat; ReHDM + STAN faithful; CTLE + feature-concat (FL); HMT-GRN;
%       role 3 = cascade variant of OUR model (CSLSL/CatDM pattern, NOT a re-implementation) -- coupling-topology comparison).
\section{Experimental Setup}
\label{sec:setup}

\subsection{Data}
\label{sec:setup-data}
Table~\ref{tab:datasets} summarizes our six datasets. Five come from Gowalla, a location-based social network (collected 2009 to 2011), covering the U.S. states of Alabama, Arizona, Florida, Texas, and California
\cite{cho2011gowalla}; the sixth is Istanbul, from the Massive-STEPS collection
\cite{wongso2025massivesteps}, a non-U.S. check on the findings.
% Data provenance (hidden): the Gowalla source is the public CC0 category-annotated dump,
% figshare.com/articles/dataset/gowalla_data/22126586 (gowalla_checkins + gowalla_spots_subset1/2,
% the exact files the ETL consumes; 36,001,959 check-ins). Date range MEASURED on the parquet
% 2026-07-09: 2009-01-21 .. 2011-08-16 -> "collected 2009 to 2011". The SNAP/cho2011 dump
% (Feb 2009-Oct 2010) is NOT the data source; cho2011gowalla is cited as the LBSN reference only.
The states range from about 114,000 check-ins and 1,109 regions in Alabama to several million check-ins and 8,501 regions in California. Every dataset uses the same seven place categories: Community, Entertainment, Food, Nightlife, Outdoors,
Shopping, and Travel. The region unit is a census tract for the Gowalla states and a mahalle (a municipal neighborhood) for Istanbul.

\subsection{Windows, splitting, and the integrity of the representation}
\label{sec:setup-windows}
\emph{Windows.} For each user with at least ten visits, we build time-ordered overlapping nine-visit windows, one starting at each visit, with the next visit as the target, giving both tasks more examples. Because many of a user's windows would end on the same final visit, we keep one full-context window ending on that visit and drop the padded duplicates.

\emph{Splitting.} We split by user with stratified five-fold cross-validation, so all of a user's windows fall in the same fold and overlap cannot leak: a test user's visits never appear in training. The split is stratified on the next-category label because the seven-class task is imbalanced: Food is the majority class in every dataset, from about 25 percent of visits in Florida to 34 percent in Alabama.

\emph{Integrity of the representation.} We train the representation once on the whole dataset and feed it to both the dedicated single-task models and the joint model; we argue, and verify, that it holds no usable information about the test visits, for three reasons. First, its objective is label-free, contrasting real graph neighborhoods against shuffled ones with small auxiliary terms reconstructing input-derived quantities; it never sees the next-category or next-region target, so no label can travel from test to train through it. Second, because the graph is built over all places, the only test-side exposure is graph structure. We measured it directly, rebuilding the representation per fold from its training users only and re-running both tasks against the full-corpus version on the same folds. The effect is within fold noise, with no systematic
gain: on region, the change is $-0.33$ points at Alabama, $+0.01$ at Arizona, and $-0.12$ at Florida. On category,
it is $+0.29$ at Alabama, $+0.27$ at Arizona, and no change ($0.00$) at Florida, measured on the in-coverage visits whose places appear in training (about 67 percent at Alabama to 87 percent at Florida). This is a place-level proxy and the one residual we cannot fully measure: the representation cannot score a place never seen in training; on the measurable visits, the large majority, the audit finds no inflation.
% NOTE: numbers from the A4 train-users-only transductivity audit (docs/studies/pre_freeze_gates/A4_RESULTS.md):
% reg AL -0.33 / AZ +0.01 / FL -0.12 (5 folds); cat POI-proxy on in-coverage subset AL +0.29 (66.8%) / AZ +0.27 (71.9%) / FL +0.00 (86.9%).
% Author TODO: extend to CA/TX/Istanbul to widen coverage further (non-blocking; the gate is on null at AL+AZ+FL).
Third, the one component that could pass visit-to-visit information is a region-transition prior, a table of how often one region follows another that biases region scores; an earlier version built on the whole dataset inflated region accuracy by 13 to 27 points across states, so any such table is built per fold, from training data only, seed by seed. The joint and dedicated models do not use this prior; it survives only in the HMT-GRN baseline (Section~\ref{sec:setup-baselines}). The place-level baseline representation (HGI) is pre-trained once on the whole dataset, like ours; the contextual baseline (CTLE) is pre-trained per fold on training users only, so no baseline input is held to a weaker standard than ours.

\subsection{Metrics and Statistical Tests}
\label{sec:setup-metrics}
For the category task, we report macro-averaged F1 (macro-F1), which averages the per-class F1 so each of the seven categories counts equally; its reference point is the majority-class floor, the macro-F1 of always predicting the most common category. For the region task, we report accuracy at ten
(Acc@10), the fraction of test visits whose true region appears among the model's ten highest-scoring guesses
(a visit whose true region is absent from that fold's training data counts as an error); its
reference point is the dedicated single-task model.

Where the joint model is meant to outperform, we test \emph{superiority}: at the four datasets with four seeds (full reruns of the experiment under different random initializations), a paired test over the per-seed means with a Holm correction across the comparisons; at the two single-seed states, a paired Wilcoxon signed-rank test over the folds. Where it is meant only to stay even, we test \emph{non-inferiority}, that it is no worse than the dedicated model by more than a two-point margin, with a two-one-sided-tests (TOST) procedure. We fix the two-point margin in advance because a mobility-aware service acts on which region will be busy, not on a single rank position (Section~\ref{sec:problem}), so a two-point shift in Acc@10 is below the granularity at which it would behave differently (for scale, with 520 to 8,501 regions, a random top-ten guess includes the true region at most about two percent of the time). The equivalence is well powered: the paired difference has a small standard deviation (0.04 to 0.15 points at the four multi-seed
datasets), giving power near 1.0 to reject a true two-point gap, so a non-inferiority conclusion reflects a real match.

\subsection{Baselines}
\label{sec:setup-baselines}
The comparisons play three roles. The first role is the per-task state of the art, on our data, under user-disjoint five-fold protocols. For next-category, this is POI-RGNN \cite{capanema2023poirgnn}, re-implemented from its published architecture and hyperparameters, and a Markov baseline over recent categories, reporting the strongest order per dataset (Markov-K, up to the nine-visit window). For next-region, this is HMT-GRN \cite{lim2022hmtgrn}, our primary comparison, an end-to-end recurrent model (trained as one piece from the raw check-ins) run on the same data, seed, and folds; we keep its shared multi-task skeleton, add a region-transition prior built per fold from training data, and drop its graph components and hierarchical beam search, which serve the next-place head that we do not predict. It is a region-native model, not a reproduction of the complete published system. We also run STAN \cite{luo2021stan}, re-implemented from its published architecture and trained from the raw check-ins with its own embeddings and sequence construction under one fixed configuration, and a ReHDM \cite{li2025rehdm} reference reported under its own published protocol. The second role is two representation controls attributing the category gain to our specific design, not to contextualization in general or extra features: CTLE \cite{lin2021ctle}, the closest prior contextual check-in embedding, run end to end at Florida and with fixed weights at Alabama, Arizona, and Istanbul; and a feature-concatenation control appending raw per-visit features to a standard place embedding under the same model. The third role compares the two ways of coupling the tasks. The dominant published coupling is the directed cascade, predicting the category first and conditioning the place prediction on it \cite{ye2013nextmove, yu2020catdm}; CSLSL \cite{huang2024cslsl}, its current form, reports the chain outperforming a parallel variant sharing one trunk. We test the cascade inside our own model rather than re-implementing those systems: we remove the shared trunk, where the two streams exchange information, and feed the predicted category forward into the region pathway, keeping the representation, outputs, and training configuration unchanged. This isolates the coupling topology (chain versus parallel) at no additional parameter or compute cost: the chain form drops the shared trunk and adds a conditioning projection of about 1,000 parameters. The standard place-level embedding \cite{huang2023hgi} isolates what the per-visit representation adds. The full training configuration for every model is in the released code (Section~\ref{sec:conclusion}).
