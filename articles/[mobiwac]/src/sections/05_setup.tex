% Section 5: Experimental Setup (about 1.1 pages). Plan: PAPER_PLAN 4 / §5.
%   5.1 Data -> Table~\ref{tab:datasets}. Five Gowalla states + Istanbul (Massive-STEPS). Seven categories.
%   5.2 Windows, splitting, integrity of the representation (overlapping windows; user-split 5-fold CV;
%       no-leakage argument; per-fold train-only region-transition prior).
%   5.3 Metrics, superiority vs non-inferiority (macro-F1 category; Acc@10 region; paired Wilcoxon for beats;
%       TOST at a two-point margin for matches, justified a-priori with a deployment-grounded rationale + TOST power).
%   5.4 Baselines (POI-RGNN + Markov-9-cat; ReHDM + STAN faithful; CTLE + feature-concat (FL); HMT-GRN;
%       role 3 = cascade variant of OUR model (CSLSL/CatDM pattern, NOT a re-implementation) -- coupling-topology comparison).
\section{Experimental Setup}
\label{sec:setup}

\subsection{Data}
\label{sec:setup-data}
Table~\ref{tab:datasets} summarizes our six datasets. Five come from Gowalla, a location-based social network, covering the U.S. states of Alabama, Arizona, Florida, Texas, and California
\cite{cho2011gowalla}; the sixth is Istanbul, from the Massive-STEPS collection
\cite{wongso2025massivesteps}, a non-U.S. check on the findings.
% Data provenance (hidden): the Gowalla source is the public CC0 category-annotated dump,
% figshare.com/articles/dataset/gowalla_data/22126586 (gowalla_checkins + gowalla_spots_subset1/2,
% the exact files the ETL consumes; 36,001,959 check-ins). Date range MEASURED on the parquet
% 2026-07-09: 2009-01-21 .. 2011-08-16 -> "collected 2009 to 2011". The SNAP/cho2011 dump
% (Feb 2009-Oct 2010) is NOT the data source; cho2011gowalla is cited as the LBSN reference only.
The states range from about 114,000 check-ins and 1,109 regions in Alabama to about 3.2 million check-ins and 8,501 regions in California. Every dataset uses the seven place categories of Section~\ref{sec:problem}; Istanbul's source collection maps its own labels onto them. The region unit is a census tract for the Gowalla states and a mahalle for Istanbul.

\subsection{Windows, splitting, and the integrity of the representation}
\label{sec:setup-windows}
\emph{Windows.} For each user with at least ten visits, we build time-ordered overlapping sliding windows of nine visits, one starting at each visit, with the next visit as the target, giving both tasks more examples. Near the end of a user's history, every start position within the last nine visits yields a shorter, padded window whose target is the same final visit; we keep only the full-length window ending there and drop these padded duplicates. The resulting prediction horizon is short but heavy-tailed: the median time from the last visit in a window to its target ranges from 0.4 hours in Florida to 5.5 hours in Istanbul, while 5 to 27 percent of targets lie over 3 days ahead.

\emph{Splitting.} We split by user with stratified five-fold cross-validation, so all of a user's windows fall in the same fold and overlap cannot leak: a test user's visits never appear in training. The held-out fold is the validation data; we reserve no third split. The split is stratified on the next-category label because the seven-class task is imbalanced: Food is the majority class in every dataset, from about 25 percent of visits in Florida to 34 percent in Alabama (Table~\ref{tab:datasets}).

\emph{Integrity of the representation.} We train the representation once on the whole dataset and feed it to every model, dedicated and joint. We verify that it passes no usable information about the test visits, on three grounds. First, its training objective is label-free: it contrasts real graph neighborhoods against shuffled ones and never sees a next-category or next-region target. Second, we measured what the whole-dataset graph could pass to the test side. Rebuilding the representation per fold, from that fold's training users only, moves both tasks by at most a third of a point (region $-0.33$ to $+0.01$; category $0.00$ to $+0.29$, at Alabama, Arizona, and Florida), within fold noise. This measurement covers the visits whose places appear in training (67 to 87 percent); visits to places unseen in training are the one part it cannot reach. Third, the one component that could pass information between visits, a region-transition prior (a table of how often one region follows another), is built per fold from training data only, after an earlier whole-dataset version inflated region accuracy by 13 to 27 points. Our joint and dedicated models do not use this prior; it appears only in the HMT-GRN baseline (Section~\ref{sec:setup-baselines}). The baseline representations meet the same standard: HGI is pre-trained once on the whole dataset, like ours, and CTLE per fold on training users only.
% NOTE: numbers from the A4 train-users-only transductivity audit (docs/studies/pre_freeze_gates/A4_RESULTS.md):
% reg AL -0.33 / AZ +0.01 / FL -0.12 (5 folds); cat POI-proxy on in-coverage subset AL +0.29 (66.8%) / AZ +0.27 (71.9%) / FL +0.00 (86.9%).
% Author TODO: extend to CA/TX/Istanbul to widen coverage further (non-blocking; the gate is on null at AL+AZ+FL).

\subsection{Metrics and statistical tests}
\label{sec:setup-metrics}
For the category task, we report macro-averaged F1 (macro-F1), which averages the per-class F1 so each of the seven categories counts equally; its reference point is the majority-class floor, the macro-F1 of always predicting the most common category. For the region task, we report accuracy at ten
(Acc@10), the fraction of test visits whose true region appears among the model's ten highest-scoring guesses
(a visit whose true region is absent from that fold's training data counts as an error); its
reference point is the dedicated model.

A claimed gain and a claimed match require different tests, because a difference that fails to reach significance is not
by itself evidence of a match. A written analysis plan, fixed during development and before any result was read,
assigned one test to each task, with the two-point margin pinned there: \emph{superiority} for next-category,
\emph{non-inferiority}
for next-region. It assigned the tests per task, not per dataset, and did not cover next-region superiority, so the four
next-region gains of Section~\ref{sec:results-part2}
are secondary results outside it. The plan registered a paired Wilcoxon signed-rank test; at four seeds its exact
one-sided $p$ cannot fall below $0.0625$, so we report a paired $t$ on the per-seed means, with the 90\%
confidence interval of the paired difference, and the registered test alongside it. Both, and this departure, are
released with the code. A \emph{seed}
is one complete repetition of the five-fold experiment: it sets the random initialization and the user partition, so
each seed draws its own division of the users, and within a seed both models are built from the same partition; folds
and seeds are the two axes of repetition. On every dataset, both models use four seeds ($4\times5=20$
measurements) and the tests pair the per-seed means ($n{=}4$), with a Holm correction~\cite{holm1979}
across the six next-category comparisons and, separately, across the four next-region ones. The \emph{non-inferiority}
claim is that the joint model is no worse than the dedicated model by more than a two-point margin; we test it with the
two one-sided tests (TOST) procedure~\cite{lakens2017tost}
. The two-point margin is fixed in advance as well: a mobility-aware service acts on which region will be busy, not on a
single rank position (Section~\ref{sec:problem}
), so a two-point shift in Acc@10 is below the granularity at which such a service would behave differently. The
precision of the equivalence test is measured across these four partitions: the paired difference's standard deviation
is 0.01 to 0.18 points across the datasets, and the intervals pass a margin as small as one point at Alabama and Arizona
(Section~\ref{sec:results-part2}).
% WITHIN-SEED SHARING: WHAT IS ESTABLISHED AND WHAT IS NOT. Corrected 2026-08-05 after a reviewer found
% this stated as a measured fact. It is a CODE-LEVEL property, not something measured on the artifacts:
% src/data/folds.py builds the joint-model folds at :1247 and the dedicated next-task folds at :1159 with
% the SAME call, StratifiedGroupKFold(n_splits, shuffle=True, random_state=self.seed) split on the next-task
% rows grouped by user identifier. Given the same input rows and the same seed, both arms therefore receive
% the same partition by construction. That conditional matters: LANE2_OVERLAP_VALIDATION.md:75 records
% partitions NOT bit-identical across arms, and its own parenthetical gives the reason -- "(different
% windowed rows)". That caveat is about the overlap-versus-non-overlap comparison, where the arms consume
% different windowings, not about two models reading one windowing.
% STATISTICAL_PROTOCOL.md:187-190 asserts the same-folds property for the headline family and warns that "If
% a future re-run regenerates folds for one arm, the headline silently becomes unpaired". The reported runs
% did generate on the fly (see fold_partition_and_seeds.md §6), so the guarantee rests on the arms consuming
% identical rows rather than on a frozen cache. [VERIFY: a direct comparison of the saved fold indices of a
% joint run against a dedicated run at the same seed and dataset would settle it by measurement. Not run;
% no fold_indices*.pt survives under output/ in this checkout.]
% CORRECTED 2026-08-04, on the author's explicit instruction to fix the dissertation copy AND the
% original MobiWac source so the two stay in sync. He caught the underlying error in Appendix A of the
% dissertation and it reached this section too.
% WHAT WAS FALSE: the seed definition said the five-fold experiment is repeated "over the same folds",
% and the equivalence sentence said the precision is measured "on this fixed partition". Neither holds.
% src/data/folds.py passes the run's seed straight into StratifiedGroupKFold(random_state=...) (:1159,
% :1247, :1453); the only literal 42 in that file is the DEFAULT value of the seed parameter (:1061), and
% scripts/train.py:1874 passes seed=config.seed from --seed. The canon guard (train.py:1961) calls 42 the
% development seed and requires --seed in {0,1,7,100} for paper-grade numbers. This project's own
% LANE2_OVERLAP_VALIDATION.md:75 already recorded that partitions "are NOT bit-identical across arms".
% Measured on a synthetic 300-user frame, with a same-seed control that reproduces the partition exactly:
% roughly four users in five change fold when the seed changes. Full record and the reverification
% commands: articles/dissertacao/science/fold_partition_and_seeds.md.
% THE TESTS REPORTED HERE ARE UNAFFECTED, and the new wording says why rather than leaving it implicit.
% Pairing happens WITHIN a seed, where both arms share one partition, and the tests pair the per-seed
% MEANS (n=4), which never requires fold k of one seed to be fold k of another. No number in this
% section changes. What changes is that the reported intervals now correctly cover partition variability
% as well, which makes the equivalence claim stronger rather than weaker; the sentence therefore says
% "across these four partitions" instead of implying a single one.
% Probes: R13-foldseed7, R13-foldseed8.
% [COD-006a, round8, 2026-07-30] "The equivalence is well powered:" -> "The precision of the equivalence
% test is measured on this fixed partition:". Author decision, PENDENCIAS 5.2, quoted: "Let's change only
% the second point about the: 'The equivalence is well powered'." The defect is a category error, not a
% number: "well powered" is PROSPECTIVE power, a property of a design under an assumed effect size, and at
% n=4 seeds it is not something this study estimated. What the sentence then presents is OBSERVED
% precision, the paired difference's standard deviation and the interval widths on the folds that were
% run, so the clause now names that instead. Both numbers are the ones already in the sentence and neither
% moved; no test, no verdict and no dataset scope changes. The earlier revision of this sentence also
% carried "giving power near 1.0 to declare equivalence when the true difference is near zero", which had
% already been cut before this round; nothing reinstates it.
% DELIBERATELY NOT TOUCHED, and this is the author's ruling, not an oversight: "fixed during development
% and before any result was read" stays. He scoped the decision to "only the second point", and the
% analysis plan genuinely was fixed before results were read (protocol committed 2026-06-21, first board
% cells 2026-06-22, provenance in the block above). check_audit_claims.py COD-006b therefore still reports
% NOT APPLIED, which is the CORRECT state for it: nobody asked for that removal.
% Applied to BOTH trees, since Chapter 5 is under review and the two texts must stay identical:
% src/chapters/5_mobiwac/05_setup.tex and articles/[mobiwac]/src/sections/05_setup.tex. Verified after
% editing: the two paragraph lines still differ only in the three "mobiwac:" \ref label prefixes.
% ANALYSIS-PLAN WORDING (2026-07-25 audit, author-approved). Sources for this paragraph:
%   docs/studies/closing_data/v17_completion/STATISTICAL_PROTOCOL.md -- §1 family table (cat: superiority;
%   reg: NON-INFERIORITY only, per task, all six datasets) + §5.2 (Holm within the six-dataset cat set;
%   TOST cells explicitly NOT pooled into it) + §3.2 (delta_reg = 2 pp, pinned pre-unblind).
%   Protocol committed 2026-06-21 (c96c67e3); first board cells 2026-06-22 -> the a-priori claim holds
%   for what the document contains.
%   Wilcoxon -> paired t departure + the 0.0625 exact floor at n=4: v17_completion/stats_n20/RESULTS.md,
%   "Deviation log (protocol §8)" entries 1-3. Reproduced 2026-07-25: every n=4 exact one-sided Wilcoxon
%   sits at 0.0625; the paired t gives Holm-adj p <= 1.0e-06 at all six cat cells.
%   NO region-superiority family is registered anywhere in the protocol -> the four reg gains are labeled
%   secondary here. Holm scope sentence now names both families explicitly (was "across the comparisons",
%   which implied a coverage the analysis does not have).
%   REGISTERED TEST NOW RUNS (2026-07-25): the four Istanbul dedicated-category per-fold sidecars were
%   recovered from the A40 and committed at h3_istanbul/step3_runs/cat_ceiling_perfold/ (per-seed means
%   match cat_ceil_s*.txt exactly), closing stats_n20/RESULTS.md LIMITS #2. The complete six-dataset
%   pre-registered family (per-fold n=20 paired one-sided Wilcoxon, Holm m=6) now runs for the first time:
%   every dataset rejects at Holm-adj p = 5.7e-06, 20/20 folds positive, exact n=20 floor 9.54e-07.
%   The four reg cells reject in their own m=4 family (Holm-adj 3.8e-06). NO verdict changed.
%   Generator + gated output: stats_n20/m2_prereg_perfold.py, m2_prereg_output.txt (24/24 board gates).
%   The duplicated scale sentence ("a random top-ten guess ... two percent") was cut here to fund these
%   lines; 06_results.tex:35-36 carries it verbatim, which is where the reader needs the anchor.

\subsection{Baselines}
\label{sec:setup-baselines}
The comparisons play three roles. The first role is played by the per-task state of the art, on our data, under user-disjoint five-fold protocols. For next-category, this is POI-RGNN \cite{capanema2023poirgnn}, re-implemented from its published architecture and hyperparameters, and a Markov baseline that predicts the category that most often follows the recent ones (Markov-K, best order per dataset); for region, we also compute a first-order Markov floor over region transitions, under the same sliding-window protocol and fold splits as our models. For next-region, the primary comparison is HMT-GRN \cite{lim2022hmtgrn}, an end-to-end recurrent model (trained as one piece from the raw check-ins) evaluated on the same data, folds, and initialization. We keep its shared multi-task skeleton and add a region-transition prior built per fold from training data. We drop its graph components and hierarchical beam search, which exist to serve its next-place target, a target that we do not predict. The result is a region-native model (region is one of its own prediction targets), not a reproduction of the complete published system. We also run STAN \cite{luo2021stan}, re-implemented from its published architecture and trained from the raw check-ins with its own embeddings and sequence construction under one fixed configuration. We adapt its output layer to rank region candidates, spatial objects that still support its candidate-distance matching; we do not adapt it to category, where distance matching has no meaning. A ReHDM \cite{li2025rehdm} reference is reported under its own published protocol.

The second role is played by two representation controls that attribute the category gain to our specific design, not to contextualization in general or to extra features: CTLE \cite{lin2021ctle}, the closest prior contextual check-in embedding, run end to end at Florida and with frozen weights (no fine-tuning) at Alabama, Arizona, and Istanbul; and a feature-concatenation control appending raw per-visit features to a standard place embedding under the same model.

The third role is a comparison between the two ways of coupling the tasks. The dominant published coupling is the directed cascade, predicting the category first and conditioning the place prediction on it \cite{ye2013nextmove, yu2020catdm, huang2024cslsl}. We test the cascade inside our own model rather than re-implementing those systems: we remove the shared trunk, where the two streams exchange information, and feed the predicted category forward into the region pathway, keeping the representation, outputs, and training configuration unchanged. This isolates the coupling topology (chain versus parallel) at no additional parameter or compute cost: the chain form drops the shared trunk and adds a conditioning projection of about 1,000 parameters. A final control, the standard place-level embedding \cite{huang2023hgi}, isolates what the per-visit representation adds. All other choices, including the nine-visit window, were fixed during development; the full training configuration for every model is in the released code (footnote~\ref{fn:code}).
