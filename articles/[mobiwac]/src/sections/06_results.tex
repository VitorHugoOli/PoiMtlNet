% !TeX root = ../main.tex
% ============================================================================
% [2026-08-12] EVIDENCE BASE REPLACED. Read this before any comment below it.
%
% Every live value in this file comes from the v18 board:
%   docs/results/closing_data/v18/joint_best_perfold.json   (24 joint cells, per fold)
%   docs/studies/closing_data/v18/data/v18_results.json     (dedicated arms, per fold)
%
% COMMENTS BELOW THIS BLOCK MAY DESCRIBE THE SUPERSEDED BOARD. They are kept because they
% record real editorial decisions, and a decision does not stop being a decision when its
% numbers change. Do NOT read their CELLS, COUNTS, or VERDICTS as current, and do not
% restore a sentence from them without re-deriving it from the sources named above.
% In particular, any "honesty rule" phrased as an invariant of the old ladder (category
% beating at every dataset, region at four) is a record of what was once true, not a rule.
% ============================================================================
% sections/06_results.tex -- Section VI of the MobiWac 2026 camera-ready (accepted 2026-08-26).
% Base text: the dissertation's Chapter 5 (articles/dissertacao/src/chapters/5_mobiwac/06_results.tex),
% ported 2026-09-06 (CONSOLIDATION_PLAN 3.34). Camera-ready edits carry a dated comment beside them.
\section{Results}
\label{sec:results}

\subsection{The check-in-level representation improves the category task}
\label{sec:results-part1}
The check-in-level representation reaches a higher next-category macro-F1 than a standard place
embedding (HGI~\cite{huang2023hgi}) at every dataset
(Table~\ref{tab:substrate}). Only the input representation differs, a vector per visit versus a vector per place. The check-in-level column is the dedicated category model of Table~\ref{tab:results} at seed 0, and the place-level column swaps its input.
% [2026-09-06, camera-ready] "both arms ... the place-level arm" -> "the two columns ... the
% place-level column": "arm" is banned for this audience (GLOSSARY section 3). No claim changed.
The gaps run from $+0.23$ to $+6.29$ points and stay under one point at the three largest
datasets, so what the comparison establishes is a consistent direction rather than a large effect.

The benefit is category-only by construction: the region task reads the region-level vectors of
the same graph, not the per-visit vectors (Section~\ref{sec:method-model}).
% [2026-09-06, cut C11] The geometry paragraph (silhouette 0.57 vs 0.00, nearest-neighbor purity
% 0.98 vs 0.78, both from an earlier build and aggregated per place; CAMERA_READY C1c) is OUT
% entirely, as the mobiwac list requires; the category-only sentence, which used to rest on "the
% same geometry does not separate regions", now rests on the construction (the region task never
% reads the per-visit vectors). The CTLE control and the feature-concatenation control STAY.
CTLE~\cite{lin2021ctle}, the closest prior contextual embedding, also gives each visit its own vector, but it pretrains on place identifiers and timestamps alone, so the category vocabulary never enters its training, whereas our graph reads each visit's category and time of day as input features. At Florida, we fine-tune CTLE together with the task model, using its authors' defaults and the same 64 dimensions and windowing as ours. It reaches $33.45$ macro-F1 at its best epoch and $29.69$ at its final one (an epoch is one pass over the training data), below the place embedding under the same best-epoch rule.
% [2026-09-06, camera-ready, plan 3.35(a)] "about two points below the place embedding" DROPPED:
% the two-point magnitude was measured against the pre-v18 place column; against the reported
% place-level Florida value (37.13, Table III) the CTLE best-epoch score of 33.45 is 3.68 below, a
% number this paper does not print (AGENT_GUARDRAILS N2: no derived quantity in prose). The
% ordering, which is what the sentence claims, is unchanged. Same form as the src_fix text.
% [2026-09-06, later] This block had been inserted mid-sentence and swallowed the frozen-weights
% CTLE result (AL/AZ/Istanbul) into its last line; repaired. Comments live between sentences.
With frozen weights, fed to the same single-task model, CTLE repeats the ordering at Alabama, Arizona, and Istanbul. These CTLE results establish the ordering between the two representation families rather than a
difference against the values in Table~\ref{tab:substrate}.
% [2026-09-02, AUTHOR-APPROVED, errata Table B.5 of the supplement; Q13] Two defects in the
% concatenation-control sentences. (1) NUMBER: "under a tenth of the place-to-check-in gap" is
% contradicted by Table 9, printed right below: the gaps are +1.62 (AL), +2.58 (AZ), +0.23 (FL),
% so the cited gains of +2.0 / +1.7 / +0.8 are 123% / 66% / 348% of them, not under a tenth. The
% delivered gains and the delivered Table 9 gaps are kept as the pair (internally consistent);
% the re-run in wrapup/erratas/errata_Q13_concatenation_scope.tex (+1.73/+1.70/+1.02 vs
% 1.56/2.50/0.21) is NOT quoted: it does not reproduce Table 9 fold by fold and the cause is open.
% (2) INFERENCE: "the gain therefore comes from the hierarchical per-visit representation, not
% from ... feature injection" was the reverse of what the numbers show, and the drafted errata's
% inversion ("comes mostly from the per-visit features ... not from the hierarchical structure")
% overshoots the other way. The text now states what the control measures and no more: most of
% the category difference is already available in the raw features; the remainder is small and
% this control does not separate it. Scope is the CATEGORY axis, inherited from "the benefit is
% category-only" above; nothing is asserted about what the hierarchy contributes (on the delivered
% substrate the region pathway is detached from the check-in encoder by construction,
% Check2HGIModule.py:653, so any such statement would be a tautology read as a finding).
A feature-concatenation control joins the place embedding with the same raw
per-visit features that our graph reads (the category one-hot plus hour and
day-of-week encodings) and feeds the result to the same single-task model. On
the category axis, at Alabama, Arizona, and Florida this control raises the
place embedding by $+2.0$, $+1.7$, and $+0.8$ macro-F1, against
place-to-check-in gaps of $+1.62$, $+2.58$, and $+0.23$ at the same states
(Table~\ref{tab:substrate}). Most of the category difference is
therefore already available in the raw per-visit features, and this control does not separate
what the check-in-level representation adds beyond them on this axis.
% [2026-09-07, Fable review] "is small" dropped: at Alabama and Florida the control's gain exceeds
% the place-to-check-in gap, so "small" read as small-and-positive, which the numbers do not say.

% Table (representation): check-in-level (Check2HGI) vs place-level (HGI) next-category macro-F1,
% same single-task model, configuration and folds (seed 0 x 5 folds), sliding windows throughout.
% [2026-09-06, camera-ready] The pre-v18 provenance block that stood here (Istanbul re-footing,
% per-column values and margins of the leaked generation) was removed under CONSOLIDATION_PLAN 3.41;
% the live cells and their sources are in CAMERA_READY.md section 3.3 and in tables/tbl2_substrate.tex.

% [NEEDS SIGN-OFF 25 | CLOSED] Author: keep the figure, but the pointer sentence was loose -- it
% needed to give the reader a reason to look. Rewritten 2026-08-04 to say what the figure adds over
% the prose (the two measures side by side, so the size of the gap is readable at a glance).
% The sentence above the figure carries the four numbers and is REPRODUCED FROM THE SUBMITTED
% ARTICLE, so it was deliberately NOT restructured to point at the figure instead: that would be a
% parity-bearing edit for no reader gain. Only the dissertation-only pointer sentence changed.
% [2026-09-06, camera-ready] FIGURE 3 AND ITS POINTER REMOVED TOGETHER, as the comment that stood
% here required ("Remove figure and pointer together, or neither -- gate L4 requires every float to
% be referenced in prose"). The figure was cut from the 8-page build by the author's decision of
% 2026-07-09 and was restored only in the dissertation, which has no page limit; the article's page
% budget is 8 again. The asset stays at figs/fig3_embquality.{py,tex,pdf} and still carries
% PRE-v18 constants measured on the archived design_k engine -- see CAMERA_READY.md section 5, row
% C1c, and decision D13. Regenerate before any reuse. The two separability numbers themselves stay
% in the prose above.

% [REV-029, 2026-07-26] Declaration moved to the paragraph boundary: it previously sat inside the
% pointer paragraph above, with no blank line, so the float was declared mid paragraph.

\subsection{One model, two tasks}
\label{sec:results-part2}
% [REV-029, 2026-07-26] Table 3 and Figure 7 were both declared HERE, before any prose of this
% subsection, which is why the rendered build put them ahead of the heading that introduces them
% (and made p.68 floats-only). Both declarations were moved down to the paragraph boundary that
% follows their first reference; no prose was reordered and declaration order (table before
% figure) is preserved, so the float numbers are unchanged.
One model serves both tasks, and every difference on both axes stays inside the bound its axis
supports. On next region it
outperforms the dedicated model at Texas and California, the two datasets with the most regions, and
at the other four it stays within the two-point margin registered before any result was read
(statistical non-inferiority, assessed with two one-sided tests). On next category it outperforms the
dedicated model at Florida, and at every dataset the difference between the two is equivalent to zero
within half a point, simultaneously across all six.

Half a point is worth placing against the two comparisons that give the category axis its scale. The
input representation moves the same metric by $+0.23$ to $+6.29$ points, up to thirty-two times the
largest difference the choice of architecture makes, and the joint model stands at least $3.06$ points
above the strongest external baseline at every dataset. Whether one model or two produce the category
prediction is therefore the smallest of the three effects at work here.
% [round9c, PENDENCIAS_RESOLVIDOS 2.11 (arquivado 2026-08-02), author decision: option B, 2026-07-30] The lead read "One model
% outperforms or matches the dedicated models on both tasks", which collapses the four-of-six /
% TOST partition this paper's own section 5.3 and section 6.2 state precisely. Applied to BOTH
% TREES in the same pass and verified textually identical afterward, per NORTH_STAR's regime for
% the under-review chapter: articles/[mobiwac]/src/sections/06_results.tex carries the same
% sentence, and the paper's ERRATA.md records it. No number, test or verdict changes; Arizona is
% not upgraded. Verified identical with the comment-stripping sweep, across line wraps.
For scale: always predicting the most common category reaches between $5.7$ and $7.3$ macro-F1
depending on the dataset (the majority-class floor, lowest at Florida, whose category mix is the
most even), and a random region top-ten guess is right at most about two percent of the time
(Section~\ref{sec:setup-metrics}).

% [2026-09-06, cut C8] Coverage restatement -> pointer; Section IV-B and the Table II footnote carry it.
Every reported model is one saved model per fold, read at its validation-selected epoch: each
dedicated model at its task's best epoch, and the joint model at the epoch selected by the geometric
mean of its two validation metrics, with both tasks read from that one model, which is what a
deployed system can serve. Reading each task at its own best epoch instead would describe no single
saved model, and it is more favorable to the joint model, by at most $0.23$ macro-F1 and $0.93$
Acc@10 points at any one seed, enough to turn four further category comparisons and two further
region comparisons into improvements that survive the same Holm correction. Every verdict in this
paper is the stricter convention's.
% [2026-09-06, cut C6] Epoch-convention paragraph ~150 -> ~100 words; the convention, the 0.23 /
% 0.93 worst-seed gaps and the verdict inversions (four category, two region) all kept.
% [2026-09-06, camera-ready] "checkpoint(s)" -> "saved model(s)" (GLOSSARY section 3, CONV-6) and
% "cells" -> "comparisons" (radio-cell collision). The reported convention is joint-best (author
% ruling 2026-07-18): one saved model per fold, selected on validation by the geometric mean of the
% two task metrics, both tasks read there. The 0.23 / 0.93 worst-seed gaps and the verdict
% inversions (four category, two region) are CAMERA_READY section 5, S12. The comment that stood
% here quoted the pre-v18 loss weights and convention gaps and was replaced.

% [REV-029, 2026-07-26] Moved here from the head of this subsection, where it was declared
% before any prose of Section 5.6.2 (the rendered build put it a full page ahead of that
% heading). This is the paragraph boundary closing the table's first reference. It still
% precedes Figure 7's declaration and follows Table 2's, so both float numbers are unchanged.
% [2026-09-06, camera-ready] BLOCO DE PROVENIENCIA v17 REMOVIDO DAQUI (linhas 164-190 e 196-201 da
% versao herdada do Capitulo 5). Continha a escada v17 completa -- categoria conjunta
% de categoria e de regiao (os valores estao em CAMERA_READY.md seccao 4, a lista de nunca-citar --
% nao se reimprimem aqui, para o portao nao ter de abrir excepcao para a sua propria documentacao),
% os tectos dedicados, e um bloco de instrucoes que mandava nao relaxar a regra de que a categoria
% supera em todos os datasets e a regiao em quatro deles. Sob a geracao sem vazamento nenhuma dessas afirmacoes se
% sustenta: a categoria supera so na Florida e a regiao so no Texas e na California.
% Um comentario nao e renderizado, mas E lido -- por agentes e por quem edita -- e uma instrucao
% escrita que diz "nao relaxes" a regra errada e o vector mais provavel para um numero com vazamento
% reentrar no texto. Apanhado pelo portao gate_v17.sh na primeira execucao.
% A proveniencia viva das celulas esta em CAMERA_READY.md seccao 3; a lista de nunca-citar na
% seccao 4; a sensibilidade do tecto do Arizona era material de carta-resposta de uma submissao
% anterior e nao tem funcao aqui.

On region (Acc@10), the joint model outperforms the dedicated model at Texas and California. The two
datasets where it outperforms are the two with the largest region vocabularies, which is where the
region task has the most classes to rank and where the dedicated model has the most to gain from an
auxiliary signal.
% [2026-09-07, author-approved] "is hardest" -> "has the most classes to rank" (review finding:
% Arizona has the lowest dedicated Acc@10 and no gain there, so "hardest" was the wrong reading of
% the region count). The two-confounder sentence stays attached; P1 remains a pair.
% [2026-09-06, camera-ready] Protected pair P1, kept LITERAL from the chapter: this sentence and
% the next ("observation ... rather than as a law") license each other and are never separated.
% An interim gate rule banned the substring "where the region task is hardest" and this clause was
% briefly cut; the rule was corrected (gate commit 98cae8dd) to target the abstract's isolated
% form only, and the clause is restored.
The ordering is not monotone inside that pair, since California has more regions than Texas and a
slightly smaller gain, and region count co-varies with corpus size across these states, so the
grouping is read as an observation about where the benefit appears rather than as a law relating
gain to region count. Why the benefit appears there is a separate question: at California, a
dedicated region model widened to the joint model's size scores above the joint model
(Section~\ref{sec:discussion}), so the gain at that dataset is not attributed to sharing between
the tasks, and Texas has no control of matched size.
% [2026-09-06, camera-ready, D4] ONE SENTENCE ADDED after the protected pair (plan P1: the
% mechanism sentence and the "observation rather than a law" sentence stay together, unchanged).
% The addition separates WHERE the benefit appears (the author's observation, which holds) from WHY
% (open, and at California answered against sharing by the capacity control). Numbers stay in the
% fifth limit of Section VII; this sentence carries none. Source: CAMERA_READY section 6, D4.

On category, Florida is the one dataset where the difference survives the
Holm correction across the six ($+0.19$; 90\% CI $+0.14$ to $+0.25$; corrected $p=0.011$), with 19 of the
20 folds favoring the joint model. The other five differences do not survive it, and because the
equivalence margin was registered for the region axis only, they are reported by the bound their intervals support
rather than as matches: Istanbul ($+0.08$; $+0.01$ to $+0.15$), Arizona ($-0.00$; $-0.04$ to
$+0.03$), California ($-0.00$; $-0.03$ to $+0.02$), Texas ($-0.13$; $-0.19$ to $-0.08$) and Alabama
($-0.19$; $-0.33$ to $-0.04$).
% [2026-08-12] The connective was "so", which reads as an inference from a test. It is not one: at a
% margin at or above the largest interval endpoint, two one-sided tests pass by construction, because
% both endpoints already lie inside the margin. The sentence now states the bound as what it is, a
% property read off the intervals, and names the endpoint that sets it (Alabama, 0.3338).
The intervals themselves carry what can be said about magnitude:
the widest of them reaches $0.33$ points from zero, at Alabama, and with a Bonferroni adjustment
across the six comparisons the widest reaches $0.49$, so all six differences stay within half a point
of zero at once.
% [2026-09-07, Fable review] 0.34 -> 0.33 (the printed interval endpoint is -0.33; 0.3338 rounds to
% 0.33 at the table's precision), and the simultaneous bound named: 0.489 under Bonferroni across the
% six comparisons (CAMERA_READY section 5, route B; WRITING_LAW section 3). Both below half a point.
The bound is read off the intervals rather than established by a
further test.

On region, Texas ($+1.21$; $+1.13$ to $+1.29$; corrected $p=0.00013$) and California ($+1.06$;
$+1.03$ to $+1.08$; corrected $p<10^{-4}$) survive the correction, with all 20 folds favoring the
joint model at both. The remaining four stay within the registered two-point margin, and their
direction is stated rather than rounded away: all four are deficits, at Alabama ($-0.87$; $-1.00$
to $-0.75$), Arizona ($-0.44$; $-0.62$ to $-0.25$), Florida ($-0.16$; $-0.19$ to $-0.13$) and
Istanbul ($-0.08$; $-0.16$ to $-0.002$), and every one of the four intervals lies entirely below
zero. A test in the reverse direction, applied post hoc to the same six region comparisons and
corrected across them, resolves three of the four, and Istanbul's is not resolved. Each of the four
stays inside the margin, but none of them is a tie.
% [2026-09-06, camera-ready] "clears the margin comfortably" -> "stays inside the margin":
% "clears" is on the GLOSSARY section 8 idiom list and "comfortably" is a decorative intensifier;
% the registered surface is "stays within the two-point margin" (WRITING_LAW section 3).

% [REV-029 round 2, 2026-07-26] Moved once more, one paragraph further down. At the previous
% position this figure and the (now two-block) results table competed for the same page and
% between them filled it, leaving page 71 without body prose and splitting the subsection's
% argument across three pages. Declaring it after the statistics paragraph gives the page text
% to set; declaration order relative to the results table is unchanged, so Figure 7 keeps its
% number. Measured after the rebuild that followed this edit: p.71 carries 360 word-tokens of body
% prose (263 before, table only), and of the 22 floats, 21 render at or after the page of their
% first prose reference. The exception is Chapter 4's Figure 3, which renders one page ahead of its
% reference; its placement directive is unchanged by this round and a float landing one page early
% is ordinary LaTeX behavior, not the heading-precedence defect this edit addressed.

The joint model is also above every external baseline reported, on both tasks,
across all six datasets (Table~\ref{tab:results}): on region, the primary
region-native comparison HMT-GRN~\cite{lim2022hmtgrn}, STAN~\cite{luo2021stan}
trained from the raw check-ins, and a ReHDM reference~\cite{li2025rehdm}; on
category, POI-RGNN~\cite{capanema2023poirgnn} and the Markov-K floor. These externals run on their own embeddings, so this comparison
also includes the representation advantage of Section~\ref{sec:results-part1}; the
controlled comparison for the joint model remains the Dedicated column of
Table~\ref{tab:results}. The first-order Markov region floor of Section~\ref{sec:setup-baselines},
computed under our windows and folds, reaches $51$ to $72$ Acc@10 across the
datasets; the joint model exceeds it by $4.1$ to $10.0$ points on all six
datasets.

That floor is also above the three external region systems at most datasets: HMT-GRN falls below
it at all six, the ReHDM reference at three, and STAN at four. The floor reads a strong signal that
our sliding windows expose, since they advance one visit at a time and the target region is the
last visited region in $32.9$ percent of the windows at Alabama, and the three systems do not meet
it on equal terms (HMT-GRN on our data, folds, and initialization, STAN on our folds with its own
embeddings and sequences, ReHDM under its own published protocol). We therefore treat the floor,
not the external systems, as the reference the region task has to clear, and the dedicated and
joint models are above it at every dataset.
% [2026-09-06, cut C4] Two paragraphs (~260 words) -> one (~110). Kept, as the mobiwac list
% requires: the floor above HMT-GRN at 6/6 (ReHDM 3, STAN 4), the reason (stride-1 windows, region
% persistence 32.9 percent at Alabama), the protocol asymmetry of the three systems, and "the floor
% is the reference". Dropped: "we do not claim a single explanation" (the sentence now states the
% two facts and no cause), and the framing sentences.
% [round5, fact gate BLOCKER] Previous sentence: "At Alabama, 22.4 percent of windows contain their
% own target region as a genuine revisit." WRONG QUANTITY -- 22.4 is a PLACE-level "target reappears
% in its own 9-history" rate on NON-OVERLAPPING windows (PIPELINE_AUDIT_2026-06-03.md:24), while the
% floor is region-level on stride-1 windows. Replaced with the region persistence share measured on
% the floor's own windows: 32.91% (31,704 of 96,326), see region_persistence_alabama.json.
% A first attempt at this replacement quoted acc1_mean 0.32064 instead, which is a fitted table's
% top-1 ACCURACY rather than a share of windows -- the same wrong-quantity class, caught in review.
% [v2 review B-2, 2026-07-26] RECONCILIATION added on persona 11's blocker
% (src_utils/_review_v2/11_poi_mobility_expert_report.md). The chapter designated these systems "the
% per-task state of the art, on our data" and printed a non-learned floor above all of them, without
% connecting the two. A referee reads that pair as evidence the re-implementations are under-trained
% and discounts the whole external comparison, including the parts that favour the contribution.
% Every figure QUOTED, none computed here:
%   floors: docs/results/closing_data/markov_floor_stride1/<state>.json, key
%     markov_1step_region_acc10_mean -- AZ 0.5123 CA 0.5909 TX 0.6010 AL 0.6226 IST 0.6506 FL 0.7247
%   externals: this paper's own results table (HMT-GRN AZ 43.70 CA 49.61 TX 53.85 AL 57.05
%     IST 60.40 FL 63.74; ReHDM below floor at CA/TX/FL; STAN below at AZ/CA/AL/IST)
%   region persistence: markov_floor_stride1/region_persistence_alabama.json,
%     region_persistence_pct 32.91 -> 32.9% (31,704 of 96,326 windows have target == last region).
%     Generated by scripts/embedding_eval/region_persistence.py from the SAME check-in stream and
%     windowing as the floor, with the window count GATED against the floor's n_windows (96,326 =
%     96,326, PASS).
%     [correction] An earlier version quoted aggregate.markov_1step_region.acc1_mean 0.32064 ->
%     32.1%. That is the top-1 ACCURACY of a fitted transition table, NOT a share of windows -- the
%     same wrong-quantity class as the 22.4 defect below, and it slipped through precisely because
%     the two numbers land close. Persistence is now measured, not borrowed from a model score.
%     [round5] REPLACES a mis-citation: PIPELINE_AUDIT_2026-06-03.md:24 (AL 22.4%) is a PLACE-level
%     "target reappears in its own 9-history" rate on NON-OVERLAPPING windows, so it did not support
%     the region-level sentence it was attached to. Fact gate B-1/M-3.
%   margin range 4.1 to 10.0: DERIVED, not quoted -- joint column of Table~\ref{tab:results}
%     minus markov_floor_stride1/<state>.json key markov_1step_region_acc10_mean, per dataset.
%     [2026-08-12] RE-DERIVED on the final joint cells, all six: FL 76.54-72.47=4.07 (narrowest),
%     CA 64.54-59.09=5.45, TX 66.15-60.10=6.05, AL 69.24-62.26=6.98, AZ 59.04-51.23=7.81,
%     Istanbul 75.08-65.06=10.02 (widest). Rounds to the printed 4.1 to 10.0. The FLOOR span
%     (51 to 72) is unchanged: the floors are properties of the data, not model results, so only
%     the margins move. The narrowest margin is still at Florida and the widest still at Istanbul.
%     Deliberately re-derived from the chapter's own printed cells
%     rather than copied from MARKOV_FLOOR_STRIDE1.md, whose +4.95/+10.38 use pre-final joint values
%     (the Istanbul and Florida joint cells of the leaked generation, never-cite list). Fact gate N-2.
% Counts re-derived from those two sources this session: HMT-GRN 6 of 6 below, ReHDM 3 of 6, STAN
% 4 of 6. No result of this study changes; what changes is that the chapter now names the floor
% rather than the externals as the reference the region task must clear, which is the more
% conservative reading and the one its own numbers support.
% [2026-07-27] Causal explanation dropped. The prior wording said all three systems "predict a
% place and are read at the region level through the place-to-region map", which contradicts this
% chapter calling HMT-GRN region-native (region is one of its own targets) at :418, :622 and :755.
% The paragraph now states the protocol asymmetry only, every clause quoted from the baselines
% subsection at :418: HMT-GRN same data/folds/initialization; STAN same folds, own embeddings and
% sequence construction; ReHDM its own published protocol (also :622 footnote, :873). Counts,
% revisit share and the floor reading are unchanged.
% NOTE for the author: docs/results/closing_data/MACS_BOARD_RESULTS.md:47 still asserts "HMT reg
% clears the Markov floor". That held against the older non-overlapping floor (AL 0.4701, the
% old_floor_nonoverlap block in the same JSON) and was never revisited when the floor was recomputed
% under the shipped stride-1 windowing (AL 0.6226). The chapter inherited the inconsistency rather
% than creating it; the internal record should be corrected separately.

At Istanbul, the non-U.S. city, both differences are within a tenth of a point of the dedicated
models (category $+0.08$ macro-F1, region $-0.08$ Acc@10), so what repeats on a different continent
and region unit is the bounded picture, with no gain on either task.
% [2026-09-06, cut C10] The "External validity (Istanbul)" subsection (~130 words) -> one sentence
% at the end of VI-B: +0.08 / -0.08 kept, the per-axis bounded reading kept, the absolute cells and
% the "comparable quantity" sentence dropped (Table II carries them). Label sec:results-external
% removed; Section I now points at sec:results-part2.
% [2026-09-06, camera-ready] Four changes, no number changed. "both tasks land within" -> "both
% differences are within" (motion metaphor, GLOSSARY section 8); "staying well within" -> "staying
% within" (intensifier); "the gain over the ceiling" -> "the difference against the dedicated model"
% (plan 3.35(b): the Istanbul region difference is -0.08, a deficit, so "gain" was wrong); the
% closing "The U.S. result repeats ..." -> a sentence that names what repeats, per axis (plan
% 3.35(d)): at Istanbul neither superiority holds, so what Istanbul shares with the U.S. states is
% the bounded picture, not the Texas/California gains; each bound is quoted on its own axis.
