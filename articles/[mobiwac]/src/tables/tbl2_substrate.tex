% Table 2: The representation makes the category task learnable (Part 1).
% Check2HGI vs HGI next-category macro-F1, matched single-task head.
%
% STATUS: the values below are the NON-OVERLAP prior measurement
% (5-fold, seed 42, matched next_gru STL head; paired Wilcoxon p = 0.0312
% each, 5/5 folds positive). The whole paper is on the overlapping-window
% board, so this substrate comparison must be RE-SCORED under overlap; the
% overlap margin (Delta_cat re-scored) is BLOCKED and carries a TBD
% placeholder. The +15..+29 non-overlap gap is the prior we expect to hold.
%
% Per-visit-context share (how much of the gain is per-visit context, not
% extra supervision): about 64-72 pct at the small states, 89-90 pct at the
% large states (measured against a per-place-averaged version of the same
% embedding). Shown as a single text band, not a per-state column, until the
% overlap re-score lands.
\begin{table}[t]
\caption{The check-in-level representation (Check2HGI) makes next-category prediction far
more learnable than a standard place embedding (HGI). Read this as: a vector per visit, not
a vector per place, is what drives the category gain. Values are next-category macro-F1
under a matched single-task head; the table shows the non-overlap prior measurement, and the
overlap-board re-score (one windowing for the whole paper) is in progress and marked TBD.}
\label{tab:substrate}
\centering
\begin{tabular}{l r r r}
\hline
\textbf{State} & \textbf{HGI} & \textbf{Check2HGI} & \textbf{$\Delta$ (overlap)} \\
\hline
AL & 25.26 & \textbf{40.76} & TBD \\
AZ & 28.69 & \textbf{43.21} & TBD \\
FL & 34.41 & \textbf{63.43} & TBD \\
TX & 31.89 & \textbf{60.24} & TBD \\
CA & 31.13 & \textbf{59.94} & TBD \\
\hline
\multicolumn{4}{l}{\footnotesize Prior (non-overlap) $\Delta$: AL $+15.50$, AZ $+14.52$,} \\
\multicolumn{4}{l}{\footnotesize FL $+29.02$, CA $+28.81$, TX $+28.34$ (paired Wilcoxon} \\
\multicolumn{4}{l}{\footnotesize $p=0.0312$ each, $5/5$ folds positive). Two bands: about} \\
\multicolumn{4}{l}{\footnotesize $+15$ at the small states, about $+28$ to $+29$ at the large.} \\
\end{tabular}

\vspace{2pt}
{\footnotesize A controlled test attributes most of the gain to the per-visit context, not
to extra supervision: about 64 to 72 percent of the category lift at the small states and 89
to 90 percent at the large states is recovered against a per-place-averaged version of the
same embedding. The overlap-board re-score (the $\Delta$ column) is pending; until it lands,
treat the exact margin as provisional and read the non-overlap band as the prior.}
\end{table}
