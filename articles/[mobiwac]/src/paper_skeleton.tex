% =====================================================================
% MobiWac 2026 -- paper skeleton (IEEE two-column conference).
% Predicting the Next Category and Region of a Visit:
%   A Check-in-Level Multi-Task Study on Mobility Data.
%
% This file is the assembly point. Prose lives in sections/, results in
% tables/, figures in figs/, references in references.bib. Fill the section
% stubs from PAPER_PLAN section 4; the tables and figures are wired here.
%
% Preamble follows IEEE-conference-template-062824.tex; tikz is added for
% the model figure (figs/fig2_model.tex needs \def\FIGINPUT{1}).
%
% Compile: pdflatex paper_skeleton -> bibtex paper_skeleton -> pdflatex x2.
% =====================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
% tells the standalone figure files to render in \input mode, not standalone.
\def\FIGINPUT{1}
% figures and tables are \input from these subfolders.
\graphicspath{{figs/}}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

% ---------------------------------------------------------------------
% Title and authors (single-blind: real names stay). De-anonymize before
% submission. Working title leads with the finding, not the model name.
% ---------------------------------------------------------------------
\title{Predicting the Next Category and Region of a Visit:\\
A Check-in-Level Multi-Task Study on Mobility Data}

\author{\IEEEauthorblockN{Vitor H. O. Silva, Germano B. dos Santos, Fabr\'icio A. Silva}
\IEEEauthorblockA{Laborat\'orio de Intelig\^encia em Sistemas Pervasivos e Distribu\'idos (NESPeD-LAB)\\
Universidade Federal de Vi\c{c}osa, Florestal, MG, Brazil\\
E-mail: \{vitor.h.oliveira, germano.santos, fabricio.asilva\}@ufv.br}
}

\maketitle

% ---------------------------------------------------------------------
% Abstract (~180 words) and keywords. Draft in PAPER_PLAN section 4.
% Motivation first; no model name in the first sentence; no system number.
% ---------------------------------------------------------------------
\begin{abstract}
Location-based social networks (LBSNs) record where people go and what they
do, one check-in at a time. If we can anticipate the next visit, mobile and
urban services can act ahead of demand. Two coarse questions usually suffice:
the next category (the kind of place) and the next region (which part of the
city). These are normally handled by separate models, so we ask whether one
model can learn both, and what sharing a single representation costs. We first
build a check-in-level representation that describes each visit in its own
context, instead of giving every place one fixed vector. Across five U.S.
states, this lifts next-category prediction by a wide margin over a standard
place embedding \cite{huang2023hgi} (about $+28$ to $+40$ macro-averaged F1),
and most of the gain comes from the per-visit context. We then train one
multi-task model for both tasks. At every state it beats a dedicated category
model (about $+4.7$ to $+7.7$ macro-F1), and on region it beats the dedicated
model where the region space is large and matches it (statistically, within two
points) where it is small. The spatial gain grows with the number of regions.
On a non-U.S. city (Istanbul) the result holds: it beats on category and stays
within two points on region.
\end{abstract}

\begin{IEEEkeywords}
next-category prediction, next-region prediction, multi-task learning,
check-in-level representation, location-based social networks, mobility data
\end{IEEEkeywords}

% ---------------------------------------------------------------------
% Body. Sections live in sections/ ; fill from PAPER_PLAN section 4.
% Figures and tables are placed near the section that first cites them.
% ---------------------------------------------------------------------
\input{sections/01_introduction}

\input{sections/02_related}
\begin{figure*}[t]\centering
\input{figs/fig1_dataflow}
\caption{From a user's check-in trail to two predictions. Each visit is placed in a
four-level graph (check-in, POI, region, city) and described in its own context; overlapping
nine-visit windows then feed the model, which outputs the next category and the next region.}
\label{fig:dataflow}
\end{figure*}     % Fig 1: input-graph data flow (cited in section 2.1 / 4.1)

\input{sections/03_problem}

\input{sections/04_method}
\begin{figure*}[t]\centering
\input{figs/fig2_model}
\caption{The single multi-task model. Two input streams, a check-in (semantic)
window and a region (spatial) window, pass through private per-task encoders into a shared
bidirectional cross-attention stack (the only shared parameters). A GRU head reads the shared
semantic output for next-category; a dual-tower region head combines that shared output with a
private spatial tower that runs on the raw region window for next-region. One model, one
forward pass, two predictions.}
\label{fig:model}
\end{figure*}        % Fig 2: the single model, dual-tower + cross-attention (section 4.2)

\input{sections/05_setup}
\input{tables/tbl1_datasets}   % Table 1: dataset statistics (section 5.1)

\input{sections/06_results}
\input{tables/tbl2_substrate}  % Table 2: representation, Check2HGI vs HGI category (section 6.1)
\input{figs/fig3_embquality}   % Fig 3: category embedding quality (section 6.1)
\input{tables/tbl3_results}    % Table 3: one model, two tasks, the main result (section 6.2)
\input{figs/fig4_deltas}       % Fig 4: signed deltas across states, the headline figure (section 6.2)

\input{sections/07_discussion}

\input{sections/08_conclusion}

% ---------------------------------------------------------------------
% References. Keys live in references.bib (already de-anonymized and
% audited; see PAPER_PLAN section 8). Choose a final IEEE bst at write-time.
% ---------------------------------------------------------------------
% NOTE (QA): IEEEtran.bst is not present in this TeX install; using the
% built-in IEEE numeric style `ieeetr` so the build resolves everywhere.
% For final submission on full TeX Live / Overleaf you may restore IEEEtran.
\bibliographystyle{ieeetr}
\bibliography{references}

\end{document}
