% =====================================================================
% MobiWac 2026 -- paper skeleton (IEEE two-column conference).
% Predicting the Next Category and Region of a Visit:
%   A Check-in-Level Multi-Task Study on Mobility Data.
%
% This file is the assembly point. Prose lives in sections/, results in
% tables/, figures in figs/, references in references.bib. Fill the section
% stubs from PAPER_PLAN section 4; the tables and figures are wired here.
%
% Preamble follows IEEE-conference-template-062824.tex; tikz is added for
% the model figure (figs/fig2_model.tex needs \def\FIGINPUT{1}).
%
% Compile: pdflatex main -> bibtex main -> pdflatex x2.
% =====================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{url}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
% tells the standalone figure files to render in \input mode, not standalone.
\def\FIGINPUT{1}
% figures and tables are \input from these subfolders.
\graphicspath{{figs/}}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\renewcommand{\dbltopfraction}{0.9}
\renewcommand{\topfraction}{0.9}
\renewcommand{\textfraction}{0.07}
\setcounter{dbltopnumber}{3}
\setlength{\textfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\floatsep}{4pt plus 2pt minus 1pt}
\setlength{\dblfloatsep}{4pt plus 2pt minus 1pt}
\renewcommand{\arraystretch}{0.90}

\begin{document}

% ---------------------------------------------------------------------
% Title and authors (single-blind: real names stay). De-anonymize before
% submission. Working title leads with the finding, not the model name.
% ---------------------------------------------------------------------
\title{Predicting the Next Category and Region of a Visit:
A Check-in-Level Multi-Task Study on Mobility Data}
% [2026-09-06, camera-ready] Forced line break removed: with it the title rendered on three lines
% with "Data" alone on the third. The wording is the accepted EDAS title and does not change
% (venue rule: the final title must match the accepted one). The two math-mode numbers in the
% abstract were set as plain text: the venue asks for no math in the abstract.

% [2026-09-06] AUTORES SEPARADOS. As normas do MobiWac 2026 dizem, literalmente, "Do not group the
% authors together", e exigem na versao final o nome legal completo, a afiliacao em italico com pais,
% e o email de cada autor. Os tres partilham a mesma afiliacao, que por isso se repete -- e a pratica
% normal do IEEEtran. Fonte: scomminc.com/pp/ieee/mobiwac26.htm.
% [2026-09-06, autorizado pelo autor: "Pode adicionar o nome todo"] NOMES LEGAIS COMPLETOS, como a
% norma exige ("All Authors' Full Legal Names ... FULL first/given, middle, and last/family names").
% Vitor e Fabricio saem do registo EDAS verbatim. ⚠ O nome do meio de Germano ("Barcelos") NAO vem
% do EDAS, que regista so "Germano dos Santos": foi tomado do "Germano B. dos Santos" que o artigo
% usa desde a submissao. CONFIRMAR COM ELE antes do upload -- um nome legal errado no Xplore e
% permanente, e esta e a unica das tres formas que nao tem fonte documental.
\author{
\IEEEauthorblockN{Vitor Hugo de Oliveira Silva}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
vitor.h.oliveira@ufv.br}
\and
\IEEEauthorblockN{Germano Barcelos dos Santos}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
germano.santos@ufv.br}
\and
\IEEEauthorblockN{Fabr\'icio Aguiar Silva}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
fabricio.asilva@ufv.br}
}

\maketitle
\bstctlcite{IEEEexample:BSTcontrol}

% ---------------------------------------------------------------------
% Abstract (~180 words) and keywords. Draft in PAPER_PLAN section 4.
% Motivation first; no model name in the first sentence; no system number.
% ---------------------------------------------------------------------
\begin{abstract}
Location-based social networks record where people go and what they do, one check-in at a time.
If we can anticipate the next visit, mobile and urban services can cache content or reserve
capacity ahead of demand. Two coarse questions are usually enough: the category of the next visit
and its region.

We give each check-in its own vector instead of giving every place one fixed vector. With the same
model, folds, windows, and training configuration, and only the input changed, this improves next-category prediction at
every one of six datasets and in every fold, by 0.23 to 6.29 points of macro-averaged F1, under
one point at the three largest, and a paired test separates the two representations at five of the
six. Five datasets are U.S. states and one is a non-U.S. city.

We then ask whether one model can answer both questions in a single forward pass. On next region it outperforms a dedicated single-task model at the two datasets with the
largest region vocabularies, by about one point, and at the other four it stays within a two-point
margin registered before any result was read. The joint model is also the larger model, so we do not attribute that gain to sharing. On next category it
outperforms the dedicated model at one dataset, and the other five differences are equivalent to
zero within half a point. One model therefore serves both tasks at a cost bounded on both axes.
\end{abstract}
% [2026-09-06, camera-ready, REBALANCE, author-approved: PROPOSTA_REEQUILIBRIO.md section A]
% The abstract now leads with the representation under its controlled comparison and gives the
% joint model the second half. Departures from the proposal text, each for a law reason: the
% em-dashes became commas (WRITING_LAW: no em-dash); "the difference is significant at five of the
% six" -> "a paired test separates the two representations at five of the six" (WRITING_LAW
% section 3: "significant" only with the test named; Florida p = 0.067); "reaches that gain" ->
% "scores above the joint model" (the control beats the joint model by +0.41, 5/5 folds; "reaches"
% understates, as the mobiwac audit ruled for Section VII); the capacity clause is its own
% sentence (no semicolon, author feedback 2026-09-06). Dataset names left out on purpose (Table I).
% Verdict verbs unchanged: "outperforms" only where Holm survives. 276 -> ~215 words.
% [2026-09-06, Fable review of Etapa 1] "under one point at the three largest" ADDED (FL +0.23,
% CA +0.88, TX +0.99: the smallness caveat the five-of-six clause alone does not carry); the joint
% model re-enters as the question the paper asks ("We then ask whether one model ..."), so the
% abstract frames what Sections I and VIII frame.


% [2026-09-06] Separador virgula -> ponto-e-virgula: exigencia literal das normas do MobiWac 2026
% ("These terms must be separated using semi-colons"), scomminc.com/pp/ieee/mobiwac26.htm.
\begin{IEEEkeywords}
mobility data; next-category prediction; next-region prediction; multi-task
learning; check-in-level representation; location-based social networks
\end{IEEEkeywords}

% ---------------------------------------------------------------------
% Body. Sections live in sections/ ; fill from PAPER_PLAN section 4.
% Figures and tables are placed near the section that first cites them.
% ---------------------------------------------------------------------
\input{sections/01_introduction}

\input{sections/02_related}
\begin{figure*}[!t]\centering
\resizebox{\textwidth}{!}{\input{figs/fig1_dataflow_new}}
\caption{From a check-in sequence to two predictions. Each visit is embedded in a four-level graph
trained without task labels, and sliding nine-visit windows of the resulting per-visit and per-region
vectors feed a single model.}
\label{fig:dataflow}
\end{figure*}     % Fig 1: input-graph data flow (cited in section 2.1 / 4.1)

% \input{sections/03_problem}  % merged into 04_method (cut C1, 2026-09-06)

\input{sections/04_method}
\begin{figure}[!t]\centering
\resizebox{\columnwidth}{!}{\input{figs/fig2_model}}
\caption{The single multi-task model. A check-in (semantic) window and a region (spatial) window
pass through private encoders into the shared trunk, the only place where the two tasks
interact. The category output uses the trunk's output alone; the region output
combines it with a private spatial path.}
\label{fig:model}
\end{figure}        % Fig 2: the single model, dual-tower + cross-attention (section 4.2)

\input{sections/05_setup}
\input{tables/tbl1_datasets}   % Table 1: dataset statistics (section 5.1)

\input{tables/tbl3_results}    % Table 3: one model, two tasks, the main result (section 6.2);
                               % input BEFORE 06_results so the table* float lands with the prose
                               % that reads it, not after the references (V1 panel edit 3, 2026-07-18)
\input{sections/06_results}
\input{tables/tbl2_substrate}  % Table 2: representation, Check2HGI vs HGI category (section 6.1).
% Fig 3 (category embedding-quality bars) CUT (author decision 2026-07-09): its four numbers
% (silhouette 0.53/0.00, kNN purity 0.98/0.79, over AL/AZ/FL) are stated in section 6.1 prose.
% The asset stays at figs/fig3_embquality.{py,tex,pdf} and carries the SUPERSEDED values; regenerate
% it from docs/results/embedding_eval/ before any reuse.
% \input{figs/fig4_deltas}  % Fig. 4 CUT 2026-09-06 (author decision, page budget): its twelve deltas are all in Table II; asset kept in figs/       % Fig 4: signed deltas across states, the headline figure (section 6.2)

\input{sections/07_discussion}

\input{sections/08_conclusion}

\section*{Acknowledgment}
The authors would like to acknowledge the support of the Research Support Foundation of the
State of Minas Gerais (FAPEMIG), CNPq and CAPES.
% [2026-09-07] Acknowledgment added at the advisor's request, text verbatim; unnumbered section, before
% the bibliography, as the venue's instructions place it.

% ---------------------------------------------------------------------
% References. Keys live in references.bib (already de-anonymized and
% audited; see PAPER_PLAN section 8). Choose a final IEEE bst at write-time.
% ---------------------------------------------------------------------
% IEEEtran.bst is bundled in this folder (src/IEEEtran.bst, from CTAN) next to
% IEEEtran.cls, so the official IEEE bibliography style builds locally and on Overleaf.
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
