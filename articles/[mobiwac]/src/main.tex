% =====================================================================
% MobiWac 2026 -- paper skeleton (IEEE two-column conference).
% Predicting the Next Category and Region of a Visit:
%   A Check-in-Level Multi-Task Study on Mobility Data.
%
% This file is the assembly point. Prose lives in sections/, results in
% tables/, figures in figs/, references in references.bib. Fill the section
% stubs from PAPER_PLAN section 4; the tables and figures are wired here.
%
% Preamble follows IEEE-conference-template-062824.tex; tikz is added for
% the model figure (figs/fig2_model.tex needs \def\FIGINPUT{1}).
%
% Compile: pdflatex main -> bibtex main -> pdflatex x2.
% =====================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{url}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
% tells the standalone figure files to render in \input mode, not standalone.
\def\FIGINPUT{1}
% figures and tables are \input from these subfolders.
\graphicspath{{figs/}}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\renewcommand{\dbltopfraction}{0.9}
\renewcommand{\topfraction}{0.9}
\renewcommand{\textfraction}{0.07}
\setcounter{dbltopnumber}{3}
\setlength{\textfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\floatsep}{4pt plus 2pt minus 1pt}
\setlength{\dblfloatsep}{4pt plus 2pt minus 1pt}
\renewcommand{\arraystretch}{0.95}

\begin{document}

% ---------------------------------------------------------------------
% Title and authors (single-blind: real names stay). De-anonymize before
% submission. Working title leads with the finding, not the model name.
% ---------------------------------------------------------------------
\title{Predicting the Next Category and Region of a Visit:\\
A Check-in-Level Multi-Task Study on Mobility Data}

% [2026-09-06] AUTORES SEPARADOS. As normas do MobiWac 2026 dizem, literalmente, "Do not group the
% authors together", e exigem na versao final o nome legal completo, a afiliacao em italico com pais,
% e o email de cada autor. Os tres partilham a mesma afiliacao, que por isso se repete -- e a pratica
% normal do IEEEtran. Fonte: scomminc.com/pp/ieee/mobiwac26.htm.
% ⚠ NOMES LEGAIS: o registo EDAS tem "Vitor Hugo De Oliveira Silva", "Germano dos Santos" e
% "Fabricio Aguiar Silva". Confirmar com o autor antes de submeter; as normas pedem o nome legal
% completo, nao as iniciais.
\author{
\IEEEauthorblockN{Vitor H. O. Silva}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
vitor.h.oliveira@ufv.br}
\and
\IEEEauthorblockN{Germano B. dos Santos}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
germano.santos@ufv.br}
\and
\IEEEauthorblockN{Fabr\'icio A. Silva}
\IEEEauthorblockA{\textit{NESPeD-LAB}\\
\textit{Universidade Federal de Vi\c{c}osa}\\
Florestal, MG, Brazil\\
fabricio.asilva@ufv.br}
}

\maketitle

% ---------------------------------------------------------------------
% Abstract (~180 words) and keywords. Draft in PAPER_PLAN section 4.
% Motivation first; no model name in the first sentence; no system number.
% ---------------------------------------------------------------------
\begin{abstract}
Location-based social networks record where people go and what they
do, one check-in at a time. If we can anticipate the next visit, mobile and
urban services can cache content or reserve capacity ahead of demand. Two coarse questions about the next
visit are usually enough: its category (the type of place) and its region (which
part of the city). These are normally handled by separate models. We therefore
test whether one model can learn both tasks, and what sharing a single model costs
in per-task accuracy. We first
build a check-in-level representation that describes each visit in its own
context, instead of giving every place one fixed vector. Across six datasets
(five U.S. states and one non-U.S. city, Istanbul), this improves next-category
prediction over a standard place embedding at every dataset, by $0.2$ to $6.3$ points of
macro-averaged F1, with every fold favoring it. We then train one
multi-task model for both tasks and read both answers from a single saved
model. On next region it outperforms the dedicated model at Texas and California, the two
datasets with the most regions, by about one point, and at the other four it stays within
a two-point margin registered before any result was read. On next category it outperforms
the dedicated model at Florida, and the other five differences are equivalent to zero within
half a point. One model therefore gives both answers in one forward pass at no cost beyond
these bounds; a capacity control at California shows that a dedicated region model of the
same size recovers the region gain there, so whether that gain comes from sharing or from
model size remains open.
\end{abstract}
% [2026-09-06, camera-ready, abstract] Four changes against the ported chapter abstract, each
% tied to a CAMERA_READY ledger line. (1) "the advantage tracks the geometry of the per-visit
% vectors..." REMOVED: C1c, the geometry constants are a 2026-06-24 measurement on a pre-v18
% engine, aggregated per POI. (2) "matches it (statistically, within two points)" -> "stays within
% a two-point margin registered before any result was read": WRITING_LAW section 3 surface for the
% region axis; "matches" is banned on both axes (author ruling 2026-09-06). (3) the one-fifth-of-a-
% point magnitude phrase + "no difference ... exceeds two points [on both tasks]" -> "equivalent to zero
% within half a point": the category surface (derived bound 0.334 pp per dataset, 0.489 pp
% simultaneous); the two-point margin is registered for region only and may not be quoted on
% category. (4) "does better where the region task is hardest" REMOVED: C4, the region-count
% reading is dead under joint-best (California has more regions than Texas and a smaller gain).
% ADDED, under author decision D4 (declare the capacity relation): the California control,
% docs/results/P1/region_head_california_*_capmatched_s0.json vs joint_best_perfold.json
% california_s0_joint, width 352 = 97.4% of the joint budget, 64.910 vs 64.503 Acc@10, +0.406,
% seed 0, five folds. Texas has no capacity-matched arm, so the sentence is scoped to California.
% Verdict verbs: "outperforms" only at region TX/CA and category FL (Holm within family).

\begin{IEEEkeywords}
% [2026-09-06] Separador virgula -> ponto-e-virgula: exigencia literal das normas do MobiWac 2026
% ("These terms must be separated using semi-colons"), scomminc.com/pp/ieee/mobiwac26.htm.
mobility data; next-category prediction; next-region prediction; multi-task
learning; check-in-level representation; location-based social networks
\end{IEEEkeywords}

% ---------------------------------------------------------------------
% Body. Sections live in sections/ ; fill from PAPER_PLAN section 4.
% Figures and tables are placed near the section that first cites them.
% ---------------------------------------------------------------------
\input{sections/01_introduction}

\input{sections/02_related}
\begin{figure*}[!t]\centering
\resizebox{0.66\textwidth}{!}{\input{figs/fig1_dataflow}}
\caption{From a check-in sequence to two predictions: each visit is embedded in a four-level graph
(check-in, place, region, city) trained without task labels. The graph yields two sets of
vectors, one per visit and one per region; sliding nine-visit windows of each feed a single
model that outputs the next category and the next region.}
\label{fig:dataflow}
\end{figure*}     % Fig 1: input-graph data flow (cited in section 2.1 / 4.1)

\input{sections/03_problem}

\input{sections/04_method}
\begin{figure}[!t]\centering
\resizebox{\columnwidth}{!}{\input{figs/fig2_model}}
\caption{The single multi-task model. A check-in (semantic) window and a region (spatial) window
pass through private encoders into the shared trunk, the only place where the two tasks
interact. The category output uses the trunk's output alone; the region output
combines it with a private spatial path. One model, one forward pass, two predictions.}
\label{fig:model}
\end{figure}        % Fig 2: the single model, dual-tower + cross-attention (section 4.2)

\input{sections/05_setup}
\input{tables/tbl1_datasets}   % Table 1: dataset statistics (section 5.1)

\input{tables/tbl3_results}    % Table 3: one model, two tasks, the main result (section 6.2);
                               % input BEFORE 06_results so the table* float lands with the prose
                               % that reads it, not after the references (V1 panel edit 3, 2026-07-18)
\input{sections/06_results}
\input{tables/tbl2_substrate}  % Table 2: representation, Check2HGI vs HGI category (section 6.1).
% Fig 3 (category embedding-quality bars) CUT (author decision 2026-07-09): its four numbers
% (silhouette 0.53/0.00, kNN purity 0.98/0.79, over AL/AZ/FL) are stated in section 6.1 prose.
% The asset stays at figs/fig3_embquality.{py,tex,pdf} and carries the SUPERSEDED values; regenerate
% it from docs/results/embedding_eval/ before any reuse.
\input{figs/fig4_deltas}       % Fig 4: signed deltas across states, the headline figure (section 6.2)

\input{sections/07_discussion}

\input{sections/08_conclusion}

% ---------------------------------------------------------------------
% References. Keys live in references.bib (already de-anonymized and
% audited; see PAPER_PLAN section 8). Choose a final IEEE bst at write-time.
% ---------------------------------------------------------------------
% IEEEtran.bst is bundled in this folder (src/IEEEtran.bst, from CTAN) next to
% IEEEtran.cls, so the official IEEE bibliography style builds locally and on Overleaf.
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
