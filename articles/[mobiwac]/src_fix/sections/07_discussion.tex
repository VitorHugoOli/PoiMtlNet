% Section 7: Discussion and Limitations (about 0.5 page). Plan: PAPER_PLAN 4 / §7.
% One model serves both tasks; region is where joint training pays (outperforms at TX/CA, the two
% largest region vocabularies; within the margin at the other four). Category outperforms at FL and
% holds within a fifth of a point elsewhere. Component attribution is OPEN: the trunk ablations move
% both tasks by <0.2 pp, below single-seed resolution, and TX/CA are unmeasured at board strength.
% The TX/CA category gap carries a stated tuning hypothesis (configuration transferred, not
% validated there). Limitations (2 since 2026-07-18, CA/TX n=20):
% transductive representation (planned training-only variant); the mobility-aware service is background
% motivation, not evaluated. Small-state region is non-inferior within two points (a match, not a beat).
% 2026-07-08 (P7/C4-C6): the usage sketch is now quantified (shortlist enrichment from Tbl 3 + shortlist
% compactness + near-miss distance vs the random-pair floor, all from analysis/{near_miss,shortlist_compactness}_
% RESULTS.md; four datasets, single-seed tag inline) and the closing paragraph carries the moura2025 succession
% clause. Still motivation-only: bare prediction properties, no service metric.
\section{Discussion and Limitations}
\label{sec:discussion}

A single model serves both tasks in one forward pass, and both answers are read from one saved
checkpoint. The region task is where joint training pays: the model outperforms the dedicated region
model at Texas and California, the two datasets with the largest region vocabularies, and remains
within the two-point margin at the other four, while preserving a private spatial path for the
region output (Table~\ref{tab:results}; Fig.~\ref{fig:deltas}). On category it outperforms the
dedicated model at Florida and holds within a fifth of a point of it elsewhere.

Two aspects of the design bear on that region result. The shared trunk carries semantic context
between the two streams, and the private spatial path keeps the region output free to specialize.
Which of them produces the advantage is not decided by the evidence here: direct ablations of the
trunk, at the datasets where they were run, move both tasks by less than two tenths of a point in
either direction, which is below what a single-seed five-fold comparison resolves. Isolating the
trunk at Texas and California, at the full four-seed protocol, is the experiment that would
attribute the gain, and it remains open.

The category picture at Texas and California invites a specific hypothesis. Batch size and learning
rate were searched for the dedicated category model at every dataset and for the joint model at the
smaller datasets; at Texas and California the joint model runs a configuration transferred from
those searches rather than one validated on them. Texas carries the second-largest category deficit of the
six ($-0.13$; Alabama's $-0.19$ is the largest, and it is a dataset where the joint model was
searched), and California's is under a hundredth of a point. A search conducted for the joint model
at Texas and California would test whether the transferred configuration is what holds their
category output slightly below the dedicated model. Until it is run this remains a hypothesis rather
than an explanation, and Alabama is a reminder that a searched dataset can show a deficit of the same
size.

%The gain is substantial, and where we could test it the gain is a property of the joint architecture rather than of
%cross-task transfer: at the three datasets where the region pathway is held at its initial values, the category gain
%survives in full. Which component of that architecture produces the gain is not settled by the controls reported here (
%Section~\ref{sec:results-part2}).

A service could treat the model's top-ranked next regions as an anticipatory
set: at California, ten regions out of 8{,}501 contain the true next region 64.54
percent of the time. On four datasets (Alabama, Arizona, Florida, and Istanbul; a single seed over five folds), the ten shortlisted
regions lie a median of 3 to 8 kilometers from the shortlist's centroid, against 17 to 176 kilometers for ten regions
drawn at random from the same candidate set (median over 10,000 draws). This remains motivation, not a measured service
result.
% RATIO CLAUSE DELETED 2026-08-05, on the author's instruction to remove what is wrong in this section
% ("exclusa e refaça os que estiver errado nessa sessão"). The deleted words were "over 500 times better
% than picking ten at random". Four independent defects, any one of which is disqualifying:
%   1. It is a DERIVED quantity stated in prose, which AGENT_GUARDRAILS §2 N2 forbids outright: "No mental
%      arithmetic, rounding, aggregation, percentage conversion, or delta-taking in prose."
%   2. Its only source in the repository is a PLANNING document, not a results record:
%      articles/[mobiwac]/MOBILITY_SCIENCE_BRIDGE_PLAN.md:77-84 tabulates "California | 8,501 | 65.66% |
%      0.12% | ~547x", and :88-93 is a "Ready-to-paste replacement" sentence carrying this very phrase. The
%      chapter's declared source of truth (docs/studies/closing_data/RESULTS_BOARD.md) states no such ratio.
%   3. It was computed from a SUPERSEDED value: the 547x uses 65.66 (seed 0), while the chapter reports
%      65.69 (n=20). The prose paired a new numerator with a ratio derived from the old one, and the two
%      round the same way, which is exactly why it survived review.
%   4. Its denominator is the floor this project's own record tells the reader NOT to use.
%      articles/[mobiwac]/BRIDGING_METRICS.md:21-22: "The headline reg Acc@10 (~60-77) should be read
%      against the Markov-1 floor, not the ~1 % random floor." Counted in the JSON rather than trusted from
%      a table: docs/results/P0/simple_baselines/california/next_region.json gives random acc10_mean
%      0.0011768859597505001 and markov_1step_region acc10_mean 0.5208956360464413. Against the Markov-1
%      floor the chapter itself adopts, this cell is not a multiple of 500.
% AN HONESTY FLAG HAD ALREADY ANTICIPATED THIS EXACT SENTENCE and was never discharged:
% storyline/archive/process/06_honesty_under_pressure/honesty_flags.md:82 (flag F7) names the
% "65.69%, 500x better than random" line as a temptation, usable only with its convention, and marks it
% [VERIFY at adaptation].
% WHAT IS NOT DELETED, and why: the 65.69 percent and the 8,501 are traced and correct, and the
% enrichment OBSERVATION is real. Only the unsourced multiplier goes. Replacing it with a floor value
% would be a new claim needing the author's sign-off, so it was not done here.
% Swept for propagation before deleting: the ratio appears nowhere else in either text. The "547" hits in
% tables/mobiwac/{results,datasets}.tex are Arizona's region count, 1,547 (checked, not assumed).
% Probes: R13-noratio, R13-ratio-abs.

% AUDIT NOTE 2026-08-04, no edit needed here, recorded so a later sweep does not "fix" a correct
% sentence. The fold-partition correction of that date changed "over the same folds" in
% 05_setup.tex, because a SEED changes the partition. This occurrence is different and is TRUE as
% written: it compares the joint model against the dedicated models WITHIN one run, where both read
% the same partition by construction. That is exactly the property the paired comparison relies on.
% See articles/dissertacao/science/fold_partition_and_seeds.md §7 for the distinction.

Four limits qualify these results. First, our representation is trained once over all places; visits to places
never seen during training are the single effect that we cannot fully isolate. A planned follow-up trains it on each
fold's training places only and embeds unseen visits. Second, epoch selection consults the fold that the score is then
read on
(Section~\ref{sec:setup-windows}), so every absolute score reported here is optimistic. The
comparison between the joint model and the dedicated models is affected far less, for two reasons
that we can state rather than assume: the selection rule is the same for both models on the
same folds, an epoch chosen on validation and read on that fold, with each model selected on its own
validation objective (Section~\ref{sec:results-part2}), and, for the category comparison, the dedicated
model receives the wider search, a per-dataset sweep over batch size and
learning rate against one configuration held fixed across all six datasets for the joint model, while
both sides of the region comparison run a fixed configuration. Where that search applies, the residual
favors the dedicated model, which makes the reported category difference conservative. It does
not follow that the bias cancels exactly. Third, we do not build or evaluate a mobility-aware service; it is background
motivation, and the paper's claims are the prediction results themselves. Fourth, each visit node in the
representation graph is linked to the visit that follows it, and because category is a node input feature
the vector of an earlier visit could absorb the category of the next one. A linear probe screened for this
during development (Section~\ref{sec:setup-windows}), which bounds only the information exposed by a
linear read and does not establish what a nonlinear sequence model could recover.
% FOURTH LIMIT ADDED 2026-08-05 on the author's ruling ("Add it as a fourth limit"), applied to BOTH texts.
% WHY IT WAS A GAP. "Three limits qualify these results" is a closed enumeration, and a closed enumeration
% is a completeness claim. ERRATA.md:73-80 records that this very count was raised from "Two" to "Three"
% precisely to admit a disclosed threat, which establishes that the number functions as the paragraph's own
% inventory of threats. The forward-edge channel is disclosed in this chapter, in the author's voice, at
% 05_setup.tex:82-84 -- "In the Check2HGI graph, each visit node is linked to the visit that follows it.
% Because category is a node input feature, the vector of an earlier visit could absorb the category of the
% next one" -- and §7 named none of it: zero live occurrences of "node", "probe", "screen", "absorb" or
% "linear" before this edit. The one representation limit §7 did carry, unseen places, is a DIFFERENT
% channel (transductive), which _round13/72_leak_screening_search.md separates explicitly from the forward
% edge. So the paragraph's scope demonstrably included this class of threat while omitting this instance.
% EVERY CLAUSE BELOW IS ALREADY SOURCED IN §5; NOTHING NEW IS ASSERTED. The mechanism is 05_setup.tex:82-84
% (modal, "could absorb", not asserted); the screen is :84-86; the residual is the chapter's own bounded
% conclusion at :128-129, "bounds only the information exposed by a linear read" and "does not establish
% what a nonlinear sequence model could recover". The three limits of the screen itself (linear probe;
% Florida only, one initialization over five folds; ancestor builds rather than the reported build) stay in
% §5, where they are stated at :125-129, and are reached by the cross-reference.
% WHAT IS DELIBERATELY NOT SAID: no quantified leak, no claim that the channel does not leak, no claim that
% it does. _round13/71_graphnode_features.md establishes the channel is OPEN (the category one-hot is
% columns 0-6 of the node feature vector; the forward adjacency is present in 96,326 Alabama windows with 0
% not adjacent), which is not evidence that it leaks; and RESCREEN.md:94-95 records that the linear gate
% missed one leak, which is why the residual is stated rather than dismissed. Any of those would be a new
% claim needing sign-off.
% Probes: R13-leak4th, R13-leak4th2, R13-limitcount.
% SCOPE NARROWED 2026-08-05. The sentence said "the dedicated model receives the wider search, a
% per-dataset sweep over batch size and learning rate", unqualified, inside the paragraph that bounds the
% optimistic-score bias for BOTH tasks. The sweep covers the dedicated CATEGORY model only, and this
% chapter's own results section says so in the same words in both copies: 06_results.tex:95 (paper
% 06_results.tex:50) -- "The dedicated category model is tuned per dataset over batch size and learning
% rate (the dedicated region models use the strongest fixed configuration)". The primary record agrees:
% docs/studies/closing_data/v17_completion/CEILINGS_N20_FINAL.md:41 is headed "Category ceiling -- recipe"
% and no region-side sweep is recorded anywhere under v17_completion/.
% WHY THIS MATTERED ENOUGH TO CHANGE: for the REGION comparison both sides run a fixed configuration, so
% the "wider search" mitigation does not hold there, and the "therefore ... conservative" conclusion that
% follows inherited the over-general scope. This is the same defect class as the "identically" correction
% logged at ERRATA.md:139-148 -- a mitigation made to sound stronger than its evidence, which understates a
% limitation. The conclusion is now attached to the comparison it is licensed for.
% THE FIRST REASON IS UNCHANGED AND IS TRUE AS WRITTEN. "the selection rule is the same for both models on
% the same folds" compares the two models within one run, where they share a partition by construction;
% verified against science/fold_partition_and_seeds.md §7 during today's fold/seed sweep. Its narrowing to
% "each model selected on its own validation objective" is the recorded decision at ERRATA.md:139-148 and
% must survive any later edit.
% "the comparator" was also replaced: it is unregistered, and GLOSSARY §2 registers "dedicated single-task
% model" and forbids synonym cycling, which naming it a third way in one paragraph would be.
% Probes: R13-sweepscope, R13-sweepscope2.

% [2026-07-27] "arm" replaced by "models" per this repo's own GLOSSARY.md §3, which lists it as
% never-use ("clinical-trial word, foreign to this audience"). The dissertation carries the same fix.
% [round6, 2026-07-28] "applied identically" NARROWED, applied here and in the dissertation's Chapter 5.
% Logged in ../../ERRATA.md. The word modified a mitigation inside the limitations paragraph, and it
% asserted more than Section 6 states: there, "each dedicated model at its task's best epoch, and the
% joint model at the epoch selected by its joint validation score (the geometric mean of the two task
% metrics)". The procedure is shared, the objective is not, so a reader met the mitigation without the
% asymmetry. Making a mitigation sound stronger than it is understates a limitation, which is the same
% class of defect as overstating a result. "On the same folds" is kept, the non-cancellation caveat in
% the next sentence is untouched, and no number or verb changes.

Our representation is a fixed per-visit vector that any downstream model
can consume. Moura et al.~\cite{moura2025mobilityaware}, whose structural
analysis of tourist check-ins reads past visits only, name machine learning
as a next step; this study moves in that direction.
