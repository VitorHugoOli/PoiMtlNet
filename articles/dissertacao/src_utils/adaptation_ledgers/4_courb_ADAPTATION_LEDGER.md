# Adaptation ledger — Chapter 4 (CoUrb 2026 -> `4_courb.tex`)

Source of record: `articles/CoUrb_2026/src_en/` (the verified EN translation of the published
paper, DOI 10.5753/courb.2026.22960, pp. 323-336). Audited numbers: `articles/CoUrb_2026/ERRATA.md`
+ `articles/CoUrb_2026/slides/judge_feedback.md` (verified this session). Every departure from the
`src_en` text is listed here; this file feeds Appendix B. Items marked (E#) trace to ERRATA.md.

## A. Errata applied silently in the chapter text (author ruling, NORTH_STAR §5.7)

| # | Where in chapter | Published text (src_en verbatim) | Chapter text | Source |
|---|---|---|---|---|
| A1 (E1) | §4.4.3 Next-POI Prediction, first paragraph; §4.5 Conclusion, second paragraph; §4.1 Introduction (abstract-derived claim dropped with the abstract, see C1) | "the spatio-temporal models outperform the original MTLNet in 16 of the 21 evaluated combinations" / "wins in 16 of the 21 evaluated combinations" | "outperform the original MTLNet in 15 of the 21 evaluated combinations, with one additional technical tie in *Outdoors* in Florida, where the *baseline* mean exceeds the best variant by 0.02 percentage points, a gap within one standard deviation" (conclusion: "outperforms the *baseline* in 15 of the 21 evaluated combinations, with one additional technical tie") | judge_feedback.md §2 ("contagem estrita ... 15/21"; "baseline 21,61 vs Sphere 21,59 — baseline ainda vence por 0,02 pp (dentro de sigma)"); ERRATA #1 |
| A2 (E2) | §4.1 Introduction, results paragraph; §4.4.2 category results; §4.5 Conclusion | "average gains of 20 to 24 percentage points" | "average gains per state of 20.2 to 22.0 percentage points, considering the better of the two spatial encoders in each combination" | judge_feedback.md §2 (FL +20,24 / CA +20,91 / TX +21,98, best-of-two per row); ERRATA #2. The 22.0 endpoint is the ERRATA/NORTH_STAR sanctioned rounding of 21.98; the "better of the two encoders" qualifier is the disclosure judge_feedback requires |
| A3 (E1) | §4.1 Introduction, results paragraph | "wins in most scenarios" | "outperforms the *baseline* in most scenarios" | WRITING_LAW banned verdict verb ("wins"); claim strength unchanged ("most scenarios" kept) |
| A4 | Next-POI table (Table 4.3) | FL Outdoors baseline cell bolded in the published table (`\textbf{21.61 ± 0.99}`) | Bold KEPT exactly as published (per TRANSLATION_NOTES: not re-bolded or reclassified); the technical-tie reading lives in prose only | Fidelity to published table |

NOT applied here: ERRATA #3 (`silva2025mtlnet` bib venue fix) — that is a bibliography-level
correction; the dissertation global `references.bib` already carries the corrected entry
(venue CBIC = Congresso Brasileiro de Inteligencia Computacional, DOI 10.21528/CBIC2025-1191324,
no "Submetido" note). No chapter-text change needed.

## B. Re-typeset departures (mechanical, per the common rules)

| # | Change | Detail |
|---|---|---|
| B1 | "this work" -> "this chapter" | 11 occurrences across intro/related/metodology/conclusion (machine-counted in the source sections); also "This work is organized as follows" -> "This chapter is organized as follows" and "proposes ... this work proposes" -> "this chapter proposes". Grep for `this (paper\|article\|work)` returns zero |
| B2 | Section levels | Paper `\section{...}` -> `\section` under the `\chapter`; paper subsections preserved one level down. Labels renumbered to chapter-prefixed scheme: `sec:courb:intro`, `sec:courb:related`, `sec:courb:methods`, `sec:courb:results`, `sec:courb:conclusion`, `fig:courb:arquitetura` (was `fig:arquitetura_arch`), `fig:courb:distribuicao` (was `fig:distribuicao_espacial`), `tab:courb:dataset` (was `tab:dataset`), `tab:courb:category` (was `table:poi_category`), `tab:courb:next` (was `table:next`), `eq:courb:film`, `eq:courb:time2vec`, `eq:courb:concat` (was `eq:film`, `eq:time2vec`, `eq:concat`). All internal `\ref`s updated |
| B3 | Tables re-set in booktabs | The three tables (dataset, category F1, next-POI F1) re-typeset from `src_en/resultados/*.tex`: `\hline`/vertical-rule format -> `\toprule`/`\midrule`/`\bottomrule`, no vertical rules; `±` glyph -> `$\pm$`; `\resizebox` -> `adjustbox{max width=\textwidth}`; `\textnormal` caption wrappers dropped. Every cell value, bolding, and row/column order byte-identical to the source tables (except A4 note above, where the published bold is also kept) |
| B4 | Captions (mechanical part only) | Tables: captions above (already so in source). Figures: captions below; Figure 4.1 caption split into two sentences (content unchanged). **RECLASSIFIED 2026-07-26 (round 4, NEW-9):** the three table lead sentences and the Figure 4.2 reading-instruction sentence that this row used to carry are NOT mechanical — a sentence that interprets a table is content. They moved to section C as C4-C7. This row now covers only caption placement and the Figure 4.1 sentence split |
| B5 | Figure assets | Copied to ASCII-safe paths: `imagens/arquitetura_modelo.png` -> `figures/courb/arquitetura_modelo.png`; `imagens/subáreas/distribuicao_estados.png` -> `figures/courb/distribuicao_estados.png` (accent dropped from directory). Both are 1-col-suitable rasters (arquitetura 1102x348 was full-text-width `figure*` in the paper; distribuicao 5389x1643); both set at `width=\textwidth`, no stretching beyond natural aspect |
| B6 | `figure*` -> `figure` | The architecture figure used the 2-col spanning `figure*` + `\makebox` centering hack; dissertation is 1-col, plain `figure` + `\centering` |
| B7 | Front matter dropped | Paper `\title`, authors, `\address`, abstract, resumo, and Acknowledgments section are not reproduced in the chapter (coletânea re-typeset convention; venue/authors/DOI stated in the preface; acknowledgments belong to the paper record, not the chapter body). **Round 4 (REV-028):** the omission was recorded here but never told to the reader. Appendix B §B.2 now states it, and states the parallel omissions in Chapters 3 and 5 in the same paragraph. Verified 2026-07-26: CBIC `main.tex`:43-49 (abstract + IEEEkeywords) and :77-78 (`\section*{Acknowledgment}`); CoUrb `src_en/main.tex`:49 (abstract), :53 (resumo), :73 (`\section*{Acknowledgments}`); MobiWac `src/main.tex`:66 (abstract), :89-92 (IEEEkeywords), and **no acknowledgments section anywhere in that source** |
| B8 | Commented-out source blocks dropped | The `%`-commented HAVANA and Space2Vec paragraphs, the commented Nash equation, and the commented PT alternative future-work block in `src_en` are not carried into the chapter (they do not render in the published paper either) |
| B10 | Inline-math re-typeset, §4.3.3.1 POI Encoder (4_courb.tex:175) **NEW round 4 (REV-021)** | Published (`src_en/sections/metodology.tex`:107): "The POI Encoder is trained independently to generate the category *embedding* $\mathbf{E}_{\text{POI\_category}} \in \mathbb{R}^{64}$." Chapter: "The category *embedding* $\mathbf{E}_{\text{POI\_category}} \in \mathbb{R}^{64}$ is generated by the POI Encoder, which is trained independently." Clause order reversed so the unbreakable inline expression is not trapped at the end of line 1. Every content word preserved; no quantity, symbol, scope, or claim changes, hence mechanical (section B), not a content departure. Clears the 15.14 pt overfull hbox that `src/build/main.log`:1728 records at lines 175-176. Not an equation: the nearby display equations set clean. Nothing shrunk, no adjustbox. Measured from `src/dissertacao.pdf` p.50: text block 453.1 pt wide, `\parindent` 37.0 pt, so the published run-up plus the expression up to the relation symbol needed ~468.2 pt; reordered, the whole expression sits at ~227 pt. **CORRECTION 2026-07-26 (v2 review, L5 gate finding F1):** the claim "Every content word preserved" was FALSE AS APPLIED. The edit appended the second half of the published sentence to the end of its own last comment line, so the clause "built by Delaunay triangulation over the geographic coordinates, with edges weighted by logarithmic decay of the Haversine distance and penalization for connections between distinct counties (GEOIDs)" did not render, and the chapter printed a sentence ending mid-clause at "a spatial graph". Verified before repair: "Haversine" and "GEOIDs" each occurred 0 times in `src/dissertacao.pdf`. The clause is restored verbatim from `src_en/sections/metodology.tex`:106, matching the published PT record (p. 9). The row stays MECHANICAL because the restored text is the published text; the false assertion is left visible above rather than edited out. A regression gate was added: `src_utils/check_trapped_prose.py`, wired into `src_utils/check.sh` |
| B9-item | Chapter title | Kept the stub's `\chapter{Article 2: ST-MTLNet\: Spatio-Temporal Point-of-Interest Representations for Multi-Task Learning}` + `\label{ch:courb}` exactly as found (common rule). Note: the stub's EN title says "Point-of-Interest Representations" where the paper title is "Representations of Points of Interest"; left as-is since the stub pattern is protected — flagged below |

## C. Additions required by the dissertation frame (not in the published paper)

> **Count (round 4, NEW-9):** EIGHT non-published sentences or blocks exist in this chapter,
> C1 through C8 below. Appendix B previously declared "three marked additions", which
> undercounted by five: it omitted the three table lead sentences and the Figure 4.2 reading
> instruction (all four were misfiled under section B as mechanical, now reclassified as
> C4-C7) and predates the seed sentence (C8). Each of the eight was verified present in
> `src/chapters/4_courb.tex` and absent from `articles/CoUrb_2026/src_en/` on 2026-07-26 by
> 8-gram diff over the union of `main.tex`, `sections/*.tex`, and `resultados/*.tex`.
> Appendix B (§B.2) now enumerates all eight.

| # | Addition | Mandate |
|---|---|---|
| C1 | `chapterpreface` paragraph: translated-reproduction statement + DOI + `\cite{paiva2026stmtlnet}`; Vitor's contribution note (2nd author, presenter, author of baseline MTLNet = Ch.3); sample-stratified-split protocol note ("weaker ... than the user-disjoint cross-validation adopted in Chapter 5"); time-index sentence ("conclusions ... of the time, for that configuration"); the NORTH_STAR §3 floor sentence verbatim ("this chapter isolates the representation effect with MTLNet as its only baseline; it does not revisit the MTL-versus-single-task question, which Chapter 5 reopens") | Task SPECIFICS; NORTH_STAR §3/§4 |
| C2 | New subsection §4.2.5 "The MTLnet framework" (label `sec:courb:mtlnet-recap`), 2 paragraphs: recap of Ch.3's artifact by name with `\ref{ch:cbic}`, the naming-variant note ("the published paper typesets the name as MTLNet, and this chapter preserves that form"), the time-indexed CBIC null-result recap ("performed on par with the dedicated single-task models at a higher training cost, a conclusion of the time for that configuration"), and the bridge into §4.3 | Task SPECIFICS (mandatory bridging subsection); NORTH_STAR §3 bridging devices |
| C3 | One protocol-honesty sentence added to §4.4.1 Experimental Setup: "The split is stratified by sample, not by user, so the *check-ins* of one user may appear in both training and validation; Chapter 5 adopts a stricter user-disjoint protocol." | ERRATA.md honesty items ("say so explicitly"); split fact VERIFIED FIRSTHAND 2026-07-23 from the CoUrb codebase (NORTH_STAR §4 Ch.4: plain StratifiedKFold, userid dropped, `PoiMtlNet_Novo/src/etl/mtl/create_fold.py` L190-199/L225-228) |
| C4 | Dataset-table lead sentence (4_courb.tex:258): "Texas concentrates the largest volume of *check-ins* of the three states, while Florida provides the smallest." Reads Table 4.1; states no number the table does not show. **Reclassified from B4, round 4** | WRITING_LAW §5 (every results table introduced by a lead takeaway sentence) |
| C5 | Category-table lead sentence (4_courb.tex:284): "the two ST-MTLNet variants reach higher mean F1 than the original MTLNet in every category and state." Reads Table 4.2. **Reclassified from B4, round 4** | WRITING_LAW §5 |
| C6 | Next-POI-table lead sentence (4_courb.tex:327): "the proposed variants keep the higher mean F1 in most category-state combinations, while the *baseline* retains a few categories." Reads Table 4.3. **Reclassified from B4, round 4** | WRITING_LAW §5 |
| C7 | Figure 4.2 reading-instruction sentence (4_courb.tex:278): "Each panel plots the POI coordinates (longitude on the horizontal axis, latitude on the vertical axis) for one state; the dense, overlapping clusters of the two categories illustrate the co-location pattern discussed in the text." Interprets the figure, so it is content, not typesetting. **Reclassified from B4, round 4** | WRITING_LAW §5 (figures self-contained, 2-4 interpretive sentences) |
| C8 | One protocol-disclosure sentence added to §4.4.1 after C3 (4_courb.tex:238): "The released code of record pins a single random seed, so the five folds constitute one repetition of the experiment rather than several, and the reported standard deviations are the spread across folds at that seed; Chapter 5 repeats its five-fold experiment at four random initializations." **NEW round 4 (REV-012)** | Chapter 5 reports four seeds and a banca comparing the two chapters will ask. Same precedent as C3 (a protocol fact the published paper left implicit, recovered from the released code). VERIFIED FIRSTHAND 2026-07-26 in the author-provided CoUrb-era codebase `/Users/vitor/Desktop/mestrado/temp/tarik-new` (repo TarikSalles/Spatial_Embeddings, commit 58fd219b, branch main), `PoiMtlNet_Novo/src/etl/mtl/create_fold.py`: `random_state: int = 42` (:162, committed :159), `torch.manual_seed(random_state)` (:180 / :177), `np.random.seed(random_state)` (:181 / :178), and both splitters `StratifiedKFold(n_splits=k_splits, shuffle=True, random_state=random_state)` with **no `groups=` argument** (:226, :229 / :223, :226). `pipelines/mtlnet_trainer.py`:49-55 calls `create_folds` without overriding `random_state`. Deliberately NOT claimed: that the published runs used this exact file. The literal 42 is kept out of the prose (a fact about the code, not a reported parameter of the paper) |

## D. Numbers ledger (every numeral in prose -> source)

- 15/21 strict wins + 1 technical tie; 0.02 pp gap within one std: `slides/judge_feedback.md` §2 (and §7 P0).
- +20.2 / +22.0 pp per-state means (best-of-two-encoders): judge_feedback.md §2 (20,24 / 20,91 / 21,98) via ERRATA #2 sanctioned rounding.
- All F1 cells (62.44±2.00, 60.78±1.63, 64.57±1.03, 64.89±1.20, 63.59±1.45, 64.73±0.72, 48.67±0.66, 51.28±0.57, 21.61±0.99, 21.59±1.91, and every table cell): `src_en/resultados/tabela_comparativa_f1_{category,next}.tex`, byte-checked.
- Dataset counts (990,518 / 65,009 / 20,301; 2,535,573 / 148,314 / 36,106; 3,355,419 / 135,570 / 37,522): `src_en/resultados/tabela_dataset.tex`.
- Hyperparameters (64/192/256/1728/576 dims, 7 classes, 8 heads / 4 layers, L_h=9, tau=0.15, 10 km / 70 km, alpha=0.5, S=16, 10 km-10,000 km, 5 folds, 80/20, fewer than five visits, 2/3/4-layer MLPs): `src_en/sections/metodology.tex` + `results.tex`, reproduced verbatim.
- Gowalla collection window "February 2009 and October 2010": `src_en/sections/conclusion.tex` verbatim.
- DOI 10.5753/courb.2026.22960, pp. 323-336: CLAUDE.md §1 owner list (verified resolving 2026-07-18).

## E. Flags for the author / reviewer pass

- [NEEDS SIGN-OFF] Nash-MTL caveat: ERRATA honesty items say "Nash-MTL caveat as in Ch.3 (solver-bug time-indexing)". The task's preface spec did not list it, and inserting a solver-bug claim into Ch.4 prose would be a new claim (AGENT_GUARDRAILS C2). NOT added; the methodology reproduces the paper's Nash-MTL description verbatim. If the author wants the caveat here, one preface sentence or a footnote mirroring the Ch.3 wording is the place. **Round 4: still not added, by explicit author ruling for this round** (no Nash caveat in Ch.3 beyond the Ch.3 preface pointer fix, and none in Ch.4). Related round-4 fact worth recording: the Ch.3 preface used to say "the following chapters do not rely on it", which is false for this chapter (4_courb.tex:115 trains with the Nash-MTL balancer, :84 states the same for the MTLnet baseline). The Ch.3 pointer now names Chapter 5, which reports a fixed loss weighting and states that no balancer, Nash-MTL included, improved on it (5_mobiwac.tex:180-186). No change to this chapter was needed.
- [VERIFY] Stub chapter title wording: "ST-MTLNet: Spatio-Temporal Point-of-Interest Representations for Multi-Task Learning" (kept from the stub, per the keep-the-pattern rule) vs the paper's own EN title in `src_en/main.tex`: "ST-MTLNet: Spatio-Temporal Representations of Points of Interest for Multi-Task Learning". Confirm which form the author wants on the chapter heading.
- [VERIFY] The `\:` in the stub's chapter title (`ST-MTLNet\:`) renders as a thin space, NOT a colon — confirmed in this session's compiled PDF (heading and running header read "ST-MTLNet Spatio-Temporal ..."). Kept untouched per the keep-the-stub-pattern rule; if a literal colon is wanted, replace `\:` with `:` (Ch.3 and Ch.5 stubs carry the same construct — a global decision).
- Cite keys kept AS-IS from `src_en/references.bib` for the Phase-4 merge: silva2025mtlnet, cho2011friendship, jure2014snap, caruana1997multitask, wu2024torchspatial, feng2017poi2vec, rahmani2019category, velickovic2019deep, huang2023learning, sitzmann2020implicit, rußwurm2024geographiclocationencodingspherical, mai2023sphere2vecgeneralpurposelocationrepresentation, sun2020go, sun2024transtarec, kazemi2019time2vec, Liao2018, Xia2020, Halder2022, Xu2023, Lim2022, nash, perez2018film, baxter2000model, grover2016node2vec, church2017word2vec, belkin2003laplacian, 10.1145/2661829.2662002. Plus `paiva2026stmtlnet` (preface only; already in the global bib). Note for Phase 4: `church2017word2vec` is on the CBIC errata list (-> mikolov2013word2vec) — a bib-merge decision, not touched here.
