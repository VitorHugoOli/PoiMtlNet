% !TeX root = ../main.tex
This chapter states what the joint model does and what it measures. The two tasks read
the same history, so one model can learn them together. Macro-F1 counts every category
equally, and the dedicated single-task model is the reference point.
