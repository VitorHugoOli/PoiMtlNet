% !TeX root = ../main.tex
The shared trunk needs to be described plainly, and the neighbor vectors describe
the behavior of the center. The table shows several differences between the two
configurations.
