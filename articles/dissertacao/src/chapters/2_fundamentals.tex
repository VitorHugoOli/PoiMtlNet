% !TeX root = ../main.tex
% !TeX root = ../main.tex
% Chapter 2 -- Fundamentals (Fundamentacao Teorica). Frame chapter, English.
% Draft 2 (panel-reviewed), assembled 2026-07-23 from the section drafts of 2026-07-21 into this
% one file; sections in source order 2.1-2.5, lineage table inlined in Section 2.2. Every section
% obeys WRITING_LAW.md + GLOSSARY.md and closes with its own citation/number ledger.
% Bibliography: single global dissertation .bib. New keys are in fundamentals/_bib/new_references_ch2.bib;
% all other keys are already in the project bibs (see COVERAGE_EVALUATION.md and _bib/BIB_NOTES.md).


\chapter{Fundamentals}
\label{ch:fundamentals}

This chapter provides the concepts shared by the three studies. It first distinguishes
the POI prediction tasks (Section~\ref{sec:fund:tasks}). It then traces mobility
representations from one-hot identifiers to Check2HGI
(Section~\ref{sec:fund:repr}), explains the main forms and risks of multitask learning
(Section~\ref{sec:fund:mtl}), and defines the data and evaluation protocol
(Section~\ref{sec:fund:eval}). Section~\ref{sec:fund:relevance} connects these
elements to the questions addressed in the article chapters.
% Section 2.1 -- POI-prediction tasks


\section{Point-of-interest prediction tasks}
\label{sec:fund:tasks}

This section establishes what each model predicts before reviewing how those
predictions are made. Location-based social networks (LBSNs) record mobility as
check-ins, each linking a user, a POI, and a timestamp. Their geographic and temporal
detail supports data-driven studies of cities~\cite{silva2019urbancomputing}. The
records also reveal recurring behavior: people often return to a small set of places
and make longer trips less frequently~\cite{cho2011gowalla}. An entropy analysis
estimated the potential predictability of an individual's next location at about 93
percent~\cite{song2010limits}. Because this estimate concerns next-location prediction
at a coarse spatial resolution, it shows that mobility contains learnable regularity
but does not provide a reference point for the category and region metrics defined in
Section~\ref{sec:fund:eval}.

\subsection{Task boundaries and notation}
% [NEEDS SIGN-OFF: raised round 3, 2026-07-24 | gate fix B-5, 2026-07-24] The §2.1 "93% is the ceiling any model
% should be read against" was an unscoped universal contradicted by §2.4 (Song's bound
% is next-location at coarse resolution, not category-F1/region-ranking). Rescoped to
% next-location + forward-ref to §2.4; figure and "learnable at all" role kept. Persona
% 14 APPROVE-WITH-EDIT text. Revert to the prior wording if you prefer the original.

Human-mobility models address distinct targets, from crowd-flow estimation to
next-location prediction~\cite{luca2021mobilitysurvey}. Because these targets are not
interchangeable, model comparisons require explicit task boundaries. This dissertation
studies three tasks: one static and two sequential.

% [ROUND 12, item 44] THE PAGE-COUNT COMPACTION OF ROUND 10 IS UNDONE, on the author's
% authorization now that the page budget is suspended. Round 10's own report
% (src_utils/_round10/29_ch2_definitions.md sec. 4) records three display equations inlined and four
% lead-in sentences shortened, and states both were done for the page count and not for
% readability. Restored to display form here and below: $H_i$ (Definition 2.2), $g_{cat}$
% (Definition 2.3), and the pair $f_{cat}$ / $f_{reg}$ (Definitions 2.4 and 2.5). Restored lead-ins:
% "Three definitions separate the targets" (sec. 2.1), "One further target must be named" (sec. 2.2),
% and "rather than a figure of speech" (the gradient-conflict subsection). The FOURTH item on round
% 10's list, the duplicated place-embedding limitation sentence, is NOT restored: it was a
% deduplication rather than a clipped lead-in, its content is now Definition 2.7 itself, and round
% 11 (item 31) rebuilt that passage when it split the merged definition head. No equation restored
% here carries a label, because none is referenced from prose; the labelled equations of this
% chapter (eq:fund:mtl-total, eq:fund:cosine, eq:fund:macro-f1, and the rest) are labelled because
% something points at them.

\subsubsection{Check-ins and histories}

% [round12 STEP 1, 2026-08-03] The second sentence binds $c_p$ and $r_p$, which were UNBOUND before
% this edit. MEASURED, and the first version of this comment asserted a measurement that had not run
% (an auditor caught it; the grep cell had failed with empty output and I wrote the claim anyway):
%   - `c_p\in\mathcal{C}` and `r_p\in\mathcal{R}`: ZERO live occurrences anywhere in the tree at
%     HEAD, so neither symbol was ever given a type. The one live occurrence of each today is this
%     edit itself.
%   - bare `c_p`: ONE live occurrence at HEAD, at :119, and it is a CONSUMER -- the static-classifier
%     equation $g_{\mathrm{cat}}(\mathbf{e}_p)\longrightarrow c_p$ read the symbol without the
%     chapter ever having declared it. That is the defect this step fixes, and it is sharper than
%     "the symbol was absent": the symbol was in USE and undefined.
%   - bare `r_p`: ZERO live occurrences at HEAD. It enters with this edit and with the
%     $r_i=r_{p_i}$ of step 2, which is its first consumer. Author's ruling of
% 2026-08-03: option (a), keep the section order, and apply the definition redesign.
Let $\mathcal{U}$, $\mathcal{P}$, $\mathcal{C}$, and $\mathcal{R}$ denote the sets of
users, POIs, category classes, and region classes. Each POI $p\in\mathcal{P}$ carries a
category $c_p\in\mathcal{C}$ and lies in a region $r_p\in\mathcal{R}$.

\begin{definition}[Check-in]\label{def:fund:checkin}
The $i$th check-in of user $u$ is the tuple
\begin{equation}
    x_i=(u,p_i,t_i,c_i,r_i),
\end{equation}
% [round12 STEP 2, 2026-08-03] Was "$c_i\in\mathcal{C}$ is its category, and
% $r_i\in\mathcal{R}$ is its region". The equalities state that a check-in's category and region
% are the visited POI's attributes, which is what makes $\mathbf{e}_{p_j}$ well typed later. The
% set memberships still hold, now by way of the bindings in the notation prose above.
where $p_i\in\mathcal{P}$ is the visited POI, $t_i$ is its timestamp,
$c_i=c_{p_i}$ is its category, and $r_i=r_{p_i}$ is its region.
\end{definition}

\begin{definition}[Check-in history]\label{def:fund:history}
The check-in history of length $\ell$ preceding check-in $x_i$ is the ordered sequence
\begin{equation}
    H_i=(x_{i-\ell},\ldots,x_{i-1}).
\end{equation}
% [round12, 2026-08-03] THE AUTHOR WROTE THIS PASSAGE HIMSELF, closing AD-6 (sharpen the vague
% retention sentence, which read "A target label is withheld from it when one of the sequential tasks
% is trained"). His version names the target per task and states whose label it is, which is what
% answers the leakage question. Three sharpenings on top of it, and none rewrites his content:
%   1. FORWARD REFERENCE. His sentence was the FIRST live use of "next-category prediction" and
%      "next-region prediction" in the chapter, which are defined at Definitions 2.7 and 2.8 further
%      down. The symbols were fine ($c_i$ and $r_i$ are bound by Definition \ref{def:fund:checkin}),
%      but naming an undefined task is the same defect class the redesign of this round removed.
%      Fixed by pointing forward EXPLICITLY rather than by renaming his tasks away.
%   2. THE POSITIVE HALF WAS IMPLICIT. Excluding the label of $x_i$ is the half that rules out
%      leakage; the converse -- that the categories and regions of PAST visits are legitimate input --
%      was left unsaid, and that omission is exactly what prompted the question. Now one clause.
%   3. LINE WIDTH: rewrapped to the file's convention, near 85 columns. Revert the wrapping if the
%      author prefers his own; the words are unchanged either way.
For the two sequential tasks studied here (Definitions~\ref{def:fund:nextcat}
and~\ref{def:fund:nextreg}), the prediction target is $c_i$ for next-category
prediction and $r_i$ for next-region prediction. These labels belong to $x_i$, not
to the preceding check-ins in $H_i$: the categories and regions carried by the
visits in $H_i$ are observed input, and only the attributes of $x_i$ itself are
withheld.
\end{definition}
% ---------------------------------------------------------------------------
% CITATION / NUMBER LEDGER -- 2.1 (every claim traceable; verbs bound to tests)
%  silva2019urbancomputing  LBSN traces at high geo/temporal resolution; urban computing (S2 abstract, opened)
%  cho2011gowalla           periodic short-range + social long-range movement (Gowalla origin; KDD 2011)
%  song2010limits           93% potential predictability (VERIFIED firsthand, PDF Science 327:1018). "80%" is
%                           a low-predictability EXAMPLE, not a population figure -- not quoted.
%  luca2021mobilitysurvey   DL-for-mobility spans several distinct tasks (survey)
%  Xu2023                   static category classification (semantic annotation) is non-sequential
%  liu2016strnn             ST-RNN: time/distance-specific transition matrices (next PLACE)
%  feng2018deepmove         DeepMove: attentional recurrent over periodic mobility (next PLACE)
%  kong2018hstlstm          HST-LSTM: hierarchical LSTM w/ ST transitions (next PLACE) [new bib]
%  yang2020flashback        Flashback: attends past hidden states, sparsity (next PLACE)
%  luo2021stan              STAN: spatio-temporal attention, non-adjacent visits (next PLACE)
%  lian2020geosan           GeoSAN: geography-aware self-attention, grid (next PLACE) [new bib]
%  yang2022getnext          GETNext: global trajectory-flow map + transformer (next PLACE)
%  lin2021ctle              CTLE: context/time-aware per-visit location embedding (bridge to 2.2)
%  Lim2022                  HMT-GRN: region auxiliary to place (beam-search constraint)
%  yu2020catdm              CatDM: category auxiliary (candidate pruning)
%  zhu2022drrgnn            next-region as an end target (region granularity)
%  capanema2023poirgnn      POI-RGNN: next category as end target [ERRATA: CBIC cites wrong paper -> this key]
%  NUMBERS: only 93% (song2010limits), "seven categories", region counts ("a few hundred to several thousand")
%           -- region counts stated qualitatively here; exact per-dataset counts belong to Ch.5, not Ch.2.
% ---------------------------------------------------------------------------

% [round12 STEP 3, 2026-08-03] NEW SUBSUBSECTION, and the reason it sits HERE rather than in §2.2.
% The author ruled option (a) on 2026-08-03 (PENDENCIAS_RESOLVIDOS §6.24 (arquivado 2026-08-03)): keep the section order, and move the
% two representation definitions up into §2.1 so that no definition consumes a symbol defined later.
% Before this edit, Definition \ref{def:fund:catclf} read $\mathbf{e}_p$ roughly three hundred lines
% before the definition that introduces it. THE HEAD IS THE AUTHOR'S: "Check-in and place embedding",
% chosen by him on 2026-08-03 from the two registered candidates, after his revocation of the term
% "place representation" invalidated the head he had given earlier. It is not inferred from any other
% sentence of his. The two definition environments below are MOVED VERBATIM from §2.2, including the
% probe-pinned sentence of the check-in-level definition character-for-character (R12-eqxi,
% R11-def27), and each provenance comment block moved with its definition.
\subsubsection{Check-in and place embedding}

A task definition must also state what the model reads. Two representations recur
throughout this dissertation, and the three studies vary which one supplies the model
input.

% [ROUND 11, item 31] The two concepts below were ONE definition head until this round
% ("Place embedding and check-in-level representation", round 10). Round 10's own report
% (src_utils/_round10/29_ch2_definitions.md §4 and §7 item 2) records the merge as a page-count
% measure, not a content decision, and the defect it left was visible in the cross-references:
% :242 and :247 both pointed at the same label while meaning opposite halves of it. The author
% lifted the page budget and authorized the split. The wording of each half is round 10's,
% unchanged except for the dropped "instead", which contrasted with a sentence no longer in the
% same block.
% [round12 STEP 3] MOVED from §2.2 with the definition. The limitation paragraph that used to sit
% between the two definitions STAYS in §2.2, where it introduces the move from HGI to Check2HGI;
% its \ref is now a backward reference to this block.
\begin{definition}[Place embedding]\label{def:fund:placelevel}
A place embedding assigns one vector $\mathbf{e}_p$ to each POI $p$, so every check-in
at that POI enters the model with the same representation.
\end{definition}

% [round12, 2026-08-03, the author's finding] THIS DEFINITION NAMED NO SYMBOL. Definition 2.7 gives the
% place-level vector as $\mathbf{e}_p$ and this one said only "one vector to each check-in", which left
% the central contrast of the chapter half-formalized: the two representations are the pivot of the whole
% argument, and only one of them could be written down.
% THE SYMBOL IS $\mathbf{e}_{x_i}$, and it was chosen rather than invented freely:
%   - it parallels $\mathbf{e}_p$ exactly, differing only in what the subscript indexes, which IS the
%     distinction being defined (a place versus a check-in);
%   - $x_i$ is already the registered symbol for the $i$th check-in (GLOSSARY §1.1, Definition 2.1), so
%     the subscript introduces no new object;
%   - measured before minting: $\mathbf{e}_{x_i}$, $\mathbf{e}_{x}$, $\mathbf{e}_i$, $\mathbf{z}_i$ and
%     $\mathbf{z}_{x_i}$ occur ZERO times anywhere in the live tree, so nothing is overloaded. The only
%     $\mathbf{e}$ subscripts in use are $p$, and the generic $1$/$2$/$+$/$-$ of the discriminator
%     equations.
% GLOSSARY §1.1 ALREADY IMPLIED THIS GAP: its $\mathbf{e}_p$ row reads "distinct from a per-visit
% Check2HGI vector" while giving that vector no symbol of its own.
% REGISTRY ROW: WRITTEN, 2026-08-03, and this sentence had to change with it. It used to read "A
% registry row for $\mathbf{e}_{x_i}$ is PROPOSED to the author and is not written by an agent; the
% notation table is his" -- true when written, and made FALSE by the act of writing the row. The author
% authorized the exception explicitly (PENDENCIAS_RESOLVIDOS §6.26 (arquivado 2026-08-03) option 3: he
% told me to paste both rows rather
% than paste them himself), which closes AD-5. The general rule still holds and is not weakened by this
% one instance: the notation table is the author's, and an agent proposes rows rather than minting them.
% The row for $f_{\mathrm{place}}(H_i)$ landed in the same commit for the same reason -- it too was in
% live use, at Definition \ref{def:fund:nextplace}, with no registry entry.
% [round12 STEP 3] MOVED from §2.2 with its comment block. The sentence below is pinned by R12-eqxi and
% R11-def27 and is carried character-for-character.
\begin{definition}[Check-in-level representation]\label{def:fund:checkinlevel}
A check-in-level representation assigns one vector $\mathbf{e}_{x_i}$ to each check-in
$x_i$, so two visits to the same POI may enter the model with different
representations.
\end{definition}

% [round12 STEP 3, AD-1] THE REPRESENTATION MAP AS A NUMBERED DEFINITION, not a displayed equation in
% prose: the author chose thirteen definitions on 2026-08-03 ("Vamos de treze"), which makes $\rho$ a
% numbered object of the chapter. The remark that follows is corrected for defects F-1, F-2 and F-3 of
% src_utils/_round12/49: it is scoped to the SEQUENTIAL tasks (the static task reads $\mathbf{e}_p$
% directly, so "every predictive model" was false of it), and Chapter 5's two streams are both named.
\begin{definition}[Representation map]\label{def:fund:repmap}
A representation map assigns to each check-in a vector,
\begin{equation}
    \rho(x_i)\in\mathbb{R}^{d},
\end{equation}
and extends to histories elementwise,
\begin{equation}
    \rho(H_i)=\bigl(\rho(x_{i-\ell}),\ldots,\rho(x_{i-1})\bigr).
\end{equation}
\end{definition}

Every model of the sequential tasks in this dissertation reads $\rho(H_i)$ rather than
$H_i$ itself. Chapter~\ref{ch:cbic} defines the input at the place level,
$\rho(x_j)=\mathbf{e}_{p_j}$. Chapter~\ref{ch:courb} retains a vector tied to the visited
POI but enriches it with separate spatial, temporal, and categorical components.
Chapter~\ref{ch:mobiwac} moves the input to the check-in level. Its semantic stream reads
the per-visit vector $\rho(x_i)=\mathbf{e}_{x_i}$, while its spatial stream reads the
trained vector of the visit's region node. The task definitions remain fixed, while
each study uses its own form of $\rho$.

\subsubsection{The three experimental tasks}

Three definitions separate the targets. The first is static and reads a place; the
other two are sequential and read a history.

\begin{definition}[Category classification]\label{def:fund:catclf}
Category classification predicts the category of a POI from its representation:
\begin{equation}
    g_{\mathrm{cat}}(\mathbf{e}_p)\longrightarrow c_p.
\end{equation}
At evaluation time, POI $p$ is held out from the classifier-training fold, so
``unknown POI'' means unknown to the classifier, not necessarily absent from the graph
used to learn $\mathbf{e}_p$.
\end{definition}

\begin{definition}[Next-category prediction]\label{def:fund:nextcat}
Next-category prediction maps a check-in history to the category of the next visit:
\begin{equation}
    f_{\mathrm{cat}}(H_i)\longrightarrow c_i.
\end{equation}
\end{definition}

\begin{definition}[Next-region prediction]\label{def:fund:nextreg}
Next-region prediction maps a check-in history to the region of the next visit:
\begin{equation}
    f_{\mathrm{reg}}(H_i)\longrightarrow r_i.
\end{equation}
\end{definition}

The task pairing changes across the studies. Chapters~\ref{ch:cbic}
and~\ref{ch:courb} combine category classification
(Definition~\ref{def:fund:catclf}) with next-category prediction
(Definition~\ref{def:fund:nextcat}). Chapter~\ref{ch:mobiwac} combines next-category
prediction with next-region prediction (Definition~\ref{def:fund:nextreg}). All three
studies use seven categories: Community, Entertainment, Food, Nightlife, Outdoors,
Shopping, and Travel. For the region task, the target is a census tract in the United
States datasets and a \emph{mahalle} in Istanbul. These output spaces require different
metrics and reference points, as Section~\ref{sec:fund:eval} explains.

\subsection{Related sequential prediction settings}

One further target must be named, because it is the task that the models this section
reviews were built to solve.

% [round12, 2026-08-03, the author's finding] THIS DEFINITION BROKE THE PATTERN OF ITS OWN NEIGHBORS.
% Definitions 2.3, 2.4 and 2.5 each state their task as a function: $g_{\mathrm{cat}}(\mathbf{e}_p)
% \to c_p$, $f_{\mathrm{cat}}(H_i) \to c_i$, $f_{\mathrm{reg}}(H_i) \to r_i$. This one described the
% mapping in words only, which made the third task look like a different KIND of object from the other
% three at exactly the point where the chapter's job is to keep the three formally distinct.
% $f_{\mathrm{place}}$ follows the established naming: $f$ for the sequential predictors that read a
% history (as against $g$ for the static classifier that reads a place vector), subscripted by the
% target. The target is $p_i \in \mathcal{P}$, both already registered (Definition 2.1, GLOSSARY §1.1).
% NAMING THE FUNCTION SHARPENS THE SCOPE STATEMENT RATHER THAN WEAKENING IT. The exclusion now applies
% to a defined object: the reader sees precisely which mapping is not being learned, instead of being
% told in prose that something adjacent is out of scope. The sentence keeps the exclusion explicit, and
% no chapter of this dissertation reports a result for $f_{\mathrm{place}}$.
% A registry row for $f_{\mathrm{place}}(H_i)$ in GLOSSARY §1.1 is PROPOSED and not written here: the
% notation table is the author's, as the two representation symbols are.
\begin{definition}[Next-place prediction]\label{def:fund:nextplace}
Next-place prediction maps a check-in history to the identity of the next visited POI:
\begin{equation}
    f_{\mathrm{place}}(H_i)\longrightarrow p_i.
\end{equation}
It is named to delimit the scope of the dissertation, and no chapter reports a result
for $f_{\mathrm{place}}$.
\end{definition}

Next place is the dominant task in the field, and methods developed for it provide the
methodological basis for next-category and next-region prediction. Early approaches
relied on recurrent architectures. ST-RNN uses time-specific and distance-specific
transition matrices~\cite{liu2016strnn}, while DeepMove adds attention over long and
sparse visit histories~\cite{feng2018deepmove}. HST-LSTM incorporates
spatial-temporal context into a hierarchical recurrent cell~\cite{kong2018hstlstm}.
Flashback retrieves past hidden states according to the temporal and spatial gaps
between visits~\cite{yang2020flashback}.

Later approaches make attention central to the architecture. STAN connects
non-adjacent visits through spatio-temporal correlations~\cite{luo2021stan}. GeoSAN
combines a hierarchical geographic grid with self-attention~\cite{lian2020geosan}, and
GETNext introduces a global trajectory-flow map into a
transformer~\cite{yang2022getnext}. Every model named here predicts the exact next
place, so none of them is a direct baseline for the targets studied in this dissertation.

\subsection{Category and region as end targets}

Two strands of this literature support the task choice in this dissertation. The first
concerns how a visit is represented. CTLE learns context-aware and time-aware location
embeddings, allowing the same POI to have different vectors in different
visits~\cite{lin2021ctle}. This per-visit view motivates the representation discussed in
Section~\ref{sec:fund:repr}.

The second strand concerns the role of category and region predictions. Some methods
use them to support next-place prediction rather than as final outputs. HMT-GRN uses a
predicted region to constrain the search for a place~\cite{Lim2022}, and CatDM predicts
a category to reduce the set of candidate places~\cite{yu2020catdm}. Other studies make
one of these properties the final target. Activity-region prediction addresses the
region directly~\cite{zhu2022drrgnn}, while POI-RGNN predicts the next
category~\cite{capanema2023poirgnn}. This dissertation uses next category and next
region as final outputs of equal status in one model.
% Section 2.2 -- Representations for mobility (the chapter's spine)


\section{Representations for mobility}
\label{sec:fund:repr}

This section traces the representations used in the dissertation and explains why the
final study moves from a place embedding to a check-in-level representation.

\subsection{From identifiers to graph embeddings}

The representation line begins with identifiers and progresses to vectors learned
from graph structure.
% [NEEDS SIGN-OFF: NUM-4, round6, 2026-07-28] Number RE-ANCHORED and its convention added.
% The clause previously read "the category F1 on Alabama, over five folds, rose monotonically
% from 0.74 to 0.82 across the swept values": two means with no spread, no swept range, and no
% epoch budget, and both quoted from research/embeddings/hgi/CLAUDE.md:117, which itself ROUNDS
% the sweep table -- a computed step between the page and the source of record. Now quoted from
% the table, research/embeddings/hgi/README.md:544 (header, "5 folds x 50 epochs") and :548-551:
%   w_r 0.4 -> 0.7388 +/- 0.0205 | 0.5 -> 0.7678 +/- 0.0211
%   w_r 0.6 -> 0.7944 +/- 0.0186 | 0.7 -> 0.8186 +/- 0.0123
% Monotonicity is the source's own word (CLAUDE.md:117; preprocess.py:38) and is readable off
% those four rows without computing anything. The adopted 0.7 is the shipped default:
% preprocess.py:23 DEFAULT_CROSS_REGION_WEIGHT = 0.7. The published w_r = 0.4 and the
% region-representation goal were verified firsthand in the HGI PDF, Eq. 2 ("wr is a factor to
% differentiate intra- (wr = 1) and cross-region (wr = 0.4) edges") and its abstract.
% [VERIFY: averaging convention of the swept "Cat F1"] Every source records "Cat F1" without
% naming macro or weighted averaging, so the sentence says "category F1" and not "macro-F1";
% author to confirm the convention, or drop the two values and keep the clause qualitative.

A one-hot identifier marks a place or category by position but encodes no relationship
between identifiers. Distributed representations instead learn dense vectors whose
geometry reflects relationships in the data. Skip-gram places symbols with similar
contexts near one another~\cite{mikolov2013word2vec}. DeepWalk transfers this idea to
graphs by treating random walks as sentences~\cite{perozzi2014deepwalk}, and node2vec
adjusts the walk to emphasize homophily or structural
roles~\cite{grover2016node2vec}. Graph neural networks learn these relationships
through neighborhood aggregation. Graph convolutional networks use a localized
spectral rule~\cite{kipf2017gcn}, graph attention networks learn the weight of each
neighbor~\cite{velivckovic2017graph}, and GraphSAGE learns aggregators that can embed
nodes not seen during training~\cite{hamilton2017graphsage}.

\subsection{Hierarchical graph infomax}

The graph-infomax methods supply the unsupervised representations used across the
three studies. The idea these methods share is simple enough to state before any of
the notation: the model learns useful vectors by being asked to tell a true pairing of
two parts of the data from a corrupted one, and it needs no labels to do so, because
the data itself says which pairing is the true one.
% AUT-19 (PENDENCIAS §4): the subsection was already substantial, 584 words, and it did explain the
% mechanism (mutual information between adjacent levels, the bilinear discriminator, the logistic
% function, the four HGI stages). What it lacked was an entry point: it opened on what infomax
% MAXIMIZES before saying what infomax IS, so a reader met the objective before the idea. The
% sentence added here is didactic, which §1 of WRITING_LAW allows in a dissertation, and it introduces
% no term outside the registry. Probe: R13-aut19.

The representations used in this work are trained without category or region labels.
Mutual-information estimators provide one way to learn such
representations~\cite{belghazi2018mine}. Deep InfoMax maximizes agreement between
local features and a global summary~\cite{hjelm2019dim}, and Deep Graph Infomax (DGI)
applies this objective to graph nodes and a graph-level
summary~\cite{velickovic2019deep}. DGI supplies the objective and the place embeddings
used in the first study. Hierarchical Graph Infomax (HGI) extends that objective
across POI, region, and city levels~\cite{huang2023hgi}.

HGI is the place-level representation that the final study replaces, so its reference
point matters here. The mechanism itself is set out in the original paper, and the
account below is limited to what the later chapters rely on. Its own stated goal is to
learn urban region representations from POIs in a fully unsupervised manner, and it
reaches that goal by extending the two scales of DGI to three: POI, region, and city.
The name states what is maximized. Training raises the mutual information between
representations at two adjacent levels of that hierarchy, and it does so without
evaluating that quantity in closed form. A bilinear discriminator combines two
embeddings through a learned weight matrix and passes the result through a logistic
function, and the loss rewards a high score for a true pair and a low score for a
false pair. No label of any downstream task enters that comparison, so the
representation is obtained without supervision.

One consequence of the design matters for the later chapters. A pretrained category
encoder supplies the initial POI features, one graph convolution layer over a Delaunay
graph of the POIs of the study area adds spatial context to each of them, multi-head
attention aggregates the POI embeddings of a region, and an area-weighted sum over
regions produces one city embedding. The training signal updates the POI encoder, the
aggregation function, and the region encoder together, so a POI embedding is optimized
to score high against the embedding of its own region and low against the embeddings
of other regions. Region membership also enters earlier, through the edge weights of
the POI graph, which decrease with the distance between two POIs and are reduced
further when the two POIs lie in different regions. The POI-level output is therefore
not a description of the place in isolation. It already reflects the region the place
belongs to.

HGI was developed and evaluated for urban region representation. Its reported
experiments estimate urban functional distributions, population density, and housing
price in Xiamen Island and Shenzhen, and the object under evaluation there is the
region embedding. The POI embedding is an internal stage in producing it. This
dissertation repurposes that POI-level output for sequential prediction, a use the
original evaluation does not cover. In this role, HGI supplies the place-level
baseline in the later studies and the direct basis of Check2HGI.

The move to other study areas left one setting of that baseline open. Huang et al.\
reduce an edge between POIs of different regions to 0.4 of its weight in the
experiments named above, and this work did not transfer that value untested. In
experiments on one of the state datasets of this dissertation, over the same five
folds used elsewhere, a value of 0.7 gave the best category F1 among the values tried,
and the later studies adopt it. What that comparison settles is one hyperparameter of
the place-level baseline. It is not evidence about how HGI and Check2HGI compare.
% [round12, 2026-08-03] THE 0.7 SENTENCE (item 42, second half), on the author's authorization:
% "Sobre citar que na dissertacao usamos 0,7, podemos citar por alto, algo como: 'Em experimentos
% avaliamos que para nossos datasets 0,7 gerava os melhores resultados', como vamos remover o
% appendix E nao precisa referenciar ele." NO \ref to apx:hgi-tuning: there was none in this file
% before this edit (checked live, comment-stripped), and none is added.
% NUMBER PROTOCOL. The paragraph quotes exactly ONE number of its own, 0.7, and it is not a
% result: it is the shipped default, research/embeddings/hgi/preprocess.py:23
% DEFAULT_CROSS_REGION_WEIGHT = 0.7. The RESULT behind it is stated qualitatively ("the best
% category F1 among the values tried"), which is readable off the source-of-record table without
% computing anything: research/embeddings/hgi/README.md:544 (header, "5 folds x 50 epochs") and
% :548-551, whose four rows are 0.4 -> 0.7388 +/- 0.0205, 0.5 -> 0.7678 +/- 0.0211,
% 0.6 -> 0.7944 +/- 0.0186, 0.7 -> 0.8186 +/- 0.0123 (mean +/- standard deviation across the five
% folds). The largest of the four is the 0.7 row, so "best among the values tried" is the table's
% own ordering and CONTRADICTS NOTHING in the NUM-4 cells, which stay where they are and keep
% their spreads and their convention. "one of the state datasets" is Alabama (README.md:544 heads
% the table "Alabama w_r sweep"); the state is not named in prose because the sweep is a single
% state and the sentence claims no more than that. The [VERIFY] on the averaging convention of
% "Cat F1" (see the NUM-4 ledger earlier in this section) still stands, which is why this
% paragraph writes "category F1" and not "macro-F1".
% THE PUBLISHED 0.4 WAS LOCATED FIRSTHAND THIS SESSION in the source PDF, science/articles/
% Learning urban region representations with POIs and hierarchical graph infomax.pdf, p.137
% (sheet 4) sec. 3.3, Eq. 2 and the sentence under it: "wr is a factor to differentiate intra-
% (wr = 1) and cross-region (wr = 0.4) edges", and "The choices of the parameters ... and wr =
% 0.4 (cross-region) are in view of the previous practices". Prose states it as a reduction to
% 0.4 of the intra-region weight, which is what wr = 1 versus wr = 0.4 in that sentence says.
% "the experiments named above" is the preceding paragraph's Xiamen Island and Shenzhen, so the
% study areas are named once in this subsection, not twice.
% SCOPE WARNING RELOCATED, deliberately, and this is the item's constraint (b). The warning used
% to live in the last two sentences of chapters/apx_g_hgi_tuning.tex ("This sweep supports
% adaptation of one baseline hyperparameter; it is not evidence about the relative performance of
% HGI and Check2HGI"). That appendix prints as APPENDIX E of the defense volume (counter order in
% content.tex: apx_a -> A, apx_c -> B, apx_e -> C, apx_f -> D, apx_g -> E) and is being moved out
% of that volume by another agent. A warning in a volume the reader of Chapter 2 no longer has is
% not a warning, so the limit is carried INSIDE this paragraph, in its last two sentences, rather
% than pointed at. The appendix file is not edited here (not mine, and another agent is in it).
% [round11, 2026-08-03] HGI MECHANISM EXPANDED (item GER-02, second part; the author's ruling is
% that the HGI explanation is worth improving because it is "uma das abordagens mais importantes
% para as contribuicoes da dissertacao"). The previous text was five sentences of correct but
% compressed mechanism. Nothing in it was contradicted; every clause of it survives in substance.
% EVERY MECHANISM CLAIM BELOW WAS READ THIS SESSION at the source PDF on disk,
% science/articles/Learning urban region representations with POIs and hierarchical graph
% infomax.pdf = huang2023hgi, ISPRS J. Photogramm. Remote Sens. 196 (2023) 134-145,
% doi 10.1016/j.isprsjprs.2022.11.021 (journal pagination cited; PDF sheet in parentheses).
%   goal + unsupervised   p.134 (sheet 1) abstract: "learning urban region representations (vector
%                         embeddings) with points-of-interest (POIs) in a fully unsupervised manner".
%   three scales          p.136 (sheet 3) end of Related works: "we extend DGI to model the
%                         interactions between three scales, i.e., POI, region, and city".
%   what is maximized     p.134 abstract: "trained through maximizing the mutual information among
%                         the POI - region - city hierarchy"; p.139 (sheet 6): "a region-centered
%                         hierarchical mutual information maximization".
%   no closed-form MI     p.138 (sheet 5) sec. 3.7: "The general idea of HGI follows the paradigm of
%                         contrastive learning". The MI is never estimated in closed form: the losses
%                         Eq. 12-13 p.139 are log-scores of the discriminator D, nothing else. [round12,
%                         item 42] The prose sentence naming the contrastive paradigm was CUT in the
%                         trim; what this row now sources is the surviving clause "without evaluating
%                         that quantity in closed form".
%   discriminator         p.139 below Eq. 13: D_pr(p_i, r_k) = sigma_L(p_i W_pr r_k), "sigma_L is a
%                         sigmoid function". Prose says "logistic function" per GLOSSARY. "bilinear
%                         discriminator" is the registered term (GLOSSARY sec. 3), already used by
%                         the Check2HGI equations later in this section.
%   no downstream labels  p.138-139 sec. 3.7: "The training of HGI only relies on POIs and region
%                         boundaries, and not on any ground truth data in the downstream tasks."
%   category encoder      p.136 sec. 3.1(1): "A POI category encoder phi_c is pretrained to generate
%                         the initial POI features, as categorical information generally plays a key
%                         role in defining the meaning of a POI." Paraphrased to avoid the banned
%                         "plays a crucial role" template shape (WRITING_LAW sec. 4).
%   Delaunay + weights    p.137 (sheet 4) sec. 3.3: "all the POIs in a study area are connected using
%                         DT to form a graph"; Eq. 2 a_p = log((1+L^1.5)/(1+l^1.5)) x w_r, "l
%                         represents the spatial distance between the two POIs ... w_r is a factor to
%                         differentiate intra- (w_r = 1) and cross-region (w_r = 0.4) edges". The
%                         DIRECTION is what the mechanism sentence carries (decreases with distance;
%                         reduced across regions). [round12, item 42] The published 0.4 is now ALSO
%                         stated, once, in the adaptation paragraph below, which is where the retuning
%                         to 0.7 is explained; the same Eq. 2 sentence sources both, and the two do
%                         not collide because the paragraph says whose value each one is.
%   one GCN layer         p.137 sec. 3.3: "we apply a one-layer GCN encoder"; p.138 (sheet 5) top: the
%                         POI embedding is updated with "the information from itself and its spatial
%                         context". [round12, item 42] The paper's "transformed combination ... which
%                         captures its uniqueness" wording was CUT with the trim; prose now says only
%                         that the layer adds spatial context, which is the clause quoted here.
%   multi-head attention  p.138 sec. 3.4: the POI embeddings of a region are aggregated by multi-head
%                         attention into the region raw embedding. [round12, item 42] The per-head
%                         "each head represents a perspective" sentence was CUT; the aggregation
%                         itself is what prose retains.
%   city embedding        p.138 sec. 3.6, Eq. 10: "area-weighted summarization", aw_i "is the area
%                         proportion of region r_i in the study area (city)".
%   positives/negatives   p.139 sec. 3.7: for a region, positives are "the POI embeddings P_i within
%                         the region", negatives "the POI embeddings P_j in another region r_j". This
%                         is what the surviving sentence "score high against the embedding of its own
%                         region and low against the embeddings of other regions" rests on. [round12,
%                         item 42] The CITY-level pair (row-wise feature shuffling), the two worked
%                         examples of p.135/p.137, and the single balancing weight alpha of Eq. 11
%                         were all CUT as mechanism detail, so their ledger rows are removed with
%                         them; the reader who wants them goes to the original paper, as the author
%                         directed.
%   joint training of     p.136 sec. 3.1(6): the hierarchical infomax objective trains "the components
%   the three modules     of phi_p, AGG_poi-region, and phi_r".
%   what HGI was FOR      p.134 abstract and p.139 (sheet 6) sec. 4.1: three downstream tasks,
%   (the honest caveat)   "estimating urban functional distributions, population density, and housing
%                         price", study areas "Xiamen Island and Shenzhen, China". The evaluated
%                         object is the REGION representation throughout; the POI embedding is an
%                         intermediate product (sec. 3.3 -> 3.4). The caveat sentence the previous
%                         text carried is kept and made specific, per the item's constraint.
% NOT WRITTEN, though the source supports them, because this is a fundamentals chapter and not a
% paper summary: hard negative sampling with the cosine window (p.139), the PReLU activations, the
% Laplacian-Eigenmaps term of the category encoder (p.136 Eq. 1), and all reported results.
% NO NEW GLOSSARY TERM IS INTRODUCED. Every technical word used here is already registered or
% already live in this chapter: bilinear discriminator and logistic function (GLOSSARY sec. 3),
% POI / place, region, DGI, HGI, Check2HGI (sec. 2 and sec. 3); Delaunay graph, graph convolution and
% multi-head attention are the source's own mechanism names and are used in Ch.3 and Ch.4 prose
% already (chapters/3_cbic/method.tex:35, chapters/4_courb/related.tex:18).
% CONSISTENCY CHECKED against the two places the chapter already commits to about HGI: the lineage
% table row ("Extends graph infomax over the POI, region, and city hierarchy to yield region-aware
% place embeddings", tables/frame/lineage.tex:26) and the place-level framing of sec. 2.2. The new
% paragraph on how regional context reaches a POI embedding is the EXPLANATION of the table's
% "region-aware", not a new claim.

\subsection{From a fixed place vector to visit context}

The move from HGI to Check2HGI begins with information that a fixed POI vector cannot
express.

% [round12 STEP 4, 2026-08-03] THE TWO DEFINITION ENVIRONMENTS THAT STOOD HERE MOVED UP INTO §2.1,
% into the new subsubsection "Check-in and place embedding", under the author's option-(a) ruling
% (PENDENCIAS_RESOLVIDOS §6.24 (arquivado 2026-08-03)). Their provenance comments moved with them. What STAYS here is the prose that
% USES them: the limitation paragraph below, which introduces the move from HGI to Check2HGI, and the
% Check2HGI sentence further down.
% REFERENCE DIRECTION: backward in both cases now, which is the point of the move.
Definition~\ref{def:fund:placelevel} carries one limitation for this research: a
weekday morning and a Saturday night at the same place have identical inputs at the
representation level. CTLE shows an alternative by learning context-aware and
time-aware location embeddings whose vectors change with the
visit~\cite{lin2021ctle}.

% [round12 STEP 4] The check-in-level definition that stood here moved up to §2.1 with its comment
% block; the sentence below keeps its reference, now backward.
Check2HGI reaches the check-in level of Definition~\ref{def:fund:checkinlevel} by
adding the check-in below the place in the hierarchy.

\subsubsection{Context encoders}

% [round12 STEP 5, 2026-08-03, AD-2 option 2] Was "while retaining a place-level input". The author
% ruled that the unresolved level of Chapter 4's temporal channel is NOT recorded as an erratum
% (PENDENCIAS_RESOLVIDOS §6.22 (arquivado 2026-08-03)) and that Chapter 2 uses the NEUTRAL form: a vector that is a function of the
% visited POI, with no claim about the level at which the temporal component is learned. The two
% wordings that remain FORBIDDEN are "of the visit's timestamp", which would assert per-visit, and
% "aggregated", which would assert a combination the code does not perform. The question itself is
% documented as LO-12 in src_utils/LEFT_OUT.md and is closed rather than pending. This sentence now
% matches the remark in §2.1, which says the same thing in the same terms.
A per-visit representation needs temporal and spatial context in addition to the
identity of the visited POI. The second study supplies that context through separate
encoders while its input stays a function of the visited POI.
Time2Vec combines linear and periodic components for temporal
features~\cite{kazemi2019time2vec}. SIREN uses sinusoidal activation functions to
represent continuous signals~\cite{sitzmann2020implicit}. Space2Vec encodes location
and spatial context at several scales~\cite{mai2020multiscalerepresentationlearningspatial},
while Sphere2Vec represents coordinates on a sphere rather than on a flat
surface~\cite{mai2023sphere2vecgeneralpurposelocationrepresentation}. A related
approach combines spherical harmonics with sinusoidal
networks~\cite{russwurm2024geographiclocationencodingspherical}.
% AUT-22 (PENDENCIAS §4) = GER-06, the SAME defect found independently by the author and by the
% advisor's reviewer, already dispositioned [YOU APPLY] with the words "FiLM is a conditioning
% mechanism, not a mobility representation, and belongs with the sharing topologies in 2.3".
% Applied ONCE, here, for both items: the two FiLM sentences moved to §2.3.1, where the sharing
% topologies are set out. Nothing about FiLM is lost; it is now introduced next to the hard-sharing
% definition it modifies, which is what makes it legible.
% The two Ch.4 sentences that followed them stay in this subsection: they are about the INPUT
% representation (the controlled swap of one place embedding for three encoders), which is this
% subsection's subject, so they are not part of the move.
Chapter~\ref{ch:courb} keeps the MTLnet architecture but replaces its single
place embedding with spatial, temporal, and categorical encoders. The controlled
change isolates the effect of the input representation.

\subsubsection{The check-in level}

Check2HGI completes the move from contextualized inputs to one learned representation
per visit. It adds a fourth level below the place and learns one vector per visit
without using task labels. The hierarchy has
three adjacent-level boundaries: check-in to place, place to region, and region to
city. Its objective is the fixed-weight sum
\begin{equation}\label{eq:fund:check2hgi}
    \mathcal{L} = 0.4\,\mathcal{L}_{c2p} + 0.3\,\mathcal{L}_{p2r} + 0.3\,\mathcal{L}_{r2c},
\end{equation}
where $\mathcal{L}_{c2p}$, $\mathcal{L}_{p2r}$, and $\mathcal{L}_{r2c}$ correspond
to these three boundaries. Each term uses a bilinear discriminator,
\begin{equation}\label{eq:fund:check2hgi-disc}
    \mathcal{D}(\mathbf{e}_1, \mathbf{e}_2) = \sigma(\mathbf{e}_1^{\top}\mathbf{W}\mathbf{e}_2),
\end{equation}
where $\mathbf{W}$ is learned and
$\sigma(z)=1/(1+e^{-z})$ is the logistic function. The discriminator is linear in
either embedding when the other is held fixed, and its output lies between 0 and 1.
For each boundary, the loss favors a high score for a true pair and a low score for a
false pair:
\begin{equation}\label{eq:fund:check2hgi-term}
    \mathcal{L}_{*} = -\log \mathcal{D}(\mathbf{e}^{+}, \mathbf{e}^{+})
    - \log\left(1 - \mathcal{D}(\mathbf{e}^{+}, \mathbf{e}^{-})\right),
\end{equation}
Here, $\mathbf{e}^{+}$ belongs to a true pair, such as a check-in and the place where
it occurred, whereas $\mathbf{e}^{-}$ is substituted from another example in the
batch. No prediction target appears in these equations, which is why the
representation is described as trained without task labels.

% [NEEDS SIGN-OFF: COD-015, round6, 2026-07-28] New frame prose: the Check2HGI loss, which the
% document did not carry anywhere. The author approved adding it.
% WHY HERE AND NOT IN Ch.5: this paragraph is where the dissertation builds the infomax line,
% DGI's local-global objective then HGI's hierarchical one, so the fourth boundary reads as a term
% added to an objective met two paragraphs earlier, and the frame places it before both chapters
% that use the representation. Ch.5 §5.4.1 states the same construction in words for a paper
% audience under a page limit; that chapter is under review, so an equation there would be a
% two-file change to the submitted text and its argument does not need one.
% SOURCE, transcribed not invented: docs/context/check2hgi_overview.tex, section "Funcao de
% Perda" (Portuguese). Its three display equations, in order, are the three above; symbols,
% weights, subscripts and the star on L_* are the source's own. Dropped in transcription: its
% \underbrace annotations (their content is in the sentence instead) and its Portuguese labels.
% The three weights are also the shipped defaults, verified in the running code:
% research/embeddings/check2hgi/model/Check2HGIModule.py:51-53 (alpha_c2p=0.4, alpha_p2r=0.3,
% alpha_r2c=0.3), assembled at :1192-1195; the same three in pipelines/embedding/check2hgi.pipe.py:43-45.
% Per-boundary term at :1159, :1184, :1189; discriminator at :1003-1018 (matmul, elementwise
% product summed, torch.sigmoid) and :246 "Bilinear transformation weights for discrimination at
% each boundary".
% TWO SCOPE FACTS THE AUTHOR SHOULD KNOW. (1) e^- is "substituted from elsewhere in the batch",
% which is the source's optimized path (its "Corrupcao de Embeddings": negatives permute
% already-computed embeddings rather than re-running the encoder), and what the code does by
% default; the equations are indifferent to how e^- is produced. (2) The auxiliary terms Ch.5
% mentions (masked reconstruction and anchor, weights 0.3 and 0.1) are NOT in this equation: the
% source does not carry them and their code defaults are 0.0 (Check2HGIModule.py:68, :178),
% enabled per run. So this is the three-boundary objective as the source states it, and the prose
% does not claim it is the complete training loss of every Ch.5 run. [VERIFY: whether Ch.5's two
% auxiliary terms should be named here too; that needs the run configuration of the shipped
% representation, which I did not establish this session.]
% GLOSSARY DEPENDENCY: RESOLVED 2026-07-28. "bilinear discriminator" and "logistic function" were
% not in the registry, the drafting agent declined to self-authorize them and recorded the
% paragraph as BLOCKED (the behavior GLOSSARY §1 asks for), and both are now registered in §2 with
% the equation they name. What remains is ordinary sign-off on the wording. Prose says "logistic
% function", never "sigmoid".

Where a place embedding answers ``what is this place,'' a check-in-level representation
answers ``what is this visit,'' and
Chapter~\ref{ch:mobiwac} develops this representation and the joint model built on it.
Table~\ref{tab:fund:lineage} traces the full progression, from DGI through HGI to the
check-in level and the joint model that uses it.

\subsection{Model lineage}

% AUT-24 (PENDENCIAS §4): the old opener read "The representation changes are paired with a
% controlled progression in model architecture." Two faults, both named in WRITING_LAW §1. "are
% paired with" says nothing about who pairs them or why, and "a controlled progression" is an
% abstract noun doing the work a verb should do. It also opened a subsection whose body is concrete
% (the final model keeps the MTLnet lineage and swaps residual layers for cross-attention blocks),
% so the entry was more abstract than what it introduced. The replacement names the subject and what
% it does. No new claim; the three architectures and their order are already the content of the
% subsection.
% ONE REVISION, worth recording because the first attempt introduced a defect of its own: I first
% wrote a second pointer to Table~\ref{tab:fund:lineage} here. The preceding subsection already
% closes on that pointer at :681, so the two would have referenced the same table in consecutive
% sentences. Dropped.
% A blank line separates this block from the prose below it. Without one, the trapped-prose gate
% reads the comment tail as running into the first prose line and reports a broken sentence.
% Probe: R13-aut24.

Each study changes the input representation, and each also changes the architecture
that consumes it. Naming both sequences together lets a later chapter be read against
the step it altered.

The final joint model remains part of the MTLnet lineage. It retains the task-specific
input encoders, output heads, and division between shared and task-specific parameters.
MTLnet uses FiLM-conditioned residual layers as its shared middle. The joint model
replaces that component with cross-attention blocks, allowing each task stream to
read the other while retaining its own feed-forward parameters. A private spatial
path also gives the region output direct access to the region sequence before the
streams interact. The models therefore differ in their sharing topology and in the
private input available to the region output. This relationship allows the negative
result in Chapter~\ref{ch:cbic} and the positive result in
Chapter~\ref{ch:mobiwac} to be interpreted as two stages of one model lineage.
% [NEEDS SIGN-OFF: COD-013, round6, 2026-07-28] New frame prose. The author approved naming the
% joint model's descent from MTLnet; it was carried only by the lineage table's row order.
% Placed HERE and not in Ch.5 for the same reason as the equation above: the fact is about the
% RELATION between two chapters' artifacts, which is the frame's subject, and it is what licenses
% reading Ch.3's null against Ch.5's positive result. Within the paper the joint model stands alone.
% ESTABLISHED FROM THE CODE, read this session:
%   src/models/mtl/mtlnet_crossattn_dualtower/model.py:42  class MTLnetCrossAttnDualTower(MTLnetCrossAttn)
%   src/models/mtl/mtlnet_crossattn/model.py:207           class MTLnetCrossAttn(MTLnet)
%   src/models/mtl/mtlnet/model.py:39                      class MTLnet(nn.Module)   (the base)
%   -> the chain is MTLnet -> CrossAttn -> DualTower.
% "overrides exactly one component": mtlnet_crossattn/model.py:362 def _build_shared_backbone,
% docstring :368 "Override MTLnet's FiLM + shared_layers with cross-attention blocks"; its body
% registers crossattn_blocks and two LayerNorms and nothing else. The FiLM + residual stack it
% replaces is mtlnet/model.py:193-197, built from :179 in the base class.
% "inherits encoders, heads, parameter partition": mtlnet_crossattn/model.py:208-214 class
% docstring; accessors shared_parameters/task_specific_parameters/reg_specific_parameters at
% :554, :563, :578. "each stream keeps its own feed-forward weights": :55-66 (_CrossAttnBlock
% docstring) and :63 "Separate FFNs per task, no parameter sharing". The second specialization:
% mtlnet_crossattn_dualtower/model.py:1-9 ("The ONLY behavioral change is that the reg head ... is
% handed the raw post-pad-mask region sequence") and :86-90.
% Registry id deliberately absent from prose per WRITING_LAW §2. No number is quoted here and no
% result is claimed; the two verdicts referenced are the ones Ch.3 and Ch.5 already state.

\input{tables/frame/lineage}

% ---------------------------------------------------------------------------
% CITATION / NUMBER LEDGER -- 2.2 (the spine; DGI -> HGI -> check-in level explicit)
%  mikolov2013word2vec   skip-gram distributed embeddings (one-hot -> dense) [new bib; ERRATA: CBIC
%  church2017word2vec -> this]
%  perozzi2014deepwalk   DeepWalk: skip-gram on graph random walks [new bib]
%  grover2016node2vec    node2vec: biased-walk neighborhood objective
%  kipf2017gcn           GCN: localized spectral neighbor aggregation [new bib]
%  velivckovic2017graph  GAT: learned attention over neighbors [ERRATA: cite ICLR 2018 version]
%  hamilton2017graphsage GraphSAGE: inductive aggregators [new bib]
%  belghazi2018mine      MINE: MI estimable/maximizable by gradient descent [new bib]
%  hjelm2019dim          Deep InfoMax: local-global MI representation [new bib]
%  velickovic2019deep    DGI: node reps by local-patch/global-summary MI (base of lineage) [ERRATA: consolidate DGI
%  triple-key]
%  feng2017poi2vec       POI2Vec: geographical influence in POI embedding (early mobility place-embedding)
%  huang2023hgi          HGI: MI across POI-region-city hierarchy (VERIFIED firsthand, PDF). Direct base.
%                        REV-019 (2026-07-26): the "region-aware place embeddings" wording is CORRECT and was kept
%                        (the implementation exports both a POI-level and a region-level embedding,
%                        research/embeddings/hgi/hgi.py:101-110 + :176-199). Added instead the two qualifications a
%                        domain examiner asks for: (a) Huang et al.'s stated goal is urban REGION representation
%                        (abstract, VERIFIED firsthand in the repo PDF at articles/dissertacao/science/articles/:
%                        "learning urban region representations ... evaluated on estimating urban functional
%                        distributions, population density, and housing price"), so the POI-level intermediate is
%                        REPURPOSED here for a sequential task the source never addresses; (b) the baseline was
%                        retuned, w_r 0.4 -> 0.7. Sources for (b) in the [VERIFY] block at the clause itself.
%  kazemi2019time2vec    Time2Vec: linear+periodic time encoding (general, cited at origin) [new bib]
%  sitzmann2020implicit  SIREN: sinusoidal implicit neural reps (general)
%  mai2020...spatial     Space2Vec: multi-scale position encoder (general)
%  mai2023sphere2vec...  Sphere2Vec: spherical location encoder (general)
%  russwurm2024...spherical spherical harmonics + SIREN location encoder (general; already in CoUrb bib --
%                        existing key, ASCII-renamed 2026-07-25 from the former non-ASCII spelling
%                        and re-typed from arXiv preprint to the ICLR 2024 record)
%  perez2018film         FiLM: feature-wise linear modulation. FIX (domain F3): in Ch.3/4 FiLM conditions on
%                        TASK IDENTITY (per-task gamma/beta); context enters by concatenation, NOT via FiLM.
%  lin2021ctle           CTLE: context/time-aware per-visit embedding = the static->contextual hinge
%  ENCODER ATTRIBUTION (domain F1 BLOCKER, FIXED): MTLnet (Ch.3) uses ONLY the place embedding + per-task FiLM;
%                        the decomposed spatial/temporal/categorical encoders are CoUrb's (Ch.4) contribution.
%                        Space2Vec (mai2020...) is named as background but adopted in NEITHER chapter.
%  Check2HGI / check-in-level representation: the MobiWac work (submitted, under review). NO bib cite here --
%                        silva2025mtlnet is the CBIC/MTLnet paper, NOT Check2HGI (fact-gate F1 BLOCKER, FIXED).
%                        Deferred to \ref{ch:mobiwac} + the lineage table, as the lineage table already does.
%  NUMBERS: none quoted in 2.2 (no dataset statistics). "fourth level", "seven"/counts not used here.
%  CROSS-REF: Table~\ref{tab:fund:lineage} = the model-lineage table.
% ---------------------------------------------------------------------------
% Section 2.3 -- Multi-task learning

\section{Multitask learning}
\label{sec:fund:mtl}

Multitask learning (MTL) trains related tasks together in the expectation that shared representations improve
generalization~\cite{caruana1997multitask}. This section explains how tasks share a model,
why that sharing may fail, and how multitask optimization responds to
the failure. This dissertation builds joint models for two evolving task pairs. The first pair combines static category
% [round12, 2026-08-03] "place representation" -> "place embedding". The author registered
% "place representation" and revoked it hours later ("vamos usar so place embedding para evitar
% conflitos e interpretacoes dubias"), which left this ONE live line using a term absent from the
% fail-closed registry. It was the chapter's only use of that phrasing against eight uses of
% "place embedding", so this is a one-off inconsistency and not a house convention. No claim changes:
% the object is the same vector, Definition~\ref{def:fund:placelevel}.
classification from a place embedding with next-category prediction from a check-in history. The final joint model
reads a check-in history to predict both the next category and the next region of a visit. Whether the shared
computation helps depends on the sharing topology and the training objective.

\subsection{Sharing topologies}
% [round9, register track] SPLIT for the register law (WRITING_LAW §1, hard-phrasing class (b);
% gate src_utils/check_register.py, chained-qualification shape). The previous single sentence ran
% 38 words with three qualifying connectives ("rather than", "though", "rather than") and three
% commas, and held its main verb "is" until word 21. It is now two sentences, each with one idea and
% its verb early. NOTHING ABOUT THE CLAIM CHANGED: the shared principle, the contrast with a single
% common trunk, and the cross-attention-not-gating qualification all survive verbatim in substance.
% One "rather than" was replaced by "instead of" to avoid raising the negative-parallelism density
% that check_negative_parallelism.py holds under a ceiling; the second is kept because it scopes how
% the principle is realized.

The first design choice is where task-specific computation ends and shared
computation begins.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9, 2026-07-30] TWO SENTENCES REWRITTEN, found independently by the style and the
% AI-credibility personas as the same class the author caught in Appendix F: prose whose subject is the
% document rather than the work.
%   "each method named in the NEXT PARAGRAPH is a different answer" -- a forward pointer into the
%     document's own layout. The methods differ from each other whatever order they are printed in.
%   "CASTING IT THAT WAY OBLIGES ONE TO SAY what an optimum would be" -- announces that a statement is
%     coming instead of making it. WRITING_LAW §1 sub-class 4 (self-reference to the writing).
% A third instance the personas flagged, the caption clause explaining why a column exists, is already
% gone: the 197-to-64-word caption cut removed it.

\begin{definition}[Hard parameter sharing]\label{def:fund:hard}
Hard parameter sharing passes all inputs through a single shared trunk before
branching to task-specific output heads, so every task uses the same hidden
representations~\cite{ruder2017mtloverview}.
\end{definition}

\begin{definition}[Soft parameter sharing]\label{def:fund:soft}
Soft parameter sharing gives each task its own complete network and couples the
networks by penalizing differences between their
parameters~\cite{ruder2017mtloverview}.
\end{definition}

Definition~\ref{def:fund:hard} is the topology of the first joint model of this
dissertation, and it carries the risk this section addresses. A shared trunk need not
treat both tasks identically. Feature-wise Linear Modulation (FiLM) scales and shifts
features according to an additional input~\cite{perez2018film}, and MTLnet uses it to
condition its shared layers on task identity, so the two tasks read the same
parameters through different scalings.
% AUT-22 / GER-06 (see the removal note in §2.2.3.1): FiLM arrives here rather than among the
% mobility representations, because it conditions a shared trunk and is therefore a property of the
% sharing topology. The third sentence is new connective prose and states only what the CBIC
% architecture does, which Ch.3 already describes; \cite{perez2018film} moved with the sentence and
% no reference was added. Probe: R13-aut22.

\begin{definition}[Negative transfer]\label{def:fund:negtransfer}
Negative transfer occurs when joint training leaves a task worse than its dedicated
single-task model.
\end{definition}

Because hard sharing is rigid and soft sharing requires many parameters, several
architectures explore intermediate topologies. Cross-stitch units learn linear
combinations of activations from different task networks~\cite{misra2016cross}.
The multi-gate mixture-of-experts shares expert subnetworks but learns a
separate routing gate for each task~\cite{ma2018mmoe}. Progressive layered
extraction separates shared and task-specific experts across successive
gates~\cite{tang2020ple}, while DSelect-k makes expert selection sparse and
differentiable~\cite{hazimeh2021dselectk}. These methods selectively share
computation instead of forcing every task through one fixed trunk.

The joint model in Chapter~\ref{ch:mobiwac} builds a partial hard-shared interaction
subsystem based on this principle, using bidirectional cross-attention rather than
routing gates. The tasks do not reuse the same weight matrices, but their streams
cross within this jointly optimized module. Each stream queries the other to read its
features, which couples their gradients without forcing their representations into a
single bottleneck.

\subsection{Multi-objective optimization}

The second design choice is how several task losses determine one parameter update.

Shared parameters can produce the negative transfer of
Definition~\ref{def:fund:negtransfer}, so task relationships must be measured rather
than assumed~\cite{standley2020tasks}. The training problem has
multiple losses, and a standard scalar objective writes them as
\begin{equation}\label{eq:fund:mtl-total}
    \mathcal{L}_{\mathrm{total}}(\boldsymbol{\theta})
    = \sum_{k=1}^{K} w_k\,\mathcal{L}_k(\boldsymbol{\theta}),
\end{equation}
% AUT-27 (PENDENCIAS §4): $\mathcal{L}_k$ was the ONE symbol in this equation with no gloss. The
% prose already bound $K$, $\boldsymbol{\theta}$ and $w_k$; the per-task loss, which is what the
% whole subsection is about, was left for the reader to infer. Measured before the edit: exactly one
% live occurrence of the symbol in the chapter, inside this equation.
% A note on how this was verified, because the first instrument lied: a normalized search for "L_k"
% reduces to two characters and matches almost any text, so that reading was discarded as an
% artifact and the symbol itself was located instead.
% Probe: R13-aut27.
over $K$ tasks and shared parameters $\boldsymbol{\theta}$, where $\mathcal{L}_k$ is the
loss of task $k$. A balancing method changes the weights $w_k$ or the update direction
that they induce. The scalar sum does not
remove the multi-objective nature of the problem~\cite{sener2018mgda}. One parameter
setting exhibits Pareto dominance over another when it is no worse on every task loss
and better on at least one. A setting is Pareto optimal when no other setting dominates
it, and the corresponding loss vectors form the Pareto front.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9c, PENDENCIAS_RESOLVIDOS 2.23 (arquivado 2026-08-02), readability report R-5, author decision "Aplique o R-3,5,6 e o EX-6"]
% SPLIT AT THE FIRST SEMICOLON, which is the fix the report prescribes. The sentence was 66 words with
% one colon and two semicolons, the longest in the new block and the fourth longest in the chapter, and
% its parallelism was false: the Nash-MTL clause carries two claims (what is proved, and what is not)
% while the other two carry one each. Now: Nash-MTL alone with its two-part claim, then CAGrad and
% Aligned-MTL, which are genuinely parallel and joined by "and". NO CONTENT MOVES and no citation
% changes; the three guarantee statements are word-for-word what they were.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9, 2026-07-30] ATTRIBUTION CORRECTED (M2, MTL persona). It read "Two of these papers state the
% residual limitation THEMSELVES", which reads as a self-criticism. Both papers make that criticism
% about the methods they SUPERSEDE, as the motivation for their own contribution: CAGrad's abstract
% says prior heuristics "could converge to any Pareto-stationary point" and offers its own convergence
% to a minimum over the average loss; Aligned-MTL p.2 says such approaches "terminate once the Pareto
% front is first reached ... might provide a suboptimal solution" and offers drift along the front to a
% pre-defined weighting, naming Nash-MTL among its targets at p.5. The limitation is real and the
% citation is correct for it; what was wrong was who the criticism was aimed at.

\subsubsection{Guarantees and their limits}
% [round9, 2026-07-30] EVERY GUARANTEE ABOVE WAS READ AT ITS SOURCE PDF THIS SESSION, downloaded
% from arXiv and located by page. The point of the paragraph is that the five methods do NOT claim
% the same thing, so each clause is anchored:
%   nash                 arXiv:2202.01017v2, ICML 2022 per the arXiv comment field, 19 pp.
%                        p2: "a point is called Pareto stationary if there exists a convex
%                          combination of the gradients at this point that equals zero. Pareto
%                          stationarity is a necessary condition for Pareto optimality."
%                        p6 Theorem 5.4: the sequence "has a subsequence that converges to a Pareto
%                          stationary point". p6 and p14 Theorem 5.5: "if we also assume that all the
%                          loss functions are convex then the sequence ... converges to a Pareto
%                          optimal point" -- hence "only under an added convexity assumption".
%   liu2021cagrad        arXiv:2110.14048v2, NeurIPS 2021, 20 pp. Def. 3.1 p4 (dominance,
%                        optimality, stationarity as min_w ||g_w(theta)|| = 0). Theorem 3.2(1) p5:
%                        "all the fixed points of CAGrad are Pareto-stationary points". Its p1 states
%                        the family criticism quoted in the last clause: prior methods "lack
%                        convergence guarantee and/or could converge to any Pareto-stationary point".
%   senushkin2023aligned arXiv:2305.19000v1, 16 pp. Def. 1 p5; theorems pp12-13, "converges linearly
%                        to a Pareto-stationary point" with pre-defined task weights. p2 and p5 carry
%                        the same criticism: methods converging to "an arbitrary Pareto-stationary
%                        solution" tend to overfit a subset of tasks.
%   yu2020pcgrad         arXiv:2001.06782v4, NeurIPS 2020, 27 pp. Theorem 2 p5 is the one-step
%                        inequality L(theta_PCGrad) <= L(theta_MT) under its three stated conditions
%                        (the angle and magnitude of the two gradients, a curvature lower bound, a
%                        step-size bound); Theorem 1 p4 is the two-task convex convergence dichotomy.
%                        MEASURED: the string "Pareto" occurs ZERO times in the paper. The instrument
%                        was validated on the same extraction (27 pages, 90,294 characters, 185
%                        "gradient", 25 "theorem", 14 "cosine similarity"), because a grep returning
%                        zero is a broken instrument until proven otherwise. Hence "no Pareto claim
%                        at all", which is a statement about the paper, not about the method's worth.
%   sener2018mgda        arXiv:1810.04650v2, NeurIPS 2018, 15 pp. Def. 1 p3 carries dominance (a) and
%                        Pareto optimality, the Pareto set and the Pareto front (b) -- the two
%                        definitions this paragraph's predecessor attributes to it. p4: "Although
%                        every Pareto optimal point is Pareto stationary, the reverse may not be
%                        true."
% DELIBERATELY NOT CITED: Desideri's original MGDA paper. It has no entry in references.bib, it was
% reachable this session only as a secondary reference inside the two papers above, and no sentence
% here needs it (AGENT_GUARDRAILS R1: a reference is usable only when its own source was opened).
% GLOSSARY: Pareto dominance, Pareto optimality, Pareto-stationary point and gradient conflict were
% registered in GLOSSARY.md §4 and §6 BEFORE this prose landed, per the fail-closed rule and the
% author's decision in PENDENCIAS_RESOLVIDOS 2.12 (arquivado 2026-08-02) ("DESICAO: A."). Source ledger: src_utils/_round9/31_pareto.md.
% NO NUMBER IS QUOTED in these three paragraphs, by design (CONSIDERATIONS work-list item 28: define
% the conflict measure in Chapter 2, report its value only where it was measured).
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9, 2026-07-30] TERM NAMED (style persona, REQUIRED). The sentence defined the concept without
% ever using its registered name: "gradient conflict" appears 2x in GLOSSARY.md §4 and appeared ZERO
% times in this chapter's live prose, so the registry row that Appendix F's orthogonality result points
% back to had no anchor in the reader's text. The GLOSSARY is fail-closed in one direction (no unregistered
% term in prose); this was the other direction, a registered term with no prose to anchor it, which no
% gate checks. The fix is one word: the definition now carries the registered name (see GLOSSARY §4).
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9, 2026-07-30] SENTENCE CORRECTED, and it was a technical error, not a wording one. It read
% "...which puts a limit on what any of these methods can contribute." FALSE for every method named
% except PCGrad, and the MTL-expert persona proved it with a worked counter-example that I reproduced
% numerically before accepting: for g1=[3,0], g2=[0,1] (cosine exactly 0), PCGrad's rule projects only
% when the cosine is negative, so it is a no-op; MGDA's min-norm solution weights [0.10, 0.90] and
% deviates 53.1 degrees from the equal-weight sum; Aligned-MTL sees a condition number of 3.0, a
% system it exists to rescale. Uncertainty weighting and GradNorm never read the angle at all.
% THE DISSERTATION ALSO CONTRADICTED ITSELF: Chapter 5 (5_mobiwac/02_related.tex) reports Nash-MTL
% exceeding equal weighting on next-category at Alabama by 0.68 points. If orthogonality left a
% balancer nothing to contribute, that number could not exist.
% The replacement keeps the true half (the angular channel is empty, which is PCGrad's channel) and
% states what the others still do, tied to the measurement this document actually has.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9, 2026-07-30] THREE CORRECTIONS IN ONE SENTENCE, all from the MTL persona, all verified here
% before applying. The sentence read: "finds the two tasks' gradients statistically indistinguishable
% from orthogonal on the datasets measured there; it reads that result as the reason no balancing
% method improved on a fixed weighting in this work".
%  (M4) "STATISTICALLY INDISTINGUISHABLE" IS FALSE AS STATED. Re-measured at the fold unit over
%       _round7/gradient_cosine_observations6.parquet: a one-sample t-test on the five fold means
%       rejects zero at THREE of the seven datasets -- alabama p=0.0125, california p=0.0478,
%       georgia p=0.0093. What the appendix actually establishes is EQUIVALENCE within +/-0.05 (TOST),
%       which is a different and weaker claim, and it prints both. "Equivalent within a margin" is now
%       stated with the margin, and the three departures are named rather than hidden.
%  (M3) "THE REASON" DROPPED CHAPTER 5's QUALIFIERS. That chapter scopes the finding to nineteen loss
%       and gradient methods on its own datasets; "the reason ... in this work" read as a general
%       explanation. Now "one part of the reason", which is what the evidence carries.
% The margin is written in words ("five hundredths") because §2.3 has not introduced the TOST notation
% at this point in the chapter; the appendix states it as +/-0.05 where the test is defined and also
% writes it as "five hundredths" in its own prose, so the two agree.
% [CORRECTION 2026-07-30, found by review] THIS SENTENCE FIRST SAID "two hundredths", which is 0.02.
% The margin tested is +/-0.05. Two hundredths is TIGHTER than anything that was run, so the sentence
% asserted a stronger equivalence than the evidence supports -- and the provenance comment beside it
% said +/-0.05 in the same cell, so the contradiction was visible on screen as I wrote it. No +/-0.02
% TOST exists anywhere in this project. The wrong number reached the rendered PDF at p.23 and commit
% 91e7956a before it was caught.

The guarantees of balancing methods are usually weaker than Pareto optimality. A
Pareto-stationary point is one at which a convex combination of the task gradients is
zero. This condition is necessary but not sufficient for Pareto
optimality~\cite{nash}. Nash-MTL proves convergence of a subsequence to such a point
and reaches Pareto optimality only under an additional convexity assumption that does
not hold for a deep network~\cite{nash}. CAGrad has Pareto-stationary fixed
points~\cite{liu2021cagrad}, and Aligned-MTL converges to one when task weights are
fixed in advance~\cite{senushkin2023aligned}. PCGrad makes no Pareto claim of its own. Under
conditions on two task gradients, loss curvature, and step size, it guarantees that
one projected update does not produce a higher multitask loss than the original
gradient~\cite{yu2020pcgrad}. Reaching the Pareto front would still leave the balance
between tasks unspecified~\cite{liu2021cagrad,senushkin2023aligned}. This
dissertation therefore claims no Pareto property for its models. It judges each task
against its dedicated single-task model under the tests defined in
Section~\ref{sec:fund:eval}.
% [round9, 2026-07-30] WHY THIS PARAGRAPH EXISTS AND WHAT IT DELIBERATELY DOES NOT DO.
% It answers two advisor items recorded in src_utils/CONSIDERATIONS.md: G8.4 (the section describes
% solutions to a conflict it never defines or measures) and G10 (the non-conflict finding needs a
% technical argument; in his words the concept "parece jogado no artigo e sem contexto"). The measure
% is defined here so Appendix F's result has something to point back to.
%   THE APPENDIX IS CITED BY \ref AND NEVER BY DATASET COUNT. Its coverage was being extended while
% this was written, so "the datasets measured in Appendix F" is the only durable phrasing; a count
% here would be a second place to correct and a second place to disagree.
%   THE MECHANISM CLAIM IS ATTRIBUTED, NOT ASSERTED. "it reads that result as the reason" points at
% apx_f_cosine.tex \label{apx:cosine:mechanism}, which makes exactly this claim and carries its own
% [NEEDS SIGN-OFF]. That appendix also bounds itself to one architecture family
% (\label{apx:cosine:extension}: its mechanism section "applies only to models shaped like this
% one"), so nothing here extends the reading to Chapter 3's model, which was never measured. The
% hedge "part of the reason" is deliberate: the representation argument of Chapters 4 and 5 rests on
% those chapters' own results, not on this appendix.
% [NEEDS SIGN-OFF: raised round 9, 2026-07-30 | AUTHOR -- this paragraph restates Appendix F's
% mechanism claim in the frame chapter. The claim is the appendix's and is already flagged there, but
% repeating it in Chapter 2 widens its audience, and under AGENT_GUARDRAILS C2 a connective claim
% about what the arc shows is still a claim. Approve the appendix and this follows; reject it and this
% paragraph loses its last two clauses and keeps the definition.]

\subsubsection{Gradient conflict}
% [v2 review F-08, 2026-07-26] CAGrad description corrected (persona 10, who read arXiv:2110.14048).
% The prior clause said CAGrad "seeks an update close to the average gradient while bounding
% conflict". The constraint half was right; the objective half was not. The paper's problem is a
% MAXIMIN, max_d min_i <g_i, d> subject to ||d - g_0|| <= c||g_0||: it maximizes the worst per-task
% local improvement inside a ball around the average gradient, rather than bounding conflict. The
% method's own authors describe it that way. No claim of this dissertation depends on the wording;
% the correction is to describe a cited system as its own authors describe it (AGENT_GUARDRAILS
% citation protocol).

Gradient conflict describes one source of negative transfer, and it is a measured
quantity rather than a figure of speech.

\begin{definition}[Gradient conflict]\label{def:fund:conflict}
Let $\mathbf{g}_i$ and $\mathbf{g}_j$ be the gradients of two task losses with respect
to the shared parameters, and let $\varphi_{ij}$ be the angle between them, so that
\begin{equation}\label{eq:fund:cosine}
    \cos\varphi_{ij}
    = \frac{\mathbf{g}_i^{\top}\mathbf{g}_j}
           {\lVert \mathbf{g}_i\rVert\,\lVert \mathbf{g}_j\rVert}.
\end{equation}
The two tasks conflict at that point when $\cos\varphi_{ij}<0$~\cite{yu2020pcgrad}.
\end{definition}

The cosine between their gradients reads how the two requested updates are aligned and
ignores how large they are: $+1$ means the tasks ask for the same update, $-1$ for
opposite ones, and $0$ that the requests are orthogonal, each leaving the other's
objective unchanged to first order. PCGrad changes a direction only when this cosine is
negative. Other methods may still react to differences in gradient magnitude or task
loss. In Chapter~\ref{ch:mobiwac}, two such methods exceed equal weighting on one
dataset but not on the others. Appendix~\ref{apx:cosine} evaluates the final joint
model and finds the task gradients equivalent to orthogonal within a margin of five
hundredths at every dataset measured there. A paired test separates the mean from zero
at three datasets, but those small departures remain within the equivalence margin.
The combined result is that the deviations are detectable but too small to represent
meaningful directional conflict at the scale considered by the balancers.

\subsection{Loss-balancing methods}
% ---------------------------------------------------------------------------
% CITATION / NUMBER LEDGER -- 2.3 (verbs bound to tests; no result upgraded)
%  caruana1997multitask   MTL = inductive transfer via shared representation (defines MTL)
%  ruder2017mtloverview   hard vs soft parameter sharing (frames design space) [new bib, @misc arXiv:1706.05098]
%  misra2016cross         cross-stitch soft sharing [ERRATA DOI -> 10.1109/CVPR.2016.433]
%  ma2018mmoe             MMoE: per-task gates over shared experts (routing)
%  tang2020ple            PLE/CGC: shared vs task-specific experts (structured sharing) [new bib]
%  hazimeh2021dselectk    DSelect-k: differentiable sparse expert selection [new bib]
%  PLE lineage FIX (domain F6): joint model adopts the structured-sharing PRINCIPLE (shared+task-specific),
%                        realized with cross-attention, NOT a PLE/MoE descendant. Reworded, no "descends from".
%  standley2020tasks      negative transfer / which tasks to learn together; joint can hurt (VERIFIED arXiv abstract;
%  existing CBIC key).
%                        FIX (both gates F2): negative transfer DEFINED here, folded onto standley -- Zhang2020 REMOVED
%                        (Zhang2020 = iMTL next-POI recommender, NOT negative-transfer literature; reserved for
%mobility use if needed).
%  sener2018mgda          MTL as multi-objective optimization; weighted sum valid only if no conflict (VERIFIED arXiv
%  abstract; existing CBIC key)
%  kendall2018uncertainty homoscedastic-uncertainty loss weighting [new bib; DOI 10.1109/CVPR.2018.00781 verified +
%  arXiv:1705.07115]
%  chen2018gradnorm       GradNorm: gradient-magnitude balancing
%  liu2019dwa             Dynamic Weight Average (DWA): weights from the loss rate of change
%                         [PDF opened round 12; name p.2 + Sec. 4.1.3 p.5, Eq. 7 p.5]
%  yu2020pcgrad           PCGrad: gradient projection off conflict
%  liu2021cagrad          CAGrad: conflict-averse update w/ convergence [new bib]
%  navon2022nashmtl       Nash-MTL: bargaining-game gradient combination [ERRATA: consolidate nash double-key/slash]
%  senushkin2023aligned   Aligned-MTL: condition number of the linear system of gradients as the
%                         stability criterion, aligning that system's ORTHOGONAL components (the
%                         paper's own word, abstract; "principal-component alignment" was this
%                         ledger's earlier gloss and was wrong -- corrected round 11)
%  liu2023famo            FAMO: O(1) loss-decrease balancing
%  lin2022rlw             RLW: random weighting is competitive [new bib]
%  xin2022domtl           controlled study: MTL optimizers often do not outperform tuned fixed weights
%  kurin2022scalarization Kurin et al. NeurIPS 2022 "In Defense of the Unitary Scalarization": plain loss-sum + std
%                        regularization matches/improves specialized optimizers (VERIFIED firsthand arXiv 2201.04122
%abstract
%                        + NeurIPS 2022 venue). Added per domain F4 (canonical scalarization-skeptic anchor). [new bib]
%  vandenhende2022mtl     dense-prediction MTL survey (anchor)
%  yu2024survey           recent comprehensive MTL survey (existing CBIC key)
%  Liao2018               MCARNN: activity + next place (parallel MTL, mobility)
%  huang2024cslsl         CSLSL: causal cascade time->activity->location (category intermediate)
%  silva2025mtlnet        MTLnet null result: hard sharing on place embedding ~ single-task (TIME-INDEXED, "for that
%  configuration")
%  NUMBERS: none quoted. MTLnet null stated as configuration-specific
%  ("does not outperform ... for that configuration"), no metric upgraded.
%  HONESTY: banned "beat" REMOVED (domain/fact F7): all four instances -> "outperform"/reworded. Region-as-end-target
%  claim
%           SCOPED to the multi-task co-equal setting (domain/fact F3): single-task region prediction (zhu2022drrgnn,
%   2.1) exists.
%           Null result written plainly and time-indexed; no result verb upgraded.
% ---------------------------------------------------------------------------

% [round10, GER-09 lineage] THE ATTRIBUTIONS BELOW ARE THE ONLY NEW CLAIM IN THIS SUBSECTION, and
% each first-author surname was read at the record of the work itself this session, not retyped from
% another bibliography. arXiv API title query, first author and identifier returned:
%   kendall2018uncertainty  arXiv:1705.07115  Alex Kendall        (Crossref 10.1109/CVPR.2018.00781,
%                           author family names Cipolla/Gal/Kendall, CVPR 2018)
%   chen2018gradnorm        arXiv:1711.02257  Zhao Chen           abstract: "We present a gradient
%                           normalization (GradNorm) algorithm"
%   liu2019dwa              arXiv:1803.10704  Shikun Liu          (Crossref 10.1109/CVPR.2019.00197,
%                           Liu/Johns/Davison; author line read at p.1 of the PDF, round 12). The
%                           paper's headline contribution is the MTAN attention architecture and
%                           DWA is a separable weighting scheme introduced alongside it, hence
%                           "alongside their attention architecture" rather than a bare
%                           "introduce". Body evidence for the name, the definition and the
%                           separability is in the round-12 block at the clause itself.
%   liu2023famo             arXiv:2306.03792  Bo Liu              abstract: "we introduce Fast
%                           Adaptive Multitask Optimization FAMO, a dynamic weighting method"
%   sener2018mgda           arXiv:1810.04650  Ozan Sener          (two authors: Sener and Koltun)
%   yu2020pcgrad            arXiv:2001.06782  Tianhe Yu           v4 PDF opened this session, p.3:
%                           "We define the gradients as conflicting when cos phi_ij < 0."
%   liu2021cagrad           arXiv:2110.14048  Bo Liu
%   nash                    arXiv:2202.01017  Aviv Navon
%   senushkin2023aligned    arXiv:2305.19000  Dmitry Senushkin
% TWO DIFFERENT LIUS, which is why the initials are written: Shikun Liu (DWA) and Bo Liu (CAGrad,
% FAMO) are different people, and a bare "Liu et al." three times would assert one research line
% where there are two. The initial is the minimum that keeps the sentence true.
% NO GUARANTEE CLAIM IS MADE HERE. What each method PROVES is stated once, in the guarantees
% subsection above, whose per-clause page-level provenance block is unchanged and not contradicted:
% this subsection says only who introduced what and what class it belongs to.

Balancing methods differ in whether they modify loss weights or task-gradient
directions.

The first class sets the weights $w_k$ of Equation~\ref{eq:fund:mtl-total} from
different signals. Kendall et al.\ weight each loss by the task's homoscedastic
uncertainty~\cite{kendall2018uncertainty}. Chen et al.\ introduce GradNorm, which
balances gradient magnitudes and training rates~\cite{chen2018gradnorm}.
% [round12, 2026-08-03] PROVENANCE, and one CORRECTION, for the clause that follows. The PDF of
% arXiv:1803.10704v2 (science/articles/1803.10704v2.pdf) was opened this session, so the claim is
% now located in the source rather than inferred from the abstract, and the [VERIFY] this block
% replaces is closed. What was checked, in the BODY, not the abstract:
%   NAME (p.2, end of Sec. 1, and the heading of Sec. 4.1.3 on p.5): the authors' own name is
%     "Dynamic Weight Average (DWA)", singular "Average". The sentence SAID "dynamic weight
%     averaging", which is not the paper's name; corrected, an R2 fix of the same class as the
%     Aligned-MTL one recorded below, not a style change. Chapter 3 is a version of record and
%     keeps CBIC's own "Dynamic Weight Averaging" wording; that divergence is deliberate.
%   DEFINITION (p.2: "adapts the task weighting over time by considering the rate of change of the
%     loss for each task"; Eq. 7 and its text, p.5: w_k(t-1) = L_k(t-1) / L_k(t-2), and "the loss
%     value Lk(t) is calculated as the average loss in each epoch"). The previous wording, "using
%     recent changes in the task losses", was fair but vaguer than the paper; the clause now names
%     the rate of change and the ratio of the two previous loss values.
%   "ALONGSIDE" HOLDS, and this was the word at issue. DWA is presented as a SEPARABLE weighting
%     scheme, not as part of MTAN: it is introduced in the experimental section (Sec. 4.1.3, p.5)
%     as one of three weighting schemes, and Tables 2 and 3 (pp. 6-7) run it on the Split, Dense
%     and Cross-Stitch baselines as well as on MTAN, so it is not tied to the architecture. The
%     paper also distinguishes it from GradNorm on exactly that axis (p.5: GradNorm "requires
%     access to the network's internal gradients", DWA "only requires the numerical task loss").
%   ATTRIBUTION (p.1): Shikun Liu, Edward Johns, Andrew J. Davison. "S. Liu" is correct, and the
%     initial stays load-bearing against B. Liu (CAGrad, FAMO) in this same paragraph.
%
% [round12] The blank line below is load-bearing for a GATE, not decoration. check_trapped_prose
% flagged the line above as trapped prose, and it was a FALSE POSITIVE WITH A REAL CAUSE: that line
% ends on a self-contained sentence carrying no file name, no line reference, no finding id and none
% of the author-directed vocabulary the checker's ledger test looks for, and the next source line was
% body prose. By those tests it is indistinguishable from a genuine tear, so the checker was right to
% raise it. The fix is to match this file's own convention (29 of its 34 comment blocks are separated
% from following prose by a blank line) rather than to widen the checker's exemption list, which would
% cost real detection to buy silence on a cosmetic point.

S. Liu et al.\ introduce Dynamic Weight Average alongside their attention
architecture, deriving each weight from the rate of change of that task's loss,
measured as the ratio of its two previous loss values~\cite{liu2019dwa}, and B. Liu et
al.\ propose FAMO, which tracks loss changes without computing every task gradient at
each step~\cite{liu2023famo}.

The second class changes the update direction instead. The multi-objective reading
begins with Sener and Koltun, who cast multitask learning as multi-objective
optimization~\cite{sener2018mgda}. Yu et al.\ introduce PCGrad, which projects away a
conflicting component in the sense of Definition~\ref{def:fund:conflict}~\cite{yu2020pcgrad}.
B. Liu et al.\ introduce CAGrad, which protects the worst per-task improvement within a
neighborhood of the average gradient~\cite{liu2021cagrad}. Navon et al.\ introduce
Nash-MTL, which treats gradient combination as a bargaining problem~\cite{nash}, and
% [round11, 2026-08-03] CORRECTED, and the correction is an R2 fix, not a style change. THIS SENTENCE
% SAID "adjusts the principal components of the gradient system". The paper says it aligns the
% ORTHOGONAL components: arXiv:2305.19000v1 abstract, opened this session, "eliminates instability in
% the training process by aligning the orthogonal components of the linear system of gradients".
% "Principal" is not a synonym for "orthogonal" here -- it invites a principal-component-analysis
% reading the authors do not claim -- and the paper's own stated criterion is the CONDITION NUMBER of
% the linear system of gradients, which is what the sentence now names. Found while clearing the last
% debt of round 10: four of the nine lineage attributions had been verified only by the sub-agent that
% wrote them, and I checked all four at their own records. Three were exact (kendall2018uncertainty,
% "considering the homoscedastic uncertainty of each task"; sener2018mgda, "explicitly cast multi-task
% learning as multi-objective optimization"; and Sener and Koltun is correctly two authors, not "et
% al."). This one was not.
% [round12] The liu2019dwa [VERIFY] that stood here is CLOSED: the PDF is now on disk and both halves
% of the clause were located in the body. The page-level provenance sits with the clause itself,
% above, and the caution the flag protected survived the check -- "alongside" is right.
Senushkin et al.\ introduce Aligned-MTL, which uses the condition number of the linear
system of task gradients as a stability criterion and aligns that system's orthogonal
components~\cite{senushkin2023aligned}.

These methods must be compared with
simple alternatives. Random loss weighting can be competitive~\cite{lin2022rlw};
specialized optimizers often fail to improve on a tuned fixed-weight
baseline~\cite{xin2022domtl}; and plain loss summation with standard regularization
can match or improve on them~\cite{kurin2022scalarization}. Surveys of general and
dense-prediction MTL summarize the same design
space~\cite{vandenhende2022mtl,yu2024survey}. For this dissertation, a balancing
method is useful only if it improves on a tuned fixed weighting.

\subsection{Multitask learning for mobility prediction}

Prior mobility applications show several ways to connect auxiliary targets to an
exact-place recommendation.

In mobility, MTL has been used almost entirely in the service of next place. MCARNN jointly
predicts activity and place through a context-aware recurrent
unit~\cite{Liao2018}. CSLSL instead predicts time, then activity, and then location in
a cascade~\cite{huang2024cslsl}. iMTL couples next-POI recommendation with the
handling of uncertain check-ins~\cite{Zhang2020}. Halder et al.\ develop multitask
next-POI recommenders that account for queue time and, in the journal extension, user
interest~\cite{Halder2021,Halder2022}. TME instead applies tree-guided multitask
embedding to static semantic POI annotation~\cite{Xu2023}. HAMTL sets location
prediction as its main task and category prediction as an auxiliary task, and its
hierarchical decoder predicts the category of the next destination first and then
refines the prediction of the specific location using that predicted
category~\cite{wang2025hamtl}. The same group had already published IeMTLF, an
interaction-enhanced multitask framework for next location
prediction~\cite{wang2024iemtlf}.

These methods confirm that category information and auxiliary tasks can support
mobility prediction, but they do not instantiate the pair studied in the final
chapter. Among the works reviewed here, none treats next category and next region as
co-equal end targets of one joint model.

This dissertation addresses that gap starting with MTLnet, which evaluates hard
parameter sharing over a static place embedding~\cite{silva2025mtlnet}. That initial
configuration fails to outperform dedicated single-task models, motivating the
representation and topology refinements developed in subsequent chapters.
% Section 2.4 -- Datasets and evaluation


\section{Datasets and evaluation}
\label{sec:fund:eval}
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% relative clause ending on a stranded preposition is the sub-case where a non-native reader's parse
% breaks: "reference points each result is read" scans as a clause until "against" arrives.

This section defines the datasets, metrics, reference points, and validation protocol
used to interpret the results. It also states which statistical tests support the
comparison verbs used in the final study.
% [round9c, PENDENCIAS_RESOLVIDOS 2.21 (arquivado 2026-08-02), the advisor's flag, 2026-07-30] "the tests that license the verbs used
% to report a comparison" -> "the statistical tests that decide which verb a comparison may be
% reported with". THE TERM IS LEGITIMATE and it is not banned: a test does license a verb, and
% AGENT_GUARDRAILS section 4b and WRITING_LAW's honesty rules use exactly that framing. The advisor's
% objection is about a READER, and it holds at THIS site: this is the FIRST occurrence in the
% document (defense build p. 24, section 2.4's opening paragraph), where "license" arrives with no
% antecedent, and the reader who has not read the glossary meets a verb-of-permission applied to
% grammar. The replacement says the same thing in the sentence's own terms and costs three words.
% MEASURED, all 13 occurrences of the license family in live prose, read one by one, not sampled, and
% counted so the buckets sum to 13: EIGHT are the verb sense and FIVE are dataset licensing.
% Of the eight verb-sense occurrences, two were opaque (this one and the section-2.4.4 opener at
% p. 25); one is self-explaining because it names the verb in quotation marks ("either test licenses
% the verb ``outperforms''", p. 26); two are in Chapter 6 and one in the AI-disclosure appendix, all
% three ordinary English with a concrete object; and TWO are in the gradient-cosine appendix, which
% uses the same statistical sense correctly ("A test that merely failed to reject zero would license
% one statement", "An equivalence test licenses the positive claim instead", both p. 98).
% The five dataset-licensing occurrences are all in the data-ethics appendix (Apache License 2.0 and
% the like), an unrelated sense that this item does not touch.
% APPENDICES ARE NAMED HERE, NOT LETTERED, AND THAT IS DELIBERATE. The filenames and the printed
% letters do not correspond: apx_f_cosine.tex prints as APPENDIX D (p. 98), apx_e_ethics.tex prints as
% APPENDIX C (p. 95), and apx_d_ceiling.tex is in the supplementary volume. Phase 0 relettered the
% cosine appendix F -> D, and the main volume prints A, C, D because B and D-as-filed live in the
% extra volume. Measured in the rendered defense build of 101 pages: the only appendix headers in the
% main volume are A (p. 91), C (p. 95) and D (p. 98). A letter written from a filename is wrong here,
% which is why this note names each appendix by its subject instead.
% Only the two opaque ones are rewritten. The p. 26 sentence is kept because it is where the usage
% becomes concrete, and rewriting it would remove the definition rather than the opacity; Appendix D's
% two are kept because each names what is licensed in the same sentence.

\subsection{Datasets}

The geographic scope combines five state-level datasets with one city outside the
United States.
% [COD-015d, round8, 2026-07-30] TWO METRIC PROMISES REMOVED, because no chapter reported either one.
% Author decision, PENDENCIAS_RESOLVIDOS 5.5 (arquivado 2026-07-30): "Pode refazer por conta propria", on a choice he
% framed as delete the
% two promises (his recommendation, and mine) or run an analysis round to report them.
% MEASURED FIRST, not assumed, and the sweep used check_audit_claims.live_text so that provenance
% comments quoting these very terms are not counted as prose (AGENT_GUARDRAILS 4b V4). Command, runnable
% from articles/dissertacao, prints one row per chapter group:
%   python3 - <<'PY'
%   import re,sys; sys.path.insert(0,"src_utils")
%   from check_audit_claims import live_text
%   from pathlib import Path
%   pats={"MRR":r"\bMRR\b|mean reciprocal rank|reciprocal rank",
%         "Delta_m":r"relative multi-task performance|multi-task performance change"}
%   swept=[]; skipped=[]
%   for d in [Path("src"),Path("../[mobiwac]/src")]:
%       for f in sorted(d.rglob("*.tex")):
%           if "build" in str(f): skipped.append(f); continue
%           swept.append(f)
%           t=live_text(f); h={k:len(re.findall(v,t,re.I)) for k,v in pats.items()}
%           if any(h.values()): print("HIT",f,h)
%   print("swept",len(swept),"| skipped under build/",len(skipped))
%   for f in swept: print("  swept:",f)
%   PY
%   # EXPECT before this edit: one HIT line, src/chapters/2_fundamentals.tex {'MRR': 1, 'Delta_m': 1}.
%   # EXPECT after it: no HIT line at all.
%   # EXPECT either way: "swept 70 | skipped under build/ 5", then 70 "swept:" lines, 54 of them under
%   #   src/ and 16 under ../[mobiwac]/src/. The counts are the SCOPE claim above, so the command that
%   #   measures the terms now also proves what it looked at.
% Scope of that sweep, stated because an unreported exclusion is how the round-6 table went wrong:
% 70 .tex files in total. 54 under src/ with build/ excluded (it holds generated copies), of which the
% 17 files under src/tables/ are a SUBSET and not an additional group, plus the 16 files of the
% submitted manuscript at articles/[mobiwac]/src/. Neither term
% occurs in any results table of any chapter, so the promises were not merely unmentioned in prose,
% they were unreported everywhere.
% CORRECTED 2026-07-30, and the correction is the point: this line first said "59 .tex files under
% src/ (build/ excluded)". 59 is the count INCLUDING build/; excluding it gives 54. So the number was
% right about a set I had not swept and wrong about the one I had, which is the V1 failure in its
% purest form: I reported a count without re-running the command that produced it. It also added the
% 17 table files as though they were a separate group, when src/tables/ lies under src/ and was
% already inside the 54. Re-measured: 54 + 16 = 70 swept, 0 hits after the edit, and the sweep
% reproduces the ONE-hit-per-term result before it.
% SECOND CORRECTION, 2026-07-30, and it is the same defect one layer up. The paragraph above ended
% "whose file list is now printed rather than described". THAT WAS FALSE WHEN WRITTEN. The command at
% the time ended `if any(h.values()): print(f,h)`, which prints only the files that HIT and never the
% files it looked at, and I had not touched the command in the edit that made the claim. So the note
% correcting a miscount asserted a change to itself that did not exist, which is exactly the class it
% was written to correct. Caught in review, not by me.
% Rather than delete the sentence I made it true: the command now collects `swept` and `skipped`,
% labels hits with "HIT", and ends by printing "swept N | skipped under build/ M" followed by one line
% per swept file, so the SCOPE claim in this paragraph is verifiable from the same command that
% measures the terms. RUN VERBATIM after amending, by extracting the block from this file and
% executing it rather than retyping it: rc=0, 0 HIT lines, "swept 70 | skipped under build/ 5", and
% 70 "swept:" lines, 54 under src/ and 16 under ../[mobiwac]/src/.
% ONE MORE INSTRUMENT ERROR WORTH RECORDING, mine, found while checking that last split: counting the
% manuscript files with `grep -c mobiwac` over the swept list returns 29, not 16, because thirteen
% DISSERTATION paths contain that string too (src/chapters/5_mobiwac/*, src/tables/mobiwac/*). The
% command's own 54/16 split is the correct one; the grep was the wrong question, in the same way the
% 59-vs-54 count was.
% WHAT REPLACED THEM. The Acc@10 sentence keeps its limitation and states it directly ("a model can
% rank the true region tenth as cheaply as first") instead of naming a second metric to fix it, so the
% honest point the MRR clause was carrying survives without promising a figure. The Delta_m sentence is
% replaced by what the document actually does: per-task comparison against the dedicated single-task
% model. The clause about the checkpoint is added rather than removed because WRITING_LAW section 5
% requires the checkpoint-selection rule to be defined with the metrics, and it is measured, not
% assumed: 5_mobiwac/06_results.tex:86 and :363 both name the joint validation score as the geometric
% mean of the two task metrics. It is deliberately called a selection rule and not a reported score,
% which is the distinction the earlier draft of this sentence got wrong by asserting that no aggregate
% is reported anywhere.
% CONSEQUENCE HANDLED, NOT LEFT: dropping the Delta_m sentence dropped the only \cite of
% maninis2019attentive in the document, which would leave an entry that BibTeX silently omits from the
% printed bibliography while it still sits in references.bib. The entry is removed from
% src/references.bib in the same commit, its verified provenance comment kept there as a retired
% record so the work of verifying it is not lost. Bibliography goes 100 entries to 99; measured with
% grep -c '^@' src/references.bib and against the built main.bbl \bibitem count.
% [NEEDS SIGN-OFF: COD-015d, round8, 2026-07-30 | AUTHOR -- this narrows the scope of the fundamentals
% chapter by retiring two metric definitions, and it drops one reference from the bibliography with
% them. Both metrics remain available if a later analysis round reports them; nothing else in the
% chapter depends on either.]

The experiments use two sources of check-ins. Gowalla is the main source, and the
dissertation evaluates Alabama, Arizona, Florida, California, and
Texas~\cite{cho2011gowalla}. Istanbul check-ins come from Massive-STEPS, which also
draws attention to the field's continued dependence on old
datasets~\cite{wongso2025massivesteps}. The Foursquare dataset for New York and Tokyo
is another common benchmark~\cite{yang2015tsmc}, but it provides context only and is
not used in the experiments reported here.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round6, N-2, fact gate persona 06/07] REPAIRED. This clause used to read "Chapter 3 reports
% five-fold cross-validation without identifying the split axis", which was true of the PUBLISHED
% paper and became FALSE earlier the same day: the COD-007 addition at 3_cbic/results.tex now states
% "The folds are formed by a stratified splitter over the samples rather than over the users". The
% frame was asserting an absence the chapter it describes no longer has. The protocol-recovery pass
% predicted this exact contradiction in writing before it happened; the repair was drafted, and then
% not applied because the two files sat in different tracks' remits. Recorded because a predicted,
% drafted, unapplied repair is a process failure, not a writing one.
% The paragraph's point is unchanged and now rests on the two facts that carry it: both published
% studies split by sample, only Ch.5 splits by user. The split axis of Ch.3 is now stated in Ch.3,
% so the frame does not need to characterize its absence.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [round9c, PENDENCIAS_RESOLVIDOS 2.21 (arquivado 2026-08-02), the advisor's flag, 2026-07-30] "license verbs in Chapter 5 alone" ->
% "govern the comparison verbs of Chapter 5 alone". Second of the two opaque sites (defense build
% p. 25). "license verbs" is the most compressed form in the document: no quoted verb, no object,
% and the reader has to already know the convention to parse it. The scope claim, that only Chapter 5
% is covered, is unchanged, and the sentence two paragraphs below still shows the convention
% concretely by naming ``outperforms'' in quotation marks.

\subsection{Metrics and reference points}

Each sequential target has its own metric, and the scores are not combined into one
claim of task performance.
% ---------------------------------------------------------------------------
% CITATION / NUMBER LEDGER -- 2.4 (numbers quoted, not computed; conventions named)
%  DATASETS FIX (fact F4): "two ... serve as the ground" = Gowalla + Massive-STEPS/Istanbul (the two USED).
%                        Foursquare (yang2015tsmc) is named as CONTEXT ONLY, explicitly not evaluated on (was
%mis-counted as a 3rd "ground").
%  cho2011gowalla        Gowalla origin, 5 US states used (KDD 2011)
%  wongso2025massivesteps Massive-STEPS Istanbul benchmark; over-reliance on old datasets [preprint arXiv:2505.11239]
%  yang2015tsmc          Foursquare NYC/Tokyo (TSMC2014), most-cited (815), covers both cities. CONTEXT ONLY (not
%  used). [new bib]
%  sokolova2009measures  macro-F1 = mean of per-class F1 (VERIFIED PDF). Primary category metric. [new bib]
%  maninis2019attentive  Delta_m = avg relative per-task change vs single-task (VERIFIED PDF, Sec.4 + Table 3). Sign
%  convention defined in addendum. [new bib]
%  gambs2012mmc          mobility Markov chain = non-learned floor (targets next PLACE) [new bib]
%  song2010limits        93% = next-LOCATION predictability bound (VERIFIED firsthand PDF). FIX (both gates F5): NOT
%  presented
%                        as a ceiling on category macro-F1 or region Acc@10; the dedicated single-task model is the
%operative ceiling.
%  kohavi1995crossval    stratified k-fold CV (VERIFIED by author). [new bib; Zenodo DOI 10.5281/zenodo.19712698 for
%  a resolvable id]
%  pedregosa2011sklearn  scikit-learn + StratifiedGroupKFold user-disjoint splitter.
%                        SINGLE cite (author ruling); splitter behavior stated
%                        defensively (v1.0/2021 addition). [new bib]
%  REV-006 SCOPING (2026-07-26, fixed in the common text per the author's instruction, NOT in the errata).
%    The protocol paragraph and the chapter roadmap presented user-disjoint splitting and the inferential tests
%    as governing the whole document; they govern Chapter 5 only, and both halves are now scoped. Verified
%    firsthand: Ch.5 splits by user (5_mobiwac.tex, "a test user's visits never appear in training"); Ch.4
%    explicitly does not (4_courb.tex, "stratified by sample, not by user", repeated in its preface); Ch.3 states
%    5-fold cross-validation without naming the axis in the published text. Significance testing is Ch.5-only:
%    a sweep for p-value/Wilcoxon/t-test/Holm/TOST returns nothing in either published chapter, and Ch.3's one
%    adjacent line argues from overlapping standard deviations with no test. The closing sentence therefore reads
%    "wherever this dissertation reports a test" rather than "throughout the document".
%  CLASS-WEIGHTING CONTRADICTION (author pending item 3.6, 2026-07-26). The prose said "The training pipeline
%    counters the same imbalance with class-weighted cross-entropy", contradicting 5_mobiwac/04_method.tex:37 (Eq. 5.1
%    context): "$L_{cat}$ and $L_{reg}$ are plain unweighted cross-entropy losses ... Class-weighting, tested on
%    both outputs, lowered both region accuracy and category macro-F1" (identical in the version of record,
%    articles/[mobiwac]/src/sections/04_method.tex:39). Fixed in Ch.2, not Ch.5, and NOT scoped to an earlier
%    study: neither published chapter describes a class-weighted training loss, and the only class weighting in
%    the codebase is inside the DGI embedding pre-training (research/embeddings/dgi/model/GCNEncoder.py:41-48),
%    not the prediction pipeline this sentence is about. The sentence now names the real remedy (macro-F1 as the
%    reporting metric) and reports the Ch.5 finding.
%  wilcoxon1945          paired signed-rank test ("outperforms" bound to it) (VERIFIED PDF). [new bib]
%  holm1979              Holm step-down family-wise correction
%  lakens2017tost        TOST equivalence within a margin ("matches" bound to it, two-point margin) [new bib]
%  NUMBERS: 93% (song2010limits, verified, scope-corrected); "roughly a third" Food = a REPRESENTATIVE STATE (Alabama
%           Next-Category CHECK-IN distribution, Food 34.2% per docs/context/TASKS.md; qualitative, NOT recomputed;
%           fact F9: addendum provenance pointer to be corrected to the check-in table, not the 32.5% POI-count table);
%           "10" in Acc@10 (metric definition); "two-point margin" (TOST convention, GLOSSARY).
%  OOD FORESHADOW (domain F8): region metric foreshadowed as "unseen region counts as a miss"; full OOD-Acc@10
%  definition in Ch.5.
%  Acc@10 and the majority-class floor: conventions with no origin DOI -- defined defensively, cited
%           conventionally. MRR was in this line too and its definition is REMOVED from the chapter
%           (COD-015d, round 8): no chapter reported it. See the COD-015d block below.
%  pedregosa2011sklearn: StratifiedGroupKFold is a scikit-learn v1.0/2021 feature; single-cite ruling (author) --
%  2011 paper for the
%           library, splitter behavior stated defensively in prose.
%           kohavi1995crossval: claim PLAUSIBLE (Zenodo re-deposit id), author to
%           confirm original IJCAI-95 text.
%  Delta_m ("relative multi-task performance change", maninis2019attentive) is REMOVED from the chapter
%           (COD-015d, round 8): no chapter reported it either. See the COD-015d block below.
%  four-of-six + two-point margin (referenced in 2.5): confirm against Ch.5 RESULTS_BOARD.md / PAPER_PLAN §3 at
%  adaptation (fact NOTE 10).
% ---------------------------------------------------------------------------

\subsubsection{Next category}

Next-category prediction uses macro-F1 because the class distribution is imbalanced.
Food accounts for roughly one third of the check-ins in a representative state, so
plain accuracy can hide poor performance on less frequent classes. For $C$ classes,
macro-F1 is
\begin{equation}\label{eq:fund:macro-f1}
    \operatorname{MacroF1} = \frac{1}{C}\sum_{c=1}^{C}
    \frac{2P_cR_c}{P_c+R_c},
\end{equation}
where $P_c$ and $R_c$ are precision and recall for class $c$. The unweighted mean
gives every class equal importance~\cite{sokolova2009measures}. It does not show which
individual classes improve, and it can be low even when overall accuracy is high. The
models use unweighted cross-entropy rather than a reweighted loss. In
Chapter~\ref{ch:mobiwac}, class weighting lowered both category macro-F1 and region
accuracy.

\subsubsection{Next region}

Next-region prediction uses accuracy at 10 (Acc@10):
\begin{equation}\label{eq:fund:acc10}
    \operatorname{Acc@10} = \frac{1}{N}\sum_{i=1}^{N}
    \mathbb{I}\!\left[y_i \in \operatorname{Top10}(\hat{\mathbf{p}}_i)\right],
\end{equation}
where $y_i$ is the true region and $\operatorname{Top10}(\hat{\mathbf{p}}_i)$ contains
the ten regions with the largest predicted scores. The metric is the fraction of test
check-ins for which the correct region appears in this set. It does not distinguish
first place from tenth and does not measure the probability assigned to the true
% AUT-30 (PENDENCIAS §4). TWO defects, both fixed here.
% (a) FIRST USE: "OOD" was used as a bare acronym and the expansion "out-of-distribution" arrived
%     about eighty characters LATER, inside the sentence the author reported he could not follow.
%     WRITING_LAW §1 and §7 require the expansion AT first use. The acronym is now expanded on its
%     first appearance and the later sentence no longer carries the expansion redundantly.
% (b) THE SENTENCE ITSELF: the author's complaint was not the acronym alone. The old sentence stated
%     an arithmetic identity in words, opening with "Equivalently", which asks the reader to hold two
%     formulations at once to learn what the metric IS. It is now a direct definition. No number and
%     no convention changes; the identity is the same one, read forward instead of sideways.
% A related record correction belongs with this edit: content.tex's own comment at :262-264 lists
% OOD among acronyms that "never appear in prose", which stopped being true. Handled there.
% Probe: R13-aut30.
region. Regions absent from the training partition count as errors. The reported
out-of-distribution discounted, or OOD-discounted, Acc@10 is therefore the Acc@10
measured on the in-distribution check-ins, multiplied by one minus the fraction of
check-ins whose region is out of distribution. Chapter~\ref{ch:mobiwac} gives the
complete evaluation procedure.

\subsubsection{Joint-model selection and floors}

Dedicated single-task models evaluate each target independently using its respective
metric. Evaluating a joint model, however, requires a single validation signal that
considers the performance of both outputs simultaneously. The validation protocol
achieves this by combining category macro-F1 and region Acc@10 into a scalar
selection score:
\begin{equation}\label{eq:fund:joint-selection}
    S_{\mathrm{joint}} = \sqrt{\operatorname{MacroF1} \times \operatorname{Acc@10}}.
\end{equation}
Under this \emph{joint-best} convention, both reported task scores come from the same
validation-selected checkpoint, reflecting the balanced performance of a single
deployable system.

To interpret these scores, model performance is evaluated between lower reference
floors and upper benchmark ceilings. At the lower bound, basic baselines establish
performance floors: a majority-class predictor for category classification, and a
first-order Markov model over training visits for sequential
transitions~\cite{gambs2012mmc}. At the upper bound, the primary reference ceiling for
evaluating multitask gains is the dedicated single-task model for each output.
Theoretical limits from prior work, such as the 93~percent potential predictability
reported for coarse location prediction~\cite{song2010limits}, do not serve as
ceilings for seven-class macro-F1 or region ranking.

\subsection{Validation and statistical decisions}

The validation design determines which generalization claim and comparison verb each
result can support.

The validation protocol determines whether a result generalizes beyond familiar
users. The studies use stratified cross-validation~\cite{kohavi1995crossval}, but the
split changes across chapters. Chapters~\ref{ch:cbic} and~\ref{ch:courb} stratify
samples, so one user's check-ins may occur in both training and validation.
Chapter~\ref{ch:mobiwac} instead uses a grouped, stratified splitter that keeps each
user on one side of a fold~\cite{pedregosa2011sklearn}. A seed in that chapter is one
complete repetition of the five-fold experiment with a different random
initialization. Four seeds over one fixed set of five folds produce twenty fitted
models per configuration. The fixed folds isolate random-initialization variation but
do not measure uncertainty from resampling the users.

Chapters~\ref{ch:cbic} and~\ref{ch:courb} report fold means and standard deviations
without significance tests. The inferential rules therefore apply to
Chapter~\ref{ch:mobiwac}. Its superiority verdict uses a paired $t$ test on the four
per-seed means. A Wilcoxon signed-rank test on the twenty paired fold results is
reported alongside it and reaches the same decisions~\cite{wilcoxon1945}. Holm
correction controls family-wise error across the six dataset
comparisons~\cite{holm1979}. A superiority test cannot establish that a difference is
small enough to accept. For Alabama and Arizona, two one-sided tests establish that
the joint model is statistically non-inferior within a two-point
margin~\cite{lakens2017tost}. Accordingly, \emph{outperforms} is reserved for paired
superiority and \emph{matches} for statistical non-inferiority within that margin.
% Section 2.5 -- Relevance (synthesis; NO fresh citations; argument-only ledger at its end)


\section{Relevance}
\label{sec:fund:relevance}

The preceding sections introduce the prediction targets, the representations, multitask
learning, and the evaluation protocol one at a time. They are parts of a
single problem. This section states how they combine, before the three studies put
them to work.

\subsection{The parts of one problem}
% [NEEDS SIGN-OFF: raised v1 assembly, 2026-07-23 | gate L3 fix A-2. The "weekday lunch / Saturday night out" image now
% appears only in Ch.1 (the signed-off mechanism beat); this hinge sentence was reworded
% to state the same static-vector limit without duplicating the image. Revert if you
% prefer the repetition as a deliberate motif.]
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% MOVED HERE 2026-07-30: this block sat between "cannot be assumed;" and "it has to be measured",
% splitting one sentence across a comment. It renders correctly, but check_trapped_prose flags a
% comment tail followed by a line that opens mid-sentence, and it is right to: that shape is how a
% swallowed sentence looks. The comment now sits between complete sentences.

The argument begins with the targets: Next place, next category, and next region have
different output spaces and different meanings for a correct answer. This dissertation
treats next category and next region as final outputs rather than as intermediate
steps toward next-place prediction.
% [round9c, PENDENCIAS_RESOLVIDOS 2.23 (arquivado 2026-08-02), readability report R-3, author decision "Aplique o R-3,5,6 e o EX-6"]
% ONE CLAUSE ADDED, as the report prescribes, "after \"fixed-weight baseline\"". Section 2.3 ends by
% handing the reader a live question (orthogonality bounds what any balancer can contribute) that
% section 2.5, the chapter's summing-up and the last thing before Chapter 3, did not close: the report
% measured zero occurrences of "Pareto", "orthogonal", "cosine" and "conflict" in its 532 words, so
% the summary carried only the pre-Pareto argument and a banca member would read the new block as a
% digression.
% NO NEW CLAIM. This restates section 2.3 in the section built to restate, and the measurement it
% points at is Appendix D's, which is cited from section 2.3. The report offered a replace-rather-than-add
% option on the strength of a prior pass's SF-10 (section 2.5 stating its gap three times); ADD was
% chosen because the sentences SF-10 names carry the "measured against the dedicated model" convention
% that the chapter's honesty rules require, and cutting one to fit a clause would trade a rule for a
% rhythm. Length cost: three lines of a 532-word section.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text. Kept, unrelocated in meaning, for review.]
% [GATE FIX B-3, 2026-07-24] "unlocks for" -> "enables in": "unlock" is on the inherited
% WRITING_LAW/GLOSSARY §4 ban list; claim unchanged (persona 14 APPROVE-WITH-EDIT). Author may reword.

Although this dissertation reports on those two targets, its three studies do not all
train the same pair. Chapters~\ref{ch:cbic}
and~\ref{ch:courb} pair next-category prediction with category classification, the
static task that labels a place from its own representation.
Chapter~\ref{ch:mobiwac} keeps next category but replaces that static task with next
region. The replacement follows from the representation rather than from a change of
interest: a place embedding gives a static classifier one fixed vector per place,
whereas a check-in-level representation describes a single visit, and the static task
is a less natural fit for that input than a second sequential target.

Two choices increase what the input representation must carry: treating next category
and next region as final outputs, and replacing the static task. A place embedding
can encode semantic and geographic relationships, but it assigns the same vector to
every visit to a place, so it carries nothing about the visit being predicted.
Check2HGI answers that limit by adding the visit as a level of its own.
% ---------------------------------------------------------------------------
% ARGUMENT LEDGER -- 2.5 (SYNTHESIS: no new citations; all claims trace to 2.1-2.4)
%  Clause 1 -> Ch.3 (CBIC): does naive MTL help, place embedding + hard sharing? (honest null)
%  Clause 2 -> Ch.4 (CoUrb): is the representation the lever, not the architecture? (decompose/enrich input)
%  Clause 3 -> Ch.5 (MobiWac, SUBMITTED/UNDER REVIEW): what does a check-in representation unlock?
%  RESULT WORDING (bound to tests, from NORTH_STAR + GLOSSARY, NOT upgraded):
%    FIX (fact F6): win verb now EXPLICITLY bound -- "by paired superiority tests, outperforms ... on the next category
%    everywhere ... and on the next region at four of six datasets"; "matches ... by non-inferiority testing" at the
%other two.
%    AZ/AL pair never upgraded to a win. Forward-points to Ch.5 ("Chapter 5 reports these tests in full").
%    FIX (fact F7): banned "beat" -> "do not outperform" (was "fail to beat" in the MTL synthesis sentence).
%  four-of-six + two-point margin: confirm against Ch.5 RESULTS_BOARD.md / PAPER_PLAN §3 at adaptation (fact NOTE 10).
%  NO fresh literature; NO frontier material; NO repo codenames. Section does not restate 2.1-2.4 (no "in summary").
% ---------------------------------------------------------------------------

Multitask learning is the mechanism that would let two tasks use what they have in
common, and it is not a free gain. Hard sharing can leave a task below its dedicated
single-task model. The balancing methods proposed to prevent that outcome often do not
improve on a tuned fixed-weight baseline. For the pair of tasks studied here, the
gradients are close to orthogonal on the shared parameters, which leaves such a method
little to correct. Whether joint training helps therefore cannot be assumed. It has to be
measured against the dedicated single-task model for each output, under a protocol
that does not favor the joint model.

Section~\ref{sec:fund:eval} defines that protocol, and the final study applies it in
full. Chapters~\ref{ch:cbic} and~\ref{ch:courb} instead use sample-stratified splits
and report fold means without significance tests. In the final study, user-disjoint
cross-validation limits leakage between a user's training and test visits. Macro-F1 and OOD-discounted
Acc@10 describe different outputs and are read against explicit reference points.
Paired superiority tests support the verb \emph{outperforms}, while non-inferiority
tests within a two-point margin support \emph{matches}. These rules prevent an
observed mean difference from being reported as a conclusion that its test does not
support.

\subsection{Gaps, questions, and answers}

Whether joint training helps this task pair is still open, and two gaps in the
literature explain why. The field has a place-level representation, but not a
check-in-level one built for these targets. It studies multitask learning, but it has
mostly treated the region as an intermediate signal on the way to a place rather than
as an end target.

Each study addresses one question. Chapter~\ref{ch:cbic} asks whether a
place-level representation and hard parameter sharing are sufficient for a joint
model. For that configuration, the joint model does not outperform the dedicated
single-task models. Chapter~\ref{ch:courb} keeps the model fixed, asks whether the
input representation, rather than the sharing topology, is the bottleneck, and finds
that it is. Chapter~\ref{ch:mobiwac} asks what a check-in-level representation and a
different sharing topology enable for two sequential targets. There, the joint model
outperforms the dedicated model on next category at all six datasets, and on next
region it outperforms at four of them and matches the dedicated model at the other
two. Chapter~\ref{ch:mobiwac} reports which datasets fall on each side.

Those answers also decide something practical. Anticipating what kind of place a
person will visit, and where that visit will occur, can support recommendation,
navigation, transit planning, and the allocation of resources by area. A service that
needs both predictions can now take them from a single model to maintain and one
forward pass, instead of two dedicated single-task models. The gain is operational,
not computational. Chapter~\ref{ch:mobiwac} reports the joint model as the larger
artifact, and a forward pass through it costs more than running the two dedicated
models, so the reduction is in the number of models to train and maintain.
