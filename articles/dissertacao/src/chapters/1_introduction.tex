% !TeX root = ../main.tex

% !TeX root = ../main.tex
% Chapter 1 -- Introduction (Introducao Geral)
%
% ---------------------------------------------------------------------------
% DISSERTATION TITLE (working decision, author 2026-07-23; final call with the
% advisor before the front matter is built -- Resumo/Abstract header must match
% the folha de rosto exactly):
%   WORKING TITLE:
%     From Representations to a Single Joint Model: Multi-Task Learning for
%     Point-of-Interest Category and Region Prediction
%   ALTERNATES for the advisor to consider (all pass the two-factor rule --
%   name both the representation and the sharing/architecture factor, or
%   neither; see storyline/AVAL_NECESSARIA_3_ptBR.md D2):
%     1. Representation Drives, Sharing Delivers: A Check-in-Level Multi-Task
%        Study of Next Category and Region Prediction
%     2. The Visit, Not the Place: Check-in-Level Representations and a Joint
%        Model for Next Category and Region Prediction
%     3. One Visit, One Vector: From a Published Null to a Joint Model for
%        Next Category and Region Prediction
% ---------------------------------------------------------------------------
%
% Draft 1 (2026-07-23). Written from the approved beat budget
% (storyline/audit/ch1_beat_budget.md) and the synced spine (NORTH_STAR §6.1).
% Obeys WRITING_LAW.md + GLOSSARY.md. Citation ledger: 1_citations.md (same folder).
% Rule of this chapter: it NARRATES; the prefaces, recaps, and Chapter 6 litigate.


\chapter{Introduction}
\label{ch:intro}


\section{Context and motivation}
\label{sec:intro:context}
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text.
% Kept, unrelocated in meaning, for review.]
% [FAB-12, round 9] "users" plural at the advisor's request. The sentence is generic (it
% describes what the record type is, not one person's visit), so the plural reads better and
% no claim changes.

Location-based social networks record visits through \emph{check-ins}. Each check-in
states that a user visited a place, or point of interest (POI), at a given time.
Although these traces are noisy, human movement is highly regular: an analysis of
large-scale mobility traces estimated the potential predictability of an individual's
next location at about 93 percent~\cite{song2010limits}. Anticipating what kind of
place a person will visit and where that visit will occur can support recommendation,
navigation, transit planning, and the allocation of resources by area. The broader
study of mobility also informs urban planning, disease-spread analysis, and pollution
research~\cite{luca2021mobilitysurvey}. Among the properties of a visit that these
services rely on, the category of the place carries its semantic
meaning~\cite{Xu2023}, and it is one of the two properties this dissertation predicts.
% AUT-04 (PENDENCIAS §4): the place-category clause used to hang off the end of the
% mobility-applications sentence with "while", which gave no reason for it to be there. The
% paragraph had already moved from urban planning to disease analysis, and the clause read as a
% third unconnected item. It now leads INTO the two tasks, which is the work the sentence was
% supposed to do. \cite{Xu2023} is unchanged; no new claim and no new reference.
%
% AUT-05 (PENDENCIAS §4): two sentences removed here. They introduced "neighboring geospatial
% tasks", a term the document never defines, and jumped to the methodology of the second study
% three paragraphs before the studies are introduced.
% WHERE THE CONTENT WENT, measured after the deletion rather than assumed before it, because my
% first version of this comment was WRONG and claimed both references survive in §2.2.3.1:
%   mai2023sphere2vec...  DOES appear in §2.2.3.1 (Sphere2Vec, coordinates on a sphere), so the
%       spatial-encoder material is genuinely presented where it belongs, and also in Ch.4.
%   wu2024torchspatial    appeared ONLY here and in Ch.4 (intro, related, methodology x2). Deleting
%       this sentence therefore removed its only frame-chapter citation. It is NOT orphaned in the
%       bibliography (Ch.4 still cites it four times), so no dangling entry results, but the frame
%       no longer points at it. That is the correct outcome: a benchmarking library for spatial
%       representation learning is a methods reference for the study that used it, not a motivating
%       reference for the opening paragraph.
% Probes: R13-aut04, R13-aut05.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text.
% Kept, unrelocated in meaning, for review.]
% [FAB-13, round 9] "the two prediction tasks that are the object of study of this
% dissertation", his wording, which is more precise about what the two tasks are TO the document.
% [FAB-16, round 9] The semicolon before "this dissertation does not address it" is gone: he read
% it as an AI tell, and WRITING_LAW §4 bans semicolon braids independently. Now two sentences.
%
% AUT-16 (PENDENCIAS §4): the words "static" and "sequential" divide the three tasks throughout the
% document, and Ch.1 used "static" with no gloss while never using "sequential" at all, so a reader
% met the contrast only in Ch.2 (§2.1.1.3, which does gloss both). The appositive in the paragraph
% below supplies the distinction at first use, as WRITING_LAW §1 requires, without moving the formal
% definitions out of Ch.2.
% This comment sits at the PARAGRAPH boundary, not beside the sentence it describes, and that is not
% cosmetic: placed mid-paragraph it split "outside the scope of / this work" across a comment block,
% which the trapped-prose gate caught as a broken sentence. Probe: R13-aut16.

This dissertation studies two properties of the next visit. \emph{Next-category
prediction} identifies the semantic category of that visit, and \emph{next-region
prediction} identifies where it will occur. These tasks differ from \emph{next-place
prediction}, which identifies the exact establishment and is outside the scope of
this work. The two tasks above are sequential, because each one reads a history of
visits. The first two studies also use \emph{category classification}, a static task
that reads a single place rather than a history, predicting the category of a POI held
out from classifier training. The task
pair changes in the final study, as explained in Section~\ref{sec:intro:arc}.
Chapter~\ref{ch:fundamentals} formally defines the three tasks that appear in the
experiments.

Because next category and next region are predicted from the same visit history, one
model may serve both tasks. Multitask learning (MTL) trains related tasks jointly so
that they can share information~\cite{caruana1997multitask}. This approach has been
used in vision~\cite{kokkinos2016ubernet}, clinical
prediction~\cite{lipton2015learning}, and language
modeling~\cite{wei2022finetuned}. For the setting studied here, its operational appeal
is a single model to maintain and one forward pass that produces both predictions,
instead of two dedicated single-task models. Joint training does not guarantee better
predictions, however. Shared parameters can harm one task, a failure known as negative
transfer. Whether MTL would help these POI prediction tasks, and which design choices
would determine the result, was an open question at the start of this research.
% [ORPHANED 2026-08-02 by the author's clean-tree pass: the sentence this block annotated is no longer in the text.
% Kept, unrelocated in meaning, for review.]
% [NEEDS SIGN-OFF: raised v1 assembly, 2026-07-23 | gate L3 fix A-1. Original sentence was near-verbatim with Ch.5
% ("one artifact to train, version, and deploy, and one forward pass whose single set
% of inputs produces both answers at once"); reworded here so the paper chapter keeps
% the original wording. Claim unchanged. Revert if you prefer the duplication.]


% [FAB-19, round 9] "and the arc of this dissertation" removed from the heading at his request:
% the section IS the research question, and the arc is what the section body then narrates. The
% \label is untouched on purpose -- sec:intro:arc is cited from three other files.


\section{Research question}
\label{sec:intro:arc}

\textbf{Does multitask learning help point-of-interest prediction (next category and
next region), and what does the answer depend on?}

The three studies answer this question in sequence. The first reports a negative
result, the second identifies its main bottleneck, and the third tests the resulting
solution. This progression is itself part of the contribution because each study
narrows the explanation supported by the evidence.

% AUT-07 + AUT-10 (PENDENCIAS §4). TWO defects in one sentence, so one edit closes both.
% AUT-07: "static place categories" is NOT in the registry. GLOSSARY §1 registers the task as
% "category classification", and the fail-closed rule forbids a term the registry does not carry;
% WRITING_LAW §2 independently bans synonym-cycling, and this was a second name for a task the
% same chapter had already named two paragraphs earlier. One occurrence, which is exactly the case
% a sampled sweep misses.
% AUT-10: "hard parameter sharing" was used three times in this chapter with no gloss, and its
% formal definition sits in Ch.2 (§2.3.1). WRITING_LAW §1 requires the definition at FIRST use.
% The gloss here is an appositive and the formal definition stays in Ch.2, so nothing is duplicated.
% Probes: R13-aut07, R13-aut10.
Chapter~\ref{ch:cbic}, published at CBIC 2025, introduces MTLnet, the first joint model
developed in this research. It combines a place-level graph embedding with hard
parameter sharing, in which the tasks share one trunk of hidden layers and separate
only at their output heads, and predicts category classification and the next category
in a sequence. For that configuration, the joint model did not consistently outperform
the two dedicated single-task models and required more training time. The study
treated this null result as a finding and proposed three possible explanations,
including an input representation that might not provide enough information for both
tasks.

Chapter~\ref{ch:courb}, published at CoUrb 2026, examines that explanation through a
controlled comparison. The study keeps MTLnet unchanged and replaces its monolithic
64-dimensional place embedding with separate spatial, temporal, and categorical
encoders. Category performance then increases substantially in every state evaluated.
Because the architecture remains fixed, the study identifies the input representation
as the bottleneck for that stage of the research.

Chapter~\ref{ch:mobiwac}, submitted to MobiWac 2026 and under review, develops the
resolution. Any place-level embedding assigns a place the same vector on every visit; a
representation that cannot tell a weekday lunch from a Saturday night out at the same
place is working against both tasks at once. That observation is the hypothesis the
final study tests. It moves the representation to the check-in level: one vector per
visit. Acting on another of the first study's candidate explanations, it also replaces
the shared hidden layers with cross-attention between task-specific streams, addressing
the earlier concern about hard parameter sharing. Under a check-in level representation, static
category classification is a less natural companion task than a second sequential target,
% [round10, PENDENCIAS §6 FAB-22, author's ruling: "nao precisamos tirar tudo, o ponto e o excesso de
% informacao. Podemos comentar algo somente sobre o fato que os experimentos foram realizados em 5
% estados dos estados unidos e um dataset non-U.S para metritos de generalizacao. Tambem, podemos
% comentar que usamos metodos estatisticos para comprovar essa diferenca. Enfim a ideia e ser mais
% direto. Nao remover tudo."]
% MOST OF THIS RULING WAS ALREADY SATISFIED when I re-measured: the per-dataset counts and the
% "four of six ... TOST" detail he objected to had already left this paragraph, and the sentence
% already named the five states, Istanbul, and the two test families. ONE CLAUSE WAS MISSING, and it
% is the half of his ruling that carries the argument: that Istanbul is there BECAUSE it is not a
% United States dataset, which is what makes it evidence about generalization rather than a sixth
% dataset. Naming the purpose is also what keeps the sentence honest without the numbers: the claim
% it makes is about scope, and the scope is now stated.
so the final task pair becomes next category and next region. The study evaluates five
United States states and, to test whether the finding holds outside that setting,
Istanbul as a non-United-States dataset, comparing the joint and dedicated models with
paired superiority and non-inferiority tests. The results support a positive answer for
this configuration; Chapter~\ref{ch:mobiwac} reports the task-specific outcomes.

The task pair thus changes across the studies: the first two combine static category
classification with next-category prediction, whereas the final study combines
next-category and next-region prediction. The dissertation does not treat these as one
unchanged experiment. It presents a sequence in which a result valid for one
configuration leads to a diagnosis and then to a different, explicitly bounded
solution.


\section{Objectives}
\label{sec:intro:objectives}

The general objective is to determine whether multitask learning helps next-category
and next-region prediction and to identify the conditions that shape the answer. Four
specific objectives connect this question to the article chapters and to their
consolidation:

\begin{enumerate}
    \item Evaluate whether a joint model with hard parameter sharing benefits static
    category classification and next-category prediction when compared with
    dedicated single-task models (Chapter~\ref{ch:cbic}).
    \item Determine how the input representation affects that result by enriching and
    decomposing the input while keeping the architecture fixed
    (Chapter~\ref{ch:courb}).
    \item Design and validate a check-in-level representation and a joint model for
    next-category and next-region prediction (Chapter~\ref{ch:mobiwac}).
    % AUT-12 (PENDENCIAS §4, author ruling A): ch:conclusion -> ch:mobiwac. The clause names the
    % PROTOCOL (user-disjoint cross-validation, significance testing, non-inferiority), and that
    % protocol belongs to the final study, which is Chapter 5. Chapter 6 is the Conclusion and is
    % not a study. The old reference resolved, so the build reported zero undefined references and
    % every gate passed while the rendered page printed "(Chapter 6)". Probe: R13-obj4.
    \item Consolidate the evidence under the user-disjoint cross-validation,
    significance-testing, and non-inferiority protocol used in the final study
    (Chapter~\ref{ch:mobiwac}).
\end{enumerate}

% [COD-003, round8, 2026-07-30] Objective 4 said "a leakage-guarded statistical protocol". Changed to
% "a user-disjoint statistical protocol" on the author's decision, PENDENCIAS_RESOLVIDOS 5.1 (arquivado 2026-07-30)
% ("Valide no texto,
% pf"), and the substitution is his own proposed wording. The reason it is not cosmetic: Chapter 5
% states that it LIMITED the future-edge channel of the representation, not that it closed it, so
% "leakage-guarded" promised a property of the representation pipeline that the protocol does not
% deliver. What the protocol does guarantee is the user-disjoint split, which is what the clause now
% names. The claim is strictly weaker, and no number moves.
% The apposition that follows lost its own "user-disjoint" in the same edit, because the adjective now
% sits in the head noun phrase and the pair read "a user-disjoint statistical protocol, the
% user-disjoint cross-validation with". That deletion is mechanical de-duplication, not a scope
% change: the components named (paired significance and non-inferiority testing) are untouched.


\section{Scope and assumptions}
\label{sec:intro:scope}

The experiments use observational check-in traces from two sources: Gowalla data for
five United States states (Alabama, Arizona, Florida, California, Texas) and the Istanbul subset of the Massive-STEPS
benchmark. % [round11, PENDENCIAS §6 FAB-15, author's ruling "opcao 1" and his confirmation of 2026-08-03: "Cut
% regions units, the taxonomy, cut most as possible, but let them in a specific place in the
% introduction, so someone reading just the introduction has this context. The point is we don't need to
% be repeating this all the time, mainly because our approach is generic and not fixed on this taxonomy."
% MEASURED FIRST. The units had ALREADY left §1.1, where Fabricio objected; this sentence in §1.4 was the
% only live occurrence of "census tract", "mahalle" or the seven classes anywhere in the introduction, so
% there was no repetition WITHIN the chapter to cut. The repetition he means is across the frame: Chapter
% 2 §2.1 is the definitional home (:119-121) and the next sentence here already points at it for the
% output spaces and geographic units. So this is compaction, not deletion: the concrete instantiations
% stay, once, because he asked that an introduction-only reader keep the context.
% WHAT CHANGED, and it is the framing rather than the facts. "over the seven-class taxonomy defined in
% Chapter 2" made the taxonomy read as part of the task DEFINITION; the targets are now stated plainly
% and the label sets follow as properties of the data used here, which is what they are.
% WHAT I DELIBERATELY DID NOT WRITE. His reason is that the approach is generic and not fixed on this
% taxonomy, and a sentence asserting taxonomy-independence would overreach: Chapter 6 limitation 2
% (6_conclusion.tex:384-385) states that next-category prediction uses seven top-level classes and that
% "a finer taxonomy may change the effect of joint training". The formulation admits other label sets;
% whether the RESULT survives them is explicitly open. Removing the definitional framing conveys his
% point without making a claim the document contradicts one chapter later.
The prediction targets are the next category and the next region. In the data used
here the category space has seven classes, and a region is a census tract in the United
States or a \emph{mahalle} in Istanbul.
The joint model operates under a single-model constraint: one trained artifact must produce
both outputs in one forward pass. These choices define the design scope.
Chapter~\ref{ch:fundamentals} specifies the output spaces and geographic units, and
Chapter~\ref{ch:conclusion} discusses the limitations that affect how the results may
generalize.


\section{Organization of this dissertation}
\label{sec:intro:organization}

This dissertation is organized as a collection of works, where each central chapter is an
independent article. The articles were re-typeset in a common format, and the second
article was translated into English. Their content follows the published versions or,
for the article under review, the submitted manuscript.
% Corrections introduced during adaptation are recorded in the errata appendix of \extravolume.
% [round7, 2026-07-29] "the errata appendix" -> "the errata appendix of \extravolume". This
% sentence is the promise the whole errata regime rests on, so it had to keep pointing at a
% document the reader can actually obtain once the appendix left this one. It names the
% appendix by role rather than by letter because the surrounding sentence is about the
% practice, not about a location; the sentences that send a reader to a specific correction
% (Ch.3's preface and footnotes, Ch.4's preface, Ch.5's preface, the conclusion) carry the
% letter. \extravolume is defined in preamble.tex.
% [round7 comment-trim pass, 2026-07-29] TRAPPED PROSE, REPORTED AND NOT FIXED. The lead-in
% sentence "The collection is organized as follows:" sits on the line above INSIDE this comment
% block, so it never reaches the PDF: rendered page 15 runs straight from "rather than silently
% edited." into the first bullet with no lead-in. Verified in the render, not the source.
% check_trapped_prose.py does not catch it (it returns 0 suspects for this file) because the
% swallowed text is on a comment line rather than a prose line following one.
% NOT FIXED HERE because restoring it would add a line of prose to page 15, and this pass is
% required to leave the rendered PDF byte-identical. Author: move the sentence out of the comment,
% then re-measure the page counts. Reported in src_utils/_round7/27_comment_trim.md.
% The collection is organized as follows:

\begin{itemize}
    % [FAB-23, round 9] ", Fundamentals" dropped at his request: the \ref renders the number and the
    % chapter title is already in the sumario, so repeating it here is redundant.
    \item \textbf{Chapter~\ref{ch:fundamentals}} defines the shared background: POI
    prediction tasks, mobility representations, multitask learning, datasets,
    metrics, and evaluation protocols.
    \item \textbf{Chapter~\ref{ch:cbic}} reproduces the first article, published in
    English at the XVII Congresso Brasileiro de Intelig\^encia Computacional
    (CBIC 2025, DOI~10.21528/CBIC2025-1191324). This dissertation's author is its
    first author.
    % Venue verified 2026-07-23 against the official CBIC 2025 site
    % (cbic2025.dcc.ufmg.br): XVII edition, promoted by SBIC, Belo Horizonte,
    % October 27-30, 2025.
    \item \textbf{Chapter~\ref{ch:courb}} presents an English translation of the second
    article, published in Portuguese at the X Workshop de Computa\c{c}\~ao Urbana
    (CoUrb 2026, DOI~10.5753/courb.2026.22960), held with the Simp\'osio
    Brasileiro de Redes de Computadores e Sistemas Distribu\'idos (SBRC 2026).
    Tarik S.\ Paiva is the first author. This dissertation's author is the second
    author, contributed the MTLnet baseline, and presented the work at the event.
    % Venue verified 2026-07-23 against SBC OpenLibrary (sol.sbc.org.br/index.php/
    % courb): X edition, May 25-29 2026, Praia do Forte/BA, with the XLIV SBRC.
    \item \textbf{Chapter~\ref{ch:mobiwac}} presents the third article, submitted to the
    23rd ACM International Symposium on Mobility Management and Wireless Access
    (MobiWac 2026) and under review. This dissertation's author is its first
    author.
    % Venue verified 2026-07-23 against the official symposium site
    % (mobiwac-symposium.org/2026): 23rd edition. Update the status wording
    % here and in sections 1.1-1.2 the moment a decision arrives.
    % [FAB-24, round 9] Same as FAB-23.
    \item \textbf{Chapter~\ref{ch:conclusion}} answers the research question across the
    three studies, states their joint limitations, and connects each limitation
    to future work.
\end{itemize}

Each article chapter begins with a preface that identifies its venue and publication
status. The prefaces also distinguish the conclusion supported at the time of each
article from the final position of the dissertation.


\section{Contributions}
\label{sec:intro:contributions}

The contributions are organized into four groups.

\begin{description}
    \item[Theoretical.] The studies show that the input representation and its sharing
    topology determine whether MTL helps these POI prediction tasks. A null result
    with a place-level representation and hard sharing therefore does not conflict
    with a positive result obtained from a check-in-level representation and a
    different form of sharing.
    \item[Software.] The work provides MTLnet (Chapter~\ref{ch:cbic}), Check2HGI and the
    joint model built on it (Chapter~\ref{ch:mobiwac}), and reproducible training
    and evaluation pipelines for Chapters~\ref{ch:cbic} and~\ref{ch:mobiwac}. The
    corresponding code repositories are referenced in those chapters.
    \item[Empirical.] The final study compares joint and dedicated models on six
    datasets under user-disjoint cross-validation. Each configuration has twenty
    fitted models: four repetitions of the complete five-fold experiment, each
    with a different random initialization and the same fixed folds. The analysis
    uses paired tests on the four per-seed means, non-inferiority tests, and a
    leakage audit. Because every seed uses the same fold partition, the reported
    intervals do not include uncertainty from resampling the user splits.
    % [NEEDS SIGN-OFF: raised round 4, 2026-07-26 | REV-014, 2026-07-26] "twenty repetitions per configuration
    % (four seeds, five folds)" -> the 20 fits kept, the inferential unit (n = 4)
    % named, plus the fixed-partition clause. Sources: GLOSSARY rows "paired
    % superiority test" + "n = 20 (fitted models) and n = 4 (inferential unit)";
    % stats_n20/RESULTS.md:65-67; STATISTICAL_PROTOCOL.md:187-190.
    % [round10, PENDENCIAS_RESOLVIDOS §6.10 (arquivado 2026-08-03) BLQ-2, author's ruling: "mantenha everywhere para o categoria e
    % especifique onde for preciso para o next-region"] This bullet was the LAST site collapsing the
    % region verdict into "either outperforms or matches", which discards the partition. Chapter 6
    % :126-127 already states it correctly and this now mirrors that wording. The category half keeps
    % its universal because it is true at all six datasets; only the region half needs the partition.
    % WRITING_LAW §3 bans bare "everywhere", so the category clause names the six datasets instead.
    \item[Practical.] One model produces both predictions in one forward pass. It
    outperforms the dedicated models on the category task at all six datasets and on
    the region task at four of the six; at the other two it remains statistically
    non-inferior within a two-point margin (TOST).
\end{description}

% [round5, persona 03 S3-05] "seed" first reached the reader here, on rendered p. 16, while its
% definition sits in 5_mobiwac/05_setup.tex on p. 66 -- a fifty-page forward dependency in a frame
% chapter, where WRITING_LAW's define-at-first-use rule is in full force. The gloss added here uses
% the registry's own wording, so Ch.5's definition is unchanged and stays canonical. The Abstract and
% Resumo already comply by writing "random initializations" instead of the bare term.