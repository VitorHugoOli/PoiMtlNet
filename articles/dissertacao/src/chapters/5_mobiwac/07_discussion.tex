% !TeX root = ../../main.tex
% chapters/5_mobiwac/07_discussion.tex -- one section of Chapter 5 (MobiWac 2026, submitted, under review).
% Split out of chapters/5_mobiwac.tex on 2026-07-28 (author request: the paper
% chapters were single long files, while the originals under articles/[mobiwac]/src/sections/
% are one file per section). The split is PURELY MECHANICAL: content was cut at
% \section boundaries and not edited. The chapter file now \inputs these in order.
% Editing rules are unchanged and still governed by the chapter's status above:
% see NORTH_STAR section 4 before changing any sentence here.
\section{Discussion and Limitations}
\label{sec:mobiwac:discussion}

One model serves both tasks, in one forward pass it outperforms the dedicated category
model at all six datasets, and on region it outperforms the dedicated model at four of them
and matches it within a two-point margin at the other two, with the region output keeping a
private spatial path (Table~\ref{tab:mobiwac:results};
Figure~\ref{fig:mobiwac:deltas}). The gain is substantial, and where we could test it the gain is a
property of the joint architecture rather than of cross-task transfer: at the three datasets
where the region pathway is held at its initial values, the category gain survives in full.
Which component of that architecture produces the gain is not settled by the controls reported
here (Section~\ref{sec:mobiwac:results-part2}).
% RESTORED 2026-08-05 on the author's ruling ("A parked note — restore it").
% WHAT HAPPENED, recorded because the sync policy for this chapter is load-bearing: these three
% sentences were commented out here under a marker %[AUTHOR COMMENT] while remaining LIVE in the paper
% source, and my commit 71d119ce swept that uncommitted state in without naming it. The marker occurred
% exactly once in the tree and was not written by me; the author confirms it was a note parked for his own
% attention, not a removal.
% WHY RESTORING IS THE CORRECT STATE AND NOT A JUDGEMENT CALL: this softened wording IS the recorded
% outcome of NEEDS_SIGN_OFF 28 (closed 2026-08-05) and of the ERRATA.md entry at :212-228, both of which
% record it as present in BOTH texts. A comment-out in one copy only was a third state neither record
% described. Both copies are now byte-identical in this passage again.
% DO NOT STRENGTHEN THIS ATTRIBUTION. NEEDS_SIGN_OFF.md:1516-1536 records the author asking to restore
% the stronger wording that credits the shared trunk with the category lift, and that request being
% DECLINED on evidence: four studies show the category gain survives removal of the trunk (the Florida
% cross-attention ablation, the zeroed-mixing arm, the category-weight sweep, and the cascade arm). His
% final ruling was "suavizar, sem desenvolver alem disso por hora". Probe: R13-mech-soft.

% [round5, cross-check] This sentence credited the SHARED TRUNK with the category lift, sixteen
% lines after the paragraph at :713 refuses exactly that attribution. Rewritten to name the joint
% architecture, which is what the freeze control and the capacity-matched control support. The
% private spatial path claim is unchanged: it is architectural, not an attribution of the gain.
% [round6, COD-004, author decision: soften the attribution AND add the scope clauses] Three changes,
% and one of them is a RENDER defect that no source-level review had caught.
% (1) THE RENDER DEFECT. The two cross-references were written "\\ref{...}" with a doubled backslash,
%     introduced in 877b2109. In LaTeX that is a line break followed by the literal text "ref{...}",
%     so the defense PDF printed "reftab:mobiwac:results; Figure reffig:mobiwac:deltas)" on p.75, in
%     the opening sentence of the Discussion, with two spurious line breaks. Measured, not inferred:
%     read out of the text layer of src/build/main.pdf before the fix. It produced no warning and no
%     undefined-reference count, because there was no \ref command left to be undefined, which is why
%     undef_ref=0 in every build report since. Now single-backslash, so the two pointers resolve.
% (2) THE ATTRIBUTION. The sentence said "the joint architecture lifts the next-category task". That
%     is weaker than the "shared trunk" version it replaced, but it still asserts a locus, and this
%     chapter refuses to name one: at the freeze-control paragraph above it says "we attribute the gain
%     to the joint architecture rather than to any named component of it" and closes "We therefore do
%     not name the shared trunk as the source, and we do not present the ablation as evidence against
%     it either." The 44 lines of comment at that site record why: the freeze control removes region
%     TRAINING, so it eliminates the region-teaches-category reading but cannot separate the trunk from
%     the category stream's own encoder, the per-stream feed-forward blocks, or the added depth; and the
%     repository holds a development arm that tests the trunk directly and disagrees, F50 at Florida,
%     five folds by fifty epochs, category macro-F1 68.36 +- 0.74 with cross-attention on against
%     68.32 +- 0.67 off, a difference of -0.04 +- 0.13 that a paired Wilcoxon cannot separate from zero
%     (W+ = 5, p = 0.6250), whose own record calls the null "misleading" and a "hidden compensation
%     effect". Both figures are quoted from
%     docs/findings/archive/F50/F50_T1_5_CROSSATTN_ABSORPTION.md:19-20,:37,:229, not recomputed here.
%     The opening sentence now states the RESULT, which the tests license, and says in one clause that
%     the locus is unsettled, which is what the evidence supports. The private spatial path stays: it is
%     a statement about how the model is built, not an attribution of the gain.
% (3) THE SCOPE CLAUSES the author approved. The bare "lifts" and "keeps competitive" carried no scope
%     and no verb-test binding, in the opening sentence of the section a committee reads first. Now:
%     "outperforms ... at all six datasets" for category and "outperforms ... at four of them and
%     matches it within a two-point margin at the other two" for region. Both verbs are bound to their
%     tests per WRITING_LAW section 3 and both match the wording already used at the results sentences
%     of this chapter and in its contribution list; the region split is four outperforms (Florida,
%     Texas, California, Istanbul) and two TOST matches (Alabama, Arizona), and Arizona is NOT upgraded.
%     No number is quoted here, so no new numeric claim enters the section; each is stated with its
%     dataset count and its test, and the results table and the delta figure are named.
% [NEEDS SIGN-OFF 28 | CLOSED 2026-08-04] The author asked to restore the stronger wording, "the shared
% trunk carries the semantic context that lifts the next-category task", on the grounds that current
% studies substantiate the mechanism. THE EVIDENCE WAS AUDITED AND IT DOES NOT (see the closed
% sign-off 27 block in 06_results.tex for the four converging experiments and their numbers). His
% ruling after seeing them: soften to a substantial architecture-level gain and go no further for now.
% WHAT THIS SENTENCE MAY AND MAY NOT SAY:
%   MAY  -- the gain is substantial; it is a property of the JOINT ARCHITECTURE and not cross-task
%           transfer. Test behind it: the freeze-region control, reported in this chapter at
%           06_results.tex:197. It is scoped to THREE datasets (Alabama, Arizona, Florida), which is
%           why the sentence says "at the three datasets where" and not "at all six" -- the sentence it
%           follows spans six, and dropping the scope would silently generalize a 3-of-6 result.
%   MAY NOT -- name the shared trunk, or any other component, as the source. Four experiments say the
%           category gain survives the trunk's removal.
%   MAY NOT -- cite the capacity-matched dedicated baseline here. It is a post-submission control that
%           this chapter does not report; it belongs to Chapter 6.
% PARITY: the identical edit is applied in articles/[mobiwac]/src/sections/07_discussion.tex, per the
% under-review regime. An ERRATA.md row is owed in articles/[mobiwac]/ for this wording change.

A service could treat the model's top-ranked next regions as an anticipatory
set: at California, ten regions out of 8{,}501 contain the true next region 65.69
percent of the time, over 500 times better than picking ten at
random. On four datasets (Alabama, Arizona, Florida, and Istanbul; a single seed over five folds), the ten shortlisted
regions lie a median of 3 to 8 kilometers from the shortlist's centroid, against 17 to 176 kilometers for ten regions
drawn at random from the same candidate set (median over 10,000 draws). This remains motivation, not a measured service
result.

% AUDIT NOTE 2026-08-04, no edit needed here, recorded so a later sweep does not "fix" a correct
% sentence. The fold-partition correction of that date changed "over the same folds" in
% 05_setup.tex, because a SEED changes the partition. This occurrence is different and is TRUE as
% written: it compares the joint model against the dedicated models WITHIN one run, where both read
% the same partition by construction. That is exactly the property the paired comparison relies on.
% See articles/dissertacao/science/fold_partition_and_seeds.md §7 for the distinction.

Three limits qualify these results. First, our representation is trained once over all places; visits to places
never seen during training are the single effect that we cannot fully isolate. A planned follow-up trains it on each
fold's training places only and embeds unseen visits. Second, epoch selection consults the fold that the score is
then read on (Section~\ref{sec:mobiwac:setup-windows}), so every absolute score reported here is optimistic. The
comparison between the joint model and the dedicated models is affected far less, for two reasons we can state
outright: the selection rule is the same for both models on the same folds, an epoch chosen on validation and read
on that fold, with each model selected on its own validation objective (Section~\ref{sec:mobiwac:results-part2}),
and the dedicated model receives the wider search, a per-dataset sweep over batch size and learning rate against one
configuration held fixed across all six datasets for the joint model. The residual therefore favors the comparator,
which makes the reported difference conservative. It does not follow that the bias cancels exactly. Third, we do not
build or evaluate a mobility-aware service, this is a background motivation, and this chapter's claims are the
prediction
results themselves.
% [round6, COD-006 "identically"] RE-ANCHORED, READ, AND NARROWED. An earlier audit round recorded this
% word as REFUTED, on the ground that the per-model selection objectives are disclosed elsewhere in the
% chapter and the non-cancellation caveat is the next sentence. Both of those facts hold. The word still
% overstates, for a reason that reading the sentence alone does not show: WHAT IT MODIFIES. It modifies
% a mitigation, inside a limitations paragraph, whose job is to bound how far the optimistic-score bias
% reaches into the joint-versus-dedicated comparison. A word that makes a mitigation sound stronger than
% it is understates a limitation, and WRITING_LAW section 3 forbids that direction as firmly as it
% forbids upgrading a result.
% MEASURED, in this chapter: the two models are NOT selected identically. The selection rule stated in
% the results section reads "each dedicated model at its task's best epoch, and the joint model at the
% epoch selected by its joint validation score (the geometric mean of the two task metrics)". So the
% PROCEDURE is shared (one saved model per fold, the epoch chosen on validation, the score read on that
% same fold) and the OBJECTIVE differs by model. "Identically" asserted both. The replacement asserts
% only the first, names the second, and points at the section that states it, so a reader meeting the
% mitigation gets the asymmetry at the same time rather than eighteen pages earlier.
% What is NOT changed: "on the same folds" is kept, since it is what makes the comparison paired; the
% following sentence, "It does not follow that the bias cancels exactly", already carried the caveat and
% is untouched; and the wider-search half of the mitigation is untouched. No number, no verb, no result.
% Same edit in articles/[mobiwac]/src/sections/07_discussion.tex, with the paper's own sec:results-part2
% label, per the under-review regime; logged in that article's ERRATA.md.
% [NEEDS SIGN-OFF 29 | CLOSED 2026-08-04] The fixed-partition caveat is REMOVED, from this chapter and
% from articles/[mobiwac]/src/sections/07_discussion.tex, on the author's explicit instruction.
% ON THE RECORD, because the removal was made against the evidence rather than because of it: I checked
% the claim and it was TRUE. STATISTICAL_PROTOCOL.md:183-190 states that the board freezes folds once,
% and that the MTL-vs-STL comparison "is paired because [both] are evaluated on the same frozen overlap
% folds -- this is what licenses the per-fold paired Wilcoxon (cat) and paired TOST (reg) at n=20". The
% fixed partition is the CONDITION for the headline tests, not a defect. I recommended keeping the
% sentence and explaining that; the author chose removal after being shown this. Recorded so a later
% reader does not mistake the deletion for a correction of an error.
% If a reviewer asks what the reported intervals cover, the answer is: variation across random
% initializations, not across resampled user splits. The document no longer states this anywhere --
% verified 2026-08-04 that 1_introduction.tex no longer carries the caveat either, so the two chapters
% are at least consistent. An ERRATA.md row is owed in articles/[mobiwac]/ for the removal.
% [v2 review, 2026-07-26] Fixed-partition caveat added here. Personas 09 (statistical consequence)
% and 04 (concordance seam) independently reported that 1_introduction.tex:246-247 carries this
% caveat while Chapter 5, which reports the intervals, did not. Source: STATISTICAL_PROTOCOL.md
% :187-190 (one frozen fold partition shared across all arms and seeds). No number introduced;
% the sentence states the scope of the existing intervals, it does not requalify any of them.
% [REV-003, 2026-07-26] Adapted from the drafted sentence at
% src_utils/_review_v1/09_stats_leakage_skeptic_report.md:294-307 (finding B4). Two deliberate
% departures from that draft, both weakening it:
%  (a) the draft says the comparison "is unaffected ... and the bias cancels in the difference".
%      Exact cancellation is not demonstrated anywhere in the record, so the text says the
%      comparison carries far less of the bias and adds an explicit non-cancellation sentence.
%  (b) the two supporting facts are the ones that ARE verifiable. Identical rule across arms:
%      src/training/runners/category_cv.py:97 and next_cv.py:195 both evaluate on val_loader at
%      that task's own best_state, matching the joint runner's val-selected checkpoint
%      (mtl_cv.py:_compute_joint_selectors, geom_simple). Wider dedicated search: the per-dataset
%      batch-size x learning-rate sweep of docs/studies/closing_data/v17_completion/
%      CEILINGS_N20_FINAL.md:41-53, already disclosed in this chapter at the Table 3 lead-in.
% No number is introduced. "Two limits" -> "Three limits" is the only other change.
% [round5, MobiWac GLOSSARY §3] "arm" is on the never-use list there ("clinical-trial word,
% foreign to this audience"), and I had introduced it in this round's own limitation sentence.
% Replaced with "both models" / "the dedicated model", the glossary's substitution.

Our representation is a fixed per-visit vector that any downstream model
can consume. Moura et al.~\cite{moura2025mobilityaware}, whose structural
analysis of tourist check-ins reads past visits only, name machine learning
as a next step; this study moves in that direction.
