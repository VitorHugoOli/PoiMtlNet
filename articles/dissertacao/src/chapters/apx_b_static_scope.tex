% !TeX root = ../main.tex
% apx_b_static_scope.tex -- one section of Appendix B, kept in its own file so that a SINGLE
% commented \input line in apx_b_errata.tex suppresses it (author's condition, PENDENCIAS 2.2:
% "deixe isso facil de ser comentado"). Provenance for every number is in the comment block
% at that \input site. Do not inline this file back into the appendix.

\section{The scope of the static task in Chapter~\ref{ch:courb}}
\label{apx:errata:static-scope}

Chapter~\ref{ch:courb} reports two tasks. One is sequential: predict the category of the
next place a user visits, from the history of that user's check-ins. The other is static:
classify the category of a place from that place's own representation. This section
records what we established after publication about the second one, and it bears on how
that chapter's category results should be read.

The place embedding that Chapter~\ref{ch:courb} feeds to the static task is built from a
fine-grained class label attached to each place, and that label determines the target
category exactly. Measured over the five Gowalla state subsets the collection uses, the
fine class takes between 284 and 365 distinct values per state, every one of them maps to
exactly one of the seven top-level categories, and none maps to more than one. The
embedding is a lookup on the fine class, and the fine class is a strict refinement of the
label. So the static task's input determines its target by construction, and the reported
accuracy on that task measures a deterministic mapping rather than a learned semantic
inference.

Three qualifications matter, and none of them is a softening.

The first is that the sequential task is unaffected. Its input is a history of visits and
its target is a category the model has not seen for that step, so no equivalent identity
exists between input and label. Every claim Chapter~\ref{ch:courb} makes about the
sequential task, including the improvement that the decomposed representation produces,
stands as published.

The second is that the two chapters differ in how direct the channel is, and the
difference is one of degree rather than of kind. Chapter~\ref{ch:cbic} builds each node's
input feature from the average of that place's neighbors' one-hot categories, excluding
the place itself, so no place's own label appears in its own input. That exclusion does
not survive the graph convolution: the place graph is undirected, so a place appears in
its neighbors' features, and one convolution aggregates over the neighborhood together
with the place itself. A place's own label therefore returns to its own embedding at the
first hop, averaged in with the neighborhood. So the earlier chapter's
static task also has a path from label to representation. It is indirect and averaged,
where Chapter~\ref{ch:courb}'s is an exact lookup, and that distinction is the whole of
the difference between them.
% [round6, D-01, domain panel persona 09] CORRECTED, and this is a correction to MY OWN prose from
% earlier the same day. The paragraph used to conclude "so a place's own label never enters its own
% representation", exculpating Ch.3 entirely. THE PREMISE IS TRUE AND THE CONCLUSION DOES NOT FOLLOW.
% Excluding a node's label from its own INPUT FEATURE is not excluding it from its own EMBEDDING.
% Measured rather than reasoned: research/embeddings/dgi/preprocess.py:114-130 builds
% x_i = mean of one-hots over N(i), self excluded -- verified, x_i does NOT contain node i's label.
% But nx.from_pandas_edgelist builds an UNDIRECTED graph, so node i is in N(j) for each j in N(i),
% and POIEncoder.py:29 is a single GCNConv(cached=True) whose add_self_loops defaults to True, so
% h_i aggregates over N(i) union {i}. Reproduced the whole path on a 4-node graph: node 0's own
% Food one-hot is absent from x_0 (0.000) and present in h_0 with weight 0.667.
% So the exculpation was false and is withdrawn. What survives is the degree distinction, which is
% real and is what the author's own ruling rested on ("esse nao se aplica ao DGI que usamos no cbic",
% PENDENCIAS 2.2): CoUrb's channel is an exact deterministic function of a strict label refinement,
% CBIC's is a neighborhood average. Those are different situations. They are not absence and presence.
% [NEEDS SIGN-OFF: raised round 6, 2026-07-28 | this paragraph now says something WEAKER about Chapter 3 than the author's ruling
% assumed. He ruled the static-task problem is CoUrb's alone; the measurement says Ch.3's channel is
% indirect rather than absent. The section still concentrates the finding on Ch.4, which is where the
% exact identity is, but he should read this paragraph specifically before it ships.]

The third is that Chapter~\ref{ch:mobiwac} does not inherit the problem, for the reason
that chapter gives on its own terms: it replaces the place-level embedding with a
check-in-level representation, whose input is a single visit, and the identity described
above does not arise.

This appendix, and not the chapter, is where the statement belongs, because Chapter~\ref{ch:courb} is a
published, co-authored article and this appendix is the instrument this collection uses
for the difference between what the published text says and what we know now. The
measurement is recorded in the repository released with this work.
