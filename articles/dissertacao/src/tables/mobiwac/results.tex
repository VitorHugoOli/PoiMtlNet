% !TeX root = ../../main.tex
% mobiwac/results.tex -- one table. Extraction provenance and verification: tables/README.md
% [2026-08-12] POI-RGNN PROVENANCE, recorded here because a reviewer traced the column to the wrong
% file and it took a chain of four documents to settle. The printed FL/TX/CA cells (34.49 / 33.03 /
% 31.78) do NOT reproduce from docs/results/baselines/faithful_poi_rgnn_{florida,texas,california}_5f_*.json,
% which average 33.347 / 32.084 / 30.711, about one point lower. Those three JSONs are the PRE-BUGFIX
% May-2 draw. The repository says so itself: docs/baselines/next_category/comparison.md carries a
% "SUPERSEDED 2026-05-04" banner naming 33.35 / 30.71 / 32.08 as pre-bugfix and 34.49 / 31.78 / 33.03
% as canonical, and docs/baselines/POI_RGNN_AUDIT.md records the canonical trio against BASELINE.md.
% The Istanbul, AL and AZ cells match their JSONs exactly, which is why the mismatch looks like mixed
% provenance rather than a stale set. The printed values are correct; the JSONs are the trap. A reader
% checking this column must use the canonical chain, not the per-fold files.
\begin{table}[htbp]
% [2026-08-12, AUTHOR RULING] ROW ORDER CHANGED to ascending check-in count (AL 113,846; AZ 236,450;
% Istanbul 462,615; FL 1,407,034; CA 3,171,380; TX 4,089,892 -- read from Table
% \ref{tab:mobiwac:datasets}), replacing region count. Rows were moved whole by script, never retyped,
% so every cell and every footnote marker travelled with its dataset. The Regions column stays as a
% covariate and no longer sorts, which is why the caption now names both quantities: the ordering key
% and the column that is no longer monotone in it. No claim in the chapter reads off row order; every
% one names its datasets, so the reorder changes nothing that is asserted.
% [2026-09-02, B-27] ADDED to the caption: "it does not mark the largest value in a column." A banca
% reader annotated this table on the delivered PDF (wrapup/REVISAO_BANCA_PDF.md, B-27) asking why two
% cells are bold in the lower block and one in the upper -- he read bold as "the best value". The
% caption already excluded that reading by implication (bold marks a Holm-surviving improvement over
% the DEDICATED model, not a column maximum), but only by implication. The misreading is predictable
% because this document uses bold with the other sense two chapters earlier: tables/cbic/category.tex
% prints "the better of the MTL and Single values per row in bold, as in the published table". Same
% document, two conventions. The added clause is a negative, not a new claim: no number, mark or
% verdict changes. Caption prose only -- this caption is already dissertation-authored and diverges
% substantially from the article's own ([mobiwac]/src_fix/tables/tbl3_results.tex:22-29, which orders
% by region count and carries neither the two-block sentence nor the half-point bound).
\caption{One model, two tasks: the single joint model against the dedicated single-task models and the
external baselines, ordered by check-in count (Table~\ref{tab:mobiwac:datasets}); the region counts in
the second column do not follow that order. The upper block reports next-category (macro-F1) and the
lower block next-region (Acc@10); the two share the dataset and region-count columns, and the same
six datasets appear in the same order in both. Two kinds of mark carry two different things. Bold
and underline report magnitude only: in each row the largest value is in bold and the second largest
is underlined, and when the two largest tie at the printed precision both are in bold and no second
is marked. The statistical verdict is a separate mark beside the joint-model value: $\uparrow$ marks
an improvement over the dedicated model that survives Holm correction within its task family, and, in
the region block, $\approx$ marks a difference that stays within the two-point margin registered
before any result was read (statistical non-inferiority, TOST;
Section~\ref{sec:mobiwac:results-part2}). A row with the dedicated value in bold and $\approx$ beside
the joint value therefore reads as: the dedicated model has the larger number, and the difference is
inside the margin. Category cells carry no equivalence mark because the margin was registered for the
region axis only, so a category difference that fails superiority is reported by the bound its
interval supports, half a point.}
% [2026-09-02, AUTHOR RULING, transversal emphasis convention (from the defense deck,
% presentation/slides/main.tex:1620-1638), B-27 follow-up] The B-27 clause "it does not mark the
% largest value in a column" became FALSE the moment bold started marking the row maximum, and is
% gone. The caption now separates the two channels explicitly, which is the confusion behind the
% reviewer's B-27 question (Alabama: dedicated larger, nothing bold, read as "no best here").
% Tie rule as the deck executed it: AZ and CA category rows, 34.57 / 35.63 both bold, no underline.
% Emphasis re-verified by the writer with a row-max check over all 96 data rows of the five
% tables (2 ties, 0 mismatches); no digit changed.
\label{tab:mobiwac:results}
\centering
% [NEW-7, 2026-07-26] Was ONE eleven-column tabular inside {adjustbox}{max width=\textwidth},
% which shrank the body to a measured 8.00 pt (fold sd 6.00 pt) against 11.96 pt body text.
% Split at the cmidrule the two task blocks already carried, into two tabulars inside this one
% float: one caption, one \label{tab:mobiwac:results} (all nine \ref sites unchanged), one
% footnote block. Both halves fit \textwidth at their natural size (category 377 pt, region
% 408 pt against a 455 pt text block: 3 cm + 2 cm margins on A4), so neither is scaled and the
% body renders at full 11.96 pt. Each half keeps a {max width=\textwidth} adjustbox purely as a
% guard: at these widths it is a no-op, and if the estimate is off it clips the overshoot instead
% of producing an overfull box. If the rebuild confirms the fit, both wrappers can be dropped.
% Every value, bold mark, \sd{}, $^{\uparrow}$, $^{\approx}$, $^{\dagger}$, $^{\ddagger}$, and
% the Dataset/Regions key columns are carried over character for character; the Dataset and
% Regions columns repeat in the second half so each block reads on its own.
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{l r r r r r}
\toprule
& & \multicolumn{4}{c}{\textbf{Next-category (macro-F1)}} \\ \cmidrule(lr){3-6}
\textbf{Dataset} & \textbf{Regions} & \textbf{Markov-K} & \textbf{POI-RGNN} & \textbf{Dedicated} & \textbf{Joint (ours)} \\
\midrule
AL       & 1{,}109 & 20.50 & 23.80 & \textbf{30.77}\sd{0.07} & \underline{30.59}\sd{0.07} \\
AZ       & 1{,}547 & 23.92 & 27.64 & \textbf{34.57}\sd{0.04} & \textbf{34.57}\sd{0.06} \\
Istanbul & 520     & 24.55 & 30.12 & \underline{35.34}\sd{0.01} & \textbf{35.42}\sd{0.06} \\
FL       & 4{,}703 & 29.74 & 34.49 & \underline{37.35}\sd{0.02} & \textbf{37.55}\sd{0.07}$^{\uparrow}$ \\
CA       & 8{,}501 & 27.58 & 31.78 & \textbf{35.63}\sd{0.01} & \textbf{35.63}\sd{0.02} \\
TX       & 6{,}553 & 28.67 & 33.03 & \textbf{36.33}\sd{0.01} & \underline{36.19}\sd{0.04} \\
\bottomrule
\end{tabular}
\end{adjustbox}

\vspace{6pt}

\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{l r r r r r r}
\toprule
& & \multicolumn{5}{c}{\textbf{Next-region (Acc@10)}} \\ \cmidrule(lr){3-7}
\textbf{Dataset} & \textbf{Regions} & \textbf{HMT-GRN} & \textbf{ReHDM} & \textbf{STAN} & \textbf{Dedicated} & \textbf{Joint (ours)} \\
\midrule
AL       & 1{,}109 & 57.05 & 65.38 & 60.72 & \textbf{70.12}\sd{0.10} & \underline{69.24}\sd{0.16}$^{\approx}$ \\
AZ       & 1{,}547 & 43.70 & 53.00 & 49.86 & \textbf{59.48}\sd{0.07} & \underline{59.04}\sd{0.22}$^{\approx}$ \\
Istanbul & 520     & 60.4  & 69.33 & 61.86 & \textbf{75.16}\sd{0.01} & \underline{75.08}\sd{0.05}$^{\approx}$ \\
FL       & 4{,}703 & 63.74 & 64.49 & 72.99 & \textbf{76.69}\sd{0.01} & \underline{76.54}\sd{0.01}$^{\approx}$ \\
CA       & 8{,}501 & 49.61 & 50.26$^{\ddagger}$ & 58.52$^{\dagger}$ & \underline{63.48}\sd{0.03} & \textbf{64.54}\sd{0.04}$^{\uparrow}$ \\
TX       & 6{,}553 & 53.85 & 48.81$^{\ddagger}$ & 61.67$^{\dagger}$ & \underline{64.94}\sd{0.01} & \textbf{66.15}\sd{0.07}$^{\uparrow}$ \\
\bottomrule
\end{tabular}
\end{adjustbox}

\vspace{2pt}
{\footnotesize HMT-GRN (region-native, on
our folds, scored on visits whose region appears in training, $>$99\% of test visits in every dataset
and fold) is the primary external comparison; STAN (our re-implementation) and ReHDM (its own protocol)
are references. Markov-K: strongest order per dataset. $^{\dagger}$STAN partial folds: TX 4/5, CA 2/5
(seed 0). $^{\ddagger}$ReHDM at TX and CA: a single seed. Joint and dedicated entries: four seeds
$\times$ five folds; $\pm$: sd across seeds. Batch size was searched for the dedicated
category model at every dataset and the learning rate at four of the six, with the joint model
searched at the smaller datasets and the coverage per knob in
Section~\ref{sec:mobiwac:setup-windows}; TX and CA
carry the joint configuration transferred from those searches, and the dedicated region models use one
fixed configuration throughout.}
\end{table}
