% !TeX root = ../../main.tex
% [round9g, 2026-08-02, PENDENCIAS_RESOLVIDOS 2.24 (arquivado 2026-08-02), AUTHOR RULING] Terminal punctuation moved OUTSIDE the
% closing quotation mark, per ABNT NBR 10520:2023 (WRITING_LAW §1). Both quoted spans here are
% FRAGMENTS, not sentences -- they open lowercase and continue the host sentence -- so the period
% is the HOST's and moving it changes nothing about the quoted wording. The third site found by
% the same sweep, in tables/cbic/errata.tex, was LEFT ALONE: that quotation is a complete
% sentence beginning "Also, it is important...", so its period belongs to the quoted author and
% the author's exemption covers it.
% mobiwac/errata_scope.tex -- one table. Extraction provenance and verification: tables/README.md
% [2026-07-27, author policy] The MobiWac paper is UNDER REVIEW, so a minor factual or
% scope correction does not need an erratum: it is applied to the paper source as well, and the
% dissertation and the paper stay identical. This table therefore keeps only the corrections too
% elaborate to fold into a paper under review:
%   - the integrity paragraph, whose fourth ground cites Appendix D, which the paper has no room for;
%   - the freeze-control restatement, which cites this chapter's own results table.
% TWO ROWS WERE REMOVED because the correction now lives in BOTH texts:
%   - the balancer sentence -> [mobiwac]/src/sections/02_related.tex (19-balancer screen, both exceptions)
%   - the third limitation  -> [mobiwac]/src/sections/07_discussion.tex (with sec:mobiwac: label prefix
%     remapped to the paper's own sec:setup-windows, and "this chapter's" -> "the paper's")
% Logged in [mobiwac]/ERRATA.md. Paper rebuilt: 9 pp, 0 undefined refs, 0 undefined cites.
\begin{table}[htb]
\caption{Claim-scope corrections in the reproduced MobiWac 2026 prose (claim-scope
narrowing and comparison-basis disclosure; no experimental result altered).}
\label{tab:apx:mobiwac-scope}
\centering
\small
\begin{tabular}{@{}p{0.30\textwidth}p{0.62\textwidth}@{}}
\toprule
\textbf{Submitted claim} & \textbf{Correction in the chapter} \\
\midrule
The representation ``passes no usable information about the test visits, on three
grounds''. &
The universal negative is replaced by a bounded statement over named channels. The first
ground is scoped to the training objective, with the note that each visit's category is a
node input feature; a fourth ground is added, reporting the development audit that probes
the forward-edge channel between consecutive visits, with its three limits stated: a
linear probe, Florida at one random initialization, and earlier builds of the
representation. Every number already in the paragraph is unchanged. \\
\addlinespace
The gradient-cosine paragraph reports the development measurement, four seeds on four
Gowalla states, and points only to the mechanism test in the results section. &
One sentence is added in the chapter, pointing to the appendix that measures the same
quantity on the final model at four of the six datasets and reaches the same conclusion. The
sentence cites a dissertation appendix, so it is not carried into the submitted paper: the
referent does not exist there. No number, scope, or conclusion in the paragraph changes. \\
\addlinespace
% [2026-09-02, Wilcoxon] Author ruling: remove the promise, do not compute the test. The registered
% test was executed once, on the v17 ladder; the ladder was recomputed under the joint-best
% convention on 2026-08-11 and the sentence that REPORTED the Wilcoxon was deleted from the results
% section in that same wave (ce5a7006), while the sentence that PROMISED it survived here. Removing
% it is the honest half: what stays is the registration, the reason for the departure (the exact
% one-sided p cannot fall below 0.0625 at n=4), and the disclosure that the code release carries
% both tests -- which is true, scripts/closing_data/superiority_wilcoxon.py exists. The test WAS
% computed for this ladder in the course of deciding this
% (research/reproducibility/mobiwac_v18/wilcoxon_v18.py) and agrees with the reported t at every
% cell, so nothing is being hidden; the author chose not to add a second test to the reader's load.
The setup section states that the registered Wilcoxon signed-rank test on the twenty
matched fold differences is also reported, as a sensitivity analysis alongside the paired
$t$ test. No Wilcoxon result appears anywhere in the delivered text. &
The promise is withdrawn and the disclosure kept: the paragraph still states that the plan
registered the test, still gives the reason it is not used at the seed-level footing, and still
records that the released code carries both tests. One further word changes, from ``primary
analysis'' to ``reported analysis'', because with no second analysis named the first adjective
implied one. A clause naming the Wilcoxon as the comparison over twenty differences is removed
from the seed definition. No number, verdict, or interval changes. \\
\addlinespace
% [2026-09-02, parameter counts] The published sums close exactly against the WITHDRAWN dedicated
% category count of 644,359, which the 2026-07-23 audit produced by building the head at the module
% default depth of two layers while its own output file claims it used the ceiling config. The cells
% ran four layers: train.py:339 routes task "next" to default_next, experiment.py:530 pins
% num_layers=4, and train.py:1454-1460 merges --model-param over it rather than replacing it. The
% region half was measured, not inferred: the head instantiated exactly as cell_reg builds it gives
% 417,117 at Alabama and 1,370,685 at California, and the joint's region partition minus that head
% is exactly 465,297 at all five datasets with a delivered log, which validates both sides at once.
% Reproduce: research/reproducibility/mobiwac_v18/param_counts.py (exits non-zero if the identity
% breaks). Appendix G of the supplementary volume carried the same defect and is corrected with it.
The method section compares parameter counts, giving the joint model about 4.2 million
parameters at Alabama against 1.1 million for the two dedicated models combined, and 5.2
against 2.0 at California. The two sums are low: they were computed from a dedicated
category model of 644,359 parameters, a count the model that ran does not have. &
The sums are corrected to 1.9 and 2.8 million, from the measured 1,433,863 for the dedicated
category model and 417,117 and 1,370,685 for the dedicated region model at the two datasets.
The joint-model figures are unchanged and confirmed against the delivered execution logs. The
ratio the sentence rests on falls from about four to 2.27 at Alabama and from 2.6 to 1.84 at
California; the claim it supports does not depend on the magnitude, since the paragraph argues
that the single model's advantage is operational rather than arithmetic. \\
\addlinespace
% [2026-09-02, emphasis] SIXTH ROW. The accepted article carries ZERO underlines and uses bold for the
% verdict only (mobiwac/src_fix/tables/tbl3_results.tex), so this is a departure from it and not a
% re-typesetting choice. It is the author's ruling of the same date, carried down from the correction
% he made for the defense deck (presentation/slides/main.tex:1620-1625, :1633-1638), and it is what
% actually answers banca item B-27: bold was doing two jobs at once, so a row where the dedicated model
% holds the larger number showed no bold at all and read as "nothing is best here". The dissertation
% table now matches the projected one cell for cell. Applied by script over the printed values with the
% proof that stripping all emphasis returns the file byte-identical.
The results table uses bold for one thing only: an improvement over the dedicated model
that survives the Holm correction. The largest value in a row therefore carries no mark
unless it also happens to be that improvement, so in the rows where the dedicated model
holds the larger number nothing is emphasized. &
The two meanings are separated. Bold and underline report magnitude, the largest and second
largest value in each row, with both in bold and no second marked when they tie at the
printed precision; the statistical verdict keeps its own marks, $\uparrow$ and $\approx$,
beside the joint-model value. The caption states the split and reads out the case that
prompted it. No value, verdict or interval changes. \\
\addlinespace
% [2026-09-02, concatenation control] SEVENTH ROW. Two defects in one paragraph, and the first is
% arithmetic against a table printed on the same page. The delivered sentence called the control's
% gains "under a tenth of the place-to-check-in gap"; Table 9 (\input immediately below it) gives
% those gaps as 1.62 / 2.58 / 0.23, so the cited +2.0 / +1.7 / +0.8 are 123% / 66% / 348% of them.
% "Under a tenth" would need gaps above 20, 17 and 8 points. Same class as the CBIC B-13 row.
% THE DRAFTED ERRATA WAS NOT USED AS WRITTEN. wrapup/erratas/errata_Q13_concatenation_scope.tex
% proposes "the gain comes mostly from the features, and not from the hierarchical structure that
% reads them" -- which inverts the sentence with the same force the original erred with, and the
% data do not carry the inversion. Advisor review 2026-09-02 flagged it; the correction states only
% what the control measures.
% RE-RUN NUMBERS DELIBERATELY EXCLUDED. That errata quotes a re-execution (+1.73 / +1.70 / +1.02
% against gaps of 1.56 / 2.50 / 0.21) that does not reproduce Table 9 fold by fold, cause
% unidentified. The delivered pair is internally consistent and is what the printed text uses.
% NO DATASET IS NAMED. The direction is derivable from the delivered pair (Arizona is the one state
% where the check-in representation still leads, by 0.88), but no paired test exists on the
% delivered generation, no artifact carries that arm, and the cited gains are printed at one
% decimal, so the derived differences carry +-0.05 of slack. WRITING_LAW §3 binds comparison verbs
% to a test; subtraction of rounded numbers is not one.
% ⚠ NOTHING IS SAID ABOUT THE HIERARCHY, IN EITHER DIRECTION, AND THAT IS DELIBERATE. On this
% substrate the levels above the place are severed from the category axis by construction:
% Check2HGIModule.py:653 detaches before the region path in the delaunay_gcn mode that v14/v18 use,
% so p2r and r2c gradients never reach the check-in encoder. A null there would be a property of
% the implementation, not a result about hierarchy. Scope is the category axis, which the chapter
% already declares a few lines above ("the benefit is category-only").
The results section reports the feature-concatenation control as raising the place
embedding by $+2.0$, $+1.7$ and $+0.8$ macro-F1 at three datasets, describes that as
under a tenth of the place-to-check-in gap, and concludes that the gain comes from the
hierarchical per-visit representation rather than from feature injection. The
representation table on the same page gives those gaps as $+1.62$, $+2.58$ and $+0.23$,
which the quoted gains meet or exceed at two of the three. &
The comparison is stated against the table: the three gains are printed beside the three
gaps and no ratio is asserted, since the control exceeds the gap at two datasets and falls
short at the third. The conclusion is narrowed to what the control measures, that most of
the category difference is already available in the raw per-visit features and that what
the representation adds beyond them on this axis is small and unseparated by this control,
and it is scoped to the category axis. No claim is made about the hierarchy in either
direction. No value changes. \\
\addlinespace
% [2026-09-02, B-23] "com relacao ao primeiro ne?" -- the reader could not tell what the two
% enhancements were enhancements OF. The antecedent is in the previous paragraph (the earlier work
% cited there), one sentence away, which is enough inside a dissertation and not enough in an article
% read on its own. Four words. Author ruling: "concordo podemos fazer".
The introduction announces two enhancements without naming what they improve upon. The
antecedent, the authors' earlier model, appears in the previous sentence but is not
carried into the claim. &
The claim names its referent: two enhancements to that earlier model. Nothing else in the
sentence changes. \\
\bottomrule
\end{tabular}
\end{table}
