% !TeX root = main_academico.tex
% ============================================================================
% main_academico.tex -- the AcademicoPG deposit body (no cover, no folha de rosto, no
% approval sheet, no Resumo, no Abstract: that system generates all of them from web forms).
%
% Author decision, 2026-07-28 (src_utils/LATEX_UPGRADE.md §4, A-1/A-2/A-3): this build was
% called `final` until 2026-07-29. "Final" said nothing next to `defense` and `ppgc` -- final
% of what? -- so the target, the jobname, the output file and the switch macro all say
% academico now, after the one system the PDF is actually uploaded to.
%
% WHY THIS FILE EXISTS AT ALL. Until it did, this was the only one of the three builds that
% could not be opened in Overleaf or a GUI editor, because it lived exclusively as a pdflatex
% command-line injection in the Makefile. Every editor can select a compile ROOT; none can be
% told to add \def\ACADEMICOBUILD{} first. So the deposit build -- the one PDF that is
% actually submitted -- was the one nobody could produce without make. Now all three builds
% are a selectable root: main.tex, main_academico.tex, main_ppgc.tex.
%
% This file is deliberately TWO lines of content: the \def below and the \input after it.
% Measure it rather than trusting this sentence:
%   grep -v '^[[:space:]]*%' main_academico.tex | grep -cv '^[[:space:]]*$'   returns 2
% and src_utils/check_comment_hygiene.py fails the build if that stops being true. The count
% is anchored to the two commands by name, not to line numbers: a first draft of this header
% said ":22-23 below" and was stale before the file was committed, which is the same class of
% defect as the "four-line file" claim the rename pass removed from main.tex.
% It sets one switch and reads main.tex, so the deposit PDF cannot drift from the defense
% document except where \ifdefensebuild says it should.
%
% Build: `make academico` -> build/main_academico.pdf. Do not add content here. If something
% belongs in every build it belongs in 0_main.tex; if it belongs only in this one, gate it on
% \ifdefensebuild there (false in this build).
%
% The three builds, the two switches and what each build is: the main.tex header, which is
% the canonical telling and the one that travels with an Overleaf paste of src/.
% ============================================================================

\def\ACADEMICOBUILD{}
\input{main.tex}
