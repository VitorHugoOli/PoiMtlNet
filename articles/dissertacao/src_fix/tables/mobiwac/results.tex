% !TeX root = ../../main.tex
% mobiwac/results.tex -- one table. Extraction provenance and verification: tables/README.md
\begin{table}[htbp]
\caption{One model, two tasks: the single joint model against the dedicated single-task models and the
external baselines, ordered by region count. The upper block reports next-category (macro-F1) and the
lower block next-region (Acc@10); the two share the dataset and region-count columns, and the same
six datasets appear in the same order in both. Bold with $\uparrow$ marks an improvement over the
dedicated model that survives Holm correction within its task family. In the region block, $\approx$
marks a difference that stays within the two-point margin registered before any result was read
(statistical non-inferiority, TOST; Section~\ref{sec:mobiwac:results-part2}). Category cells carry no
equivalence mark because the margin was registered for the region axis only, so a category difference
that fails superiority is reported by the bound its interval supports, half a point.}
\label{tab:mobiwac:results}
\centering
% [NEW-7, 2026-07-26] Was ONE eleven-column tabular inside {adjustbox}{max width=\textwidth},
% which shrank the body to a measured 8.00 pt (fold sd 6.00 pt) against 11.96 pt body text.
% Split at the cmidrule the two task blocks already carried, into two tabulars inside this one
% float: one caption, one \label{tab:mobiwac:results} (all nine \ref sites unchanged), one
% footnote block. Both halves fit \textwidth at their natural size (category 377 pt, region
% 408 pt against a 455 pt text block: 3 cm + 2 cm margins on A4), so neither is scaled and the
% body renders at full 11.96 pt. Each half keeps a {max width=\textwidth} adjustbox purely as a
% guard: at these widths it is a no-op, and if the estimate is off it clips the overshoot instead
% of producing an overfull box. If the rebuild confirms the fit, both wrappers can be dropped.
% Every value, bold mark, \sd{}, $^{\uparrow}$, $^{\approx}$, $^{\dagger}$, $^{\ddagger}$, and
% the Dataset/Regions key columns are carried over character for character; the Dataset and
% Regions columns repeat in the second half so each block reads on its own.
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{l r r r r r}
\toprule
& & \multicolumn{4}{c}{\textbf{Next-category (macro-F1)}} \\ \cmidrule(lr){3-6}
\textbf{Dataset} & \textbf{Regions} & \textbf{Markov-K} & \textbf{POI-RGNN} & \textbf{Dedicated} & \textbf{Joint (ours)} \\
\midrule
Istanbul & 520     & 24.55 & 30.12 & 35.34\sd{0.01} & 35.42\sd{0.06} \\
AL       & 1{,}109 & 20.50 & 23.80 & 30.77\sd{0.07} & 30.59\sd{0.07} \\
AZ       & 1{,}547 & 23.92 & 27.64 & 34.57\sd{0.04} & 34.57\sd{0.06} \\
FL       & 4{,}703 & 29.74 & 34.49 & 37.35\sd{0.02} & \textbf{37.55}\sd{0.07}$^{\uparrow}$ \\
TX       & 6{,}553 & 28.67 & 33.03 & 36.33\sd{0.01} & 36.19\sd{0.04} \\
CA       & 8{,}501 & 27.58 & 31.78 & 35.63\sd{0.01} & 35.63\sd{0.02} \\
\bottomrule
\end{tabular}
\end{adjustbox}

\vspace{6pt}

\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{l r r r r r r}
\toprule
& & \multicolumn{5}{c}{\textbf{Next-region (Acc@10)}} \\ \cmidrule(lr){3-7}
\textbf{Dataset} & \textbf{Regions} & \textbf{HMT-GRN} & \textbf{ReHDM} & \textbf{STAN} & \textbf{Dedicated} & \textbf{Joint (ours)} \\
\midrule
Istanbul & 520     & 60.4  & 69.33 & 61.86 & 75.16\sd{0.01} & 75.08\sd{0.05}$^{\approx}$ \\
AL       & 1{,}109 & 57.05 & 65.38 & 60.72 & 70.12\sd{0.10} & 69.24\sd{0.16}$^{\approx}$ \\
AZ       & 1{,}547 & 43.70 & 53.00 & 49.86 & 59.48\sd{0.07} & 59.04\sd{0.22}$^{\approx}$ \\
FL       & 4{,}703 & 63.74 & 64.49 & 72.99 & 76.69\sd{0.01} & 76.54\sd{0.01}$^{\approx}$ \\
TX       & 6{,}553 & 53.85 & 48.81$^{\ddagger}$ & 61.67$^{\dagger}$ & 64.94\sd{0.01} & \textbf{66.15}\sd{0.07}$^{\uparrow}$ \\
CA       & 8{,}501 & 49.61 & 50.26$^{\ddagger}$ & 58.52$^{\dagger}$ & 63.48\sd{0.03} & \textbf{64.54}\sd{0.04}$^{\uparrow}$ \\
\bottomrule
\end{tabular}
\end{adjustbox}

\vspace{2pt}
{\footnotesize HMT-GRN (region-native, on
our folds, scored on visits whose region appears in training, $>$99\% of test visits in every dataset
and fold) is the primary external comparison; STAN (our re-implementation) and ReHDM (its own protocol)
are references. Markov-K: strongest order per dataset. $^{\dagger}$STAN partial folds: TX 4/5, CA 2/5
(seed 0). $^{\ddagger}$ReHDM at TX and CA: a single seed. Joint and dedicated entries: four seeds
$\times$ five folds; $\pm$: sd across seeds. Batch size was searched for the dedicated
category model at every dataset and the learning rate at four of the six, with the joint model
searched at the smaller datasets and the coverage per knob in
Section~\ref{sec:mobiwac:setup-windows}; TX and CA
carry the joint configuration transferred from those searches, and the dedicated region models use one
fixed configuration throughout.}
\end{table}
