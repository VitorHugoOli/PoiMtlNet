% !TeX root = ../../main.tex
% chapters/5_mobiwac/06_results.tex -- one section of Chapter 5 (MobiWac 2026, submitted, under review).
% Split out of chapters/5_mobiwac.tex on 2026-07-28 (author request: the paper
% chapters were single long files, while the originals under articles/[mobiwac]/src/sections/
% are one file per section). The split is PURELY MECHANICAL: content was cut at
% \section boundaries and not edited. The chapter file now \inputs these in order.
% Editing rules are unchanged and still governed by the chapter's status above:
% see NORTH_STAR section 4 before changing any sentence here.
\section{Results}
\label{sec:mobiwac:results}

\subsection{The check-in-level representation improves the category task}
\label{sec:mobiwac:results-part1}
The check-in-level representation reaches a higher next-category macro-F1 than a standard place
embedding (HGI~\cite{huang2023hgi}) at every dataset
(Table~\ref{tab:mobiwac:representation}). The comparison is controlled: target, single-task model, training configuration, windowing, epoch budget, logit adjustment, and folds are identical; only the input representation changes (a vector per visit versus a vector per place), so the difference isolates the representation. The check-in-level column is the same dedicated single-task category model reported in Table~\ref{tab:mobiwac:results}, read at seed 0 so that both arms of this comparison share one fold partition; the place-level arm is that model with its input swapped. The difference ranges from $+0.23$ macro-F1 at Florida to
$+6.29$ at Istanbul, and all five folds favor the check-in-level representation at every one of the
six datasets. A paired test over the five matched folds separates the two representations at every
dataset except Florida, where the direction is unanimous across folds but the difference does not
reach significance ($p=0.07$). What the comparison establishes is therefore a consistent direction
rather than a large effect: the input representation is one condition that affects performance
here, and it acts the same way everywhere it was measured.

The geometry of the vectors indicates why the representation helps this task in
particular: the per-visit vectors' silhouette score by category (how tight and well separated the seven labeled groups are, on a $-1$ to $1$ scale) is about $0.57$ against about $0.00$ for the place embedding, and their nearest-neighbor category purity (the share of nearest neighbors with the vector's own category) is about $0.98$ against about $0.78$ (both averaged over the five U.S. states). These two measures were computed on an earlier preparation of the same
graph and are reported here as a property of the representation family rather than of the exact
configuration evaluated in Table~\ref{tab:mobiwac:results}. The same geometry does not separate regions, so the benefit is category-only (the joint model's spatial stream instead reads the region-level vectors of the same graph, Section~\ref{sec:mobiwac:method-model}). CTLE~\cite{lin2021ctle}, the closest prior contextual embedding, also gives each visit its own vector, but it pretrains on place identifiers and timestamps alone, so the category vocabulary never enters its training, whereas our graph reads each visit's category and time of day as input features. At Florida, we fine-tune CTLE together with the task model, using its authors' defaults and the same 64 dimensions and windowing as ours. It reaches $33.45$ macro-F1 at its best epoch and $29.69$ at its final one (an epoch is one pass over the training data), about two points below the place embedding under the same best-epoch rule. With frozen weights, fed to the same single-task model, CTLE repeats the ordering at Alabama, Arizona, and Istanbul. These CTLE arms were run on an earlier preparation of the data,
so they establish the ordering between the two representation families rather than a difference
against the values in Table~\ref{tab:mobiwac:representation}. A feature-concatenation control joins the place embedding with the same raw
per-visit features that our graph reads (the category one-hot plus hour and
day-of-week encodings) and feeds the result to the same single-task model. It
does not close the gap either: at Alabama, Arizona, and Florida it raises the
place embedding by only $+2.0$, $+1.7$, and $+0.8$ macro-F1, under a tenth of
the place-to-check-in gap at each state. The gain therefore comes from
the hierarchical per-visit representation, not from contextualization alone or
feature injection.

% Table 2: The representation makes the category task learnable (Part 1).
% Check-in-level (Check2HGI) vs place-level (HGI) next-category macro-F1, matched single-task
% head + recipe + folds (seed 0 x 5 folds), overlapping-window protocol throughout.
% NOTE (2026-07-08): this is the MATCHED-recipe pair -- it is deliberately NOT Table 3's dedicated
% ceiling anymore (Table 3's ceilings are the n=20 per-state-tuned best-vs-best set; re-tuning the
% HGI arm per state was not done, so the controlled A/B here keeps the original single recipe on
% both models). Istanbul re-footed on the rebuilt dk_ovl substrate (H3): Check2HGI-SC 54.65+-0.56
% (docs/results/closing_data/baseline_compare/istanbul_check2hgi_sc.json), HGI arm unchanged
% 26.56+-0.5 (istanbul_hgi_ovl_cat.json) -> margin +28.09 (was +26.64 on the old GCN base).
% Gowalla arms unchanged: PR #50 AL/AZ/FL + PR #52 CA/TX.
% Check2HGI std: Ist 0.6 / AL 2.4 / AZ 1.3 / FL 0.5 / TX 0.2 / CA 0.3 ; HGI std: Ist 0.5 / AL 1.0 /
% AZ 1.0 / FL 0.3 / TX 0.6 / CA 0.4.
\providecommand{\sd}[1]{\,{\scriptsize$\pm$#1}}
\input{tables/mobiwac/representation}

% [NEEDS SIGN-OFF 25 | CLOSED] Author: keep the figure, but the pointer sentence was loose -- it
% needed to give the reader a reason to look. Rewritten 2026-08-04 to say what the figure adds over
% the prose (the two measures side by side, so the size of the gap is readable at a glance).
% The sentence above the figure carries the four numbers and is REPRODUCED FROM THE SUBMITTED
% ARTICLE, so it was deliberately NOT restructured to point at the figure instead: that would be a
% parity-bearing edit for no reader gain. Only the dissertation-only pointer sentence changed.
% The figure itself is a restored element (cut from the 8-page build by author decision 2026-07-09,
% restored because the dissertation is not page-limited); caption verbatim from
% articles/[mobiwac]/src/figs/fig3_embquality.tex. Remove figure and pointer together, or neither --
% gate L4 requires every float to be referenced in prose.
Figure~\ref{fig:mobiwac:embquality} plots the two separability measures reported above, the
silhouette score and the nearest-neighbor category purity, for both representations side by
side, so the size of the gap can be read at a glance rather than compared across four numbers
in the running text.

% [REV-029, 2026-07-26] Declaration moved to the paragraph boundary: it previously sat inside the
% pointer paragraph above, with no blank line, so the float was declared mid paragraph.
\begin{figure}[htbp]
\centering
\includegraphics{figures/mobiwac/fig3_embquality.pdf}
\caption{Class separability of the representations, grouping each vector by its ground-truth
category label
(cosine silhouette; leave-one-out nearest-neighbor (kNN) purity, $k{=}10$), averaged over the
five U.S. states. The check-in-level representation separates the seven categories; the place
embedding does not. Both measures were computed on an earlier preparation of the same graph, so
the figure characterizes the representation family rather than the exact configuration evaluated
in Table~\ref{tab:mobiwac:results}.}
\label{fig:mobiwac:embquality}
\end{figure}

\subsection{One model, two tasks}
\label{sec:mobiwac:results-part2}
% [REV-029, 2026-07-26] Table 3 and Figure 7 were both declared HERE, before any prose of this
% subsection, which is why the rendered build put them ahead of the heading that introduces them
% (and made p.68 floats-only). Both declarations were moved down to the paragraph boundary that
% follows their first reference; no prose was reordered and declaration order (table before
% figure) is preserved, so the float numbers are unchanged.
One model serves both tasks, and neither analysis resolves a loss worth acting on. On next region it
outperforms the dedicated model at Texas and California, the two datasets with the most regions, and
at the other four it stays within the two-point margin registered before any result was read
(statistical non-inferiority, assessed with two one-sided tests). On next category it outperforms the
dedicated model at Florida, and at every dataset the difference between the two is equivalent to zero
within half a point, simultaneously across all six.

Half a point is worth placing against the two comparisons that give the category axis its scale. The
input representation moves the same metric by $+0.23$ to $+6.29$ points, up to thirty-two times the
largest difference the choice of architecture makes, and the joint model stands at least $3.06$ points
above the strongest external baseline at every dataset. Whether one model or two produce the category
prediction is therefore the smallest of the three effects at work here, which is the sense in which
one model can replace two on this task.
% [round9c, PENDENCIAS_RESOLVIDOS 2.11 (arquivado 2026-08-02), author decision: option B, 2026-07-30] The lead read "One model
% outperforms or matches the dedicated models on both tasks", which collapses the four-of-six /
% TOST partition this chapter's own section 5.3 and section 6.2 state precisely. Applied to BOTH
% TREES in the same pass and verified textually identical afterward, per NORTH_STAR's regime for
% the under-review chapter: articles/[mobiwac]/src/sections/06_results.tex carries the same
% sentence, and the paper's ERRATA.md records it. No number, test or verdict changes; Arizona is
% not upgraded. Verified identical with the comment-stripping sweep, across line wraps.
For scale: always predicting the most common category reaches between $5.7$ and $7.3$ macro-F1
depending on the dataset (the majority-class floor, lowest at Florida, whose category mix is the
most even), and a random region top-ten guess is right at most about two percent of the time
(Section~\ref{sec:mobiwac:setup-metrics}).

Table~\ref{tab:mobiwac:results} reports the single joint model against the dedicated
single-task ceilings (the level a joint model is usually expected, at
best, to match). Batch size and learning rate were searched for the dedicated category model at
every dataset and for the joint model at the smaller datasets; Texas and California carry the joint
configuration transferred from those searches, and the dedicated region models use one fixed
configuration throughout.
Throughout, every reported model is one saved artifact per fold, read at its
validation-selected epoch: each dedicated model at its task's best epoch, and
the joint model at the epoch selected by its joint validation score (the
geometric mean of the two task metrics), with both tasks read from that one
model. This convention is the one a deployed system can actually serve, since it commits to a
single checkpoint per fold. The alternative, reading each task at its own best epoch, describes no
single saved model: it reports two different checkpoints as though one system produced both. That
alternative is more favorable to the joint model, by at most $0.23$ macro-F1 and $0.93$ Acc@10
points, and it is favorable enough to change the verdicts, turning four further category cells and
two further region cells into supported improvements. The stricter convention is reported here for
that reason, and every verdict in this chapter is the one it yields.
% AUTHOR DECISION 2026-07-18 (supersedes 2026-07-09): the REPORTED convention is now JOINT-BEST
% (author ruling, considerations_v3 #10) -- Table 3's joint cells are the one saved model per fold
% (geom_simple = sqrt(cat F1 x reg Acc@10-indist) on validation, model-SELECTION only; never enters
% the loss; training uses the fixed 0.75/0.25 sum of S4.2), both tasks read there. All pre-registered
% tests re-verified on the joint-best arrays (n=20 all six; Istanbul reg CI +0.15..+0.23 > 0, 20/20
% folds; AL/AZ TOST pass) -- see joint_best/JOINT_BEST_RESULTS.md. The sentence above discloses the
% convention and keeps diag-best as the <=0.06/<=0.11 robustness check (the inverted form of the old
% disclosure).

% [REV-029, 2026-07-26] Moved here from the head of this subsection, where it was declared
% before any prose of Section 5.6.2 (the rendered build put it a full page ahead of that
% heading). This is the paragraph boundary closing the table's first reference. It still
% precedes Figure 7's declaration and follows Table 2's, so both float numbers are unchanged.
% Table 3: One model, two tasks (Part 2) -- the main result table. v17 board (2026-07-08 renumbering).
% Joint single model (v17) vs the dedicated single-task ceilings (n=20 best-vs-best, per-dataset
% bs x LR tuned -- CEILINGS_N20_FINAL.md), plus next-region externals (HMT-GRN primary; STAN faithful +
% ReHDM v4 references) and next-category references (POI-RGNN + Markov-9-cat floor).
%
% Provenance (single source: docs/studies/closing_data/RESULTS_BOARD.md + v17_completion/):
%   Joint (MTL v17): ALL SIX = n=20 (4 seeds x 5 folds), JOINT-BEST convention (one saved model per
%     fold, geom_simple validation selector, both tasks read there) -- switched from diag-best
%     2026-07-18 per author ruling (considerations_v3 #10); source joint_best/JOINT_BEST_RESULTS.md +
%     catx_v17_n20/joint_best/. +- is cross-seed sd of per-seed means. All fp32.
%   Dedicated ceilings: n=20 best-vs-best (CEILINGS_N20_FINAL.md; cat swept over bs x LR per state,
%     reg = prior-off next_stan_flow topped to n=20). AZ cat ceiling 56.43 carries a pending 2-seed
%     top-up that could raise it to ~57.0 (Delta_cat AZ +9.35 -> ~+8.7) -- board banner.
%   cat Ded/Joint(jb): Ist 54.74+-0.09/63.32+-0.02; AL 56.82+-0.03/64.51+-0.09; AZ 56.43+-0.10/65.79+-0.02;
%     FL 74.51+-0.03/79.84+-0.02; TX 69.79+-0.08/77.24+-0.01; CA 70.60+-0.07/77.05+-0.01.
%   reg Ded/Joint(jb): Ist 75.16+-0.01/75.35+-0.04; AL 70.11+-0.10/69.70+-0.09; AZ 59.46+-0.08/59.46+-0.04;
%     FL 76.70+-0.02/77.41+-0.02; TX 64.95+-0.01/67.06+-0.01; CA 63.49+-0.02/65.69+-0.02.
%   Externals: HMT-GRN 60.4/57.05/43.70/63.74/53.85/49.61 (unchanged); ReHDM v4 row (version-uniform,
%     rehdm.md): Ist 69.33 / AL 65.38 / AZ 53.00 / FL 64.49 / TX 48.81 (seed-42 only) / CA 50.26 (seed-42
%     only); STAN faithful 61.86/60.72/49.86/72.99 + TX 61.67 (4/5 folds) + CA 58.52 (2/5 folds) --
%     partials closed-as-final with fold-count disclosure (stan_catx/STATUS.md; "infeasible" RETIRED).
%     Markov-9-cat 24.55/20.50/23.92/29.74/28.67/27.58; POI-RGNN 30.12/23.80/27.64/34.49/33.03/31.78.
% Honesty rules (do NOT relax): cat BEATS at every dataset; reg BEATS at Ist/FL/TX/CA (90% CI of the
%   paired difference above zero; all n=20 seed-level since 2026-07-18) and is non-inferior (TOST, <=2pp)
%   at AL/AZ -- AZ's CI grazes zero, NEVER upgrade AZ to a beat. The two matched reg cells are NOT bolded.
%   Never cite: STAN v4 collapse (AL 34.46/AZ 38.96), HMT-GRN AL 62.37 outlier, fp16/bf16 VOID JSONs,
%   ReHDM v2 row (66.06/54.65/65.68).
\providecommand{\sd}[1]{\,{\scriptsize$\pm$#1}}
\input{tables/mobiwac/results}

Figure~\ref{fig:mobiwac:deltas} plots the signed differences ordered by region count. On
category, every difference is small in both directions, from $-0.19$ macro-F1 at Alabama to
$+0.19$ at Florida, and Florida is the only one the tests resolve.
% Hidden AZ-ceiling sensitivity (response-letter material, author policy: hide caveats, provide on
% request): the AZ dedicated ceiling (56.43) is the strongest arm tested at the full n=20; two arms
% screened at 2 of 4 seeds scored ~57.0 (bs2048@{0.0025,0.0075}). If they held at n=20, the AZ gain
% would be ~+8.8 -- still the largest (Istanbul +8.58). Rule-clean value = 56.43 per the
% pre-registered n=20 best-vs-best estimator; the top-up was dropped 2026-07-08 (user decision,
% docs/studies/closing_data/v17_completion/A40.md).
On region (Acc@10), the joint model outperforms the dedicated model at Texas ($+1.21$) and
California ($+1.06$), and at the other four datasets it stays within the two-point margin. The two
datasets where it outperforms are the two with the largest region vocabularies, which is where the
region task is hardest and where the dedicated model has the most to gain from an auxiliary signal.
The ordering is not monotone inside that pair, since California has more regions than Texas and a
slightly smaller gain, and region count co-varies with corpus size across these states, so the
grouping is read as an observation about where the benefit appears rather than as a law relating
gain to region count.

The tests behind those verdicts are reported next, each entry giving the point estimate and its
90\% confidence interval. On category, Florida is the one dataset where the difference survives the
Holm correction across the six ($+0.19$; $+0.14$ to $+0.25$; corrected $p=0.011$), with 19 of the
20 folds favoring the joint model. The other five differences do not survive it, and because the
equivalence margin was registered for the region axis only, they are reported by the bound their intervals support
rather than as matches: Istanbul ($+0.08$; $+0.01$ to $+0.15$), Arizona ($-0.00$; $-0.04$ to
$+0.03$), California ($-0.00$; $-0.03$ to $+0.02$), Texas ($-0.13$; $-0.19$ to $-0.08$) and Alabama
($-0.19$; $-0.33$ to $-0.04$). The intervals themselves carry what can be said about magnitude:
every one lies inside a third of a point, and a set of two one-sided tests confirms that all six
differences are equivalent to zero within half a point, simultaneously across the datasets.

On region, Texas ($+1.21$; $+1.13$ to $+1.29$; corrected $p=0.00013$) and California ($+1.06$;
$+1.03$ to $+1.08$; corrected $p<10^{-4}$) survive the correction, with all 20 folds favoring the
joint model at both. The remaining four stay within the registered two-point margin, and their
direction is stated rather than rounded away: all four are deficits, at Alabama ($-0.87$; $-1.00$
to $-0.75$), Arizona ($-0.44$; $-0.62$ to $-0.25$), Florida ($-0.16$; $-0.19$ to $-0.13$) and
Istanbul ($-0.08$; $-0.16$ to $-0.002$), and every one of the four intervals lies entirely below
zero. Three of them are resolved by a test in the reverse direction after correction, and
Istanbul's is not, its interval reaching to within two thousandths of zero. Each of the four
clears the margin comfortably, which is what the pre-registered analysis asked of them, but none
of them is a tie.

% [REV-029 round 2, 2026-07-26] Moved once more, one paragraph further down. At the previous
% position this figure and the (now two-block) results table competed for the same page and
% between them filled it, leaving page 71 without body prose and splitting the subsection's
% argument across three pages. Declaring it after the statistics paragraph gives the page text
% to set; declaration order relative to the results table is unchanged, so Figure 7 keeps its
% number. Measured after the rebuild that followed this edit: p.71 carries 360 word-tokens of body
% prose (263 before, table only), and of the 22 floats, 21 render at or after the page of their
% first prose reference. The exception is Chapter 4's Figure 3, which renders one page ahead of its
% reference; its placement directive is unchanged by this round and a float landing one page early
% is ordinary LaTeX behavior, not the heading-precedence defect this edit addressed.
\begin{figure}[htbp]
\centering
\includegraphics{figures/mobiwac/fig4_deltas.pdf}
\caption{Signed differences between the joint model and the dedicated single-task models, ordered by
region count. The category differences (macro-F1) are small in both directions; the region
differences (Acc@10) are clearly positive at the two largest region vocabularies and inside the band
elsewhere. The $\pm2$-point band is the margin registered for the region axis only.}
\label{fig:mobiwac:deltas}
\end{figure}

The joint model is also above every external baseline reported, on both tasks,
across all six datasets (Table~\ref{tab:mobiwac:results}): on region, the primary
region-native comparison HMT-GRN~\cite{Lim2022}, STAN~\cite{luo2021stan}
trained from the raw check-ins, and a ReHDM reference~\cite{li2025rehdm}; on
category, POI-RGNN~\cite{capanema2023poirgnn} and the Markov-K floor. These externals run on their own embeddings, so this comparison
also includes the representation advantage of Section~\ref{sec:mobiwac:results-part1}; the
controlled comparison for the joint model remains the Dedicated column of
Table~\ref{tab:mobiwac:results}, which uses the same representation, windows, and
folds. The first-order Markov region floor of Section~\ref{sec:mobiwac:setup-baselines},
computed under our windows and folds, reaches $51$ to $72$ Acc@10 across the
datasets; the joint model exceeds it by $4.9$ to $10.3$ points on all six
datasets.

That floor is also above the three external region systems at most datasets, and the
comparison deserves a direct word rather than being left for the reader to assemble.
HMT-GRN falls below it at all six, the ReHDM reference at three, and STAN at four. Two
facts about how these numbers were produced bear on that comparison. The first concerns
the floor: it is computed under our own sliding-window protocol and fold splits. Those
windows advance one visit at a time, so the region of the last visit is a strong predictor
of the next one, and a first-order transition table reads exactly that signal. At Alabama the target region is the last visited region in $32.9$ percent of
windows.
% [round5, fact gate BLOCKER] Previous sentence: "At Alabama, 22.4 percent of windows contain their
% own target region as a genuine revisit." WRONG QUANTITY -- 22.4 is a PLACE-level "target reappears
% in its own 9-history" rate on NON-OVERLAPPING windows (PIPELINE_AUDIT_2026-06-03.md:24), while the
% floor is region-level on stride-1 windows. Replaced with the region persistence share measured on
% the floor's own windows: 32.91% (31,704 of 96,326), see region_persistence_alabama.json.
% A first attempt at this replacement quoted acc1_mean 0.32064 instead, which is a fitted table's
% top-1 ACCURACY rather than a share of windows -- the same wrong-quantity class, caught in review.
The second
fact concerns the three systems, which do not meet the floor on equal terms. HMT-GRN is
evaluated on the same data, folds, and initialization as our models. STAN runs on the same
folds, but builds its own embeddings and its own sequences from the raw check-ins. The
ReHDM reference runs under its own published protocol, so its result is not measured on our
windows or folds at all.

Neither fact establishes why the floor lies above the three systems, and we do not claim a
single explanation. We treat the floor, not the external systems, as the reference the
region task has to clear, and we read the external columns as a comparison against
published designs rather than as the standard for this task. The dedicated and joint
models are above the floor at every dataset.
% [v2 review B-2, 2026-07-26] RECONCILIATION added on persona 11's blocker
% (src_utils/_review_v2/11_poi_mobility_expert_report.md). The chapter designated these systems "the
% per-task state of the art, on our data" and printed a non-learned floor above all of them, without
% connecting the two. A referee reads that pair as evidence the re-implementations are under-trained
% and discounts the whole external comparison, including the parts that favour the contribution.
% Every figure QUOTED, none computed here:
%   floors: docs/results/closing_data/markov_floor_stride1/<state>.json, key
%     markov_1step_region_acc10_mean -- AZ 0.5123 CA 0.5909 TX 0.6010 AL 0.6226 IST 0.6506 FL 0.7247
%   externals: this chapter's own results table (HMT-GRN AZ 43.70 CA 49.61 TX 53.85 AL 57.05
%     IST 60.40 FL 63.74; ReHDM below floor at CA/TX/FL; STAN below at AZ/CA/AL/IST)
%   region persistence: markov_floor_stride1/region_persistence_alabama.json,
%     region_persistence_pct 32.91 -> 32.9% (31,704 of 96,326 windows have target == last region).
%     Generated by scripts/embedding_eval/region_persistence.py from the SAME check-in stream and
%     windowing as the floor, with the window count GATED against the floor's n_windows (96,326 =
%     96,326, PASS).
%     [correction] An earlier version quoted aggregate.markov_1step_region.acc1_mean 0.32064 ->
%     32.1%. That is the top-1 ACCURACY of a fitted transition table, NOT a share of windows -- the
%     same wrong-quantity class as the 22.4 defect below, and it slipped through precisely because
%     the two numbers land close. Persistence is now measured, not borrowed from a model score.
%     [round5] REPLACES a mis-citation: PIPELINE_AUDIT_2026-06-03.md:24 (AL 22.4%) is a PLACE-level
%     "target reappears in its own 9-history" rate on NON-OVERLAPPING windows, so it did not support
%     the region-level sentence it was attached to. Fact gate B-1/M-3.
%   margin range 4.9 to 10.3: DERIVED, not quoted -- joint column of Table~\ref{tab:mobiwac:results}
%     minus markov_floor_stride1/<state>.json key markov_1step_region_acc10_mean, per dataset.
%     Verified this session, all six: FL 77.41-72.47=4.94 (narrowest), CA 65.69-59.09=6.60,
%     TX 67.06-60.10=6.96, AL 69.70-62.26=7.44, AZ 59.46-51.23=8.23,
%     Istanbul 75.35-65.06=10.29 (widest). Rounds to the printed 4.9 to 10.3.
%     Deliberately re-derived from the chapter's own printed cells
%     rather than copied from MARKOV_FLOOR_STRIDE1.md, whose +4.95/+10.38 use pre-final joint values
%     (Ist 75.44 / FL 77.42). Fact gate N-2.
% Counts re-derived from those two sources this session: HMT-GRN 6 of 6 below, ReHDM 3 of 6, STAN
% 4 of 6. No result of this study changes; what changes is that the chapter now names the floor
% rather than the externals as the reference the region task must clear, which is the more
% conservative reading and the one its own numbers support.
% [2026-07-27] Causal explanation dropped. The prior wording said all three systems "predict a
% place and are read at the region level through the place-to-region map", which contradicts this
% chapter calling HMT-GRN region-native (region is one of its own targets) at :418, :622 and :755.
% The paragraph now states the protocol asymmetry only, every clause quoted from the baselines
% subsection at :418: HMT-GRN same data/folds/initialization; STAN same folds, own embeddings and
% sequence construction; ReHDM its own published protocol (also :622 footnote, :873). Counts,
% revisit share and the floor reading are unchanged.
% NOTE for the author: docs/results/closing_data/MACS_BOARD_RESULTS.md:47 still asserts "HMT reg
% clears the Markov floor". That held against the older non-overlapping floor (AL 0.4701, the
% old_floor_nonoverlap block in the same JSON) and was never revisited when the floor was recomputed
% under the shipped stride-1 windowing (AL 0.6226). The chapter inherited the inconsistency rather
% than creating it; the internal record should be corrected separately.

\subsection{External validity (Istanbul)}
\label{sec:mobiwac:results-external}
The joint-model results hold for a non-U.S. city under the same representation, sliding-window protocol, and training setup as the U.S. states. At Istanbul, both
tasks land within a tenth of a point of the dedicated models: category $+0.08$ macro-F1
(dedicated $35.34$, joint $35.42$) and region $-0.08$ Acc@10 (dedicated $75.16$, joint $75.08$),
the region difference staying well within the two-point margin. The comparable quantity is the gain over the ceiling, not the absolute
Acc@10, since region counts differ across datasets (520 mahalle here). The external baselines show the same ordering on both tasks
(Table~\ref{tab:mobiwac:results}).
The U.S. result repeats on a different continent and region unit.
