# netcore — new paper working folder

Ground-up rewrite on the **v14 substrate + champion G**, replacing the dropped BRACIS draft
(`articles/[BRACIS]_Beyond_Cross_Task/`). The story: **substrate carries · MTL is a Pareto gain (not a
tradeoff) · orthogonal-gradient frontier-negative · external validity.**

- **First high-level draft:** [`PAPER_DRAFT_v0.md`](PAPER_DRAFT_v0.md) — a **story + structure lock**; every
  model-derived number is flagged **[provisional]** and regenerates on the frozen overlapping-window board.
- **Execution plan** (what runs before the numbers are final): [`../../docs/studies/EXECUTION_PLAN.md`](../../docs/studies/EXECUTION_PLAN.md).
- **Claim discipline:** region is **parity**, never "beats"; the category lift is a *deployable* Pareto gain
  (head-config asymmetry); the C2 reframe is scoped to AL/AZ/FL/GE (CA/TX expected-but-unmeasured); the
  **single-model property** (one model, one forward, N tasks) is the spine — C1 routing / composite are
  supportive/diagnostic only. Verify the Kurin/Xin/Mueller citations before camera-ready (see the draft banner).
