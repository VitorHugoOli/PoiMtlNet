# Time2Vec paper-faithfulness ablation — results

Generated by `scripts/run_time2vec_ablation.py` on Alabama next-task, 2 folds × 25 epochs. F1 values are macro avg in percent.

See `plans/time2vec_paper_analysis.md` for the full deviation analysis.

## Downstream MTLnet next-task Macro F1

| Variant | F1 mean | F1 std | Per-fold | Δ vs baseline |
|---|---:|---:|---|---:|
| `baseline_current` | 15.24 | ±0.44 | 14.93, 15.55 | — |
| `rand_init` | 14.03 | ±1.39 | 15.01, 13.05 | -1.21pp |
| `feat_space` | 16.27 | ±1.62 | 15.13, 17.42 | +1.04pp |

## Interpretation guide

- If `rand_init` ≈ `baseline_current` (within ~1pp): the legacy contrastive training is doing little work — embeddings are driven by the `sin(αh+βd+b)` inductive bias, not the optimizer. Fix or drop contrastive training (strong evidence for Rec 3, end-to-end joint).
- If `feat_space` > `baseline_current`: Recommendation 1 wins; promote feat-space sampling to default in a follow-up.
- If `feat_space` < `baseline_current`: feat-space sampling removes D6 Issue A but doesn't unlock downstream gains at this scale; consider Rec 3 next.
