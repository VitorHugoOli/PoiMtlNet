# DVC parameter file for MTLnet experiments.
# Edit these values and run `dvc repro` to re-run affected stages.
#
# This file is only used by DVC — it does NOT affect scripts/train.py
# or any other code unless DVC is installed and invoked.

state: florida
engine: dgi
epochs: 50
folds: 5
