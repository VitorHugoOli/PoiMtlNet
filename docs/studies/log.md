# docs/studies/ — Cross-study outcomes log

One line per study per closure or major direction shift. **Outcomes only.** Process narrative lives in each study's own `log.md`. Append-only.

| Date | Study | Outcome |
|---|---|---|
| 2026-05-04 | `hgi_category_injection` | **CLOSED** — AZ falsified; FL/CA/TX re-open standby. |
| 2026-05-06 | `merge_design` | **ACTIVE-CLOSING** — Designs A-M and Levers 1-6 saturated/falsified; Lever 5 orphaned (rescued by `substrate-protocol-cleanup` Tier B4 on 2026-05-28). |
| 2026-05-19 | `canonical_improvement` | **CLOSED** — Tier 1-6 substrate axis exhausted at ±0.8 pp; no further single-substrate ceiling. |
| 2026-05-24 | `mtl-protocol-fix` | **CLOSED v6 final** — F1 selector recovers ~95% substrate capacity at deploy (+5.6 pp FL); P4 identifies residual gap as architectural (NOT cat-vs-reg, NOT long-tail, NOT substrate). |
| 2026-05-24 | `mtl-protocol-fix` Phase 3 | **3 follow-ups** — log_T-KD §4.5 PROMOTED (+2-5 pp Wilcoxon-strict); class-balanced sampler §4.6 FALSIFIED (−18 to −30 pp); composite STL c2hgi+HGI §4.2 ESTABLISHED as current project headline (+7 to +12 pp). |
| 2026-05-16 | `mtl_improvement` | **LAUNCHED** — T0-T8 chain on branch `mtl-improve` (backbones, loss, batch, LR, α, heads, multi-seed champion); execution pending. |
| 2026-05-28 | `substrate-protocol-cleanup` | **LAUNCHED** — Tier A-D (log_T-KD multi-seed, Designs B/J/Lever 4/Lever 5 MTL+F1, 3-snapshot routing, freeze-reg-after-peak, K/V capacity-stealing pilot, window/mask audit); small-state only; ~40-45 GPU-h budget. |
| 2026-05-31 | `embedding_eval` | **LAUNCHED** — 4-level (L0 geometry → L3 MTL) leak-aware substrate evaluation ladder; re-screen dropped improvements on the correct (region-emb) artifact. |
| 2026-06-02 | `embedding_eval` | **CLOSED (Part-1 substrate)** — champion **v14 = `check2hgi_design_k_resln_mae_l0_1`** (resln+mae cat ⊕ Delaunay reg, orthogonal stack); design_k RE-OPENED + re-validated at FL (overturned prior AL/AZ-only K≡J); leak-free multi-seed: reg +0.9-1.1pp over canonical (closes 54-78% of HGI gap, HGI keeps small edge), cat ≈ frozen-canon ≫ HGI. NO MTL benefit (v14 or routing) → regime is the wall (Part-2). See CANONICAL_VERSIONS §v14. |
| 2026-06-12 | `mtl_improvement` | **CLOSED** — the C25 class-weighting confound WAS the "MTL sacrifices reg" gap; champion **G (= canon v16, the train.py MTL default)** MATCHES the STL reg ceiling (matched Δ −0.09…−0.31) + BEATS the STL cat ceiling (+2.6…+4.1) at 4 states × 4 seeds; mechanism = gradient orthogonality (tested intrinsic); no balancer/optimizer helps; cat gain architecture-dominated (transfer +0.93 FL / −0.67 AL). Survived 3 audit passes + the X-series. Read `archive/mtl_improvement/FINAL_SYNTHESIS.md`. CA/TX + the aligned-pairing pre-freeze gate → `closing_data`. |
| 2026-06-12 | `closing_data` | **SCAFFOLDED (not launched)** — the final study: cross-study re-eval → pre-freeze gates (G0.1 aligned-pairing) → recipe FREEZE → CA/TX majors once → final 6-state tables. Launch pending user sign-off on `closing_data/PLAN.md`. |
| 2026-06-12 | `closing_data` | **RE-SCOPED (pre-launch)** — now the experimental engine for the NEW paper: STL baselines RE-RUN + champion + the BRACIS suite (T1–T5 / §0.1–0.6 via a RUN_MATRIX inventory), ALL states × 4 seeds × 5 folds under the frozen recipe; substrate identity = a freeze decision. Story chosen in a follow-up effort. |
| 2026-06-14 | pre-freeze program | **SCAFFOLDED** — `PRE_FREEZE_PROGRAM.md` orchestrator + 3 upstream studies feeding `closing_data` G0.2: `mtl_frontier` (A40, R1–R3 exploration, FIRST), `pre_freeze_gates` (A40, A2/A4/overlapping-windows), `second_dataset` (Mac, Massive-STEPS NYC ETL ∥ + validation bridge). Hierarchy: explore → gates → FREEZE → regen → validate. Launch pending user sign-off. |
| 2026-06-14 | `mtl_frontier` | **SCAFFOLDED (not launched)** — output-level/asymmetric-sharing exploration (R1 log_C co-location prior, R2 STEM-AFTB gating, R3 cross-task distillation); optimizer aisle declared closed; ≥0.3 pp lever → v17 pre-freeze gate. A40. |
| 2026-06-14 | `mtl_frontier` | **R10 ADDED (user)** — Memory-Caching / GRM gating at the LAYER level (arXiv:2602.24281, no code): GRM/SSC gated read between dual towers (primary) + GRM-soup fusion across Check2HGI hierarchy levels (speculative). "On the layers, not the transformers"; second-wave, run after R2. |
| 2026-06-14 | `pre_freeze_gates` | **SCAFFOLDED (not launched)** — A2 feature-concat control (interpretation), A4 transductivity bound (disclosure), overlapping-windows adopt/keep (base change; effect already validated AL). Must resolve before `closing_data` freeze. A40. |
| 2026-06-14 | `second_dataset` | **SCAFFOLDED (not launched)** — Massive-STEPS NYC (recommended; user-confirm): Mac ETL in parallel (cat→7-root map, coords→tracts, shipped temporal split), validation phase = temporal-split bridge. Off the freeze critical path. |
| 2026-06-14 | `baseline_gap` | **SCAFFOLDED (not launched)** — closes the audit gap: owns the net-new external baselines (B1 CTLE, B2 POI2Vec/skip-gram, B3 HMT-GRN-style MTL, B4 cascade, B5 Flashback/DeepMove) from `baseline_gap_analysis.md`. Triage → RUN_MATRIX rows/columns decision pre-freeze (P1b); runs fold into P3. A40 + Mac. |
| 2026-06-15 | `mtl_frontier` R1 | **NULL** — log_C co-location ESMM-KD prior (`prior(reg)=Σ_c P(reg\|c)·P̂(c)`, on top of log_T-KD): AL multi-seed Δreg **+0.207±0.196** (gate ≥0.3 FAIL; Wilcoxon p=0.008, 15/20 fold-seed pairs +, no cat regression), FL seed0 +0.171/cat−0.27; weight non-monotonic (peaks W=0.2, craters at 0.6). Real-but-small increment over log_T-KD (weak 7-class aux + spatial overlap). Not v17. Code behind `--log-c-kd-weight` (default off). → R2. |
| 2026-06-15 | `mtl_frontier` R2 | **NULL** (not v17) — STEM-AFTB per-layer/direction stop-grad on G's cross-attn: reg **clean multi-seed null** all states; cat lift is **AL-only & decays with scale** (AL +0.636 / AZ +0.173 / GE +0.158 / **FL −0.026**) — user-approved multi-state confirm shows no generalization (inverse-G′). Citable STEM-AFTB dose-response: cross-task gradient is small-state harmful noise; sharing topology doesn't move the reg gap (cos≈0). Directional `aftb_spec` code, G default unchanged. → R3. |
| 2026-06-15 | `mtl_frontier` R3 | **NULL** — CrossDistil (live-teacher generalization of log_T-KD): warm-up + error-correction on fwd cat→reg + new reverse reg→cat arm. Fwd refinements don't rescue R1 (AL cat −0.18); reverse AL cat +0.45 seed0 → multi-seed **+0.100±0.282** (p=0.31, seed1 −0.34); FL null. **log_T-KD saturates the output-prior family.** Code behind `--cat-kd-weight`/`--log-c-kd-{warmup-epochs,ec-lambda}`, G default unchanged. First wave R1/R2/R3 = 3 nulls, same regime. → R10. |
| 2026-06-15 | `mtl_frontier` R10 (★) | **NULL** — GRM-gated cross-attn read (learned input-conditioned generalization of R2's binary AFTB; arXiv:2602.24281 primitive "on the layers"). FIRST FL effect (FL cat +0.324 seed0) → FL multi-seed **+0.085±0.203** (p=0.31, 2 seeds negative) = noise → **GRM ≡ champion G** (the spec's exact falsifier). SSC not pursued. Code behind `--model-param crossattn_grm=True`, G default unchanged. |
| 2026-06-15 | `mtl_frontier` follow-ups | **3 MORE NULLS** (user ideas) — (1) R10 GRM null at all states (AZ −0.24/+0.14, GE +0.08/+0.27, AL 4-seed +0.14/+0.18); (2) `aux_gated` (input-dependent β in reg head): gate moves (γ 0.12→0.3 AL/0.47 FL) but AL cat +0.48 / **FL cat −0.85 crater** → re-confirms champion β→0 / aux>gated; (3) best-stack **sub-additive** (AL cat +0.456 < aftb_late +0.636, FL null) → levers interfere. Recommended next: **conditional coupling** (cat output→reg input, GETNext/iMTL — out of the cos≈0 box). |
| 2026-06-16 | `mtl_frontier` | **★ CONDITIONAL COUPLING = first genuine positive (sub-threshold); R4–R9 OPEN (HANDOFF.md).** cat posterior → reg input feature (iMTL/GETNext): FL cat **+0.235** / reg +0.070 (4/4 seeds positive, AUDIT-confirmed deterministic, matched baselines) — below the 0.3 v17 gate but the study's only real Pareto-positive. Richer 256-dim features HURT FL (−0.31) → cap is the regime (weak 7-class prior). **AUDIT (user-flagged "seed0 always best"): champion G is DETERMINISTIC; pattern = real seed variance (G seed0 weakest); earlier "non-determinism" was code drift vs old R0. No bug; nulls unchanged.** 8 levers = 7 nulls + 1 sub-threshold positive; champion G UNCHANGED, no v17. Continue: `mtl_frontier/HANDOFF.md` (push the conditional-coupling family). |
| 2026-06-15 | `mtl_frontier` | **WAVE-1 + R10 COMPLETE — 4 NULLS, champion G stands.** R1 (output-prior) / R2 (binary sharing) / R3 (live distillation) / R10 (learned gating) all reproduce but do NOT beat G+log_T-KD — airtight confirmation of the cos≈0 / data-rich / weak-7-class-aux regime. Recurring signature: single-seed promise (loosest-cat-variance state) washing out multi-seed. No v17 lever. Citable paper negative: the post-2022 MTL frontier replicates G's two wins, doesn't exceed them. R4-R9 deferred; optimizer aisle closed. See `mtl_frontier/FINDINGS.md §SYNTHESIS`. |
| 2026-06-17 | `mtl_frontier` R-CC+ | **CLOSED NULL — conditional-coupling family fully mapped; the +0.235 cap is the regime, not the injection knob.** Pushed cc along signal {calibrated/argmax/topk} × injection {FiLM/input-side concat} × output-side {learned cat→region logit prior}: every variant ties or underperforms the original additive-posterior cc. FL multi-seed (matched in-batch G): cc_e2e +0.235 (reproduced exactly), calib +0.237, argmax +0.214 (4/4 seeds +, reg +0.066…0.070 p<0.05), **input-side concat washes out +0.033** (advisor-requested control), output-side logitp +0.016 null; richer `features` already HURT. **None clears 0.3.** Advisor audit 5/5 PASS (G bit-identical, leak-free). No v17; champion G stands. R4–R9 still open (`HANDOFF.md`). See `mtl_frontier/FINDINGS.md §R-CC+`. |
| 2026-06-17 | `mtl_frontier` R4 | **DONE (paper-narrative; resolves C21 selector saga) — front profiled on the frozen champion.** Scalarization `--category-weight` sweep (FL multi-seed): the loss-weight/mixture axis is a **near-corner** (champion cw=0.75 Pareto-dominant; lowering→0.55 buys +0.05pp reg for −0.87pp cat, raising→0.85 loses on both) → tasks weakly coupled, "front collapses" falsifier datum. The real cat↔reg trade is the **deployment-epoch axis** — 12–16 Pareto epochs/run, geom_simple deploying a **stable ep18–20** point across all seeds → C21 is an epoch-deployment choice, not a weight/architecture problem. **PaLoRA-proper declined** (shared-trunk adapter mixture can't move the private-tower reg → would reproduce the near-collapsed weight-front). No model code; champion G untouched; no promote gate. See `mtl_frontier/FINDINGS.md §R4`. |
| 2026-06-17 | `mtl_frontier` R5 | **CLOSED NULL — a fired gate that is a comparand artifact (matched-baseline rigor caught it).** Per-instance log_T-KD gating by Markov-coverage (covmax/coventr, batch-mean-fixed). FL multi-seed covmax +0.472 cat (4/4 seeds) **clears the ≥0.3 gate vs the global-W-KD-ON base** — but that base is handicapped (log_T-KD costs −0.70pp cat at FL, small-state-only); vs the study's TRUE FL champion (KD-OFF G) R5 is **−0.224 cat / −0.063 reg = worse on BOTH heads**, and dominated by the trivial "KD-off at FL". On the KD's target axis (reg) gated ≤ global-W → falsifier met; AL gate fails. Independently advisor-audited → null, no v17 escalation; champion G stands. Citable mechanism datum: instance-gated KD recovers ~2/3 of log_T-KD's FL cat-cost but never beats KD-off. Lesson: read the promote-gate vs the *deployable champion*, not a lever's internal control. Code `--log-t-kd-gate` (G unchanged). See `mtl_frontier/FINDINGS.md §R5`. |
| 2026-06-17 | `mtl_frontier` R9 | **DONE — optimizer aisle closed citably (BayesAgg-MTL).** Verified the repo `bayesagg_mtl` is gradient-MAGNITUDE-variance weighting (Kendall-style), NOT faithful to ICML'24 (gradient-DIRECTION posterior). Re-ran at the CHAMPION recipe (not the 19-arm registry defaults): cat **still craters** (AL 37.754 = the 19-arm 37.75 reproduced exactly; FL 63.689) → recipe-invariant impl pathology (down-weights the 7-class cat vs the 1109-class reg head), so the 19-arm bayesagg null is now **diagnosed** (not a defaults artifact). Faithful ICML'24 port unbuilt + expected-null by the regime (cos≈0 + 19-arm + Mueller TMLR'25) → logged + deferred. R9(b) Smooth-Tchebycheff unmotivated (R4 front near-degenerate). No code changes; champion G stands. See `mtl_frontier/FINDINGS.md §R9`. |
| 2026-06-17 | `mtl_frontier` R6/R7/R8 | **CLOSED as reasoned predicted-negatives (deferred; runnable on request) — R-PROGRAM COMPLETE.** Each is a substantial from-scratch impl whose outcome is mechanistically determined by the completed regime: **R6** ForkMerge ≤ G (scheduled_static already in the 19-arm null + R4's near-corner weight-front leave no slack for val-driven weight adaptation); **R7** merge-vs-joint < G (tangent-space theory — from-scratch STL experts don't share a loss-basin; G already is the share-early/privatize-late optimum); **R8** next-visit-time aux = rising-tide null (lifts STL=MTL) + disproportionate 2→3-task refactor (codebase hardcodes n_tasks==2). Closed by reasoned prediction per *What NOT to pursue*; measured negatives one driver away if the paper wants them. champion G stands. See `mtl_frontier/FINDINGS.md §R6/R7/R8`. |
| 2026-06-17 | `mtl_frontier` | **AUDIT (per-experiment impl+outcome review + cat↑/reg↓ investigation) — no reg loss, no bug; 2 follow-up bugs fixed.** 18-agent workflow (10 reviews + 4 cat/reg lenses + 4 adversarial verifications). Core R-program (R-CC+/R4/R5/R9 + R1/R2/R3/R10/cc) all impl-correct/outcome-sound, numbers reproduced to 3dp. The cat↑/reg↓ "we're losing something" worry is **selective perception** (cat+/reg+ is the largest of 41 quadrant-pairs; correlation POSITIVE; reg positive 28/41) + a **diagnostic-best epoch-decoupling artifact** (R5 reg-down→null at the deployable checkpoint) + the **β-gated regime split** (head_beta→0 at FL ⇒ no shared reg channel to rob; β live at small states ⇒ both heads move together). DECISIVE: **MTL reg = same-substrate STL reg ceiling within −0.09…−0.31pp** ⇒ reg already maxed, not shortchanged. Found+fixed 2 inherited follow-up bugs (FU2 aux_gated shared-pathway severed → re-ran, FL craters both heads −0.91/−0.31; FU1 "4-seed" was seed-0-only → +0.045/−0.066). Champion G stands; no verdict flipped. See `mtl_frontier/FINDINGS.md §CAT↑/REG↓`+`§AUDIT`. |
| 2026-06-17 | `mtl_frontier` | **STUDY R-PROGRAM COMPLETE — champion G UNCHANGED, no v17.** R1–R10 + R-CC+ + R4/R5/R9 executed, R6/R7/R8 reasoned-closed: **9 nulls + 1 sub-threshold positive (conditional coupling +0.235) + R4 (Pareto front resolves C21) + R9 (optimizer aisle closed).** The post-2022 MTL frontier replicates but does not exceed champion G's two wins (dual-tower + log_T-KD) in this cos≈0 / dual-tower / weak-2.8-bit-auxiliary LBSN regime — a strong citable domain-frontier negative. Nothing flows to `closing_data` G0.2. PR on `study/mtl-frontier` pending. |

## How to append

One row per closure or major direction shift. Format:

```
| YYYY-MM-DD | <study-name> | <Outcome in one sentence, lead with verb (CLOSED / PROMOTED / FALSIFIED / LAUNCHED / RE-OPENED)>. |
```

**Outcomes only.** Save the why/how for the study's own `log.md`. If you can't summarise the outcome in one sentence, the outcome isn't crisp enough to be logged yet.
