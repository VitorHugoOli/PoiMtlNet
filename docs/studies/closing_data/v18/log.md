# v18 — event log

One line per event, newest at the bottom. Charter §8 deliverable.

- `2026-08-07` — v18 substrate built for all six states (forward-only graph + 4 elapsed-time node columns, `in_channels` 15). Readout equivalence proved: `prefix_forward_only` is an identity on a forward-only graph, verified to float32 epsilon over every window at 4 states.
- `2026-08-07` — first wave cells run under the *class-weighted* recipe inherited from v17. Later superseded.
- `2026-08-07` — AMP precision leak found and fixed (`PRECISION_CAVEAT.md`): a bare `export` in `cell_joint` persisted into later cells, so cat cells ran fp16-or-fp32 depending on resume state. Two results VOID. All cells now pin precision per-command via `env`.
- `2026-08-08` — retuning sweep, 103 arms. Both arms retuned so Δcat is not biased against MTL. Δcat pooled −0.363 → −0.105.
- `2026-08-08` — class weighting found to be **size-graded and sign-flipping**: alabama −1.203 (p=0.004) vs texas +1.556 (p=0.030).
- `2026-08-08` — **logit adjustment** (Menon ICLR'21, τ=0.5) beats both tuned class-weight endpoints: dedicated +2.86, MTL +3.18, all p ≤ 0.0014. τ=1.0 a flat null.
- `2026-08-09` — Fable review of `SWEEP_FINDINGS.md`. Verified every load-bearing number against the raw JSONs; caught two wording overreaches ("axis is closed", "confirmed at a large state"), both scoped.
- `2026-08-09` — §11 added to `SWEEP_FINDINGS.md`: 16 arms had been run but never written down (steps A/B/C, row 3b, reproducibility check).
- `2026-08-09` — **texas** dedicated, fold 0: logit adjustment transfers to a large state — 36.6226 vs 33.6127 (weights ON) and 35.1696 (weights OFF). +3.010 / +1.453. The size-tiered class-weight rule is dissolved.
- `2026-08-09` — **step D** finally run (planned 2026-08-08, never executed): florida MTL batch size is a **null** (geom span 0.078 over bs 8192/16384/32768). bs8192 kept everywhere.
- `2026-08-09` — **region + logit adjustment settled at two states**: alabama Acc@10 −1.841 (p=0.0002), istanbul −2.749 (p<0.0001), while macro-F1 *rises* (+0.377 / +0.671). Bayes-consistency for balanced error trading against the reported Acc@10. **τ=0 for region everywhere.** Confirmed the MTL region criterion never receives the offset (`mtl_cv.py:500`).
- `2026-08-09` — `FINAL_SETTINGS.md` **approved by the author**. 19 cat+joint sidecars moved to `v18_superseded_oldrecipe/`; the 10 region sidecars kept (recipe unchanged, and both fresh τ=0 arms reproduced the stored values *exactly*: AL 69.9956, IST 75.1563).
- `2026-08-09 06:14` — **regeneration launched** (`run_regen.sh`, seeds 0 then 1, n=10) at commit `e351d4b0`.
- `2026-08-09 06:20` — alabama s0 cat = **30.7654** (322 s), vs 28.0334 under the old recipe: **+2.73**, consistent with the +2.86 logit-adjustment gain measured in the sweep.
- `2026-08-09 06:28` — istanbul s0 cat = **35.3539** (802 s).
- `2026-08-09` — Opus audit of the live run. Two **blockers** found and fixed in `make_results.py`: (1) the end-of-wave aggregation called `make_results.py` without `score_all.py --write`, so it would have republished the **superseded** 2026-08-07 numbers under a fresh timestamp and today's SHA, silently and without crashing; (2) `PROVENANCE.md` hardcoded `cw 0.75` and no logit adjustment — a factual error about what produced the numbers. Also fixed a `None` crash that would have destroyed the whole report on any partially-failed wave.
- `2026-08-09 06:39` — alabama s0 joint cat=**30.6985** reg=**69.5928** (1167 s) — **reproduces the sweep arm `mla_alabama_tau0.5_cw0.50` to 0.0000 on both heads**, despite a different inductor cache directory.
- `2026-08-09 06:59` — istanbul s0 joint cat=**35.4227** reg=**75.2177** (1863 s) — vs the same sweep arm, +0.053 cat / +0.047 reg. Expected: `CLAUDE.md` records that under `--compile` the number is governed by the inductor cache/compile session (autotuning + reduction-order nondeterminism) and should be treated as within-fold-σ, not bit-reproducible. 0.05 pp against a fold-σ of ~1.7 (cat) / ~3.0 (reg) is far inside that. **Useful reproducibility statement for the dissertation: two independent runs of the identical recipe agree to ≤0.05 pp.**
- `2026-08-09` — ⚠ **CORRECTION.** I attributed texas cat's 3266 s → 5480 s to the recipe. Wrong: texas cat is bs8192 @ 0.005 under *both* recipes. The cause is **precision** — the old wave was restarted at 23:06:43, every earlier cell was skipped, so the bare `export MTL_DISABLE_AMP=1` inside `cell_joint` never ran and **texas s0 cat executed under fp16**, while its sidecar printed `"fp32"` unconditionally. This is the `PRECISION_CAVEAT.md` bug, already recorded there (line 28, `texas cat s0 | fp16 | 3266 s`). 1.76× matches the documented florida fp16/fp32 ratio exactly. **Consequence: the old→new dedicated-cat deltas for alabama / arizona / istanbul / texas are cross-precision and must not be cited.** Only **florida (+1.392)** and (pending) california are clean, since their old cat cells ran fp32. All joint cells were always fp32, so joint old→new deltas are clean.
- `2026-08-09 17:39` — texas s0 joint cat=**36.3424** reg=**66.8901** (22453 s). **Texas complete.**
- `2026-08-09` — **the void texas Δcat "beats" verdict is now cleanly re-measured and it does not survive.** `PRECISION_CAVEAT.md` flagged +0.961 as the only "beats" verdict in the study and voided it as fp16-dedicated vs fp32-MTL. With both arms fp32 under the approved recipe it is **+0.020** — indistinguishable from zero. The precision artifact accounted for essentially the whole effect, which confirms the caveat's diagnosis rather than merely suspecting it.
- `2026-08-09` — clean within-recipe Δ (joint − dedicated), seed 0, both arms fp32: istanbul +0.069 / +0.061, alabama −0.067 / −0.403, arizona +0.040 / −0.091, florida +0.218 / +0.397, **texas +0.020 / +1.939** (cat / reg). The large-state region advantage survives the recipe change at texas; category is ~0 everywhere.
- `2026-08-10` — **alabama seed 100 cat + reg produced on rented hardware** and merged: cat **30.7296** (fold sd 1.256), reg **70.0727** (fold sd 3.483). Run from a Claude Code session via `v18_2/scripts/modal_lane.py` on the **lane-2 account** (`vitor-h-oliveira`, A100-SXM4-40GB, serial, fp32 + `--compile --tf32`); the alabama engine was staged to that volume from this host and verified by byte count. Recipe verified identical to `run_wave.sh` by two independent checks: the mechanical per-cell flag diff, and resolving both command lines through `train.py`'s own parser + config builder — **all 73 `ExperimentConfig` fields equal** (`max_lr=0.0025`, `bs=8192`, `loss_calibration={logit_adjust_tau: 0.5}`, `learning_rate=1e-4`, 50 ep / 5 folds). Folds generated on the fly, as on every A40 cell (no frozen cache exists for `check2hgi_v18`). **Alabama now closes at n=20**: stl_cat **30.7750**, stl_reg **70.1174**. Sidecars carry a new `lane_host` field so machine provenance is explicit rather than inferred from a pid — poolability of rented and local cells remains *unestablished* (seed and machine are still confounded), so the machine is recorded, not asserted away. Cost $0.24; container terminated and verified. Alabama s100 **joint is still open** — deliberately left without a sidecar so a future wave runs it. Local archive, heartbeat and logs: `docs/results/closing_data/v18_2/modal_runs/alabama_s100_lane_20260810_021901/`.
- `2026-08-10` — **aggregation fix: the own-ceiling contrast is now PAIRED on both sides.** `cells[state]["stl_cat"]` averages every seed with a *dedicated* cell, while `joint_cat_diag_best` averages the seeds with a *joint* cell. While those sets coincided, differencing the means was harmless; they stopped coinciding the moment a cheap cell ran ahead of its joint cell (alabama s100 cat+reg on rented hardware with s100 joint unrun, and **texas s1 cat with its joint cell still running**). §1 then showed a 4-seed dedicated mean against a 3-seed joint mean, with an `n` and a p-value correctly paired at 3 seeds — the p-value was right and the Δ beside it was not measuring the same thing. `score_all.py` now emits `stl_cat_paired` / `stl_reg_paired` (restricted to seeds that also have a joint result) and computes `delta_*_vs_own_ceiling` from those; `make_results.py` §1 reads them and states the restriction in the table. The all-seed means are unchanged and still in the JSON as `stl_cat` / `stl_reg`. No-op wherever both arms have the same seeds. Effect at alabama: Δcat −0.10 → **−0.11**, Δreg −0.44 → **−0.49**. This is structural rather than incidental — the rented lane exists precisely so cat/reg can run ahead of joint, so dedicated-only seeds are now the normal state.
- `2026-08-10` — ⚠ **the pooled Δcat verdict has flipped from "matches" to "beats"** (+0.059 over 55 (state,seed,fold) pairs, paired one-sided p=0.023; previously +0.038 over 40 pairs, "matches"). This is **new data from the seed-1 wave**, not the pairing fix — the pooled figures come from `paired_diffs`, which was already paired and is untouched. Treat with the caution §1's own disclosure demands: the 5 folds within a seed share ~80 % of their training data, so pooled p-values are anti-conservative, and the honest independent unit is the seed (n=11 here). A marginal p=0.023 on non-independent replicates is **not** grounds to promote "matches" to "beats" in any write-up. Re-read once the wave completes and n is stable.
- `2026-08-10` — **alabama seed 100 JOINT closed on rented hardware; alabama is now complete at n=20 in all three families.** cat **30.7659** (fold sd 1.144), reg **69.7363** (fold sd 3.138), 607 s on an A100-SXM4-40GB via `v18_2/scripts/modal_lane.py` (lane 2), $0.52. Scored in-container by `a40_score_matched.py` + `score_joint_best.py`, so the rundir came home complete. Cell aggregate: stl_cat 30.775 / joint_cat 30.6985 (**Δcat −0.077**), stl_reg 70.1174 / joint_reg 69.6671 (**Δreg −0.450**), both "matches"; `stl_cat_paired == stl_cat` again now that every seed has both arms, which is the pairing fix behaving as designed. Recipe verified before launch by resolving the joint argv through `train.py`'s parser: **all 73 `ExperimentConfig` fields identical to `run_wave.sh`**. The per-fold log_T is inert here (`freeze_alpha=True`, `alpha_init=0.0`, all KD weights 0) — the container logged `[log_T-inert skip]` per fold, so no `region_transition_log_*.pt` was needed. Deliberately did NOT stage those files: the seed-100 copies on this host are dated 06-02 against an 08-06 substrate, i.e. two months stale, and shipping them would plant a trap for any future non-inert run.
- `2026-08-10` — **two defects found by that dry run, both fixed before any long run.** (1) **Silent partial harvest.** A `modal.Volume` handle created before the job does not see the container's `Volume.commit()` afterwards, and `vol.reload()` does not fix it: the driver downloaded only the files that existed when it started and missed the joint sidecar, both joint score JSONs, the joint log and the whole `rundirs/` tree — while reporting exit 0. A fresh `Volume.from_name()` in the same process saw all 19 files at once. On a 4-hour california joint this would have returned a green run with an empty archive. `modal_lane.py` now re-instantiates the handle on every attempt, retries until the expected per-cell sidecars appear, records `harvest_incomplete` and exits non-zero otherwise, and gained a no-cost `--refetch` recovery path. (2) **Harvested logs were tail-only**, so the 400 KB cap cut the entire startup banner — TF32, `torch.compile`, the resolved config and the first `[log_T-inert skip]` lines — and the alabama joint log came home beginning mid-progress-bar. Now head 150 KB + tail 250 KB with an explicit elision marker; on a tqdm-heavy CA/TX joint the diagnostics are the part worth keeping.
- `2026-08-10` — monitoring note: during the joint cell the GPU read **0 % utilisation for ~2 minutes** while healthy. That is the cold `torch.compile` warm-up, which is CPU-bound in inductor codegen and Triton autotuning (batch 1 took 120 s; batches 2–9 then cost ~0 s each). `utilization.gpu` cannot distinguish "compiling" from "stalled", and the lane log only carries START/DONE, so the pair reads exactly like a hang. `watch_modal.py` now prints the trainer's own progress line per cell and flags slow-first-batch as compile rather than a stall. The incremental `harvest_watch` was confirmed working end-to-end for the first time (folds banked live 0→1→2→3→4).
- `2026-08-10` — **texas seed 7 JOINT run on a rented H100** (lane 2, `modal_lane.py`): cat **36.3501**, reg **66.8218**, 6558 s, exit 0, $9.79. Against texas s0 (cat 36.3424, reg 66.8901) the agreement is +0.008 / −0.068 — the two seeds sit on top of each other. TX engine staged to lane 2 first (19.4 GB; `next_region` copied in-volume with `modal volume cp`, `next.parquet` uploaded at ~85 MB/s), all six inputs byte-verified against the host before launch.
- `2026-08-10` — **H100 vs A100/A40, measured on a real large-state cell.** Instantaneous batch rate: **18.5 batch/s vs the A40's 4.18** = 4.44×. But the **wall** ratio is **3.42×** (6558 s vs the A40's 22453 s), i.e. **1.80× the projected A100**, not the 2.33× the batch rate implied — dataset build, `torch.compile` warm-up and validation do not scale with the GPU, and on a 5-fold cell they are a large minority of the wall. **Quote the wall ratio, not the batch rate, when costing a cell.** H100 still cleared its 1.53× break-even, so it was the right card: $9.79 against a projected $11.49 on an A100, ~15 % saved. VRAM held at 27.3 GB of 80 (the A40 used 26.8 GB of 48), so an A100-40GB would have been tight; util 87–94 %, no throttling, 55–65 °C.
- `2026-08-10` — **the symmetric pairing fix earned itself immediately.** texas s7 is a JOINT-ONLY seed (no cat/reg at that seed), exactly the case the mirror-direction fix was written for. §1 correctly holds at n=5 for texas — the paired contrast still rests on s0 alone (Δcat +0.020, Δreg +1.939) — while §2, the joint-only table, moves to **n=10** (cat 36.35 ± 0.01, reg 66.86 ± 0.05). Without the fix §1 would have differenced a 2-seed joint mean against a 1-seed dedicated mean. **A joint-only cell buys §2 evidence, not §1 evidence**; texas s7 becomes citable in the headline contrast only once its cat and reg cells exist at seed 7 (≈3.2 h on the free A40).
- `2026-08-10` — ⚠ **budget: lane 2 finished the night at $27.85 metered of $28.86 credit ($0.00 billed).** **TX s100 and CA s7 were deliberately NOT launched.** For ~1 h a second, uncoordinated session (`poimtl-fl-joint`, two A100 containers) ran on the same account at ~$11.7/h alongside this run; combined burn hit ~$18–19/h and the projection crossed the credit line. A watchdog polled spend every 3 min and would have terminated **only our own sandbox** (never another session's). The other job ended at 05:24 and the TX cell completed at 05:50 with ~$1.01 of credit to spare. **Lesson for the rented lane: credit is a shared resource across sessions — check `modal container list` for foreign apps before committing to a long cell, not just your own ledger.** california is fully staged on lane 2 (14.8 GB, byte-verified) and ready to run when credit refreshes.
- `2026-08-10` — ⚠ **COST MODEL CORRECTION: on a GPU container the requested CPU/RAM is a floor, not a cap.** Reconciling the texas s7 cell against the Modal ledger: the H100 line matched the list price exactly ($7.22 for 6589 s at $3.949/h), but **CPU and memory were each billed at exactly 3.0× the request** (8 CPU / 128 GiB requested → ~24 cores / ~384 GiB billed; $2.07 and $5.61 over 1.83 h). The cell cost **$14.92**, not the **$9.79** the driver printed — an effective **$8.15/h against a modelled $5.35/h, 52 % understated**. The alabama A100 cells show the same effect at ~2.5–2.7×. Most likely a GPU slot carries the host's per-GPU share of CPU/RAM (an 8-GPU H100 node ÷ 8 ≈ 24 cores); Modal publishes no such multiplier, so this is an empirical constant to re-check, not a documented rate. `modal_lane.py` now applies `GPU_HOST_SHARE = 3.0` to the CPU/memory terms of GPU tiers only and reproduces the billed figure exactly. **Every earlier budget in this study is ~50 % optimistic for GPU work**: "TX 7+100 = $22.97" is really ~$35 and "CA 7+100 = $17.88" is really ~$27, so neither fit the $28.86 credit — the decision not to launch them was right by a far wider margin than the numbers available at the time showed. Corollary, and the advice it retracts: **trimming `--cpu`/`--memory` on a GPU container saves nothing**, so request the memory the job actually needs (128 GiB for a CA/TX MTL build) and spend the effort on choosing the card.
- `2026-08-10` — night's ledger on lane 2, for the record: **$27.85 metered of $28.86 credit, $0.00 billed.** Split: this session $16.12 (alabama preflight + cat/reg + joint + image build $1.21; **texas s7 joint on H100 $14.92**) and a second, uncoordinated session `poimtl-fl-joint` **$11.72** (two A100 containers, ~1 h, overlapping ours). **42 % of the night's spend was not this session's** — credit is a workspace-wide resource, so check `modal container list` for foreign apps before committing to a long cell, not just your own ledger.
- `2026-08-10` — **florida CLOSED at n=20 in all three families.** The four missing dedicated cells (cat+reg at seeds 7 and 100) were run on a rented A100-SXM4-40GB, packed two seeds into one container: cat **37.3359** / **37.3346**, reg **76.6944** / **76.6737**, lane wall 4315 s, exit 0, **$3.75 billed**. They sit on top of the local seeds (stl_cat spread across all four seeds is 0.05 pp, stl_reg 0.03 pp). §1 now reads **n=20, Δcat +0.239 "beats" (p=0.000), Δreg +0.391 "beats" (p=0.000)**. The point of the purchase was not the four cells themselves: florida's JOINT was already at n=20 (s7 and s100 produced by a parallel session), but with no dedicated arm at those seeds the symmetric pairing correctly held §1 at n=10 and those two paid-for joint cells could not be cited. These four unlocked them.
- `2026-08-10` — **why cat/reg were slower on a rented A100 than on the free A40, and the waste it exposed.** Packed 2-wide, the reg cell ran **455 s/fold against the A40's 332 s/fold — 1.37× slower on faster silicon.** Cause is not the GPU: `p1_region_head_ablation` scores the validation metric **on CPU** ("full val logit ≈ 4.8 GB, N_val=254883 × C=4703, to avoid GPU OOM at overlap scale"), and that phase had **8 cores in the container against 32 on the A40 box** (i9-14900K), with `OMP_NUM_THREADS` hard-pinned to 8 in the image. Two concurrent reg cells then split those 8 cores. **And the container was billed for ~24 cores the whole time** (the measured 3× GPU-host-share), so the run paid for 24 and used 8 — on the cell whose bottleneck is CPU. Fixed: `modal_lane.py` now exports `OMP_NUM_THREADS=MKL_NUM_THREADS=$(nproc)` inside the job and prints the value, and the image comment no longer presents 8 as correct. **Rule: on a GPU container, request and use the CPU/RAM floor you are already paying for — asking for less saves nothing and throttles the CPU-bound phases.**
- `2026-08-10` — three more operational lessons banked. **(a) The trainer's progress bar is per FOLD** (`Epoch n/50`), not per run; reading it as the whole cell produced a "~2 h" ETA for a 40-min job mid-flight and nearly triggered a needless kill. Multiply by `--folds`. **(b) The Modal billing ledger updates in discrete blocks** — it sat at $31.37 for four minutes — so a short-window rate measurement returns 0 or nonsense; the reliable derivation is `metered − billed` once billed is non-zero, which is how the credit balance was pinned at exactly $30.03. **(c) `--parallel` was omitted from the launch**, so cat and reg ran serially inside each seed (2 concurrent cells, not 4), costing ~20 min of extra wall; the dry run reports `cells per seed` but not the serial/parallel mode.
- `2026-08-10` — **`MTL_CHUNK_VAL_METRIC=1` dropped from the dedicated-reg cell; it was forcing the small states onto a CPU scoring path they never needed.** The flag makes `p1_region_head_ablation` accumulate the full `[N_val × C]` val logit on CPU and score it there — a real OOM guard at texas (20.1 GB) and california (19.8 GB) scale, but `_should_chunk_val_metric` **already auto-enables** above `P1_S2_AUTO_BUDGET_GB` (4 GB), so forcing it changes nothing for the large states and pushes a tiny tensor onto the CPU for the small ones: alabama 0.09 GB, arizona 0.25 GB, istanbul 0.33 GB. Measured cost: **arizona reg 343 s on a rented H100 against 185 s alone on the A40** — the only family that came out slower on faster silicon, because that phase had 8 container cores against the A40 box's 32 (i9-14900K). Florida (4.79 GB) and TX/CA are unaffected: the auto-guard fires for them either way.
- `2026-08-10` — **the equivalence behind that change was measured, and the repo's own test had never run.** `_rank_of_target` computes `1 + #{logits strictly greater}` — an exact integer count, device-independent by construction (`argsort` was explicitly rejected in its docstring to avoid platform-dependent tie breaking). The CPU-vs-CUDA regression test `tests/test_scripts/test_p1_val_chunk_guard.py::TestCpuGpuEquivalence` is `skipif(not torch.cuda.is_available())` and had therefore **never executed on the dev machine**. Executed on a rented T4 on 2026-08-10 across six shapes — the three unit-test cases plus the measured arizona and florida val folds and two heavy-exact-tie cases — over top5/top10/mrr/ndcg/accuracy/f1: **worst deviation 2.98e-08 against a reporting precision of 1e-4, i.e. 3350× below the bar**, and GPU scoring 3.8–7.6× faster on those shapes. Cells produced before and after the change remain poolable. `P1_FORCE_CPU_VAL=1` restores the old behaviour. The JOINT cell still sets the flag exactly as `run_wave.sh` does (`mtl_eval.py` has its own S2 path — deliberately not bundled into this change), and the CAT cell never needed it (`next_cv.py` does not read the variable; it was a no-op there).
- `2026-08-10` — ⚠ **`v18/run_wave.sh` still forces the flag and was deliberately NOT edited: it was mid-run** (pid 881053, california s1), and `bash` reads a script incrementally, so editing an executing one can corrupt it. Applying the same one-line change there once the wave finishes is worth roughly 20 % on the small-state reg cells. Also noted: the docstring in `p1_region_head_ablation.py` points at `tests/test_training/test_p1_val_chunk_guard.py`; the file actually lives in `tests/test_scripts/`.
- `2026-08-10` — **arizona s7 measured on H100** (3 families packed on one card): cat **34.5921** (145 s), reg **59.5550** (343 s), joint cat **34.6063** / reg **59.2728** (659 s); lane wall 659 s against 1614 s serial on the A40 = **2.45×**. Per family against the A40: cat **2.04× faster**, joint **1.72× faster**, reg **1.85× slower** (the CPU-path defect above). Values track the board (arizona stl_cat 34.55, stl_reg 59.49). The joint's 1.72× clears the H100 break-even of 1.39×, so H100 is the right card for the remaining small-state cells.
- `2026-08-10` — **texas s100 e california s7 (JOINT) rodadas em duas H100 alugadas, em paralelo.** TX s100: cat **36.3936**, reg **66.8394**, 6601 s, $15.00. CA s7: cat **35.7903** (fold sd 0.315), reg **65.4426** (fold sd 0.376), 5100 s, $11.62. Contra os seeds 0 de referência (TX 36.3424/66.8901; CA 35.7187/65.3989) os desvios são +0.051/−0.051 e +0.072/+0.044 — as células caem em cima da referência, com dispersão entre folds baixa e nenhum sinal de colapso. Ambas exit 0, teardown verificado, zero sandboxes remanescentes. **Texas joint fecha em n=20** (seeds 0/1/7/100): 36.37 ± 0.02 e 66.86 ± 0.03, com apenas 0.08 pp de amplitude em cat entre os quatro seeds. California joint vai a n=10 (0, 7); o s1 ainda está na fila do A40.
- `2026-08-10` — **razão H100 vs A40 replicada em três células e dois estados: 3.40–3.43×.** TX s7 22453→6558 s (3.42×), TX s100 22453→6601 s (3.40×), CA s7 17478→5100 s (3.43×). É a razão de **wall**, não de batch-rate: o contador de batches sugeria 4.2–4.4×, e usar esse número subestima o custo de uma célula em ~20 %. **Cotar sempre pelo wall.** Estrutura de fold medida, e ela importa para orçar: o fold 1 carrega o build do dataset e o warm-up do `torch.compile` (TX 26.3 min, CA 20.2 min) contra 21.2 e 16.2 min em regime — projetar o total a partir da taxa em regime erra para baixo em ~15 %. Utilização 84–88 %, 23–27 GiB de VRAM de 80, 52–65 °C, sem throttle em nenhuma das duas.
- `2026-08-10` — **conta nova (`vholiviera`) preparada do zero e usada para essas duas células.** Workspace vazio: volume criado, código (`src/scripts/research/pipelines`) e as engines de texas e california subidas do nespedgpu — **34.6 GiB, 12/12 arquivos conferidos byte a byte** contra o host antes de qualquer GPU ligar. Preflight OK 6/6 nos dois estados. Custo total da noite nessa conta: **$26.62 de $30, cobrado $0.00** (staging + imagem + preflight custaram $0.01). O `.env` passou a ter **duas** contas, então `modal_lane.py` ganhou `--profile` obrigatório quando há mais de uma: ele recusa adivinhar qual workspace faturar, e um `--profile` explícito **vence** um `MODAL_TOKEN_ID` exportado no shell (dizendo isso em voz alta) — o furo real do meu próprio fluxo, já que eu alternava entre as duas contas no mesmo terminal.
- `2026-08-10` — **§10b confirmado na prática, e a metade que falta.** `harvest_rundir_scores()` funcionou: as duas lanes trouxeram sozinhas a árvore `rundirs/<caminho literal>/{a40_matched,joint_best}_score.json`. Mas **`push_to_host()` não existe em nenhum script** — o espelhamento para o host continua manual, e é ele que fecha o ciclo, porque `score_all.py` lê os números de dentro do rundir e não do sidecar. Espelhados sidecar + os dois score JSONs no caminho literal, checksums 3/3 em cada célula, e o teste do §10b passa sem warnings para texas s100, california s7 **e** texas s7 (o de ontem, confirmando que o espelhamento manual anterior também está íntegro). Enquanto `push_to_host()` não existir, essa cópia é um passo obrigatório da checklist, não um detalhe.
- `2026-08-10` — ⚠ **cuidado ao ler progresso pelo heartbeat.** Ele amostra a cada 60 s, então deduzir a duração de um fold a partir de duas amostras infla o valor: cheguei a projetar $34.9 (acima do crédito) e quase interrompi as duas células por isso. O histórico completo de `folds_done` deu 21.2 min/fold para o TX contra os ~31 min que as duas amostras sugeriam, e o total real fechou em $26.62 contra os $26.7 projetados a partir do histórico. **Use a série inteira, nunca o último par de amostras** — e confira contra o ledger, que aqui bateu em 2 × $8.15/h o tempo todo.
- `2026-08-10` — ⚠ **REVERTED the same day: `MTL_CHUNK_VAL_METRIC=1` stays forced on the dedicated-reg cell. The equivalence argument did not survive measurement on the target card.** The drop was justified by "CPU and CUDA scoring are equivalent by construction, because `_rank_of_target` is an exact integer count". True, and it **does not cover the metric this cell reports**: `top10_acc` comes from `_top_k_accuracy` (`src/tracking/metrics.py:102-110`), which uses `logits.topk(k).indices`, whose tie-break *at the k-boundary* is a kernel/arch detail. Only mrr/ndcg go through the strict-`>` rank. Measured on an **H100** with a case that forces exact fp32 ties at the boundary: CPU and GPU disagreed on the top-k index set in **19 950 / 20 000 rows** and `top10_acc` moved **3.0e-04** (cpu 0.009850 vs gpu 0.009550) — **300× the 1e-6 reporting quantum**. Continuous-valued logits showed **0 % boundary ties, zero index-set disagreement and ~1e-9 agreement** across all four realistic shapes, so the failure needs a tie that real fp32 logits do not appear to produce — but that rate is unmeasured on real reg logits, every banked reg cell in the study is CPU-scored, and the prize was ~40 s per small-state cell. Homogeneity inside a pooled n=20 cell beats it. Both `v18_2/scripts/run_lane.sh` and `v18/run_wave.sh` restored (opt out with `P1_GPU_VAL=1`). **No banked result was ever affected**: the change existed for ~1 h and no cell was produced under it.
- `2026-08-10` — **the checker that "verified" the equivalence was itself defective, and a Fable review caught it.** Three faults, all mine: (1) it requested the key `f1_macro` while `compute_classification_metrics` emits `f1`, and the guard `if k in m_cpu` **silently skipped it** — so macro-F1 had zero coverage while the run reported it as tested; (2) the stated margin was **100× too generous** — reporting precision is 4 dp on a *percentage* = 1e-6 on the 0-1 scale the metrics use, not 1e-4, so the passing margin is ~34× and not 3350×; (3) it had no case that stresses the **top-k boundary** specifically, which is the only place the reported metric can actually diverge. `v18_2/scripts/check_cpu_gpu_scoring_equiv.py` now asserts the key list against what the function really returns (missing key = hard failure, never a skip), reports the boundary-tie fraction and the top-k index-set disagreement count per case so a vacuous "tie stress" case cannot pass unnoticed, and uses the correct 1e-6 bar. **Rule: a "by construction" argument must be checked against the metric you actually report, and an equivalence checker that can skip a key reports coverage it does not have.**
- `2026-08-10` — the same review corrected four stale figures now fixed in the scripts and manual: istanbul **C=520** (not ~1500 → its val logit is 0.11 GB, not 0.33), florida **N_val=254 883** (a comment said 50 976, which would have put it *below* the 4 GB guard), california **C=8501** and texas **C=6553** (headers said 8462/6530). All verified against the banked `docs/results/P1/region_head_*_s0.json`. Also: the local repo copy of `v18/run_wave.sh` had drifted from the one the A40 actually executes and has been resynced; and `p1_region_head_ablation.py:104` cites the equivalence test at `tests/test_training/…` when it lives in `tests/test_scripts/…`.
- `2026-08-10` — **the better fix, deliberately deferred.** `mtl_eval.py` already solves this properly for the joint: its "chunking" **streams per-row reductions on GPU** and never moves logits to CPU (`"streamed on GPU … NEVER CPU-moved"`, mtl_eval.py:186-190) — which is why the joint pays no CPU penalty and why its CA/TX cap costs nothing. Porting that pattern into p1 (`_streamed_cls_metrics`, ~20 lines in the p1 val loop) would remove the OOM *and* the CPU penalty *and* make scoring homogeneous at every scale. It must not be deployed mid-study: it would flip TX/CA/FL from CPU-full to GPU-streamed relative to their banked s0/s1 cells — the same inhomogeneity, on the large states. Right change at the next freeze boundary, applied to all states at once.
- `2026-08-10` — **arizona and istanbul CLOSED at n=20; four of the six states are now complete.** Eight cells run on three H100 containers in parallel (one per state+seed, families packed with `PARALLEL=1`), all exit 0, teardown verified, **$3.51 billed** against a $5.15 projection — 32 % under. arizona s100 cat **34.5943** / reg **59.3887** / joint **34.6391 · 59.1737**; istanbul s7 reg **75.1430** / joint **35.4417 · 75.2744**; istanbul s100 cat **35.3230** / reg **75.1588** / joint **35.4187 · 75.2774**. Every value lands on its local reference (istanbul reg across four seeds: 75.1563 / 75.1791 / 75.1430 / 75.1588 — a 0.036 pp spread). Board now: **istanbul, alabama, arizona, florida all n=20**; texas §1 at n=10 (4 joints but dedicated only at s0/s1); california §1 at n=5. Pooled Δcat **+0.076** over 95 (state,seed,fold) pairs, Δreg **+0.179** over 90 — both "beats", though the §1 disclosure about non-independent folds still applies.
- `2026-08-10` — **H100 sizing, measured rather than extrapolated.** arizona s7 (3 families packed, H100): lane 659 s, **VRAM peak 13.9 GiB of 79.6, median util 75 %** — the headroom that makes packing worthwhile for a small state, in contrast to a large-state joint which pins the card at 89–99 %. Per family against the A40: cat **2.04× faster**, joint **1.72× faster**, reg **1.85× slower** (the CPU-scoring path, now understood and reverted deliberately). Projections for the remaining eight cells were then built from this measurement rather than from alabama-derived ratios, and came in 32 % conservative. The run clock was cut from the 3 h default to 45 min (~2.5× the longest projection), dropping the worst case across three containers from $59.55 to $14.90.
- `2026-08-10` — two process fixes from the day's mistakes: `modal_lane.py --dry-run` now **states the execution mode** and warns when `--parallel` is absent with multiple families (omitting it silently serialised the florida run and cost ~20 min); and the local repo copy of `v18/run_wave.sh` had **drifted from the one the A40 actually executes** and was resynced (a review read the local copy and correctly reported the change as missing). Stale figures corrected across scripts and manual: california **C=8501**, texas **C=6553** (were 8462/6530), florida **N_val=254 883** (a comment said 50 976, which would have placed florida *below* the 4 GB chunking guard), istanbul **C=520**. All verified against the banked `docs/results/P1/region_head_*_s0.json`.
- `2026-08-10` — **the reg cell's CPU-scoring penalty is fixed, and the device question was settled by measurement rather than by argument.** p1 now streams the val metric with per-row accumulators (`_streamed_cls_metrics`, the same mechanism `mtl_eval.py` already uses for the joint), so the `[N × C]` buffer — 20.1 GB at texas, 19.9 GB at california — never exists on any device. Streaming is **bit-identical** to the full path on the same device: 0.000e+00 across 8 shapes including forced boundary ties and batch size 1. The separate question, *where* the per-row ops run, was answered with `P1_SCORE_AB=1`, which scores the **same logits on both devices inside one run** — the only design that isolates the device from `--compile` nondeterminism, since diffing a fresh GPU run against a banked CPU value confounds the two. On real arizona reg logits: **0/100 448 boundary ties** at 10↔11 *and* 5↔6, worst same-logit Δ **5.96e-08** (`ndcg_5`, 17× under the 1e-6 quantum), `top10_acc` **bit-identical to 10 dp**; end-to-end, 65 of 67 numeric keys identical and 2 at ≤2.98e-08. Wall on one fold × 8 epochs: **57 s → 18 s (3.2×)**. **No banked cell is affected and none needs re-running** — `P1_STREAM_GPU` defaults to 0, so the default path is the CPU one every banked reg cell used, reproduced bit-for-bit. What changes is that renting a reg cell is now viable: the "never rent CPU-bound work" rule was a consequence of a fixable defect, not a property of the family.
- `2026-08-10` — two guards added with it. The epoch-0 tie diagnostic now runs on **every** streamed run and, under `P1_STREAM_GPU=1`, a non-zero rate raises a WARNING naming the cell as possibly non-homogeneous with its CPU-scored siblings (hard-fail under `MTL_STRICT=1`) — so the arizona zero never has to be extrapolated to texas; texas measures itself. It uses `topk(11)` (2.9 ms/batch, covering both the 10/11 and 5/6 boundaries) rather than a full `sort` (83.5 ms), after an earlier version ran the sort every epoch and discarded all but the first, adding ~26 min/fold at texas — to the very cells the port exists to speed up.
- `2026-08-10` — **lesson, logged because it nearly banked a broken run: the port had never executed end-to-end before it was declared validated.** `/tmp/stream_equiv.py` exercised `_streamed_cls_metrics` directly and passed at 0.000e+00, and a Fable review passed the diff — but neither runs p1's val loop, where the epoch-0 log read `_to_cpu` **fourteen lines above its assignment**. Every streamed run died instantly on `UnboundLocalError`; the first real invocation found it. A unit test of the extracted function plus a code review is not evidence that the caller works. (The same run also masked the failure as success: `python … | grep` reports **grep's** exit status, so `TRAIN_RC=0` next to a traceback — `set -o pipefail`, or read the rc of the right process.)
- `2026-08-10` — ⚠ **CORRECTION to this morning's "0 boundary ties, GPU scoring is safe": trained fp32 logits DO tie, and the epoch-0 diagnostic gave a false all-clear.** A Fable review observed that the banked value comes from `per_metric_best` over ALL epochs while the tie check ran once, at epoch 0 — the epoch whose logits are the most spread out they will ever be. Re-measured with the diagnostic on every epoch, the same arizona reg cell shows **1/100 448 (0.0010 %) ties at the 5/6 boundary at epochs 2, 4 and 5**, and 0 at epoch 0. One tied row can move `top5_acc` by up to 9.96e-06 — about **10x the 1e-6 reporting quantum**, enough to change the 4th decimal. The **headline Acc@10 stayed at 0 ties in every epoch**, so the reported reg number is unaffected in this sample, but `top5_acc` is banked in `per_metric_best` too and the precondition is therefore not clean. **`P1_STREAM_GPU` stays OFF and the 3.2x is not collectable on study cells**; the "renting a reg cell is now viable" conclusion is withdrawn. Nothing banked is affected — the default path never changed and is byte-identical (127/127 keys, 0.000e+00, verified end-to-end on an A100 against the pre-refactor code). The gate added in the same review caught this on its first execution, which is the only reason it was found before a cell was banked under it.
- `2026-08-10` — the refactor itself is verified end-to-end on real GPU data, not just by unit test: **dedicated-reg 127/127 numeric keys identical** to the pre-refactor code, and the **MTL joint 6466/6466 keys identical** (arizona, A100, same seed, refactored vs `git show a9a2b80d^` swapped in mid-container). `StreamingClsMetrics` in `tracking/metrics.py` now backs all four call sites — mtl_eval S2, mtl_cv S1, p1, and `_single_task_train`, which had **no cardinality guard at all** and would have OOMed on any STL region run (it cat the full `[N_val, C]`; at CA/TX that is the same ~20 GB tensor). Four hardcoded `> 256` literals replaced by `should_stream`.
- `2026-08-10` — lesson, twice in one day: **a read-before-assignment slipped in AGAIN while fixing a review finding**, in the same file and the same function as the morning's `UnboundLocalError`. Caught before running, by an AST check for locals read above their first assignment — worth keeping as a habit for this file, since `py_compile` passes such code happily and the failure only appears when that branch executes. (First version of the check used `ast.walk`, which is BFS, so "first assignment" was not the lowest line number and it produced 27 false positives; take `min()` over the assignment linenos and skip function parameters.)
- `2026-08-10` — **the five remaining full-logit callers migrated; `StreamingClsMetrics` now backs every high-cardinality metric path in the repo.** `flashback_e2e`, `poi2vec_e2e` and `ctle_e2e` each cat the whole `[N_val, n_regions]` reg logit before handing it to `_ood_restricted_topk` — ~20 GB at california (8501) / texas (6553), i.e. those baselines would have **OOMed on the first large-state cell**, and the pending CTLE board cell is exactly such a run. `p1_poi_head_ablation` had the same unguarded cat at **POI** cardinality, the largest in the study; its banked `poi_head_*` results exist only because AL/AZ are small enough to fit. All four now stream and call `_ood_from_streamed`. **No banked result is invalidated**: the full-logit path *is* the reference the streamed one was proven identical to, and its failure mode is an OOM (loud), never a wrong number (silent).
- `2026-08-10` — `b3_hmt_grn` was the fifth case and the interesting one: it already streamed, **by hand**, and computed the OOD rates with running integer counts (`hits[k] / n_indist`) rather than the shared `hit[k][mask].float().mean()`. So the baseline table had **two different implementations of one metric** sitting in the same row group. It now takes the shared path, with `move_logits_to="cpu"` preserving its original per-batch CPU reduction exactly. The switch is float64-int-division vs float32-mean: **measured max 2.3e-08** across five realistic (N, hit-rate) pairs, ~43× under the 1e-6 quantum — so b3's banked AZ / FL-partial numbers stand, and a future re-run will differ only around the 8th decimal. Nine new tests pin the migration at texas- and california-shaped cardinalities, with exact ties, batch size 1, a ragged batch, and a no-OOD-rows case; one test asserts the subtle part explicitly — masking *after* top-k equals masking *before*, which holds only because top-k of row i does not depend on any other row.
- `2026-08-10` — ⚠ **CORRECTION: "the reg cell runs slower on faster silicon" was a container-configuration error, not a property of the cell.** The measured 1.85x slowdown on rented hardware (arizona reg 343 s on an H100 against 185 s on the A40) was obtained with `--cpu 8`, while the A40 box has **32 cores** — and the reg cell's bottleneck is the CPU-side val scoring, not the GPU. Measured today in ONE container (H100, `--cpu 32`, arizona reg, 1 fold x 20 epochs), varying only the thread count: **8 threads 41 s, 32 threads 23 s (1.78x), GPU scoring 18 s** — and all three return **identical metrics** (Acc@1 0.2530 / Acc@5 0.4606 / Acc@10 0.5552 / MRR 0.3542 / F1 0.0537). So the fix for the reg cell was never the GPU: it is **cores**, which are byte-identical to the banked path because the scoring device does not change. Projected with the measured 1.78x, a rented reg cell lands at **~1.04x the A40 wall** instead of 1.85x. The GPU-scoring option adds only a further 1.28x on top, which is not worth its tie exposure — the earlier framing implied the choice was CPU-slow vs GPU-fast, and the real choice is 8-cores-slow vs 32-cores-parity.
- `2026-08-10` — the economics that follow, and why the A40 still wins: on a GPU container CPU is billed at the host-share multiple, so 32 cores cost **$4.53/h** against 8 cores' $1.13/h — an H100 at 32 cpu / 32 GiB is **$9.24/h**, and the 5 missing reg cells come to **~$62 for parity with a machine that does them for $0**. The reg cell is not expensive to rent because it is slow; it is expensive because **its bottleneck is the resource Modal charges the most for**, while the joint's bottleneck (the GPU) is the one you are already paying for. That is the whole reason a 3.8x-cheaper cell on the A40 (reg 5897 s vs joint 22453 s at texas) inverts to more-expensive-than-the-joint when rented. **Rule: match the container to the cell's bottleneck, and check which resource that is before quoting a price** — a config chosen for the joint starves the reg. ⚠ The $62 assumes the 3.0x host share applies to a 32-core request; the billing sample after this probe was $0.21, too short to resolve against the block-posting ledger, so if the share is a floor (~24 cores) rather than a multiplier the real figure is nearer $38. Unresolved, and worth a >=15 min sample before any large reg rental.
- `2026-08-10` — ⚠ **the tie gate was measuring the wrong thing, and the `1/100448` that withdrew GPU scoring was a FALSE ALARM.** It counted rows where the k-th and (k+1)-th LOGITS were equal. But a tie changes a reported number only when the **target** is in the tie group — a tie between two non-target classes cannot move any metric. The correct predicate is `n_gt < k < n_gt + n_eq` (`n_gt` = strictly greater, `n_eq` = equal incl. target), and `n_gt` is free: it is `rank - 1` from the pass already made, so the check costs ONE equality pass and *replaces* the `topk(max_k+1)` the old one needed. Over-reporting measured on 4096x6553: with mildly-rounded logits the old gate fired on **48-62 rows** while true ambiguity and observed divergence were both **0**; with heavy ties it fired on **all 4096** against **90** truly ambiguous and **1** actually divergent — three orders of magnitude. Re-measured on real arizona reg logits with the correct predicate, **every epoch**: **0/100448 at both boundaries, 0 warnings, 20/20 epochs**, and the A/B worst cross-device delta fell to **1.709e-10** with `fold 0` byte-identical between devices (Acc@1 0.2530 / Acc@5 0.4606 / Acc@10 0.5552 / MRR 0.3542 / F1 0.0537). **A gate that over-reports by 3 orders of magnitude vetoes changes that are provably safe — which is exactly what it did to GPU scoring.**
- `2026-08-10` — **and the reason the reg cell wants 32 cores is that its scoring does FOUR full passes over the same `[B, C]` tensor per batch.** Profiled at the texas shape (2048x6553, 54 MB per pass): `argmax` 2.37 ms + `topk(5)` 2.81 + `topk(10)` 3.46 + `_rank_of_target` 3.63 = **12.26 ms/batch**. Two of those are information already in hand — **top-5 is a subset of top-10, and argmax is top-10's first column**. Deriving both from one `topk(10)` gives **7.67 ms, a 1.73x cut** in the CPU-bound phase. It is not unconditionally byte-identical (`topk(10)[:, :5]` may pick a different tied element than `topk(5)`; measured 1-4 divergent rows per 4096 under heavy ties, 0 under continuous), but the corrected gate above is exactly the invariant that certifies when it is — and it reads 0 on real logits. **The honest framing: the cell does not need 32 cores, it needs ~2x less memory traffic.**
- `2026-08-10` — the ordering that follows, measured rather than assumed: at **32 cores**, GPU scoring buys only **1.16x** (20.3 s -> 17.5 s), not the 3.2x seen at 8 cores. The device was never the big lever; **cores were**, and fusing the passes reduces the core requirement instead of trading it for a tie-break risk. So the improvement order is (1) fuse the redundant passes, (2) give the cell the cores it needs — free on the A40, $4.53/h rented — and (3) GPU scoring, which is now certified safe by a correct gate but is worth the least of the three.
- `2026-08-10` — **why the dedicated-region cell barely notices a faster GPU, answered with a profile instead of a guess.** During a run: **GPU utilisation 10 / 32 / 57 / 10 %, 1 779 MiB of 81 559** — the cell leaves an H100 idle. The head (`next_stan_flow`, d_model=128) is **~0.47 M parameters, ~30 GFLOP per batch**, so real compute is sub-millisecond and roughly half the wall is Python/DataLoader/kernel-launch overhead that is **identical on an A40 and an H100**. The proof is the sibling family: the dedicated-CAT cell (C=7, no chunking, scored on GPU) gets exactly **2.04x** — the launch-bound ceiling — while the region cell gets 1.2x. The 0.84x difference between them *is* the CPU scoring, and nothing else. **Honest cap: this family tops out near 2x no matter what is done to the metric; renting a bigger card for it buys CPU cores, not FLOPs.**
- `2026-08-10` — the CPU dependency costs **more than the work it performs**. Instrumented (arizona, 1 fold x 10 epochs, H100): CPU-scored wall **17.89 s** of which scoring is 2.87 s (16.1 %); GPU-scored wall **11.45 s** of which scoring is 0.34 s (2.9 %). Removing the offload saved **6.4 s** while the scoring itself was only 2.87 s — the rest is the per-batch `logits.to("cpu")` **synchronising the host every iteration and serialising the GPU**. The visible cost was a third of the real one.
- `2026-08-10` — and the offload is not even required where it runs. `_should_chunk_val_metric` auto-enables above **4 GB**; arizona's full val logit is **0.62 GB**. The CPU path runs there only because the lane drivers blanket-export `MTL_CHUNK_VAL_METRIC=1` — an OOM guard sized for TX/CA (~20 GB) being paid at every small state, where it protects nothing and costs the wall above. **Free win: scope the export to the states that need it.**
- `2026-08-10` — the "154 s vs 288 s on the same spec" anomaly was not an anomaly and not a regression: the two runs **scored on different devices**. 154 s predates the GPU-scoring withdrawal in `19fc7ef8`; 288 s is the CPU-scored default that replaced it. The ambiguity counter is NOT running per batch on the CPU path (`_diag = (epoch == 0) or not _to_cpu`), so it explains nothing here. Recorded because "same container, same cell, 1.9x apart" looked like a measurement fault and was a code-history fault.
- `2026-08-10` — ⚠ **fp16 is a DEFECT this study excludes, yet it is still the DEFAULT on CUDA in three code paths** (`mtl_eval.eval_autocast_ctx`, `_single_task_train`, `mtl_cv`), each switched off only by an env var every caller must remember. That is precisely the mechanism behind `PRECISION_CAVEAT.md`, where a leaked `export MTL_DISABLE_AMP=1` made dedicated-cat precision depend on **whether the wave had been resumed**, while the sidecars claimed fp32 throughout. `src/utils/precision.py` now makes the choice audible: engaging fp16 logs a loud one-time warning naming the caveat, and under `MTL_STRICT=1` — which `run_lane.sh` sets on every cell — it **refuses to start**. bf16, the documented fix, passes silently. Deliberately not a default flip: this repo's convention is to make a silent stumble loud rather than move the ground under existing recipes. Consequence for today's work: the fp16 tie-rate probe measured a path the protocol never takes, and the comment that justified the mtl_eval gate by fp16 has been corrected — the gate is there because it is the scored path, not because of precision.
