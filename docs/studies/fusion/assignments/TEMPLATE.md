# Assignment — Phase <PHASE> — <ISO timestamp>

<!--
  This file is generated by the Opus coordinator when the user runs
  `/coordinator <PHASE> --pack`.  One file per phase per execution cycle.
  Workers paste the relevant block into their own Claude session.

  State-pinning reminder: a (state, engine) pair is assigned to exactly ONE
  machine for the whole phase.  Do not split Alabama/fusion across machines.
-->

## <machine-id>  (role: primary/secondary/colab)

Tests: <N>
Tier mix: screen × <n_screen>, promote × <n_promote>, confirm × <n_confirm>
State(s): <AL | AZ | ...>
Engine(s): <fusion | hgi | ...>

Prompt to paste in local Sonnet:

<!-- paste-block-start -->
You are a study worker.  Run the following tests sequentially.

WORKER_ID: <machine-id>
VENV: /path/to/.venv/bin/python
TEST_IDs:
  - P1_AL_screen_base_equal_weight_seed42
  - P1_AL_screen_base_cagrad_seed42
  - ... (full list)

For each test, in sequence:
  1. cd /path/to/repo
  2. .venv/bin/python scripts/study/study.py next \
       --phase P1 \
       --test-id <TEST_ID> \
       --no-sync \
       --worker-id <machine-id>
  3. When done, rsync results/<engine>/<state>/<run_dir>/ back to mac-m4.

Do NOT call `study import` or `study analyze` — the coordinator does that.
If a test FAILS (non-zero exit), stop and report the error.
If the hash-check fails (exit 3), stop and report — do NOT run train.py.
<!-- paste-block-end -->

---

## Example: worked assignment row

### mac-m4  (primary, owns state.json)

Tests: 21
Tier mix: screen × 21
State(s): AL
Engine(s): fusion

Prompt to paste in local Sonnet:

<!-- paste-block-start -->
WORKER_ID: mac-m4
VENV: /Users/vitor/Desktop/mestrado/ingred/.venv/bin/python
TEST_IDs:
  - P1_AL_screen_base_equal_weight_seed42
  - P1_AL_screen_base_static_weight_seed42
  - P1_AL_screen_base_uncertainty_weighting_seed42
  ... (all 21 base × optim combinations for AL screen)

For each test:
  cd /Users/vitor/Desktop/mestrado/ingred
  .venv/bin/python scripts/study/study.py next \
    --phase P1 --test-id <TEST_ID> --no-sync --worker-id mac-m4

After all 21 finish, the coordinator reconciles:
  for each completed test:
    .venv/bin/python scripts/study/study.py import ...
    .venv/bin/python scripts/study/study.py analyze ...
<!-- paste-block-end -->

---

## linux-4050

Tests: 84
Tier mix: screen × 84
State(s): AL
Engine(s): fusion

Prompt to paste in linux Sonnet:

<!-- paste-block-start -->
WORKER_ID: linux-4050
VENV: /path/to/.venv/bin/python
TEST_IDs:
  - P1_AL_screen_cgc22_equal_weight_seed42
  - P1_AL_screen_cgc22_cagrad_seed42
  ... (all cgc22, cgc21, mmoe4, dsk42 × 21 optims for AL screen)

Setup before running:
  rsync -avz mac-m4:/path/to/output/fusion/alabama/ output/fusion/alabama/

For each test:
  .venv/bin/python scripts/study/study.py next \
    --phase P1 --test-id <TEST_ID> --no-sync --worker-id linux-4050

After completing, rsync results back:
  rsync -avz results/fusion/alabama/ mac-m4:/path/to/results/fusion/alabama/
<!-- paste-block-end -->

---

## colab-a100

Tests: 10
Tier mix: confirm × 10
State(s): AL, AZ
Engine(s): fusion

Prompt to paste in Colab notebook:

<!-- paste-block-start -->
# Bootstrap (run once per session)
!python /content/PoiMtlNet/scripts/study/colab_runner.py bootstrap \
  --drive-root /content/drive/MyDrive/mestrado/PoiMtlNet

# Run each confirm test
for test_id in [
  "P1_AL_confirm_dsk42_cagrad_seed42",
  "P1_AL_confirm_dsk42_aligned_mtl_seed42",
  ...
]:
    !python /content/PoiMtlNet/scripts/study/colab_runner.py run \
      --phase P1 --test-id {test_id}

# Results packaged as tar.gz under $RESULTS_ROOT/_study_artifacts/
# Sync Drive, then on mac-m4:
#   tar -xzf <archive>.tar.gz -C /tmp
#   python scripts/study/study.py import --run-dir /tmp/<run_dir> \
#     --phase P1 --test-id <test_id>
<!-- paste-block-end -->
