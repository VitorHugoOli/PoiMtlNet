# category_ensemble

Why this?
- Multi-path ensemble variant for richer category feature aggregation.

Runtime mapping:
- Model registry key: `category_ensemble`
- Runtime class: `models.heads.category.CategoryHeadEnsemble`

Source:
- In-repo implementation: `src/models/heads/category.py`
