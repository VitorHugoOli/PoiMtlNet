# category_attention

Why this?
- Attention pooling variant for adaptive feature selection before classification.

Runtime mapping:
- Model registry key: `category_attention`
- Runtime class: `models.heads.category.CategoryHeadAttentionPooling`

Source:
- In-repo implementation: `src/models/heads/category.py`
