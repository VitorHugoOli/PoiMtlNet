# next_temporal_cnn

Why this?
- Temporal CNN baseline for local sequential pattern extraction.

Runtime mapping:
- Model registry key: `next_temporal_cnn`
- Runtime class: `models.heads.next.NextHeadTemporalCNN`

Source:
- In-repo implementation: `src/models/heads/next.py`
