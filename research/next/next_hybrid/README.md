# next_hybrid

Why this?
- Hybrid sequence head combines recurrent and attention-style components.

Runtime mapping:
- Model registry key: `next_hybrid`
- Runtime class: `models.heads.next.NextHeadHybrid`

Source:
- In-repo implementation: `src/models/heads/next.py`
