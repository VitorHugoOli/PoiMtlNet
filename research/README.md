# Research Scope

This directory is reserved for embedding research pipelines and standalone
embedding experiments.

Current structure:

- `research/embeddings/<engine>/`: embedding trainers and utilities.

Runtime training code for models and losses now lives in `src/`.
