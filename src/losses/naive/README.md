# naive

Why this?
- Lightweight heuristic baseline for dynamic two-task weighting.

Runtime mapping:
- Registry key: `naive`
- Runtime class: `losses.naive.NaiveLoss`

Source:
- In-repo implementation: `src/losses/naive/loss.py`
