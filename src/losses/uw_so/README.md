# uw_so

Why this?
- Soft-optimal uncertainty weighting extends uncertainty-based scalarization with
  temperature-controlled normalized inverse-loss weights.

Runtime mapping:
- Registry key: `uw_so`
- Runtime class: `losses.mtl_baselines.SoftOptimalUncertaintyWeightingLoss`

Source:
- In-repo implementation: `src/losses/mtl_baselines.py`
