# next_temporal_cnn

Why this?
- Temporal CNN baseline for local sequential pattern extraction.

Runtime mapping:
- Model registry key: `next_temporal_cnn`
- Runtime class: `models.next.head.NextHeadTemporalCNN`

Source:
- In-repo implementation: `src/models/next/head.py`
